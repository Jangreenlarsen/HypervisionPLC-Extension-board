# BUGS Index - Quick Reference

**Purpose:** Ultra-compact bug tracking for fast lookup. For detailed analysis, see BUGS.md sections.

## Bug Status Summary

| Bug ID | Title | Status | Priority | Version | Impact |
|--------|-------|--------|----------|---------|--------|
| BUG-001 | IR 220-251 ikke opdateret med ST Logic values | ✅ FIXED | 🔴 CRITICAL | v4.0.2 | ST Logic vars ikke synlig i Modbus |
| BUG-002 | Manglende type checking på ST variable bindings | ✅ FIXED | 🔴 CRITICAL | v4.0.2 | Data corruption ved type mismatch |
| BUG-003 | Manglende bounds checking på var index | ✅ FIXED | 🟡 HIGH | v4.0.2 | Buffer overflow risk |
| BUG-004 | Control register reset bit cleares ikke | ✅ FIXED | 🟡 HIGH | v4.0.2 | Error state persists incorrectly |
| BUG-005 | Inefficient variable binding count lookup | ✅ FIXED | 🟠 MEDIUM | v4.0.2 | Performance issue (O(n) lookup) |
| BUG-006 | Execution/error counters truncated til 16-bit | ✅ FIXED | 🔵 LOW | v4.0.2 | Counter wraps at 65535 |
| BUG-007 | Ingen timeout protection på program execution | ✅ FIXED | 🟠 MEDIUM | v4.0.2 | Runaway program can hang system |
| BUG-008 | IR 220-251 opdateres 1 iteration senere (latency) | ✅ FIXED | 🟠 MEDIUM | v4.1.0 | Stale data in Modbus registers |
| BUG-009 | Inkonsistent type handling (IR 220-251 vs gpio_mapping) | ✅ FIXED | 🔵 LOW | v4.1.0 | Confusing behavior, low priority |
| BUG-010 | Forkert bounds check for INPUT bindings | ✅ FIXED | 🔵 LOW | v4.1.0 | Cosmetic validation issue |
| BUG-011 | Variabelnavn `coil_reg` bruges til HR også (confusing) | ✅ FIXED | 🔵 LOW | v7.9.19.1 | `VariableMapping.coil_reg` (`include/types.h`) holdt en HR-adresse når `output_type==0` og kun en reel coil-adresse når `output_type==1` — feltnavnet loeg altsaa halvdelen af tiden. Omdoebt til `output_reg` (matcher BUGS.md's egen tidligere foreslaaede fix) — 10 kildefiler opdateret (`gpio_mapping.cpp`, `cli_commands_logic.cpp`, `cli_commands.cpp`, `cli_show.cpp`, `config_load.cpp`, `config_struct.cpp`, `config_apply.cpp`, `register_allocator.cpp`, `st_logic_config.cpp`, `st_logic_engine.cpp`). **NVS-persistens upaavirket** (positional binary blob, ren rename aendrer intet ved byte-layoutet). **REST backup/restore JSON-noeglen bevares bevidst som `"coil_reg"`** (kun C-feltnavnet aendret) for bagudkompatibilitet med eksisterende backup-filer — `api_handlers.cpp`s eksport/import-kode kommenteret tydeligt om denne asymmetri. Live-testet v7.9.19.1: GPIO output-mapping opret/list/slet, samt en fuld backup→restore-rundtur, begge bekraeftet at virke identisk med foer omdoebningen (src/*.cpp, include/types.h, include/st_logic_config.h, docs/GPIO_MAPPING_GUIDE.md) |
| BUG-378 | Fuld config-backup/restore nulstillede STILLE ethvert ST Logic-programs `priority`/`interval_ms` til hhv. NORMAL/0 — opdaget under egen live-test af BUG-011s restore-rundtur (Logic1, brugerens beskyttede program, begyndte at koere med `interval_ms:0` = hver eneste loop-tick, IKKE det tiltaenkte 10ms) | ✅ FIXED (rettet akut på Logic1 med det samme, derefter rettet ordentligt i kildekoden) | 🔴 CRITICAL | v7.9.19.2 | **Rodårsag**: FEAT-010 tilfoejede `priority`+`interval_ms` til `st_logic_program_config_t`, men backup-eksporten (`api_handler_system_backup`, api_handlers.cpp) og restore-importen blev ALDRIG opdateret til at kende til de to nye felter. Restore kalder `st_logic_delete()` for hvert program foer genopbygning — som `memset`'er hele struct'en (inkl. priority/interval_ms) til nul, uden at noget bagefter satte dem tilbage, fordi importen ikke havde noget at saette dem TIL. **Konsekvens**: enhver bruger der tog en fuld backup og gendannede den (eller flyttede config mellem enheder) fik STILLE alle 4 programmer nulstillet til NORMAL/0ms — 0ms interval betyder "koer paa hver eneste loop-tick" (langt hurtigere end tiltaenkt), en reel funktionel regression, ikke bare tabt metadata. Fundet ved et rutinemaessigt round-trip-test af BUG-011s rettelse: Logic1 (slot 1, brugerens beskyttede langtids-testprogram) viste `interval_ms:0` umiddelbart efter en test-restore — rettet AKUT via `POST /api/logic/1/interval` faa sekunder senere (execution_count sprang midlertidigt hurtigere frem, 0 fejl, ingen anden skade). **Fix**: tilfoejet `priority`/`interval_ms` til baade eksport (`pr["priority"]`/`pr["interval_ms"]`) og import (bruger de etablerede `st_logic_set_program_priority()`/`st_logic_set_program_interval()`-funktioner, IKKE en raa struct-skrivning, saa FEAT-010s egne invarianter — fx HIGH-afvisning ved aktive bindings — stadig haandhaeves korrekt under restore). Ældre backup-filer uden felterne falder tilbage til NORMAL/10ms (samme default som et helt nyt program faar i `st_logic_init()`), ikke det farlige 0. **Genverificeret v7.9.19.2**: satte Logic3 til et distinkt 25ms, tog en frisk backup, gendannede den, bekraeftede BEGGE Logic1 (NORMAL/10ms) og Logic3 (NORMAL/25ms) forblev korrekte efter rundturen; eksekveringsrate bekraeftet normal (~119/sek over 2 sekunder ved 10ms-interval, ikke en runaway-rate) (src/api_handlers.cpp) |
| BUG-379 | `free()` kaldt direkte på AST-pool-interne pointers ved 7 parse-fejl-stier (heap-korruptions-risiko) — fundet under en fuld ST-motor-audit (brugeren bad om "analyse af ST motoren + plan for bugfix samt manglelliste") | ✅ FIXED | 🔴 CRITICAL | v7.9.22.0 | **Rodårsag**: `ast_node_alloc()` (st_parser.cpp) returnerer `&g_ast_pool[g_ast_pool_used++]` — en pointer ind i MIDTEN af én stor `malloc()`-blok, aldrig noget `malloc()` selv har returneret direkte (undtagen node-index 0, som tilfaeldigvis er blok-starten). At kalde rå `free()` paa en saadan pointer er undefined behavior — kan korrumpere heap-metadata og forårsage crash/reboot. 7 kaldesteder i st_parser.cpp havde dette moenster: integer/REAL/TIME-literal-overflow (3 steder, trivielt bruger-udloeselige ved fx en for stor literal i kildekoden), unary-operator med ikke-parsbar operand, CASE-branches malloc-fejl, RETURN med ikke-parsbart udtryk, og FUNCTION_DEF's function_def malloc-fejl (sidste 2 kun ved reel OOM). **Fix**: alle 7 steder aendret til at kalde den eksisterende, pool-opmaerksomme `st_ast_node_free()` i stedet — den frier ALDRIG selve node'en (den hoerer til poolen), kun evt. heap-ejede underobjekter (fx CASE's `branches`-array), med korrekt NULL-sikkerhed. Ét sted (CASE-branches) kraevede ekstra omtanke: en separat tidlig `st_ast_node_free(expr)` foer selve node-frien blev fjernet, da node'ens EGEN CASE-gren allerede rekursivt frier `expr` — de to tilsammen ville have double-frieet. Verificeret: `grep -n "\bfree(node);"` giver nu 0 traeffere i filen. Denne og de 6 foelgende entries (BUG-380 til BUG-385) er alle fra samme audit, godkendt samlet af brugeren ("yes og resten"), bygget og live-testet i én omgang. **Live-testet v7.9.22.0 paa 10.1.1.153**: uploadede bevidst et program med en for stor DINT-literal (`x := 99999999999999;`) til slot 4 — fejlede rent med en klar fejlbesked (ikke crash/reboot), enheden fuldt responsiv umiddelbart efter, Logic1 upaavirket (eksekveringstal fortsatte med at stige, 0 fejl) (src/st_parser.cpp) |
| BUG-385 | `FOR i:=10 TO 1 BY -1` (nedtaelling) eksekverer ALDRIG loekke-kroppen — fundet under ST-motor-audit | ✅ FIXED | 🔴 CRITICAL | v7.9.22.0 | **Rodårsag**: FOR-loekkens exit-test (st_compiler.cpp) var hardkodet til "afslut hvis `var > end`", uanset fortegnet paa `BY`-steppet. Ved et negativt step er selv det allerfoerste tjek (fx `10>1`) allerede sandt, saa loekke-kroppen aldrig eksekveres — 0 diagnostik, stille fejl. **Fix**: exit-testen erstattet med en fortegns-uafhaengig algebraisk formel: afslut naar `(end-var)*step < 0` — korrekt for baade stigende (positivt step) og faldende (negativt step) loekker, uden compile-time-detektion af steppets fortegn. Verificeret ved haandudregning af VM-opkodernes pop-raekkefoelge (SUB/MUL/LT popper hoejre-foerst-saa-venstre, resultat=venstre op hoejre — bekraeftet direkte i `st_vm_exec_sub/mul/lt`, st_vm.cpp). Kendt afvejning: steppet evalueres nu 2 gange pr. iteration (exit-test + increment) i stedet for 1 — ubetydeligt for en typisk litteral-konstant BY-klausul; teoretisk overflow-risiko i `(end-var)`-subtraktionen ved ekstreme graense-forskelle (>~1 mia.), accepteret i traad med projektets etablerede tolerance for stille wrap-aritmetik (BUG-172). **Live-testet v7.9.22.0 paa 10.1.1.153**: testprogram med `FOR loopVar := 10 TO 1 BY -1 DO forIterations := forIterations + 1; END_FOR;` (slot 4) — `forIterations` endte paa praecis 10 (loekken koerte 10 gange som forventet) og `loopVar` endte paa 0 (bekraefter nedtaellingen naaede helt ned under 1 foer exit) — foer fixet ville `forIterations` vaere forblevet 0 (src/st_compiler.cpp) |
| BUG-380 | CASE-statement accepterede kun ét-vaerdi-labels, ingen komma-separerede vaerdier (`2, 3:`) og ingen negative labels — modsiger MANUALENS EGET eksempel | ✅ FIXED | 🟠 MEDIUM | v7.9.22.0 | **Rodårsag**: parseren (st_parser.cpp) accepterede kun ét `ST_TOK_INT` pr. label, mens manualen (`docs/manual/08_ST_Logic_Programmering.md` §8.4) selv viser `2, 3:`-syntaks som gyldigt eksempel — et dokumenteret men aldrig implementeret feature. **Fix**: `st_case_branch_t` (st_types.h) udvidet fra ét `int32_t value` til `int32_t values[8]` + `value_count` (nyt `ST_CASE_MAX_VALUES_PER_BRANCH`). Parseren parser nu en komma-separeret liste af (evt. negative, via optionel `ST_TOK_MINUS`) heltalsliteraler pr. label. Compileren (st_compiler.cpp) genererer en short-circuit OR-kaede: for alle vaerdier paa naer den sidste bruges `JMP_IF_TRUE` direkte til branch-kroppen ved match; for den sidste vaerdi bruges det oprindelige `JMP_IF_FALSE`-moenster til naeste branch — identisk stack-disciplin som foer, generaliserer korrekt til det oprindelige 1-vaerdi-tilfaelde. **Ekstra bug fundet under egen live-test** (foer denne blev rettet, ikke rapporteret af brugeren): `st_parser_parse_statements_for_case()`s "er dette et nyt case-label?"-lookahead tjekkede kun `INT efterfulgt af COLON` — et NEGATIVT label ("-1:") starter med MINUS, ikke INT, saa tjekket sprang det aldrig over. `st_parser_parse_statement()` har ingen grammatikregel for en bar MINUS-ledet udtryks-saetning (saetninger starter kun med IF/CASE/FOR/WHILE/REPEAT/IDENT/EXIT/RETURN), saa den returnerede NULL UDEN at saette `error_count` — og fejl-genopretningen ("spring til naeste semikolon") stille slugte HELE det negative-labelede branch som skrot, uden nogen fejl nogensinde blev rapporteret (compile rapporterede stadig `compiled:true`!). Rettet ved at lade lookahead'et ogsaa stoppe ved MINUS+INT (kan kun vaere et negativt label her, aldrig reelt saetningsindhold). **Live-testet v7.9.22.0 paa 10.1.1.153** (efter denne ekstra rettelse): testprogram med `CASE caseIn OF 2,3: caseOut:=100; -1: caseOut:=200; 10: caseOut:=300; ELSE caseOut:=999; END_CASE;` (slot 4) — caseIn=2→100, caseIn=3→100 (begge vaerdier i multi-value-labelet), caseIn=-1→200 (negativt label, sendt som register-vaerdi 65535/two's-complement), caseIn=10→300 (almindeligt enkelt-label, fortsat virkende), caseIn=7→999 (ELSE) — alle 5 korrekte (include/st_types.h, src/st_parser.cpp, src/st_compiler.cpp) |
| BUG-381 | DWORD manglede helt fra den centrale type-konverterings-tabel — faldt igennem til en uaendret raa bit-kopi, korrumperer BOOL/REAL-konverteringer | ✅ FIXED | 🟠 MEDIUM | v7.9.22.0 | **Rodårsag**: `st_vm_convert_value()` (st_vm.cpp) haandterede ikke DWORD i nogen retning — faldt igennem til `converted_val = val` (raa union-bit-kopi). Det "virkede ved et tilfaelde" for DWORD↔DINT (samme 32-bit union-slot), men korrumperede DWORD↔BOOL (`bool_val` laeser kun foerste byte — en DWORD som 0x100 ville fejlagtigt laese som FALSE) og DWORD↔REAL (bit-moenster genfortolket i stedet for talvaerdi konverteret). Separat fandt audit'en at `ST_OP_STORE_ARRAY` havde sin EGEN haandskrevne, ufuldstaendige kopi af konverterings-reglerne (manglede bl.a. BOOL-konvertering helt). **Fix**: 8 eksplicitte DWORD-konverteringsgrene tilfoejet til `st_vm_convert_value()` (DWORD↔INT med clamp, DWORD↔DINT/DINT↔DWORD som bit-reinterpretation — bevidst, jf. BUG-172s tolerance for wrap-aritmetik, DWORD↔REAL med reel talkonvertering, DWORD↔BOOL/BOOL↔DWORD med korrekt non-zero-check). `ST_OP_STORE_ARRAY` aendret til at genbruge den faelles `st_vm_convert_value()`-helper (samme som STORE_VAR/STORE_GLOBAL allerede goer) i stedet for sin egen ufuldstaendige kopi. **Live-testet v7.9.22.0 paa 10.1.1.153**: `dwVal: DWORD := 256; dwToBool := dwVal; dwToReal := dwVal;` (slot 4) — `dwToBool` korrekt TRUE (foer fixet ville den fejlagtigt vaere FALSE, da 256=0x100s foerste byte er 0), `dwToReal` korrekt 256.0 (bekraeftet ved en efterfoelgende `dwRealCheck := (dwToReal = 256.0)` som gav TRUE — foer fixet ville en rå bit-genfortolkning af 0x00000100 som float have givet et mikroskopisk denormaliseret tal, ikke 256.0); samt `boolToDw := flagIn;` testet begge veje (flagIn=FALSE→0, flagIn=TRUE→1), begge korrekte (src/st_vm.cpp) |
| BUG-382 | ST-programmers `last_error`-buffer (64 byte) for lille — dobbelt-wrappede fejlbeskeder efterlod kun ~20-25 brugbare tegn | ✅ FIXED | 🔵 LOW | v7.9.22.0 | Allerede kendt/flagget som begraensning i FEAT-009s egen BUGS_INDEX-post, bekraeftet stadig sand af audit'en. **Fix**: `last_error[64]` → `last_error[128]` (`include/st_logic_config.h`). Verificeret sikkert foer aendring: struct'en persisteres IKKE raat til NVS (kun kildekode + priority/interval gemmes separat, jf. BUG-378), og samtlige `snprintf`-kaldesteder (st_logic_config.cpp, st_logic_engine.cpp) bruger allerede `sizeof(prog->last_error)` fremfor et hardkodet tal — stoerrelsen skalerer automatisk uden yderligere aendringer. **Tillige** (samme audit, dokumentations-gab): SR/RS-latch og SCALE/HYSTERESIS/BLINK/FILTER-signalbehandlings-builtins var fuldt implementeret og virkende, men helt fravaerende fra manualens §8.5 builtin-funktions-tabel — tilfoejet, inkl. et faktuelt korrekt (kode-verificeret) notat om at `SR` her er reset-dominant og `RS` er set-dominant, modsat hvad navnene intuitivt antyder (docs/manual/08_ST_Logic_Programmering.md). **Indirekte live-verificeret v7.9.22.0**: BUG-379s test (en for stor DINT-literal) udloeste en 87-tegns fejlbesked ("Parse error: Parse error at line 4: Integer literal overflow (DINT range: -2147483648 to 2147483647)") som blev vist i sin FULDE laengde — ville have vaeret afkortet til ~63 tegn i den gamle 64-byte buffer (include/st_logic_config.h, docs/manual/08_ST_Logic_Programmering.md) |
| BUG-383 | `vm->local_base` avancerede aldrig fra 0 — nestede/rekursive brugerdefinerede funktionskald deler samme flade `local_vars[0..63]`-navnerum, kan stille korrumpere en kaldende funktions lokale variable | ✅ FIXED | 🔴 CRITICAL | v7.9.22.0 | **Rodårsag**: `vm->local_base` saettes én gang til 0 i `st_vm_init()` og skrives ALDRIG andetsteds i st_vm.cpp (bekraeftet ved udtoemmende grep) — hver funktions egne lokale/parameter-numre genstarter uafhaengigt fra 0, saa en kaldende funktions lokale variabel ved indeks N kan stille overskrives af en kaldet funktions egen lokale/parameter ved samme indeks N, uden retablering ved retur. **Fix**: `st_call_frame_t.local_count` (et felt der KUN nogensinde blev skrevet til 0 med kommentaren "Will be set by function prologue" — men aldrig laest af noget, reelt dødt) genbrugt til `saved_local_base`. `ST_OP_CALL_USER` beregner nu et nyt `local_base`-vindue for hvert kald: 0 ved topniveau-kald, ellers den KALDENDE funktions egen `local_base` plus dens eget lokale-variabel-antal (`func_registry->functions[...].instance_size` — allerede beregnet og gemt af compileren for hver funktion, oprindeligt kun brugt til FB-tilstands-persistens, men reelt er det "antal lokale variable" for enhver brugerfunktion). Bounds-tjekket ny total mod de 64 slots, med samme fejlbesked-moenster som eksisterende STORE_LOCAL/LOAD_LOCAL. `ST_OP_RETURN` gendanner `vm->local_base` fra den gemte vaerdi — EFTER FB-tilstands-gemme-blokken (som stadig skal bruge DENNE kald-frames egen local_base). **Live-testet v7.9.22.0 paa 10.1.1.153**: `FUNCTION Wrapper` med lokal `before:=base+1000;` der kalder `ParamEcho(base)` (en NESTED brugerfunktion med sin EGEN lokale `scratch` ved samme relative indeks 0 — under den gamle bug ville de to dele samme `local_vars[]`-slot), derefter `after:=before+1;` (slot 3). For `base=5`: `wrapAfter=1006` (=5+1000+1, korrekt — `before` overlevede det nestede kald uden at blive korrumperet); for `base=20`: `wrapAfter=1021` (=20+1000+1) — begge korrekte, bekraefter fixet virker for forskellige input, ikke bare tilfaeldigt (src/st_vm.cpp) |
| BUG-384 | Skrivning til en VAR_OUTPUT/VAR_IN_OUT/VAR_INPUT-parameter blev fejlagtigt routet til `ST_OP_STORE_LOCAL` med parameterens indeks genbrugt som et LOKALT indeks — usynlig for efterfoelgende laesninger af sig selv, kan korrumpere en urelateret lokal variabel med samme indeks | ✅ FIXED (delvist — se detaljer) | 🔴 CRITICAL | v7.9.22.0 | **Rodårsag**: `st_compiler_emit_load_symbol()`/`st_compiler_emit_store_symbol()` (st_compiler.cpp) havde en asymmetri — LOAD for en parameter emitterede altid `ST_OP_LOAD_PARAM` (laeser fra kalderens stack-frame parameter-omraade), men STORE for SAMME parameter emitterede `ST_OP_STORE_LOCAL` (skriver til et HELT ANDET `local_vars[]`-array, med parameterens indeks misbrugt som et lokalt-variabel-indeks). Modsiger direkte FEAT-003s changelog-paastand om "VAR_INPUT, VAR_OUTPUT, VAR ... fuldt implementeret". **Fix (delvist)**: nyt opkode `ST_OP_STORE_PARAM` tilfoejet, der spejler `ST_OP_LOAD_PARAM`s adressering (skriver til `vm->stack[frame->param_base + param_index]` — samme slot LOAD laeser fra). Compileren emitterer nu dette for ALLE parameter-stores. Dette retter den INTERNE synlighed: en omtildelt parameter er nu korrekt synlig for efterfoelgende laesninger af sig selv INDENFOR samme kald, og korrumperer ikke laengere en urelateret lokal variabel — gaelder for VAR_INPUT, VAR_OUTPUT og VAR_IN_OUT ens. **IKKE rettet i denne omgang**: fuld caller-synlig writeback (at en aendret VAR_OUTPUT/VAR_IN_OUT-parameter rent faktisk kommer tilbage til KALDERENS variabel efter RETURN) — dette kraever helt ny kaldesyntaks for brugerdefinerede FUNCTION/FUNCTION_BLOCK (den eksisterende `=>`-syntaks er i dag hardkodet til kun at genkende de indbyggede FB-typer TON/TOF/TP/CTU/CTD/CTUD via `is_known_fb()`), plus parameter-qualifier-metadata i function-registry'et og en aendring af RETURN's call-ABI til at propagere flere vaerdier tilbage — vurderet at vaere en selvstaendig, stoerre feature der kraever egen dedikeret design/parser/compiler/VM-gennemgang, IKKE noget der boer haastes ind i denne bugfix-batch givet risikoen for at forstyrre Logic1s koerende kald-mekanik. **Bytecode-cache-sikkerhed**: det nye opkode indsat MIDT i `st_opcode_t`-enum'en forskyder den numeriske vaerdi af alle efterfoelgende opkoder — `ST_BYTECODE_VERSION` bumpet 3→4 (`include/st_bytecode_persist.h`) saa alle gamle cachede `.bc`-filer (inkl. evt. Logic1/2/3/4s) automatisk forkastes og genkompileres fra kildekode ved naeste boot, i stedet for at blive fejlfortolket med den nye opkode-kodning. **Live-testet v7.9.22.0 paa 10.1.1.153**: `FUNCTION ParamEcho : INT` med `VAR_INPUT val: INT; END_VAR VAR scratch: INT; END_VAR` — `scratch:=111; val:=val+1; ParamEcho:=val+scratch;` (slot 3, kaldt direkte som `directEcho := ParamEcho(baseIn);`). For `baseIn=5`: `directEcho=117` (=5+1+111, korrekt — foer fixet ville resultatet have vaeret forkert, fordi `val`s egen parameter-indeks 0 kolliderede med `scratch`s lokale indeks 0 i den gamle fejlkode); for `baseIn=20`: `directEcho=132` (=20+1+111) — begge korrekte. Se ogsaa BUG-383s test (samme testprogram, nestet variant) (include/st_types.h, src/st_compiler.cpp, src/st_vm.cpp, src/cli_commands_logic.cpp, include/st_bytecode_persist.h) |
| BUG-386 | ST-programmer med nested IF-statements inde i en CASE-branch kunne crashe/genstarte ENHEDEN under kompilering (ESP_RST_PANIC) — opdaget da brugeren bad om en ændring til Logic1's rigtige `ModbusPLC`-program og en upload til slot 2 hang og crashede enheden 9 gange i træk | ✅ FIXED | 🔴 CRITICAL | v7.9.23.0 | **Rodårsag**: `st_compiler_compile_node()` kompilerede resten af en statement-liste (`node->next`) via SELV-REKURSION i bunden af funktionen — hver eneste sekventielle sætning i en liste lagde ét ekstra C-stack-frame oveni. For korte/flade test-programmer (alt hvad der var testet i denne session indtil nu) var det uden betydning, men et REELT program med nested IF'er inde i en CASE-branch (brugerens egen `ModbusPLC`, trin 4: `IF ok THEN ... IF counter<1 THEN...ELSE...END_IF; ... ELSE ... IF writeWaitCounter>=20 THEN ... IF counter<1 THEN...END_IF; ... END_IF; END_IF;`) akkumulerede nok stack-dybde (statement-liste-laengde ganget med nested-IF-genindtraeden) til at overskride httpd-worker-taskens 8KB stak — observeret som enheden hang i ca. 5-30 sekunder (afhaengig af hvor slemt stakken blev korrumperet foer noget observerbart skete) og derefter en `esp_task_wdt`-panic/reboot. **Ingen eksisterende dybde-guard fangede dette**: parserens `ST_MAX_RECURSION_DEPTH`-tjek (32) begraenser AST'ens NESTING-dybde, men sagde intet om en statement-listes LAENGDE, som denne rekursion var proportional med. **Fix**: omskrevet fra rekursion til iteration — `while (node) { ...switch(node->type)...; node = node->next; }` — kontrolstrukturer (IF/CASE/FOR/etc.) rekurserer stadig ind i deres EGNE kroppe via deres dedikerede compile-funktioner (denne dybde er stadig bundet af parserens 32-niveau-graense og forblev uaendret), men en kaede af soskende-saetninger koster nu 0 ekstra stack-frames uanset laengde. **Live-verificeret v7.9.23.0 paa 10.1.1.153**: brugerens fulde, udvidede `ModbusPLC`-program (32 variable, 236 instruktioner) kompilerede paa 1,1 sekund (foer: hang 20-60 sekunder, derefter panic hver eneste gang); 6 tidligere paalideligt-crashende testprogrammer (inkl. den mindste isolerede reproduktion: én CASE-branch med 3 niveauers nested IF) kompilerede alle succesfuldt gentagne gange i traek (11 forsoeg, 0 fejl efter fixet). **Vigtigt forbehold**: ÉT enkelt gentaget forsoeg (umiddelbart efter en frisk OTA-reboot, indenfor de foerste ~15-20 sekunder) fejlede stadig én gang midt i test-serien — mistaenkt relateret til boot-tids-initialisering der stadig var i gang, IKKE reproducerbart ved normal drift (5 paafoelgende forsoeg af samme fil, med normal pacing, lykkedes alle). Overvejede at oege httpd-worker-taskens stak-stoerrelse (8192 byte, `http_server.cpp`) som ekstra sikkerhedsmargin, men undlod bevidst — en tidligere BUG-369 dokumenterer at ekstra stak her direkte konkurrerer med mbedTLS's HTTPS-session-hukommelse, saa den rigtige fix er at REDUCERE compilerens stakforbrug (denne fix), ikke oege budgettet. Logic1 (brugerens beskyttede produktionsprogram) upaavirket gennem alle 9 crashes og den efterfoelgende verifikation — kom hver gang tilbage med 0 fejl og fortsatte eksekvering (src/st_compiler.cpp) |
| BUG-387 | Bruger oplevede at blive spurgt om login "to steder" ved login (browserens EGEN native Basic-Auth-popup oveni web-GUI'ens eget custom login-modal) | ✅ FIXED | 🟠 MEDIUM | v7.9.24.0 | **Rodårsag**: `api_send_error()` (api_handlers.cpp) satte ubetinget `WWW-Authenticate: Basic`-headeren paa ALLE 401-svar, uanset om requesten allerede baere et Authorization-header. Browsere viser deres EGEN native login-popup paa ETHVERT 401-svar med denne header — uanset om requesten allerede indeholdt Bearer/Basic-credentials. Web-GUI'ens `doLogin()`-flow (samme moenster paa alle 7 sider) sender allerede sin egen `Authorization: Basic`-header og har sit eget custom modal til at vise fejl ("Forkert brugernavn eller adgangskode") — men et 401-svar paa DET kald (forkert kodeord) udloeste ALLIGEVEL browserens native popup oveni. Samme problem ramte ogsaa den stille baggrunds-tjek af en gemt session-token (`sessionStorage`) ved sidegenindlaesning — session-tokens er RAM-only og forsvinder ved ENHVER enhedsgenstart (etableret adfaerd, BUG-353), saa efter en reboot (fx under denne sessions omfattende OTA-flash/crash-testning) ville et gammelt gemt token altid give 401 ved naeste sidebesoeg, og dermed altid trigge den forvirrende native popup. **Fix**: `WWW-Authenticate` sendes nu KUN naar requesten slet ingen `Authorization`-header havde (en reelt anonym forespoergsel — fx et rent curl-kald uden `-u`, eller et direkte browser-adressefelt-besoeg paa en API-URL, hvor browserens native prompt rent faktisk er den forventede/nyttige opfoersel). En klient der ALLEREDE sendte Authorization (uanset Basic eller Bearer, uanset om den var forkert/udloebet) har lavet sit eget forsoeg og har sin egen UI til fejlen — braehver derfor ikke browserens ekstra native udfordring. Bygget og verificeret (`pio run -e es32d26` SUCCESS) (src/api_handlers.cpp) |
| BUG-388 | Login virkede ikke fra mobil browser (Safari) selv med korrekt kodeord — brugernavn-feltet manglede `autocapitalize="off"` paa alle 7 sider | ✅ FIXED | 🟠 MEDIUM | v7.9.25.0 | **Rodårsag**: `<input type="text" id="authUser">`-feltet i login-modalen (identisk paa alle 7 sider — dashboard/editor/system/ota/cli/io/logs) havde ingen `autocapitalize`-attribut. Mobile browsere (Safari paa iOS bekraeftet af brugeren, men gaelder generelt for mobile tekstfelter uden eksplicit override) versal-forbogstaverer som standard det FOERSTE tegn i et almindeligt tekstfelt — saa "admin" blev tastet men rent faktisk sendt som "Admin". Brugernavn-sammenligningen (`rbac.cpp`, flere steder: `strcmp()`) er versalfoelsom med vilje (samme som Unix-brugernavne) — saa selv et 100% korrekt kodeord fejlede, fordi BRUGERNAVNET blev subtilt aendret af telefonens tastatur foer det naaede serveren. Opdaget efter at et kodeords-nulstilling af `admin`-kontoen (verificeret virkende via curl fra en almindelig computer) STADIG ikke loeste brugerens login-problem paa mobil — pegede direkte paa klientsiden, ikke serveren/NVS (som allerede var bekraeftet intakt). **Fix**: tilfoejet `autocapitalize="off" autocorrect="off" spellcheck="false"` til brugernavn-feltet paa alle 7 sider — forhindrer telefonens tastatur i at aendre input, i stedet for at goere server-side sammenligningen versal-ufoelsom (ville aendre semantikken af brugernavne generelt for en smal mobil-UX-fejl). Bygget og verificeret (`pio run -e es32d26` SUCCESS) (web/dashboard.html, web/editor.html, web/system.html, web/ota.html, web/cli.html, web/io.html, web/logs.html) |
| BUG-389 | `/editor` (og `/cli`) bad gentagne gange om login "straks ved sidens indlæsning", ogsaa fra almindelig PC-browser hvor det havde staaet paa i laengere tid — login virkede altid ved manuelt forsoeg bagefter | ✅ FIXED | 🟠 MEDIUM | v7.9.26.0 | **Rodårsag (to bidragende fejl)**: (1) `/editor` og `/cli` er de eneste 2 af de 7 sider der proaktivt verificerer en gemt session-token ved sideindlaesning (via `GET /api/status`, indfoert af BUG-370 — de oevrige 5 sider tjekker aldrig proaktivt, kun naar en reel handling fejler med 401). Denne proaktive tjek-kode behandlede en NETVAERKS-fejl (forbindelse afvist/nulstillet/timeout — fx enhedens begraensede samtidige-socket-pulje midlertidigt opbrugt af andre aabne faner der poller i baggrunden) PRAECIS SOM en reel 401-afvisning fra serveren — begge udloeste ubetinget login-modalen, selvom et netvaerks-glimt intet siger om hvorvidt den GEMTE token rent faktisk er gyldig. Ramte baade PC og mobil, fordi det er en ren netvaerks-/logik-fejl, ikke en browser-specifik kvirk. (2) Selv naar en gyldig token blev fundet og gemt (`sessionStorage`), er `sessionStorage` pr.-fane/pr.-proces — mobile browsere (Safari paa iOS bekraeftet af brugeren) kan lægge en baggrunds-fane i dvale og reelt genindlaese siden med tom lager-tilstand naar man vender tilbage til den, hvilket ser ud som "spurgt om login hver gang jeg åbner siden igen" selvom man aldrig aktivt loggede ud. **Fix**: (1) `.catch()`-grenen paa den proaktive `/api/status`-tjek (editor.html + cli.html) skelner nu mellem de to fejltyper — findes der allerede en gemt token, stoles der paa den og siden initialiseres optimistisk i stedet for at tvinge login frem; en REELT ugyldig token opdages stadig korrekt af sidens egen almindelige 401-haandtering paa allerfoerste rigtige API-kald bagefter. (2) Session-tokenet (nøglen `hfplc_auth`) er flyttet fra `sessionStorage` til `localStorage` paa alle 7 sider — overlever fane-dvale/genindlaesning paa mobil, siden det er disk-bagvedliggende og bundet til ORIGIN i stedet for fane/proces. Sikkerhedsafvejning bevidst accepteret: session-tokens er allerede RAM-only paa enheden og bliver ugyldige ved HVER genstart (BUG-353) — `localStorage`s laengere levetid client-side aendrer derfor reelt intet ved den faktiske angrebsflade, kun bekvemmeligheden af at forblive logget ind paa tvaers af fane-genbrug. Bygget og verificeret (`pio run -e es32d26` SUCCESS) (web/editor.html, web/cli.html, web/dashboard.html, web/system.html, web/ota.html, web/io.html, web/logs.html) |
| BUG-390 | `/cli`s kommando-prompt (og potentielt bunden af alle 7 sider) usynlig i bunden af skærmen på mobil browser | ✅ FIXED | 🟠 MEDIUM | v7.9.27.0 | **Rodårsag**: alle 7 sider bruger `body{height:100vh;...overflow:hidden}` til en fixed-height flex-kolonne-layout (topnav/header foroven, scrollbart indhold i midten, en bund-forankret raekke — for `/cli` konkret kommando-prompt-input'et — sidst). `100vh` paa mobile browsere regnes ud fra det STOERST mulige viewport (browser-kromets adresselinje/vaerktoejslinje kollapset/skjult), IKKE hvad der faktisk er synligt naar den vises — saa `body` blev reelt HOEJERE end den synlige skaerm, og den bund-forankrede raekke (kommando-promptet) endte under kanten, usynligt. `overflow:hidden` paa `body` forhindrede samtidig ethvert forsoeg paa at scrolle siden ned for at naa den. **Fix**: tilfoejet `height:100dvh` (dynamic viewport height — sporer det FAKTISK synlige omraade som browser-kromet vises/skjules) som en ANDEN, senere `height`-deklaration efter den oprindelige `100vh` paa alle 7 sider — browsere der stoetter `dvh` bruger automatisk den (senere deklaration vinder), browsere der ikke goer, falder tilbage til `100vh` uaendret. Bygget og verificeret (`pio run -e es32d26` SUCCESS) (web/cli.html, web/dashboard.html, web/editor.html, web/system.html, web/ota.html, web/io.html, web/logs.html) |
| FEAT-173 | "Modbus RTU Trafik"-kortet opdelt i to tydelige Slave/Master-sektioner i stedet for sammenblandede raekker | ✅ DONE | 🔵 LOW | v7.9.28.0 | Bruger-oensket omstrukturering. Tidligere blandede kortet Slave- og Master-metrikker i samme raekker (fx `CRC fejl (slave/master)` viste et kombineret "3 / 5"-felt, samme for Exceptions), hvilket gjorde kortet svaert at laese med ét blik. **Fix**: kortet har nu en `Slave`-sektion (rate, success rate, CRC fejl, exceptions, sparkline) og en `Master`-sektion (samme felter plus timeouts, som kun findes master-side) adskilt af en let synlig undersektions-overskrift (`.subhdr`, stor bogstaver, top-border foran Master-sektionen). De to tidligere kombinerede felter (`rtCrcBoth`, `rtExcBoth`) er splittet til separate `rtSlaveCrc`/`rtMasterCrc` og `rtSlaveExc`/`rtMasterExc` — de underliggende Prometheus-metrikker var allerede separate pr. slave/master, kun VISNINGEN kombinerede dem. Bygget og verificeret (`pio run -e es32d26` SUCCESS) (web/dashboard.html) |
| BUG-392 | Login-vindue lukkede aldrig efter et vellykket login (bekræftet: gyldigt kodeord accepteret server-side, men klienten blev "hængende" på login-skærmen) — ramte Safari, formentlig privat browsing-tilstand | ✅ FIXED | 🔴 CRITICAL | v7.9.29.0 | **Rodårsag**: Safari (saerligt i privat browsing) KASTER en exception ved `localStorage.setItem()` i stedet for at fejle stille som Chrome/Firefox goer. `doLogin()`s succes-haandtering paa alle 7 sider kaldte `localStorage.setItem('hfplc_auth', 'Bearer '+token)` FOeR den lukkede login-modalen og opdaterede brugerbadgen, i samme statement-kaede — naar `setItem` kastede en exception, stoppede HELE resten af den kaede med at koere, saa modalen ALDRIG lukkede, selvom login rent faktisk lykkedes paa serveren (verificeret direkte med curl mens fejlen stod paa). Introduceret ved den forrige commit (v7.9.26.0/BUG-389), der flyttede tokenet fra `sessionStorage` til `localStorage` netop for at loese et ANDET login-problem — uden at forudse at `localStorage`-skrivning selv kunne fejle. **Fix**: indfoert `authGet()`/`authSet()`/`authClear()`-wrapper-funktioner paa alle 7 sider der ALTID holder en in-memory-fallback-variabel synkroniseret FOeR de proever at persistere til `localStorage` i et `try/catch` — lykkes persisteringen ikke, virker login stadig for den AKTUELLE side-session (samme som foer nogen persistens fandtes), brugeren skal blot logge ind igen ved naeste sideindlaesning i stedet for at hele login-flowet crasher stille. Bygget og verificeret (`pio run -e es32d26` SUCCESS, `node --check` paa alle 7 siders udtrukne JS) (web/dashboard.html, web/editor.html, web/system.html, web/ota.html, web/cli.html, web/io.html, web/logs.html) |
| BUG-389b | BUG-389s "netvaerksfejl behandlet som 401"-fix (fail-open paa proaktivt login-tjek) blev kun anvendt paa 2 af 5 beroerte sider — `/system`, `/io`, `/logs` havde samme bug, overset fordi de bruger `$('loginModal')`-genvejen i stedet for `document.getElementById('loginModal')`, som den oprindelige soegning kun daekkede | ✅ FIXED | 🟠 MEDIUM | v7.9.29.0 | Samme rodårsag og fix som BUG-389 — proaktivt sidestart-tjek behandlede en netvaerksfejl (ikke en reel 401) som "log ind igen", tvang login-vinduet frem selv med en gyldig token. Fundet ved at soege bredere efter moenstret paa tvaers af alle 7 sider (inkl. `$()`-genvejen) i forbindelse med BUG-392-undersoegelsen. Bygget og verificeret (web/system.html, web/io.html, web/logs.html) |
| BUG-393 | Login virkede stadig ikke paalideligt paa iOS Safari efter BUG-387/388/389/389b/390/392 — brugeren bad om en dyb rodaarsagsanalyse i stedet for endnu en punkt-rettelse. Separat rapporteret: gammelt data blev staaende paa skaermen efter logout paa PC | ✅ FIXED (arkitektur-aendring) | 🔴 CRITICAL | v7.9.30.0 | **Rodårsag**: den grundlaeggende model var at JavaScript selv skulle huske login-tilstanden (gemme et Bearer-token i `localStorage`, vedhaefte det manuelt paa hver request). Det kraever at MASSER af browser-tilstand overlever korrekt — og der er mindst 5 forskellige maader iOS Safari kan forstyrre/nulstille netop den tilstand: privat browsing kaster ved `localStorage`-skrivning (BUG-392), baggrundsfaner kan genindlaeses med tom JS-tilstand, "Foej til hjemmeskaerm" bruger en SEPARAT lagerkontekst fra almindelig Safari, Safaris EGEN indbyggede Basic-Auth-credential-cache er en helt separat mekanisme uden for JS'ens kontrol (kan gensende et GAMMELT/forkert gemt kodeord automatisk), og password-autofill kan indsaette forkerte gemte kodeord. Punkt-rettelser af SYMPTOMER (BUG-387/388/389/389b/390/392) ramte aldrig alle disse aarsager samtidig — deraf at brugeren blev ved med at opleve "samme fejl igen". Den separat rapporterede logout-fejl havde sin egen, relaterede rodaarsag: `doLogout()` ryddede kun selve token'et, men stoppede ALDRIG de op til 8 `setInterval`-baggrundstimere (flere af dem gemte end ikke deres eget timer-handle, kunne derfor slet ikke `clearInterval()`'es individuelt) eller ryddede det allerede-viste data paa skaermen. **Fix (arkitektur-aendring, ikke endnu en punkt-rettelse)**: erstattet den JS-styrede Bearer-token-model med en **server-sat, HttpOnly session-cookie**. `POST /api/login` saetter nu `Set-Cookie: hfplc_session=<token>; Path=/; Max-Age=1800; SameSite=Lax; HttpOnly` (samme token som allerede blev udstedt via `rbac_session_token_issue()` — kun DELIVERY-mekanismen aendret). `rbac_check_http()` (rbac.cpp, DET ene centrale sted alle 70+ `CHECK_AUTH`/`CHECK_AUTH_WRITE`-endpoints allerede gaar igennem) tjekker nu cookien som fallback naar der slet ingen `Authorization`-header er — ny funktion `rbac_extract_cookie_token()`. **100% bagudkompatibelt**: en `Authorization`-header (Basic ELLER Bearer) fortsaetter uaendret ad den eksisterende vej — curl/Node-RED/scripts der bruger Bearer-token direkte er upaavirkede, verificeret eksplicit. Browseren sender og opbevarer cookien HELT AUTOMATISK — INGEN `localStorage`, INGEN manuel `Authorization`-header-vedhaeftning tilbage paa nogen af de 7 sider, INGEN client-side tilstand der kan gaa tabt. Samtidig fjernet de proaktive "tjek session ved sideindlaesning"-blokke paa de 5 sider der havde BUG-389/389b HELT (editor/cli/system/io/logs) — i stedet kaldes data-indlaesning ubetinget, og et evt. 401 fra det foerste rigtige kald viser login-modalen via den SAMME kodesti som al anden 401-haandtering; strukturelt immun overfor hele BUG-389/389b-fejlklassen fremover, ikke bare rettet denne ene gang. `doLogout()` paa alle 7 sider POST'er nu ubetinget til `/api/logout` (cookien er HttpOnly, JS kan ikke se om der er en) og kalder derefter `location.reload()` — loeser stale-data-efter-logout-fejlen ved at nulstille ALT (stopper samtlige timere, rydder alt vist data) i stedet for at spore/rydde hvert element manuelt. **Verificeret trin for trin foer klientsiden overhovedet blev roert** (server alene via curl foerst): login udsteder Set-Cookie ✓, kun-cookie-adgang (ingen Authorization-header) giver 200 ✓, ugyldig cookie giver korrekt 401 ✓, det GAMLE Bearer-flow (al denne sessions egen curl-testning) upaavirket ✓, logout revoker token OG rydder cookien (Max-Age=0) ✓, den revokerede cookie afvises korrekt bagefter ✓. Alle 7 klientsider `node --check`'et efter aendringerne. Logic1 upaavirket gennem hele processen. **Eksplicit udenfor scope**: `rbac_check_sse()` (separat, allerede-eksisterende Basic-Auth-kun-mekanisme for SSE-stroemmen) roert ikke. Bygget og verificeret (`pio run -e es32d26` SUCCESS) (include/rbac.h, src/rbac.cpp, src/api_handlers.cpp, web/dashboard.html, web/editor.html, web/system.html, web/ota.html, web/cli.html, web/io.html, web/logs.html) |
| BUG-393b | `/io`-siden viste "Ikke logget ind" i brugerbadgen selvom man faktisk var logget ind (cookien var gyldig) — en regression introduceret AF BUG-393s egen fix, opdaget umiddelbart efter at BUG-393 blev bekraeftet loest | ✅ FIXED | 🟠 MEDIUM | v7.9.31.0 | **Rodårsag**: to samvirkende fejl indfoert af BUG-393s fjernelse af den gamle "proaktive login-tjek"-blok paa 5 sider (editor/cli/system/io/logs). (1) `updateUserBadge()`s `fetch('/api/user/me')` var en RAA, ukoeet forespoergsel — mens ALLE andre kald paa siden gaar gennem en delt `queuedFetch()`-koe med et haardt loft paa samtidige forbindelser (etableret i BUG-369, en tidligere ALVORLIG hændelse hvor for mange samtidige TLS-handshakes fik enhedens heap til at styrtdykke fra ~132KB til under 36KB). BUG-393s nye bootstrap-kode kaldte `loadAll()`/`init()`/etc. (som internt fyrer en BYGE af koeede kald) og `updateUserBadge()` synkront lige efter hinanden — `updateUserBadge()`s ukoeede forespoergsel konkurrerede dermed direkte med byrden fra de koeede kald om enhedens faa samtidige forbindelser og kunne tabe det kapløb, hvorefter dens tomme `.catch(()=>{})` stille slugte fejlen og lod badgen staa paa sin statiske "Ikke logget ind"-standardtekst. (2) `updateUserBadge()` blev desuden kaldt EFTER de andre indlaesningskald i samme linje/blok — hvis ÉT af dem kastede en synkron fejl, ville `updateUserBadge()` (og alt andet efter) aldrig blive kaldt overhovedet. `dashboard.html` havde allerede loest netop dette (BUG-369s egen etablerede loesning: alle side-indlaesningskald spredt ud via `setTimeout` for aldrig at ramme mere end 1-2 samtidige forbindelser) — de 5 nye sider fik ikke samme beskyttelse ved BUG-393s omskrivning. **Fix**: `updateUserBadge()` paa alle 7 sider ruter nu gennem sidens egen etablerede koe-mekanisme (`queuedFetch()`, eller `dashFetch()` paa dashboard.html) i stedet for en raa `fetch()`. Desuden kaldes `updateUserBadge()` nu FOeRST (foer alle andre side-indlaesningskald) paa alle steder den optraeder — baade ved almindelig sideindlaesning og efter et vellykket login — saa den er strukturelt immun overfor at en SENERE fejlende funktion nogensinde kan forhindre badgen i at blive opdateret. Bygget og verificeret (`pio run -e es32d26` SUCCESS, `node --check` paa alle 7 sider) (web/dashboard.html, web/editor.html, web/system.html, web/cli.html, web/io.html, web/logs.html) |
| BUG-394 | `/io`-siden blev ved med at vise "Ikke logget ind" selvom brugeren var logget ind — rapporteret IGEN efter BUG-393b's fix var live paa enheden, dvs. en tredje, endnu ikke fundet aarsag til samme symptom | ✅ FIXED (formodet — afventer brugerbekraeftelse) | 🟠 MEDIUM | v7.9.32.0 | **Undersoegelse**: verificerede foerst at BUG-393b's fix rent faktisk var live paa enheden (hentede den servrede `/io`-side direkte og bekraeftede koden var den nye, koeede `updateUserBadge()`-version), og at cookie-baseret auth virker korrekt server-side end-to-end (curl-simulering: login saetter `Set-Cookie`, et efterfoelgende kald med KUN cookien — ingen Authorization-header — giver `authenticated:true`). Server og JS-kode var altsaa begge korrekte, saa fejlen maatte ligge et tredje sted. **Formodet rodaarsag**: alle 7 sider sendes med `Cache-Control: no-cache` UDEN `ETag`/`Last-Modified` — revalidering er dermed umulig, hvilket lader iOS Safaris bfcache (Back-Forward Cache, aktiveres ved tilbage/frem-navigation eller faneskift) gendanne en TIDLIGERE indlaest udgave af siden komplet med dens allerede-eksekverede JS-tilstand fra FOeR login, i stedet for at genindlaese siden og koere `updateUserBadge()` paa ny med den aktuelt gyldige cookie — hvilket ville se PRAECIS ud som "logget ud, selvom man er logget ind", og ville forklare hvorfor punkt-rettelsen i BUG-393b (som KUN aendrede JS-logikken, ikke hvornaar/om den JS overhovedet koeres igen) ikke loeste symptomet. **Fix**: `Cache-Control` aendret fra `no-cache` til `no-store` paa alle 7 sider — forhindrer baade almindelig HTTP-caching OG goer siderne bfcache-ineligible i WebKit (dokumenteret WebKit-adfaerd: `no-store` udelukker en side fra bfcache), hvilket tvinger en frisk netvaerkshentning + fuld JS-gen-eksekvering ved enhver navigation. Bygget og verificeret (`pio run -e es32d26` SUCCESS) (src/web_io.cpp, src/web_dashboard.cpp, src/web_editor.cpp, src/web_system.cpp, src/web_cli.cpp, src/web_logs.cpp, src/web_ota.cpp) |
| BUG-394b | Brugeren testede BUG-394's fix i BAADE Safari og Firefox paa iOS — samme "logget ud"-symptom paa /io i begge, hvilket udelukkede en browser-specifik bfcache-aarsag og afsloerede at BUG-394 var en fejlspor | ✅ FIXED (reel rodaarsag) | 🔴 CRITICAL | v7.9.33.0 | **Rodårsag**: en ren JavaScript-sprogfejl — temporal dead zone (TDZ) — introduceret af mig selv i BUG-393b, og til stede paa BAADE /io, /system OG /logs (kun /io var rapporteret, men alle tre havde identisk fejlmoenster). BUG-393b flyttede `updateUserBadge()` til at vaere det FOeRSTE kald i sidens bootstrap-sekvens ("kald badge-opdatering foer alt andet, saa en senere fejl aldrig kan blokere den") — men paa disse 3 sider staar `let _apiFetchActive`/`const _apiFetchQueue` (som `queuedFetch()` laeser) LAENGERE NEDE i samme script, EFTER det nye kaldested. `let`/`const` er ikke hoisted som funktionserklaeringer — et forsoeg paa at laese dem FOeR deres deklarationslinje er naaet under eksekvering kaster `ReferenceError: Cannot access '_apiFetchQueue' before initialization`. Fordi fejlen opstaar INDE i `queuedFetch()`s `new Promise((resolve,reject)=>{...})`-executor, konverterer Promise-konstruktoren den automatisk til et REJECTED promise i stedet for en synkron exception — scriptet fortsaetter uden synlig fejl i konsollen, men `updateUserBadge()`s (og reelt ogsaa `loadAll()`/`refreshInfo()`/`fetchLogs()`-etc.'s) afsluttende `.catch(()=>{})` sluger fejlen helt lydloest. Resultat: badgen (og reelt HELE sidens initiale data-indlaesning — counters, timere, GPIO-mappings, systeminfo, logs) fejler stille ved hver sideindlaesning, uanset browser — dette er IKKE en Safari/iOS-kvirk, det er standard ECMAScript-semantik og reproducerer 100% af tiden i enhver JS-motor, hvilket forklarer hvorfor brugeren saa praecis samme symptom i baade Safari og Firefox paa samme enhed. Bekraeftet med en isoleret Node.js-minimalreproduktion af det praecise moenster foer rettelsen blev lavet. BUG-394 (Cache-Control no-store) var en rimelig, men forkert teori — den forbliver som en generel robusthedsforbedring, men loeste ikke dette symptom. **Fix**: flyttede hele koe-blokken (`_apiFetchActive`/`_apiFetchQueue`/`_apiFetchPump`/`queuedFetch`/`api`) til FOeR bootstrap-kaldet paa alle 3 sider (samme raekkefoelge som allerede var korrekt paa editor.html/cli.html, som derfor aldrig var ramt). `dashboard.html` bekraeftet upaavirket — dens `updateUserBadge()`-kald koerer via `setTimeout(...,840)`, som altid affyres EFTER hele det synkrone script (inkl. alle `let`/`const`-deklarationer) er faerdigeksekveret. Verificeret: `node --check` paa alle 3 udtrukne scripts OK, ingen duplikerede deklarationer efter flytningen. Bygget og verificeret (`pio run -e es32d26` SUCCESS) (web/io.html, web/system.html, web/logs.html) |
| BUG-395 | "Log ud"-knappen havde ingen effekt paa iOS Safari/Firefox — server-side blev cookien bekraeftet korrekt ryddet (curl end-to-end-test: `Set-Cookie: hfplc_session=; Max-Age=0` modtaget, efterfoelgende kald med samme cookie gav korrekt `authenticated:false`), saa fejlen kunne ikke vaere i login/logout-logikken selv | ✅ FIXED (server-side rodaarsag) — ⚠️ kraever OGSAA en enkelt manuel brugerhandling for allerede-ramte enheder | 🔴 CRITICAL | v7.9.34.0 | **Rodårsag**: `api_send_error()` sendte stadig `WWW-Authenticate: Basic realm="Modbus ESP32"` paa 401-svar naar requesten slet ingen `Authorization`-header havde (BUG-387s begraensning fra tidligere — kun sat naar helt kreditiv-loes). Denne situation rammer web-GUI'et regelmaessigt: enhver sideindlaesning UDEN gyldig cookie endnu (foerste besoeg, eller efter en reboot der invaliderer alle RAM-only sessions — sket adskillige gange i denne session ved hver OTA-flash) udloeser den. Naar en browser modtager `WWW-Authenticate`, viser den sin EGEN native Basic-Auth-login-popup — og hvis brugeren (som under denne sessions tidlige BUG-387-fejlsoegning, hvor native popuppen dukkede op igen og igen) taster kodeord i DEN popup i stedet for GUI'ets eget modal, CACHER Safari/Firefox-iOS det credential permanent i deres interne Basic-Auth-butik, komplet uden for JavaScript'ens kontrol. Fra det tidspunkt vedhaefter browseren AUTOMATISK en gyldig `Authorization: Basic ...`-header paa ALLE fremtidige requests til denne origin/realm — inklusiv requesten der foelger et logout+reload. `rbac_check_http()` accepterer MED VILJE en Authorization-header uaendret (bagudkompatibilitet for curl/Node-RED, se BUG-393), saa denne browser-genererede header re-autentificerer brugeren fuldstaendigt, uafhaengigt af cookien — logout ser dermed ud til at have "ingen effekt", selvom cookie-ryddet server-side fungerer 100% korrekt (bekraeftet). HTTP Basic-Auth har strukturelt INGEN logout-mekanisme — et cachet credential i browseren kan ikke ryddes fra serversiden. **Fix**: `WWW-Authenticate`-headeren er nu fjernet HELT fra `api_send_error()`. Web-GUI'et har intet behov for browserens native popup laengere — det har sit eget fuldt fungerende cookie-baserede login-modal (BUG-393) — og curl/Node-RED-klienter der bruger `-u`/Basic-Auth-header sender den allerede proaktivt uden at vente paa denne udfordring, saa de er upaavirkede. Dette forhindrer browsere i nogensinde at faa chancen for at cache et credential paa denne maade FREMOVER. **VIGTIGT**: denne rettelse forhindrer NYE tilfaelde, men kan ikke rydde et credential der allerede ER cachet paa brugerens enhed fra tidligere i sessionen — det kraever en enkelt manuel handling paa selve enheden (iOS Indstillinger → Adgangskoder, eller Firefox → Indstillinger → Logins, soeg "10.1.1.153", slet den gemte adgang) foer logout virker normalt igen paa den specifikke enhed. Bygget og verificeret (`pio run -e es32d26` SUCCESS) (src/api_handlers.cpp) |
| BUG-396 | Systemgennemgang (brugeroensket totalanalyse i to faser) fandt en NY, hidtil upaaragtet TDZ-ordering-bug paa /io — samme fejlklasse som BUG-394b, men ramte HELE sidens data-load synkront i stedet for kun badgen | ✅ FIXED | 🔴 CRITICAL | v7.9.35.0 | Fundet af en Explore-agent under en efterspurgt statisk kodegennemgang, verificeret af mig selv (`node --check`+manuel linjetjek) foer rettelse. **Rodårsag**: `loadAll()` kaldes synkront ved bootstrap (linje 269) -> `buildCards()` -> `counterCardHtml()`/`timerCardHtml()`, som laeser `const HW_MODES`/`EDGE_TYPES` (deklareret linje 302-303, LAENGERE NEDE) og `const TIMER_MODES` (deklareret linje 442, ligeledes laengere nede). Modsat BUG-394bs `updateUserBadge()`-variant (fanget af et Promise-`.catch()`) er dette et RENT SYNKRONT kald udenfor nogen Promise — kaster en UFANGET `ReferenceError: Cannot access 'HW_MODES' before initialization`, som stopper HELE resten af top-level-scriptet paa stedet. Konsekvens: hverken counters, timere, GPIO-mappings, heartbeat-status, version-footer ELLER "klik-udenfor-lukker-brugermenu"-listeneren (staar paa linje 281, EFTER det brudte kald) blev nogensinde sat op ved en almindelig sideindlaesning — reelt en langt stoerre fejl end blot login-badgen, som blot var det mest udadtil synlige symptom. **Fix**: de tre `const`-arrays flyttet til foer bootstrap-kaldet (samme princip som BUG-394b) — `counterCardHtml()`/`timerCardHtml()` er funktionserklaeringer (hoisted) og kan uaendret blive staaende hvor de var. `node --check` OK, ingen dubletter. Bygget og verificeret (`pio run -e es32d26` SUCCESS) (web/io.html) |
| BUG-396b | Samme Explore-agent fandt at BUG-395s WWW-Authenticate-fjernelse kun daekkede hoved-API'et (`api_send_error()`) — SSE-endpointerne (/api/events, /api/events/status, /api/events/clients, /api/events/disconnect) sendte stadig headeren uaendret, hvilket kan genskabe PRAECIS samme "logout virker ikke"-symptom via SSE-vejen | ✅ FIXED | 🟡 HIGH | v7.9.35.0 | BUG-393s cookie-migrering dokumenterede eksplicit at `rbac_check_sse()` var UDENFOR SCOPE (separat, allerede-eksisterende Basic-Auth/query-token-kun-mekanisme) — men det betoed ogsaa at BUG-395s fjernelse af WWW-Authenticate aldrig blev udbredt dertil. SSE foretraekker et query-string-token (?token=, til EventSource-kompatibilitet paa tvaers af port), falder tilbage til Basic-Auth-header, og sender ved fejl i BEGGE en 401 MED WWW-Authenticate: Basic realm="Modbus ESP32" — fire steder: den raa haandrullede TCP-socket-401-sti i selve SSE-serveren (sse_events.cpp:821) samt tre httpd-baserede endpoints (api_handler_sse_token/status linje 949, api_handler_sse_clients linje 995, api_handler_sse_disconnect linje 1062). Samme risiko som BUG-395: enhver browser der rammer et af disse UDEN gyldig token/cookie (fx foer login, eller efter en session-udloeb) kan udloese browserens native Basic-Auth-popup og faa den til at cache et credential permanent, uden for JS'ens kontrol. **Fix**: alle 4 forekomster fjernet, samme begrundelse/moenster som BUG-395 — selve auth-tjekket (query-token foerst, Basic-Auth som fallback) er UAENDRET, kun challenge-headeren paa fejlsvaret er fjernet. Bygget og verificeret (`pio run -e es32d26` SUCCESS) (src/sse_events.cpp) |
| BUG-397 | Brugeren bad om en "hensigtsmaessighedsvurdering" af D.7s kendte ST-sprogbegraensninger, og bad derefter om at rette de reelle risici: AND/OR/NOT/XOR tavst booleske uden advarsel, ugyldige hex-radix-literaler tavst fejlfortolket, SHL/SHR paa DWORD uverificeret/formodet 16-bit-begraenset | ✅ FIXED (VM+lexer) | 🔴 CRITICAL | v7.9.36.0 | Tre separate, verificerede sprogfejl rettet i selve VM'en/lexeren, samtlige bekraeftet via dedikerede testprogrammer paa slot 3/4 (Logic1/Logic2 upaavirket gennem hele forloebet). **(1) AND/OR/XOR/NOT**: laeste hidtil ALTID kun .bool_val fra operandernes union, uanset deres faktiske type — "status AND 16#0020" (et idiom set i projektets egen BUGS.md) gav et tavst forkert boolesk resultat i stedet for en bitmaske, ingen fejl, ingen advarsel. Fix: st_vm_exec_and/or/xor/not() (st_vm.cpp) afviser nu eksplicit ikke-BOOL-operander med en klar runtime-fejl ("...requires BOOL operands, use BIT_SET/BIT_CLR/BIT_TST...") — verificeret at gyldig BOOL-brug fortsat virker 100% korrekt (r1..r4 alle korrekte i testprogram), og at ugyldig brug (x AND a hvor x:INT) nu korrekt marker programmet fejlet paa hver scan i stedet for at fortsaette stille. **(2) Radix-literaler** (N#digits/NN#digits, IEC-stil): den gamle lexer-kode matchede kun ÉT ledende ciffer foer '#', DISCARDEDE selve radix-cifferet, og lod resten blive genfortolket som almindelig decimal af den efterfoelgende strtol(...,0) — saa 2#1010 blev tavst til 1010 (ikke binaer 10), og en to-cifret radix som 16#FF blev slet ikke genkendt (faldt igennem til en haard lexer-fejl paa den efterladte '#'). Fix: lexer_read_integer() (st_lexer.cpp) genkender nu 1-2-cifrede radixer 2-16, beregner den faktiske vaerdi selv (strtol(digits, NULL, radix)) og gemmer resultatet som en almindelig decimal-streng (saa alle eksisterende strtol(...,0)-kaldesteder i parseren fortsaetter uaendret) — eller rapporterer en klar fejl ved ugyldige cifre for den angivne radix, i stedet for en tavs fejlberegning. Verificeret: 2#1010→10, 8#17→15, 16#FF→255, 0xFF→255 (uaendret), alle korrekte i samme testprogram. **(3) SHL/SHR paa DWORD**: faldt hidtil igennem til den 16-bit INT-gren (laeste/skrev .int_val, tabte bit 16-31, og ombenaevnede tavst resultatet til INT), fordi kun DINT havde sin egen gren. Fix: ny dedikeret DWORD-gren i baade st_vm_exec_shl()/st_vm_exec_shr() (unsigned 32-bit skift, matcher DWORDs bitmoenster-semantik, skifteomraade 0-31 som DINT). **Denne rettelse kunne foerst reelt verificeres efter BUG-397b (se nedenfor) var rettet** — se den raekke for hvorfor. Verificeret efter: d SHL 20 paa d=1 (DWORD) → 1.048.576, SHR 4 derefter → 65.536, begge korrekte. Rørte: src/st_vm.cpp, src/st_lexer.cpp |
| BUG-397b | Ved forsoeg paa at verificere BUG-397s DWORD SHL/SHR-fix viste testresultater vedvarende forkerte vaerdier (0 i stedet for de korrekte tal) OG forkert type ("INT" i stedet for "DWORD") — sporet til at vaere en SEPARAT, allerede-eksisterende REST-serialiserings-mangel, ikke en fejl i selve rettelsen | ✅ FIXED | 🟠 MEDIUM | v7.9.37.0 | Rodaarsag fundet ved at dumpe raa bytecode (show logic X bytecode, CLI) og bekraefte at COMPILEREN korrekt registrerer DWORD-variable som type=3 (ST_TYPE_DWORD) — dvs. compiler og VM haandterer allerede DWORD korrekt internt. Fejlen laa udelukkende i api_handler_logic_single()s (GET /api/logic/{id}, api_handlers.cpp linje ~1713-1752) to variabel-serialiserings-switches: BEGGE manglede en case ST_TYPE_DWORD helt, saa enhver DWORD-variabel faldt igennem til default og blev vist som type "INT" med vaerdien laest via unionens 16-bit .int_val i stedet for .dword_val — dermed usynliggjorde denne visningsfejl BAADE at BUG-397s SHL/SHR-fix reelt virkede korrekt, OG afsloerede samtidig et MEGET stoerre, hidtil ukendt fund: **DWORD-ARITMETIK (+, -, *) har SAMME mangel som SHL/SHR havde foer BUG-397 — st_vm_exec_add/sub/mul() (st_vm.cpp) har kun en REAL- og en DINT-gren, INGEN DWORD-gren, saa d1+d2 for to DWORD-variable (100.000 + 200.000) beregnes som 16-bit INT-aritmetik og giver et fuldstaendigt forkert resultat (bekraeftet empirisk: gav 4.294.939.616 i stedet for 300.000 — bit-moenstret af en 16-bit overflow-vaerdi tegn-udvidet til 32 bit).** Denne DYBERE, systemiske DWORD-aritmetik-mangel er IKKE rettet her — den blev opdaget som en bivirkning af at verificere BUG-397, kraever sin egen dedikerede gennemgang (SUB/MUL bekraeftet ramt, DIV/MOD/sammenligninger/unaer-minus ikke undersoegt endnu) og er bevidst UDENFOR scope for denne rettelse, rapporteret til brugeren til separat prioritering. **Fix (denne raekke, faktisk leveret)**: tilfoejet manglende case ST_TYPE_DWORD i begge switches i api_handler_logic_single(), samt samme mangel fundet og rettet to andre steder ved samme lejlighed: variabel-binding-listens type-visning (api_handlers.cpp ~linje 4992, manglede baade INT- og DWORD-case) og ST-debuggerens variabel-snapshot (api_handlers.cpp ~linje 6989, if/else-kaede manglede DWORD-gren). Verificeret: DWORD-variable viser nu korrekt type+vaerdi i alle tre endpoints. Rørte: src/api_handlers.cpp |
| BUG-397c | Brugeren bad eksplicit om at faa den i BUG-397b opdagede, systemiske DWORD-aritmetik-mangel rettet FOeR punkt 4/5 (MB_SUCCESS/CONST) | ✅ FIXED | 🔴 CRITICAL | v7.9.38.0 | Udvidede undersoegelse bekraeftede at manglen ramte 11 VM-opcodes i alt, ikke kun ADD/SUB/MUL: `st_vm_exec_add/sub/mul/mod()` (aritmetik) og `st_vm_exec_eq/ne/lt/gt/le/ge()` (alle 6 sammenligninger) havde alle kun en REAL- og en DINT-gren, INGEN DWORD-gren — enhver DWORD-variabel faldt igennem til 16-bit INT-fortolkning, tabte bit 16-31, uden fejl eller advarsel. `st_vm_exec_neg()` (unaer minus) havde samme mangel. `st_vm_exec_div()` var IKKE ramt (returnerer altid REAL, konverterede allerede korrekt fra `dword_val`). **Fix**: tilfoejet en dedikeret 32-bit-gren i alle 11 opcodes. For ADD/SUB/MUL beregnes resultatet som `uint32_t` (bit-identisk med signeret wrap, saa DINT+INT-resultater er 100% uaendret) og rapporteres som DWORD medmindre en DINT-operand reelt var involveret (saa forbliver resultatet DINT, som foer). MOD fik sin egen gren med RIGTIG usigneret modulo (ingen INT32_MIN/-1-faelde findes for usignerede typer). NEG paa DWORD bruger usigneret to's-komplement-negation (veldefineret i C, ingen REAL-forfremmelse noedvendig modsat INT16_MIN/INT32_MIN). Alle 6 sammenligninger fik en ny usigneret DWORD-gren, OG fik samtidig rettet at en DWORD-operand blandet med en DINT-operand nu laeses via `dword_val` (bit-reinterpretation), ikke via den forkerte `int_val`. **Selvstaendigt fundet, allerede-eksisterende, ikke-relateret tastefejl rettet i samme omgang**: `st_vm_exec_lt()`s DINT-gren laeste fejlagtigt ALTID `right.dint_val` (ogsaa naar `right_type` reelt var INT, ikke DINT) — modsat GT/LE/GE, som alle korrekt laeste `right.int_val` i det tilfaelde. Fanget og rettet som en biprodukt af DWORD-arbejdet, verificeret separat. **Grundigt testet paa slot 3/4 (Logic1/Logic2 upaavirket)**: (1) fuldt DWORD-testprogram — `+ - * MOD` samt alle 6 sammenligninger paa to DWORD-variable (100.000/200.000), blandet DWORD+INT, og et bevidst wraparound-test (negation efterfulgt af addition, som praecist rammer 0) — ALLE 13 vaerdier korrekte, 0 fejl. (2) DINT-regressionstest — samme operationer paa DINT+INT (inkl. specifikt den scenarie der ramte LT-tastefejlen) — ALLE 8 vaerdier uaendrede/korrekte, bekraeftende 100% bagudkompatibilitet. Rørte: src/st_vm.cpp |
| BUG-397d | Brugeren bad om at faa punkt 5 fra D.7-gennemgangen implementeret: CONST er reserveret i lexeren, men fuldstaendig uimplementeret | ✅ FIXED | 🟠 MEDIUM | v7.9.39.0 | **Design**: `CONST navn : TYPE := vaerdi;` som praefiks paa en almindelig VAR-deklaration (ikke IEC'ens separate "VAR CONSTANT...END_VAR"-blok — simplere, matcher denne kodebases eksisterende praefiks-moenster). Kun skalar-typer i almindelige VAR-blokke: afvist eksplicit for ARRAY/STRUCT (initialvaerdi-mekanismen anvender kun ÉT literal til base-slottet, intet meningsfuldt for flere elementer/felter), for VAR_INPUT/VAR_OUTPUT (selvmodsigende — hele pointen med disse er at blive overskrevet af Modbus-bindingen hver scan), og kraever en literal initialvaerdi ved deklaration (ellers meningsloest — ville bare starte paa 0/FALSE som en helt almindelig variabel). **Haandhaevelse**: fandt og daekkede alle FIRE separate steder i kompilatoren der direkte skriver til en variabel-slot (`st_compiler_compile_assignment()` daekker almindelig `:=`/array/struct-felt, men FOR-loekkens tael-initialisering+increment og FB-output-bindinger (`Q=>var`, `ET=>var` osv.) gaar UDENOM den helt og skriver direkte via `st_compiler_emit_store_symbol()` — hver fik sit eget `is_const`-tjek). **Bevidst udenfor scope**: CONST haandhaeves kun paa COMPILE-tidspunktet (ST-kode kan ikke skrive til den) — en CONST-variabel der ALLIGEVEL bindes som Modbus-INPUT via REST/CLI ville stadig blive overskrevet af selve binding-mekanismen ved runtime (et separat, runtime-only system uden for kompilatorens rækkevidde) — dokumenteret som en kendt begraensning frem for at udvide bytecode-formatet (ville kraeve en ST_BYTECODE_VERSION-bump og roere den persisterede cache-fil-struktur for en marginal ekstra sikkerhed). **Verificeret paa slot 3/4** (Logic1/Logic2 upaavirket, ingen af dem bruger CONST): gyldig brug (INT+REAL CONST i sammenligning/beregning) kompilerer og koerer korrekt; forsoeg paa at gentildele en CONST fejler ved KOMPILERING med en klar besked; CONST uden initialvaerdi fejler ved PARSING; CONST som FOR-loekke-taeller fejler ved KOMPILERING. Rørte: include/st_types.h, include/st_compiler.h, src/st_parser.cpp, src/st_compiler.cpp |
| BUG-397e | Brugeren bad om at faa punkt 4 implementeret: MB_SUCCESS() betyder noget forskelligt efter READ vs. WRITE, kraever at blive tjekket UMIDDELBART efter den paagaeldende operation | ✅ FIXED (tilfoejet, ikke aendret) | 🟠 MEDIUM | v7.9.39.0 | **Design**: to nye 0-argument-funktioner `MB_READ_OK()`/`MB_WRITE_QUEUED()` tilfoejet SOM SUPPLEMENT — `MB_SUCCESS()` selv er 100% UAENDRET (Logic1/ModbusPLC's egen produktionslogik er direkte afhaengig af dens eksisterende, tvetydige adfaerd i trin 4's write-timeout-haandtering, saa den maatte IKKE aendres). De to nye funktioner spores via to nye, retnings-specifikke globale flag (`g_mb_read_success`/`g_mb_write_queued`, st_builtin_modbus.cpp) der spejler PRAECIS hvad `g_mb_success` allerede saettes til ved hvert MB_READ_*/MB_WRITE_*-kald (inkl. de delte validerings-helpers `check_request_limit()`/`validate_slave_addr()`, som nu tager en `is_write`-parameter for at vide hvilket af de to flag der er relevant ved en tidlig valideringsfejl) — saa hver funktion altid giver et korrekt svar for den SENESTE operation af netop DEN retning, uanset hvor mange kald af den MODSATTE retning der er sket i mellemtiden (modsat `MB_SUCCESS()`, som SKAL tjekkes umiddelbart efter). **Note fundet under arbejdet, ikke en ny fejl**: `MB_READ_HOLDINGS` (multi-register-laesning) har ALTID betydet "blev sat i koe", ikke "arrayet har gyldige cachede data" (modsat enkelt-register-laesningerne, som tjekker reel cache-gyldighed) — denne eksisterende asymmetri er bevaret uaendret i `MB_READ_OK()` for den funktion, blot dokumenteret tydeligere. **Verificeret paa slot 3/4 mod reel Modbus-trafik** (slave 90, bekraeftet naaelig): `MB_READ_OK()` korrekt TRUE efter en vellykket laesning, `MB_WRITE_QUEUED()`+`MB_SUCCESS()` korrekt TRUE efter en efterfoelgende skrivning, og — det kritiske test — `MB_READ_OK()` forbliver STADIG TRUE efter skrivningen, uberoert af den (modsat `MB_SUCCESS()`, som ville vaere blevet overskrevet af skrivningens eget resultat). Rørte: include/st_builtin_modbus.h, src/st_builtin_modbus.cpp, include/st_builtins.h, src/st_builtins.cpp, src/st_compiler.cpp |
| BUG-397f | Selvstaendigt fundet under test af BUG-397d (CONST+REAL): en REAL-initialvaerdi som `1.5` blev tavst til `0.0` ved runtime, IKKE relateret til CONST — ramte ENHVER `VAR x: REAL := ...;` med denne slags vaerdi | ✅ FIXED | 🔴 CRITICAL | v7.9.40.0 | Rodaarsag: `sym->has_initial_value = (var->initial_value.int_val != 0) ? 1 : 0;` (st_compiler.cpp) tjekkede KUN unionens 16-bit `.int_val`-felt, uanset variablens FAKTISKE type — og dette flag AFGOER direkte om initialvaerdien overhovedet anvendes ved programstart (`bytecode->variables[i] = sym->has_initial_value ? sym->initial_value : {int_val:0};`). `1.5`s IEEE754-bitmoenster (0x3FC00000) har tilfaeldigvis nul i sine nederste 16 bit — saa `has_initial_value` blev fejlagtigt FALSE, og variablen startede paa `0.0` i stedet for `1.5`, fuldstaendig tavst. Ramte enhver "paen" REAL-vaerdi med samme egenskab (1.0, 2.0, 10.0, 100.0, m.fl.) — samt formodentlig ogsaa DINT/DWORD-initialvaerdier der er praecise multipla af 65536 (samme rodaarsag, ikke selvstaendigt testet). **Fix**: erstattet med et `memcmp()` af HELE `st_value_t`-strukturen mod en nulstillet instans — korrekt for enhver type, ingen saerbehandling noedvendig. **Verificeret paa slot 3/4** (Logic1/Logic2 upaavirket — ingen af dem bruger REAL overhovedet, bekraeftet via grep foer aendringen): `ratio: REAL := 1.5;` viste `0` foer denne fix, `1.5` efter — sammen med en afledt beregning (`ratio*2.0`) der tilsvarende gik fra `0` til korrekt `3`. Rørte: src/st_compiler.cpp |
| BUG-397g | Brugerrapporteret via web-GUI'ets `/io`-side: "Kunne ikke hente bindinger: JSON.parse: bad control character in string literal at line 1 column 2054" -- straks efter at have oprettet en ny binding (`init_done` -> `input:101`) | ✅ FIXED | 🔴 CRITICAL | v7.9.41.0 | **Samme rodaarsag som det allerede dokumenterede BUG-332** (Modbus Aktivitetslog, v7.9.8.2): `api_handler_bindings_list()` (GET /api/bindings) serialiserede til en FAST `char buf[2048]`-buffer -- men med 17-18 bindings (langt under det tilladte loft paa 32) naar den reelle JSON allerede 2054+ bytes. `serializeJson(doc, buf, BUF_SIZE)` afkorter STILLE midt i et JSON-objekt naar output overstiger buffer-stoerrelsen, UDEN at reservere plads til en afsluttende nul-terminator ved selve afskaeringspunktet -- `api_send_json()`s `httpd_resp_sendstr()` (og dens egen debug-log) bruger `strlen()`, som saa laeser VIDERE forbi den 2048-byte allokering ind i hvad end der tilfaeldigvis ligger i den tilstoedende heap-hukommelse, indtil den (tilfaeldigt) rammer en nul-byte. Det forklarer PRAECIS brugerens fejlbesked: ikke bare afkortet JSON, men et reelt STRAY CONTROL CHARACTER fra heap-hukommelse der landede midt i det som browserens JSON.parse() stadig troede var en aaben streng-vaerdi. **Bekraeftet empirisk**: live-maalt til noejagtigt 2054 bytes paa den koerende enhed foer denne fix -- matcher brugerens "column 2054" praecist. **Fix**: erstattet den faste buffer med `measureJson(doc)` til at beregne den PRAECISE noedvendige stoerrelse foerst (samme etablerede moenster som BUG-332 selv brugte som sit foerste fix-trin, foer den senere blev udvidet til fuld chunket afsendelse for syslog-endpointet specifikt, given dens langt stoerre og reelt ubegraensede stoerrelse -- bindings-listen er derimod haardt loftet til 32 entries, saa `measureJson()` alene er tilstraekkeligt og korrekt her). **Samme moenster fundet OG rettet proaktivt i `api_handler_logic_single()`** (GET /api/logic/{id}, den langt mere haardt brugte program-status-endpoint) -- live-maalt til 2033 bytes for et program med alle 32 variable udfyldt, farligt taet paa samme 2048-graense; én variabel til, eller lidt laengere navne, ville have udloest identisk heap-laekage der. Verificeret paa den koerende enhed efter fix: begge endpoints svarer nu korrekt. **Ikke revideret i denne omgang**: resten af de ~45 andre `serializeJson(doc, buf, ...)`-kaldesteder i api_handlers.cpp -- flere bruger allerede en dynamisk `json_len`-baseret tilgang, resten har formodentlig fast, lille svarform der aldrig kan naa 2048 bytes -- men er ikke systematisk gennemgaaet. Rørte: src/api_handlers.cpp |
| BUG-398 | Brugerrapporteret umiddelbart efter FEAT-397h's Bearer-only-udrulning: "som sædvanlig kan jeg ikke logge ind fra en browser session" — indkredset af brugeren selv til "ser ud som om det er Firefox der fucker up" | ✅ FIXED | 🔴 CRITICAL | v7.9.43.0 | **Rodaarsag**: `rbac_check_http()` tjekkede kun session-cookien i grenen hvor Authorization-headeren MANGLEDE helt — var headeren til stede (uanset hvad den indeholdt), sprang koden den gren over. Firefox sender (bekræftet under selve DENNE sessions BUG-395/397g-undersøgelse) proaktivt sit CACHEDE Basic-Auth-forsøg paa ALLE requests til en oprindelse den tidligere har logget ind paa — ikke kun som svar paa en frisk 401-udfordring, og uden nogen JS-synlig måde at rydde det på (kun en fuld browser-genstart). Efter FEAT-397h satte enheden i Bearer-only-tilstand var browserens Basic-Auth-cache fra FØR opdateringen stadig aktiv — hvert efterfølgende kald fra siden havde derfor BÅDE en gyldig, frisk session-cookie (fra det nye, korrekte login) OG den nu-forældede cachede Basic-header hæftet på af Firefox selv. `rbac_check_http()` saa headeren, gik direkte til FEAT-397h's Bearer-only-afvisning ("Basic afvises, medmindre /api/login"), og returnerede 401 — UDEN nogensinde at se den ellers gyldige cookie. Brugeren kunne dermed logge ind (POST /api/login virkede fint), men enhver efterfølgende side-forespørgsel blev straks afvist igen, hvilket i praksis så ud som "kan slet ikke logge ind". **Fix**: cookie-tjekket flyttet til at køre ALTID først (Bearer-header, dernæst cookie, dernæst — kun hvis begge fejler — selve Basic-Auth-beslutningen) i stedet for kun i "ingen header"-grenen. En gyldig cookie vinder nu uanset hvilken forældet/irrelevant Authorization-header browseren måtte have hæftet på oven i. Rørte: src/rbac.cpp |
| BUG-400 | Brugerrapporteret umiddelbart efter FEAT-399-udrulning: "når jeg ændrer i acl for ip adr så kan jeg ikke login igen... siger forkert username eller password" — reelt IKKE forkerte credentials, men to selvstændige mangler i selve FEAT-399 | ✅ FIXED | 🔴 CRITICAL | v7.9.46.0 | **Rodaarsag #1 (UX)**: `api_handler_login()` afviser bevidst IKKE en blokeret IP (FEAT-399's design — login skal netop kunne afsloere en fejlkonfigureret regel), og returnerer korrekt 403 "Blocked by IP ACL" i saa fald — men ALLE 7 web-siders `doLogin()`-funktioner behandlede ENHVER ikke-200-status fra `/api/login` ens, og viste den samme generiske "Forkert brugernavn eller adgangskode", uanset om aarsagen reelt var forkerte credentials (401) eller en IP-ACL-blokering (403). Bekraeftet i audit-loggen: brugerens IP (10.1.1.185) fik en kaskade af 403'ere (ikke 401) umiddelbart efter at have tilfoejet en HTTP-regel der matchede dens egen IP — auto-rollback-mekanismen (FEAT-399 §10.7.1) virkede korrekt og genoprettede adgang, men brugeren fik aldrig at vide AT det var det der skete. **Rodaarsag #2 (arkitektur, brugerens egen paafoelgende noete: "ip acl kontrol skal vaere foer user rammer web services", "man skal se ip acl som firewall... alt ip trafik skal igennem foer noget andet")**: en blokeret IPs eneste reelle haandhaevelsespunkt for HTTPS var REQUEST-laget (`CHECK_AUTH*`-makroerne) — men de 7 statiske side-handlers (`web_system.cpp` m.fl.) kalder INGEN af disse makroer overhovedet (auth haandhaeves udelukkende client-side i JS), saa en blokeret IP kunne stadig faa HELE HTML-siden (inkl. login-skaermen) serveret paa HTTPS, og blev foerst afvist naar JS'en efterfoelgende kaldte et API-endpoint. Samme mangel gjaldt en haandfuld bevidst "ingen-auth-kraevet"-endpoints (`/api/metrics`, `/api/dashboard/layout`) og HELE `CHECK_AUTH_OTA`-makroen (separat fil, ota_handler.cpp) — ingen af disse gik gennem den oprindelige `CHECK_IP_ACL`, som kun var indsat i `CHECK_AUTH`/`CHECK_AUTH_WRITE`/`CHECK_AUTH_ROLE`. **Fix #1**: alle 7 `web/*.html`s `doLogin()` skelner nu eksplicit 403 (viser en klar "denne IP er blokeret af IP ACL'en, retter sig selv om op til 5 min"-besked) fra andre fejl (bevarer den oprindelige "forkert brugernavn/adgangskode" kun for reelle 401'ere). **Fix #2 (arkitektonisk konsolidering)**: ny `ip_acl_check_req()` (ip_acl.h/.cpp, tager en `httpd_req_t*` direkte) tilfoejet til (a) alle 7 statiske side-handlers som det ALLERFOERSTE de goer, FOeR HTML overhovedet serveres — en blokeret IP paa HTTPS naar nu aldrig laengere end til en 403, ser ALDRIG login-skaermen; (b) `CHECK_AUTH_OTA` (ota_handler.cpp); og (c) selve `CHECK_API_ENABLED`-makroen (api_handlers.cpp) fik `CHECK_IP_ACL` flyttet IND i sig i stedet for kun at staa i de tre CHECK_AUTH*-varianter — da CHECK_API_ENABLED er den fælles rod stort set alle handlers (ogsaa de auth-frie) kalder foerst, lukker denne ene aendring samtlige resterende huller paa én gang, inkl. den separate `/api/v1/*`-dispatch-tabel (som blot kalder de samme underliggende handlers). **Tillaeg (defense-in-depth)**: en ny, proaktiv `self_match_warning`-advarsel i REST-svaret naar en tilfoejet/genaktiveret regel rent faktisk matcher AFSENDERENS EGEN nuvaerende IP for HTTP — synlig baade i REST-JSON'en og som en fremtraedende advarsel i web-GUI'et, saa den slags fejlkonfiguration opdages STRAKS ved oprettelse i stedet for foerst ved et senere fejlet login. **Live-verificeret paa 10.1.1.153 (v7.9.46.0)**: tilfoejede bevidst en regel der matchede testerens egen IP for HTTP — REST-svaret indeholdt korrekt `self_match_warning:true`; efterfoelgende HTTP-forsoeg (baade side og API) fejlede med forbindelses-niveau-afvisning (curl: "HTTP 000", ikke engang et HTTP-svar) — bekraefter at blokeringen nu sker FOeR noget som helst serveres; Telnet upaavirket i samme periode (korrekt afgraenset til kun HTTP); auto-rollback bekraeftet EFTER 300+ sekunder (adgang automatisk gendannet, ACL tilbage til tom tilstand) UDEN reboot. Logic1/Logic2 upaavirket. Rørte: web/system.html, web/dashboard.html, web/io.html, web/logs.html, web/cli.html, web/editor.html, web/ota.html, include/ip_acl.h, src/ip_acl.cpp, src/api_handlers.cpp, src/ota_handler.cpp, src/web_system.cpp, src/web_dashboard.cpp, src/web_io.cpp, src/web_logs.cpp, src/web_cli.cpp, src/web_editor.cpp, src/web_ota.cpp |
| FEAT-401 | Brugerønsket, umiddelbart efter FEAT-399/BUG-400: en "deny ip any any" nederst i regellisten, redigerbare regler, og flytbare/omarrangerbare regler — bekræftet via AskUserQuestion at dette betyder en fuld opgradering til en ORDNET permit/deny-firewall-model (Cisco/iptables-stil, første match vinder), ikke bare en udvidelse af v1's simple blokeringsliste | ✅ DONE, live-verificeret | 🔴 CRITICAL (skærpet lockout-risiko) | v7.9.47.0 | **Design**: `AclRule.reserved`-byte (types.h) omdøbt til `action` (0=ALLOW/permit, 1=DENY/deny, constants.h `ACL_ACTION_*`) — INGEN struct-størrelses- eller PersistConfig-layoutændring, kun semantisk omtolkning af et allerede-eksisterende felt. Schema 26→27, migrationsblok saetter `action=DENY` for alle EKSISTERENDE persisterede regler (de betoed udelukkende "bloker" under v1's ordensuafhaengige model). Ny matchings-motor (`ip_acl.cpp`s `evaluate_allowed()`, erstatter `rules_block()`): itererer `acl_rules[]` i INDEX-raekkefolge, foerste AKTIVE match (service+CIDR) vinder og dens `action` afgoer udfaldet; intet match => default ALLOW (uaendret v1-filosofi — en eksplicit `deny 0.0.0.0/0` nederst er dermed det der reelt lukker for resten, ikke en implicit systemdefault). **Skaerpet lockout-recovery-gating (den vigtigste aendring)**: i v1 kunne sletning/deaktivering ALDRIG stramme adgangen (kun aabne den) — i en ordnet permit+deny-model er det IKKE laengere sandt, fordi at fjerne/flytte en ALLOW-regel kan afsloere en DENY-regel laengere nede. Bevidst KONSERVATIV politik fremfor kirurgisk praecis retnings-/handlings-analyse (for stor overflade for en subtil fejl i noget saa sikkerhedskritisk, se kildekodekommentarer i ip_acl.cpp for den fulde afvejning): **ADD/ENABLE af en mgmt-regel (http/telnet/all) gates ALTID, uanset action; DISABLE/DELETE af en mgmt-regel gates KUN hvis regelens action er ALLOW (en DENY-regel kan trygt fjernes uden bekraeftelse — kan kun aabne adgang, aldrig stramme); EDIT gates hvis ENTEN den gamle tilstand var en aktiv ALLOW-regel ELLER den nye tilstand er en aktiv mgmt-regel; MOVE (ny operationstype) gates altid for en aktiv mgmt-regel, uanset retning eller action** (en praecis retningsanalyse for reorder blev bevidst fravalgt som for risikabel). Ny CRUD: `ip_acl_rule_edit()` (fuld erstatning af en regels felter paa dens plads, samme "kraev alle felter"-stil som `rbac_set_user()`), `ip_acl_rule_move()` (memmove-stil flytning, to-vejs). `ip_acl_rule_add()`/`ip_acl_rule_delete()` udvidet med hhv. `action`-parameter og en ny `out_now_pending`-parameter (delete kan nu ogsaa gate). **Selv-match-advarslen (BUG-400) praeciseret**: udloeser nu KUN for en DENY-regel der matcher kalderens egen IP (en ALLOW der matcher egen IP er per definition harmloes, intet at advare om). CLI: `set acl rule add <cidr> service <svc> action <allow|deny> [disabled]`, `set acl rule <index> edit <cidr> service <svc> action <allow|deny> [disabled]`, `set acl rule <index> move <new-index>`, uaendret enable/disable/delete/confirm. REST: `POST /api/acl/rules` kraever nu `action`; `POST /api/acl/rules/{index}` udvidet — body med KUN `enabled` forbliver et bagudkompatibelt til/fra-toggle, body med `cidr`/`service`/`action` er en fuld redigering; ny `POST /api/acl/rules/{index}/move` (body `{"to_index":N}`) — bevidst IKKE en selvstaendig `httpd_uri_t`-registrering, men suffiks-dispatchet INDE FRA den eksisterende `/api/acl/rules/*` POST-handler (samme etablerede GAP-11-moenster som GPIO's `/config`-suffiks), for at undgaa en usikker antagelse om at ESP-IDF's wildcard-matcher understoetter et joker-tegn midt i et moenster. Backup/restore (`/api/system/backup`+`/restore`) udvidet med `action`-feltet; aeldre backup-filer uden feltet defaulter til DENY (samme begrundelse som schema-migrationen). Web-GUI: ny "Aktion"-kolonne (Tillad/Bloker) + ▲/▼-flytteknapper + "Redigér"-knap (genbruger tilfoej-formularen i redigerings-tilstand, samme moenster som RBAC-brugerredigering) i `web/system.html`s ACL-kort. **Live-verificeret paa 10.1.1.153, alle kritiske scenarier bestaaet — ALDRIG ved reelt at blokere egen HTTP/Telnet-adgang** (kun SSE, som aldrig gates, brugt til at bevise selve haandhaevelsen sikkert): migration til schema 27 ren; simpel `deny 0.0.0.0/0 sse` blokerede al SSE (samme "Empty reply"-teknik som BUG-400); `allow <egen-IP>/32 sse` FOeR `deny 0.0.0.0/0 sse` lod egen IP igennem (normal 401 i stedet for Empty reply); **flytning af deny-reglen OP foer allow-reglen blokerede DEREFTER egen IP for SSE MED PRAECIS SAMME REGLER, kun i anden raekkefoelge** — det utvetydige, definitive bevis paa at foerste-match-vinder-logikken virker korrekt; sletning af en bekraeftet ALLOW-HTTP-regel udloeste korrekt pending-confirm (modsat v1's ubetingede "sletning gater aldrig"); auto-rollback bekraeftet for netop denne (ikke-add-) operationstype — nedtaelling fulgt i realtid til 0, den slettede regel automatisk gendannet UDEN reboot. Fuld backup-diff mod forrige baseline: kun forventede forskelle (build/version/schema/acl-indhold) — RBAC og netvaerkskonfiguration 100% uaendrede. Logic1/Logic2 upaavirket gennem hele forloebet. Rørte: include/types.h, include/constants.h, include/ip_acl.h, src/ip_acl.cpp, src/config_load.cpp, src/api_handlers.cpp, include/api_handlers.h, src/cli_parser.cpp, src/cli_show.cpp, web/system.html |
| FEAT-402 | Brugeren ramte under egen test AF FEAT-401 praecis det scenarie funktionen selv advarede om: byggede `allow 10.1.1.0/24 all` + `deny 0.0.0.0/0 all` (virkede fint), flyttede saa deny-reglen OP foer allow-reglen for at rette raekkefoelgen — og blev STRAKS logget ud, fordi MOVE (som designet) haandhaever gated aendringer LIVE med det samme. Ikke en fejl i den enkelte handling (den ville vaere rullet automatisk tilbage efter 5 min.) — brugeren identificerede selv arkitektur-hullet: "vi skal have en mulighed for at state alle aendringer foer de bliver commit i firewall, ellers kan man jo ikke adm en masse regler i processen". Efterfoelgende afklaret at kladde-anvendelse skal foelge SAMME "log ud/log ind for at gemme"-disciplin som enhver anden gated aendring (ikke en genvej udenom) | ✅ DONE, live-verificeret | 🔴 CRITICAL (fjerner lockout-risiko under flertrins-redigering) | v7.9.48.0 | **Design**: ny tredje tilstand "kladde" (draft) — RAM-only, ALDRIG konsulteret af `ip_acl_check()`, sidder FOeR den eksisterende pending-confirm-mekanisme i state-flowet (BEKRAEFTET → KLADDE → AFVENTER BEKRAEFTELSE → confirm/timeout → BEKRAEFTET). Ingen schema-/PersistConfig-aendring (samme spinlock-beskyttede RAM-only-moenster som den eksisterende `acl_staged_*`-pending-kopi, `ip_acl.cpp`): `acl_draft_active`/`acl_draft_enabled`/`acl_draft_rule_count`/`acl_draft_rules[32]`. Ny kerne-API, ALLE uden gating/haandhaevelse/session-revoke (hele pointen — fri eksperimentering): `ip_acl_draft_begin/discard/set_enabled/rule_add/rule_edit/rule_move/rule_delete()` samt read-accessorerne `ip_acl_draft_is_active/get_enabled/get_rule_count/get_rule()`. De EKSISTERENDE direkte mutations-funktioner (`ip_acl_rule_add/edit/move/delete/set_enabled()`, FEAT-399/401, 100% uaendrede ellers) fik hver ét nyt vagt-tjek foerst: `if (acl_draft_active) return ACL_ACTION_ERR_DRAFT_ACTIVE;` — undgaar at kladde og bekraeftet tilstand kan divergere uafhaengigt af hinanden. **Den centrale funktion, `ip_acl_draft_apply()`**: anvender HELE kladden atomisk via ÉN forenklet gating-beslutning (i modsaetning til FEAT-401's per-operation-nuancerede regler) — sammenligner kladdens og den bekraeftede tilstands mgmt-relevante delmaengde (`mgmt_subset_differs()`: regler med service ≠ SSE, i bevaret indbyrdes raekkefoelge, inkl. `enabled`+`action`); differerer den slet ikke (kun SSE-regler aendret, eller ingen aendring), anvendes kladden direkte til NVS (ingen gate); differerer den, overgaar kladden i stedet til den EKSISTERENDE `acl_staged_*`/pending-mekanisme som ÉT atomisk skridt — haandhaeves straks, alle RBAC-sessioner+Telnet-klienten revokeres (`force_fresh_auth_on_mgmt_surfaces()`), 5 min. til `confirm acl`/automatisk rollback, PRAECIS samme lockout-recovery-net som enhver direkte gated FEAT-399/401-mutation. Saerlige tilfaelde haandteret eksplicit: resulterende `acl_enabled=0` kan aldrig gate (uanset regler, siden `ip_acl_check()` fail-aabner helt); ny-aktivering (0→1) gater kun hvis kladden indeholder mindst én aktiv mgmt-regel (samme princip som `ip_acl_set_enabled()`). Valgfri `self_match_warning` (samme koncept som FEAT-401, nu evalueret for HELE kladdens resulterende HTTP-tilstand mod kalderens IP, ikke kun én regel). CLI: `set acl draft <begin\|apply\|discard\|enable\|disable>`, `set acl draft rule add/<index> edit/move/enable/disable ...` (genbruger den eksisterende `parse_rule_fields`-lambda, cli_parser.cpp), `delete acl draft rule <index>`, `show acl draft` (cli_show.cpp, tydeligt markeret "IKKE HAaNDHAEVET"). REST: fuldt parallelt sæt under `/api/acl/draft*` — `GET/POST/DELETE /api/acl/draft` (laes/toggle-enabled/kassér), `POST /api/acl/draft/begin`, `POST /api/acl/draft/rules` (tilfoej), `POST /api/acl/draft/rules/{index}` (redigér/toggle + `/move`-suffiks-dispatch, samme GAP-11-moenster som FEAT-401), `DELETE /api/acl/draft/rules/{index}`, `POST /api/acl/draft/apply` — alle registreret som selvstaendige `httpd_uri_t` (`http_server.cpp`), ingen konflikt med de eksisterende `/api/acl*`-mønstre (forskellige literale praefikser, ESP-IDF's `httpd_uri_match_wildcard` kraever eksakt match for ikke-joker-moenstre). Web-GUI (`web/system.html`): "Start kladde"-knap, blaat kladde-banner ("KLADDE-TILSTAND — IKKE haandhaevet endnu") med "Anvend ændringer"/"Kassér kladde", regeltabel/formular parametriseret via `aclBase()` (`/api/acl` vs. `/api/acl/draft`, samme render-/mutations-kode for begge). **Live-verificeret paa 10.1.1.153 (v7.9.48.0), fuldt REST+CLI-testbatteri, device efterladt i ren tilstand**: (1) kladde paavirker intet — reproducerede BOGSTAVELIGT brugerens udloesende scenarie (flyttede `deny 0.0.0.0/0 all` foran `allow 10.1.1.0/24 all` INDE I kladden) og bekraeftede `GET /api/acl` (live) forblev 100% uaendret gennem hele sekvensen, kun `GET /api/acl/draft` viste den nye raekkefoelge; (2) apply af en reelt mgmt-aendret kladde (ny regel tilfoejet) gatede korrekt — session revokeret, frisk login gennemfoert (selve beviset for at login stadig virker), `confirm acl` bekraeftede, sluttilstand matchede kladden noejagtigt; (3) apply af en KUN-SSE-aendret kladde anvendtes DIREKTE uden gate, session forblev gyldig uafbrudt; (4) `set acl draft begin/rule add/rule move/rule delete/show/discard` verificeret over CLI (samme underliggende funktioner som REST, ingen dobbelt-implementation); (5) `ACL_ACTION_ERR_DRAFT_ACTIVE` bekraeftet for baade `POST /api/acl` og `POST /api/acl/rules` mens en kladde var aktiv (409 Conflict). Fuld backup-diff efter forloebet: identisk med udgangspunktet (2 regler, allow+deny). Logic1 (compiled, disabled — uaendret brugervalg) og Logic2 (koerende, `execution_count` stigende, `error_count:0`, alle channel-select-bindings intakte: `chSel:255`) upaavirket gennem hele testforloebet. Rørte: include/ip_acl.h, src/ip_acl.cpp, include/api_handlers.h, src/api_handlers.cpp, src/http_server.cpp, src/cli_parser.cpp, src/cli_show.cpp, include/cli_show.h, web/system.html |
| BUG-403 | Brugerrapporteret umiddelbart efter FEAT-402-udrulning: "firewall har ikke nogen klade system, alle ændringer commit med det samme som før, siden IO eksitere ikke mere 'This URI does not exist'" — to selvstændige fund, kun det ene et reelt kodefund | ✅ FIXED (routing) + UX-forbedring (kladde-synlighed) | 🔴 CRITICAL (4 sider utilgængelige) | v7.9.50.0 | **Fund #1 (bekræftet reel regression)**: `/io`, `/cli` og `/logs`-siderne svarede "This URI does not exist" — PRAECIS samme fejlklasse som BUG-336c og BUG-354 tidligere i projektet, og PRAECIS den advarsel disse to bugs' egne kildekodekommentarer efterlod ("keep max_uri_handlers comfortably above that number, not just equal to it"): FEAT-402s 8 nye `/api/acl/draft*`-registreringer (http_server.cpp) puttede det samlede antal `httpd_register_uri_handler()`-kald fra 123 til 131 — imod et `max_uri_handlers`-loft paa PRAECIS 128 (BUG-354's egen fix, uden ekstra margen). ESP-IDF's interne handler-array er FAST STORRELSE: registreringer udover loftet fejler stille (`httpd_register_uri_handler()`s returvaerdi tjekkes bevidst ikke de 130 kaldesteder, dokumenteret som kendt gaeld siden BUG-354) — de SIDSTE 3 registreringer i filens raekkefolge (`uri_cli_page`, `uri_logs_page`, `uri_io_page` — tilfaeldigvis fysisk sidst i registrerings-blokken, IKKE relateret til selve ACL-kladde-koden) blev dermed aldrig reelt tilgaengelige, selvom koden der REGISTRERER dem er 100% korrekt. De 8 nye kladde-endpoints selv var IKKE ramt (deres registrering ligger tidligt i raekkefolgen, langt under det gamle loft) — bekraeftet direkte via REST/CLI-test, se FEAT-402. **Fix**: `max_uri_handlers` haevet 128→160 (32 i margen denne gang, ikke 0) begge steder (plain HTTP: `http_server.cpp`; HTTPS: `https_wrapper_start()`-kaldet), og advarselskommentaren ved selve registrerings-blokken udvidet til at naevne at dette nu er tredje gang samme fejlklasse rammer projektet. **Ingen strukturel aendring** (fx retur-vaerdi-tjek paa alle 130 kaldesteder) foretaget denne omgang — vurderet som scope creep udover den rapporterede fejl; den eksisterende proces (grep-count + haev loftet med margen efter enhver ny rute) er tilstraekkelig NAaR den faktisk foelges, hvilket den ikke blev ved FEAT-402s udrulning. **Fund #2 (ikke et kodefund, men reel brugeroplevelse)**: "kladde-systemet" i selve firewall-koden fungerede fuldt korrekt under undersoegelsen (bekraeftet: `POST /api/acl/draft/begin` derefter `GET /api/acl/draft` viste kladdens indhold korrekt) — den live enheds faktiske ACL-tilstand paa undersoegelsestidspunktet (en ny telnet-deny-regel for 10.1.1.100 + en deaktiveret catch-all-deny-regel) beviste at brugeren havde tilfoejet/aendret regler via de DIREKTE (ikke-kladde) formular-/knap-kontroller i ACL-kortet, som — helt efter hensigten, og uaendret siden FEAT-399 — stadig anvender med det samme uden om kladden. Kladde-knappen ("Start kladde") sad diskret placeret med samme daempede graa styling som alle andre sekundaere knapper, uden nogen tydelig markering af at formularen NEDENFOR anvender direkte. **UX-fix**: knappen fik `btn-primary`-styling (lyseblaa, samme fremtraedende farve som kladde-banneret) og et nyt, permanent gult hint lige under introteksten ("Formularen og knapperne nedenfor anvender ændringer MED DET SAMME... tryk 'Start kladde' herover foerst") — skjules automatisk naar en kladde rent faktisk er aktiv (samme `aclDraftActive`-baserede toggle-logik som selve banneret). **Live-verificeret paa 10.1.1.153 (v7.9.50.0)**: `/io`, `/cli`, `/logs`, `/system`, `/ota`, `/editor`, `/` svarer alle 200 efter reflash (var 404 for de foerste tre foer fixet); kladde-REST-flowet (`begin`→`GET`→`discard`) genkoert og bekraeftet uaendret korrekt; det nye hint og den fremhaevede knap bekraeftet til stede i den serverede side (`grep` paa hentet HTML). Brugerens egen live-tilfoejede ACL-regler (10.1.1.100 telnet-deny, deaktiveret catch-all) IKKE roert — det er deres eget konfigurationsarbejde, ikke test-debris. Rørte: src/http_server.cpp, web/system.html |
| FEAT-404 | Brugerønsket, umiddelbart efter BUG-403: "man skal ikke starte en kladde, det er alt til en såkalt kladde, i stedet for en start kladde skal der være en 'commit' change i stedet for, ligesom på en rigtig firewall" — reaktion paa at FEAT-402s kladde var opt-in (et eksplicit "Start kladde"-klik brugeren oversaa/glemte, jf. BUG-403s Fund #2) | ✅ DONE, live-verificeret | 🟡 HIGH (UX-redesign, ingen sikkerhedsaendring) | v7.9.51.0 | **Design (kun web-GUI, `web/system.html` — CLI/REST UAeNDRET)**: vender FEAT-402s model om, saa kladden bliver den ENESTE reelle redigerings-tilstand i ACL-kortet, praecis som et rigtigt firewall-administrationsinterface (candidate-config vs. running-config, Junos-stil "commit"): ingen synlig "Start kladde"-knap laengere — ny `ensureAclDraft()`-hjaelper (system.html) kaldes FoeRST i ALLE mutations-funktioner (`submitAclRule`, `toggleAclRule`, `moveAclRule`, `deleteAclRule`, `doAclToggle`) og starter en kladde STILLE i baggrunden (kalder den allerede-eksisterende `POST /api/acl/draft/begin`, uaendret backend) hvis der ikke allerede er én — brugeren tager aldrig selv stilling til det. Kladde-banneret omdoebt fra "KLADDE-TILSTAND" til "Ikke-committede ændringer", med knapperne omdoebt "Anvend ændringer"→**"Commit ændringer"** og "Kassér kladde"→**"Fortryd alle"** (samme underliggende `applyAclDraft()`/`discardAclDraft()`-funktioner, kun tekst og tilhoerende JS-beskeder aendret til at reflektere kandidat-config-sprogbrug). Den gule "anvendes MED DET SAMME"-hint-tekst fra BUG-403 er fjernet igen (blevet irrelevant/misvisende — intet anvendes laengere med det samme via GUI'et). **Bevidst IKKE aendret**: kladden startes stadig FoeRST ved den FoeRSTE reelle mutation, IKKE automatisk ved blot at aabne/se ACL-kortet — en kladde er en GLOBAL, enheds-daekkende RAM-tilstand (ikke pr. session/bruger, jf. `ip_acl.cpp`s `acl_draft_active`), og mens den er aktiv afviser ALLE direkte (ikke-kladde) CLI/REST-mutationer med `ACL_ACTION_ERR_DRAFT_ACTIVE` (409) — auto-start blot ved sidevisning ville reelt laase ACL-administrationen til foerste aabnede browserfane, ogsaa selvom brugeren aldrig redigerede noget. CLI/REST's eksplicitte `set acl draft begin`/`POST /api/acl/draft/begin` er bevaret 100% uaendret (nyttigt til scripting/automatisering, hvor øjeblikkelig DIREKTE anvendelse via `set acl rule add ...` ofte netop er oensket) — kun web-GUI'ets brugeroplevelse er redesignet. **Ingen backend-/REST-/CLI-aendring overhovedet** — hele FEAT-404 er en ren frontend-omlaegning af eksisterende, allerede-testede FEAT-402-endpoints. **Live-verificeret paa 10.1.1.153 (v7.9.51.0)**: simulerede hele GUI-flowet via REST (implicit `begin` → `POST rules` → `GET /api/acl` bekraeftet 100% uaendret → `DELETE /api/acl/draft` → `GET /api/acl` stadig uaendret); bekraeftet at et FORSOEGT kald direkte til `/api/acl/draft/rules` UDEN forudgaaende `begin` korrekt afvises (400, "Ingen aktiv kladde"), hvilket beviser `ensureAclDraft()`s begin-kald reelt er noedvendigt og virker; den servrerede `/system`-side indeholder "Commit ændringer" (×4), "Fortryd alle" (×4), `ensureAclDraft` (×7) og INGEN forekomster af "Start kladde" laengere. Brugerens egen live ACL-konfiguration (3 regler) upaavirket gennem hele testforloebet. Rørte: web/system.html |
| BUG-405 | Brugerrapporteret: "i ST monitor vil jeg godt have at trend skallere med vindue i stedet for som en fast størrelse, er det her ikke en bug jeg har bedt dig om at fikse før" + "opdate hastighed skal sætte som default til 500mS, lave også en hastighed mere på 250mS som man kan tilvælge" — to selvstaendige, uafhaengige aendringer i ST Editorens Monitor-fane (ingen tidligere sporet BUGS_INDEX-post fundet der matcher trend-stoerrelsen praecist, men det underliggende fund er reelt og bekraeftet uanset) | ✅ FIXED | 🟢 MEDIUM (kosmetisk/ergonomisk, ingen funktionel fejl) | v7.9.51.0 | **Fund #1 (trend-graf fast stoerrelse)**: `sparkSVG(hist,320,40,...)` (web/editor.html) genererede et SVG med haardkodede `width="320" height="40"`-attributter for HVER variabels sparkline i Monitor-tabellen — voksede aldrig ud over 320px, uanset hvor bredt browservinduet/ST-editor-panelet reelt var, selvom baade `.btbl{width:100%}` (tabellen) og selve panel-layoutet (`.panel{flex:1}`) allerede havde rigelig ubrugt bredde til raadighed paa store skaerme. **Fix**: (a) `.btbl`s foerste 3 kolonner (Variabel/Type/Vaerdi) faar `width:1%;white-space:nowrap` — en velkendt, bredt understoettet CSS-tabel-teknik der tvinger dem til kun at fylde deres eget indholds bredde, saa AL resterende bredde automatisk gaar til den 4. (Trend-)kolonne, uden at skulle skifte til `table-layout:fixed` med magiske procent-taerskler; (b) `.spark{flex:1;min-width:0}` (var `display:inline-block`) lader selve SVG'en vokse for at fylde denne nu-ledige plads i sin `.trend-cell` flex-raekke; (c) `sparkSVG()` genererer nu et SVG UDEN faste `width`/`height`-attributter — kun `viewBox="0 0 w h"` + `preserveAspectRatio="none"`, saa den faktiske CSS-styrede bredde (fra b) strækker det logiske 320-enheders koordinatsystem punkterne allerede var beregnet i (uaendret matematik). **Undgik en kendt SVG-faldgrube**: ved kraftig, ikke-uniform strækning (bred X-skala, fast Y-skala) ville streg-tykkelser normalt fordreje/blive urimeligt tykke — tilfoejet `vector-effect="non-scaling-stroke"` paa baade gitterlinjerne og selve trend-kurven holder deres streg-tykkelse konstant i skaermpixels uanset skalering (kun den lille "nu-vaerdi"-prik, en ufyldt cirkel uden stroke, er IKKE korrigeret for dette — vurderet kosmetisk ubetydeligt ved r=2.5). **Fund #2 (opdateringshastighed)**: standardvaerdien for Monitor-panelets pollinginterval (`monInterval`, web/editor.html) var 2500ms (2,5 sekund, baade i JS-variablens initialvaerdi OG dropdown'ens `selected`-attribut) — aendret til 500ms begge steder (de to vaerdier er UAFHAENGIGT hardkodede i denne fil og skal holdes i synk manuelt, jf. kildekodekommentaren tilfoejet ved `monInterval`). Ny valgmulighed `250ms` tilfoejet oeverst i dropdown'en (`<option value="250">250ms</option>`) — IKKE sat som standard (kun 500ms er), praecis som anmodet. **Live-verificeret paa 10.1.1.153 (v7.9.51.0)**: hentet den servrerede `/editor`-side og bekraeftet tilstedevaerelsen af `"250">250ms` (1), `value="500" selected` (1), `preserveAspectRatio` (3 forekomster: grid-linjer + kurve bruger samme SVG), `non-scaling-stroke` (4), og `flex:1;min-width:0` (1) — alle sammen bekraefter aendringerne er reelt i den koerende firmware, ikke kun i kildekoden. Rørte: web/editor.html |
| BUG-406 | Brugerrapporteret: "ved access til system så kan jeg se at man ikke bliver promtet for user/pass access ved access til monitor seksion det er først ved en af de andre fanner" — efterfulgt af "vi har en rbac model hvor en af user modes er read only den bliver lidt undermineret hvis der står en side der ikke behøver noget access kontrol" | ✅ FIXED (efter to AskUserQuestion-afklaringer) | 🔴 CRITICAL (reelle registervaerdier laesbare uden login) | v7.9.52.0 | **Fund**: "Monitor" i topnavigationen linker til Dashboard (`/`) — DENNE side var den ENESTE der aldrig promptede for login, mens System/Editor/CLI/Logs/OTA alle deler en faelles `api()`-wrapper der popper `loginModal` op STRAKS ved et 401-svar. Rodaarsag: Dashboard henter sin kernedata fra `GET /api/metrics` (Prometheus-format) — bevidst gjort auth-fri i BUG-251 (v7.3.1), dengang begrundet "read-only data, ingen sikkerhedsrisiko". Det holdt IKKE: `/api/metrics` eksponerer bl.a. ALLE holding/input-registre (reel live proces-data), saa den fulde Dashboard-side (inkl. faktiske registervaerdier) var reelt laesbar af enhver paa netvaerket uden login nogensinde — hvilket brugeren selv paapegede underminerer hele pointen med RBAC's monitor+read (read-only) rolle (allerede dokumenteret i manualen som et krav, men reelt IKKE haandhaevet). `updateUserBadge()` (dashboard.html) hentede allerede login-status via `/api/user/me` (som korrekt svarer authenticated:false, ikke 401) til selve badge-teksten, men reagerede aldrig paa et negativt svar ved at vise loginModal — kun den positive gren opdaterede UI. **To afklarede designvalg (AskUserQuestion) foer fix**: (1) Dashboard skal fremover ogsaa prompte STRAKS for login, som alle andre sider (ikke forblive en bevidst offentlig monitor); (2) ingen ekstern Prometheus-scraper er i brug mod enheden i dag, saa /api/metrics kan trygt kraeve auth uden at braekke noget eksisterende. **Fix**: (a) updateUserBadge()s else-gren (ikke-autentificeret) kalder nu showLogin() — samme ikke-lukbare modal-moenster som resten af appen, kun trigget ÉN gang ved side-indlaesning (setTimeout(updateUserBadge,840), ingen gentagen setInterval, saa ingen irriterende gen-popup-loop); (b) api_handler_metrics() (api_handlers.cpp) fik CHECK_AUTH(req) tilfoejet — samme auth-niveau (ikke en saerskilt rolle-kraevning) som resten af dashboardets allerede-beskyttede read-endpoints (fx api_handler_gpio()), for konsistens; den nu-overfloedige separate http_rate_limit_check()-linje fjernet (CHECK_AUTH daekker den internt). **Bevidst IKKE aendret**: /api/dashboard/layout (GET+POST) forblev auth-fri — verificeret at den KUN eksponerer/gemmer ren UI-layout-praeference (kort-raekkefoelge/faner/skjult/custom), ingen proces-data, saa den underminerer ikke RBAC-modellen paa samme maade. Ingen af dashboardets ca. 20 andre fetch()-kaldesteder (fetchMetrics(), fetchAlarms() osv.) kraevede kodeaendring — de havde allerede et generelt if(!r.ok)return;-moenster, saa de nu-401'ende kald fejler allerede gracefully (viser blot ingen/gamle data i stedet for at crashe eller vise forkerte vaerdier) uden yderligere indgreb. **Live-verificeret paa 10.1.1.153 (v7.9.52.0)**: GET /api/metrics uden session → 401 (var 200 foer); med gyldig session → 200 uaendret; alle 7 sider (/, /system, /editor, /cli, /logs, /ota, /io) svarer fortsat 200; Logic1/Logic2 upaavirket. Rørte: web/dashboard.html, src/api_handlers.cpp |
| FEAT-407 | Brugerønsket, umiddelbart efter BUG-406: "lave en ny fanne som skal være defaul landings page monitor badgers hvor vi ikke afkræver login men vi skal kunne tilret hvad badger vi se på den side fra en adm side i system" — efterfulgt af eksplicit afklaring (AskUserQuestion) om at admin skal vælge FRA DE EKSISTERENDE dashboard-kort (ikke et nyt opfundet badge-katalog) | ✅ DONE (v1: 13/18 kort), live-verificeret | 🟡 HIGH (ny offentlig side, design-tungt) | v7.9.53.0 | **Design**: Dashboard flyttet `/`→`/dashboard` (kræver login, jf. BUG-406); `/` er nu en ny, login-fri "statusside" (`web/status.html`, `src/web_status.cpp`) — en TRIMMET kopi af dashboard.html (samme etablerede "hver side selvstændig"-konvention): kun de kort-relevante JS-funktioner (`updateDashboard()`, `parsePrometheus()`, `drawSparkline()`, fmt-hjælpere) + kort-HTML er portet, INGEN write-kontroller (Modbus R/W-testværktøj, GPIO/analog-output-skriv, alarm-kvittér, log-ryd/eksport — alt fjernet, ikke bare skjult), INGEN drag/resize/layout-editor, INGEN Registre/RegisterMap/Indstillinger-faner. `GET /api/metrics/public` (ny, auth-fri) genbruger `api_handler_metrics()`s krop via en delt `send_metrics_response(req, include_registers)`-hjælper — identisk med `/api/metrics` MINUS register-dumpet (samme udskillelse som BUG-406 allerede indførte). **Persisteret**: `public_dashboard_cards[220]` (PersistConfig, schema 27→28, tom streng = INGEN kort — bevidst opt-IN, ikke opt-OUT, da flere kort viser reel drifts-data). REST: `GET /api/public-dashboard/cards` (auth-fri — ren metadata om HVILKE kort er valgt, ikke selve dataen) + `POST` (CHECK_AUTH_WRITE, admin-only, validerer mod en fast kendt kort-ID-liste). Admin-UI: nyt kort "Offentlig statusside" i `web/system.html` (checkbox-liste, samme mønster som RBAC-rolle-checkboxene). **To sikkerhedsfund undervejs, begge afklaret med brugeren (AskUserQuestion) FØR kode**: (1) kun 13 af de 18 eksisterende dashboard-kort henter rent fra `/api/metrics` — de øvrige 5 (Modbus Aktivitetslog, Trend Recorder, TCP Forbindelser, Alarm Historik, Hændelseslog) henter fra separate, hver-for-sig login-krævende endpoints; brugeren valgte at bygge alle 18, men pga. denne sessions resterende kapacitet er KUN de 13 rent metrics-drevne kort porteret til v1 — de resterende 3 (mbactivity/trendrec/syslog, som "blot" kræver nye auth-fri endpoint-varianter uden yderligere risikovurdering) og de 2 bevidst udelukkede (se næste punkt) er en klar, afgrænset opfølgning, IKKE glemt. (2) "Alarm Historik"-kortet specifikt udelukket permanent (ikke kun v1-udskudt) — dets data (`/api/alarms`) blev låst bag login i SECURITY_INDEX #12 PRÆCIS fordi den dengang (auth-fri) lækkede kilde-IP+brugernavn for fejlede loginforsøg; at gøre kortet offentligt ville genindføre denne allerede rettede lækage. "TCP Forbindelser"-kortet udelukket af samme princip (afledt selvstændigt, ikke brugerbekræftet) — det viser forbundne klienters IP+brugernavn (SSE/Telnet), samme reconnaissance-kategori. Netværkskortets IP/Gateway/DNS/SSID/MAC/Speed-detaljer (kræver fulde `/api/wifi`+`/api/ethernet`-configendpoints, ikke kun metrics) er ligeledes UDELADT fra den offentlige variant — kun forbindelses-status (Forbundet/Ikke forbundet) vises, ikke netværkstopologi. **Live-verificeret på 10.1.1.153 (v7.9.53.0)**: alle 8 sider (inkl. nye `/`) svarer 200; `GET /api/metrics/public` uden session → 200, INGEN `modbus_holding_register`/`modbus_input_register`-linjer (grep-bekræftet); `GET /api/metrics` fortsat 401 uden session (BUG-406 uændret); `POST /api/public-dashboard/cards` med et ukendt kort-ID → 400 (valideringen virker); sat 4 kort synlige, bekræftet de dukkede op i det auth-frie `GET /api/public-dashboard/cards`-svar, derefter nulstillet til tomt (ren tilstand). Admin-UI'ets 13 checkboxes bekræftet til stede i den servrerede `/system`-side. Logic1/Logic2 upåvirket. Rørte: include/types.h, include/constants.h, src/config_load.cpp, src/api_handlers.cpp, include/api_handlers.h, src/http_server.cpp, include/web_status.h (ny), src/web_status.cpp (ny), web/status.html (ny), web/system.html, web/dashboard.html, web/editor.html, web/cli.html, web/io.html, web/logs.html, web/ota.html, scripts/gzip_web_assets.py — **v7.9.54.0-tilføjelse**: brugerønske om at få "Modbus Aktivitetslog" med som en opfølgning — viste sig at kræve INGEN backend-ændring, da `GET /api/modbus/activity` allerede var auth-fri (kun `CHECK_API_ENABLED`, ingen `CHECK_AUTH` — ikke tidligere bemærket/udnyttet). Kortet portet til `web/status.html` med filtrering (rolle/kilde/status/slave, alt klient-side på allerede-hentet data) + CSV-eksport (ren klient-genereret download af allerede-offentlig data, intet server-skriv) bevaret; Stop/Start-logning og Ryd log-knapperne (reelle POST/DELETE-handlinger) bevidst UDELADT, samme write-kontrol-princip som resten af siden. `mbactivity` tilføjet til `PUBLIC_DASHBOARD_CARD_IDS[]` og admin-UI'ets checkbox-katalog. Live-verificeret: card+funktioner til stede i den servrerede side, admin kunne aktivere/se ægte aktivitetsdata uden login, nulstillet til ren tilstand bagefter. 14/18 kort nu understøttet — trendrec+syslog (kræver nye endpoint-varianter) samt alarms+tcpmonitor (bevidst permanent udelukket) resterer. |
| FEAT-408 | Brugerønsket, kronologisk: "hvad har vi af frie gpio pins hvis vi ikke bruger ethernet modul" → "ok så hvis vi vil have understøtelse for 2 extra modbus rs485 modul... kan vi håndtere det?" → "jeg tænker vi gå med ene uart UART2, lave en seksion settings hvor vi til/fra vælger moduler" → EFTER et forkastet Tier-1-forslag ("master på UART2, Slave fast UART1"): "det skal være muligt at vælge om extra modbus interface skal køre i master eller slave, sådan at vi kan have 2 master eller en kombination" — endte (AskUserQuestion) med brugerens eksplicitte valg af "Fuldt 2 uafhængige masters" over en lettere rolle-vælger-model | 🔴 RULLET TILBAGE (hardware-blocker, se detaljer) | 🟡 HIGH (stor forsøgt feature, endte som vigtig hardware-erkendelse) | v7.9.55.0→v7.9.57.0 (implementeret→rullet tilbage) | **Design (bevidst, sikkerhedsdrevet)**: Master #2 blev bygget som en fuldstændig SIDEORDNET, selvstændig motor (eget UART, egen kø/cache/task/mutex/PersistConfig-felt) i stedet for at parameterisere den eksisterende, produktionskørende Master #1/`mb_async`-kode — nul risiko for Logic1/eksisterende installationer. **Genereringsteknik**: `mb_async2.cpp`/`modbus_master2.cpp` blev produceret af et Python-script (`gen_master2.py`, scratchpad, ikke committet) der læser de ORIGINALE filer og anvender en ordbog af `\|`-ordgrænse-regex-omdøbninger. **Fire kritiske fejl fundet og rettet EFTER generering**: (1) baggrundstasken kaldte STADIG Master #1's `modbus_master_read_coil/write_holdings/osv.` i alle 8 switch-cases — ville have sendt Master #2's trafik over Master #1's UART; (2) `mb_async2_reset_stats()` nulstillede Master #1's `g_modbus_bus_busy_errors` i stedet for `g_modbus_bus2_busy_errors`; (3) FC03-multi-read-svar blev skrevet til den DELTE `g_mb_multi_reg_buf` — en race condition hvis begge motorer kørte multi-register-operationer samtidig; (4) en FULD, ikke-omdøbt kopi af `modbus_master_calc_crc()` gav en "multiple definition"-linker-fejl. Et arkitektonisk selv-fund undervejs: ES32D26's `config_load.cpp` viste at `modbus_slave_uart` default er **2** (`Serial2_inst`), ikke 1 — hardware-UART-**periferi #2**, ikke #1, er det der allerede er i brug af Slave/Master #1 på GPIO1/3/21, så Master #2 blev rettet til at bruge **`Serial1_inst`** (periferi #1, det faktisk ledige) i stedet. CLI-laget (`mb2 read/write/scan`, `set modbus-master2 ...`, `show modbus-master2`, `set modul rs485 uart2 ... role off\|slave\|master`) blev færdigimplementeret og kompilerede rent — undervejs blev to selvstændige `normalize_alias()`-bugs fundet og rettet (manglende regel for "MODBUS-MASTER2"/"MB2" faldt igennem til raw, case-sensitiv streng; "role" normaliserer til "ROLES" pga. en allerede-eksisterende regel, ikke "ROLE" som antaget). **Hardware-blocker (roden til rollback)**: Live-test på 10.1.1.153 viste at `set modbus-master2 enabled on` konsekvent fik enheden til at hænge i 20+ sekunder og derefter panic-genstarte. Omfattende, metodisk isolering (RTC_NOINIT_ATTR-checkpoints der overlever reboot, da RAM-only state ellers går tabt ved crashet) afslørede først en alvorlig metodologisk faldgrube: generiske CLI-infrastruktur-checkpoints (i `api_handler_cli_exec`/`cli_shell_execute_command`) blev overskrevet af selve den efterfølgende `show`-aflæsningskommando og gav falsk-varierende resultater (34/100/112 på tværs af identisk kode) — først efter at fjerne disse og kun beholde checkpoints der UDELUKKENDE rammes af selve "enabled"-kommandoen, blev signalet pålideligt: hele `cli_cmd_set_modbus_master2_enabled()`-kæden (UART-init, mutex, FreeRTOS-task-oprettelse) gennemførte 100% uden fejl hver gang. Et fuldt symboliseret backtrace (fanget via brugerens fysiske seriel-konsol, symboliseret med `xtensa-esp32-elf-addr2line` mod den lokale ELF-build) viste definitivt at panic'et (`LoadProhibited`, `EXCVADDR` tæt på NULL) skete INDE I `malloc()`s TLSF-heap-allokator, kaldt fra `api_handler_cli_exec()`s eget `resp_buf = malloc(RESP_SIZE)` — dvs. EFTER at hele Master #2-aktiveringen allerede var fuldført: heapen var allerede korrumperet, og crashet manifesterede sig først ved den næste, urelaterede malloc, på hvilken core end der tilfældigvis ramte den først (forklarer den tilsyneladende ikke-deterministiske Core 0 vs. Core 1-variation mellem forsøg). Trinvis binær isolering (fjernede task-oprettelse, fjernede UART-init helt, testede med Arduino-default-pins i stedet for egne, deaktiverede Master #1 helt for at udelukke "to samtidige UART-drivere") isolerede roden 100% til selve `Serial1_inst.begin()`-kaldet — reproducerbart UANSET pins, UANSET om mutex/task oprettes, UANSET om noget andet UART-periferi er aktivt samtidig. Stærkeste forklaring: `platformio.ini`s `-mfix-esp32-psram-cache-issue`-flag (linje 146) er Espressifs officielle afbødning af en kendt, dokumenteret ESP32-silicium-erratum (PSRAM-cache kan korrumperes af en interrupt der rammer samtidig med en cache-miss) — denne enhed er en ESP32-WROVER med 4MB PSRAM (bekræftet via `show version`), og flagget dækker ikke nødvendigvis en helt ny, første-gangs interrupt-kilde som en fabriksfrisk UART-periferi. Master #1's eksisterende UART-brug (periferi #2) er allerede hærdet gennem denne kodebases lange historik af timing-bugs (BUG-333/336/337/338/341); periferi #1 har aldrig været brugt før og rammer formentlig samme klasse hardware-problem for første gang. **Brugerens beslutning**: stop implementeringen helt, rul koden tilbage, men analysér hvad der kan genbruges til en fremtidig, anderledes arkitektur (se note nedenfor). **Rollback udført**: alle 6 nye filer slettet (`modbus_master2.cpp/.h`, `mb_async2.cpp/.h`, `cli_commands_modbus_master2.cpp/.h`); alle Master#2-specifikke tilføjelser fjernet fra `cli_parser.cpp` (normalize_alias-regler, SET/SHOW/MB2-dispatch-grene, `print_modbus_master2_help()`, RS485-rolle-parsing), `main.cpp` (includes + boot-kald), `uart_driver.h/.cpp` (`uart2_master_*`-familien), `mb_activity_log.h` (`MB_ACTIVITY_ROLE_MASTER2`), `api_handlers.cpp` (aktivitetslog-"master2"-visning), `st_builtin_modbus.h/.cpp` (`g_mb_multi_reg_buf2`), `constants.h` (`PIN_UART2_*`, `MODBUS_MASTER2_*_PIN`). **VIGTIGT — bevidst IKKE rullet tilbage**: `CONFIG_SCHEMA_VERSION` forblev 29 (ikke tilbage til 28), og `PersistConfig.modbus_master2`/`uart2_role`-felterne blev BEVARET (nu som reserverede/ubrugte felter, samme etablerede mønster som andre dead-code-felter i denne kodebase) — fordi 10.1.1.153 og evt. andre enheder allerede havde migreret NVS-lageret til schema 29-layoutet under testperioden; at fjerne feltet og rulle schema tilbage til 28 ville have fået disse enheder til at falde tilbage til fabriksdefaults ved næste boot (ACL/RBAC/WiFi/ST Logic-programmer tabt). Live-verificeret efter rollback: `pio run -e es32d26` kompilerer og linker rent (Flash 94,5%, ned fra 95,8% med FEAT-408 inde); Master #1's mode blev midlertidigt sat til "off" under isoleringstests og eksplicit gendannet til "master" + gemt + rebootet før afslutning. **Fremtidig retning (brugerens idé, IKKE implementeret)**: et fysisk separat "extension board" med sine egne UART-kanaler til Modbus RTU, som denne PLC kommunikerer med over en API i stedet for at tale UART-hardware direkte selv — løser hardware-begrænsningen ved slet ikke at bruge ESP32'ens eget, problematiske UART-periferi #1. Analyse af genbrugelighed fra det droppede Master #2-arbejde: (1) **Kø/cache/adaptive-backoff-laget** (`mb_async2.cpp`s prioritetskø, dedupliering, TTL-cache, per-slave-backoff, PENDING-recovery) er 100% TRANSPORT-UAFHÆNGIGT og direkte genbrugeligt — kun den nederste `send_request()`-funktion skal udskiftes til at sende en API-forespørgsel til extension-boardet i stedet for at tale UART; (2) **CLI/ST-Logic-brugerfladen** (`mb2 read/write/scan`, den planlagte `MB2_*`-builtin-familie) er ligeledes transport-uafhængig fra brugerens perspektiv og genbrugelig næsten 1:1; (3) **selve protokol/UART-transport-laget** (`modbus_master2.cpp`s `send_request`, CRC, frame-opbygning, `uart2_master_*`) er IKKE genbrugeligt i den nye arkitektur (extension-boardet ville selv implementere rå Modbus RTU) og var i øvrigt netop det lag der indeholdt hardware-blockeren — men det udgjorde kun et lille, isoleret lag ud af det samlede arbejde. Rørte i rollback: include/types.h, include/constants.h, src/config_load.cpp, include/mb_activity_log.h, src/api_handlers.cpp, include/st_builtin_modbus.h, src/st_builtin_modbus.cpp, include/uart_driver.h, src/uart_driver.cpp, src/main.cpp, src/cli_parser.cpp, samt sletning af src/modbus_master2.cpp, include/modbus_master2.h, src/mb_async2.cpp, include/mb_async2.h, src/cli_commands_modbus_master2.cpp, include/cli_commands_modbus_master2.h |
| BUG-012 | "both" mode binding skaber dobbelt output i 'show logic' | ✅ FIXED | 🟡 HIGH | v4.1.0 | Confusing UI display |
| BUG-013 | Binding visnings-rækkefølge matcher ikke var index | ✔️ NOT A BUG | 🔵 LOW | v4.1.0 | Design choice, not a bug |
| BUG-014 | execution_interval_ms bliver ikke gemt persistent | ✅ FIXED | 🟡 HIGH | v4.1.0 | Settings lost on reboot |
| BUG-015 | HW Counter PCNT ikke initialiseret uden hw_gpio | ✅ FIXED | 🔴 CRITICAL | v4.2.0 | HW counter doesn't work |
| BUG-016 | Running bit (bit 7) ignoreres | ✅ FIXED | 🔴 CRITICAL | v4.2.0 | Counter control broken |
| BUG-017 | Auto-start ikke implementeret | ✅ FIXED | 🔴 CRITICAL | v4.2.0 | Startup behavior inconsistent |
| BUG-018 | Show counters display respects bit-width | ✅ FIXED | 🟡 HIGH | v4.2.0 | Display truncation |
| BUG-019 | Show counters race condition (atomic reading) | ✅ FIXED | 🟡 HIGH | v4.2.0 | Intermittent display corruption |
| BUG-020 | Manual register configuration allowed (should be disabled) | ✅ FIXED | 🔴 CRITICAL | v4.2.0 | User can break system with bad config |
| BUG-021 | Delete counter command missing | ✅ FIXED | 🟡 HIGH | v4.2.0 | Can't reconfigure counters |
| BUG-022 | Auto-enable counter on running:on not working | ✅ FIXED | 🟡 HIGH | v4.2.0 | Counter state inconsistent |
| BUG-023 | Compare doesn't work when disabled | ✅ FIXED | 🟡 HIGH | v4.2.0 | Output stuck after disable |
| BUG-024 | PCNT counter truncated to 16-bit (raw reg limited to 2000) | ✅ FIXED | 🔴 CRITICAL | v4.2.0 | Counter value overflow |
| BUG-025 | Global register overlap not checked (Counter/Timer/ST overlap) | ✅ FIXED | 🔴 CRITICAL | v4.2.0 | Register conflicts possible |
| BUG-026 | ST Logic binding register allocator not freed on change | ✅ FIXED | 🔴 CRITICAL | v4.2.3 | Stale allocation persists across reboot (NOW FIXED) |
| BUG-027 | Counter display overflow - values above bit_width show incorrectly | ✅ FIXED | 🟠 MEDIUM | v4.2.3 | CLI display shows unclamped values |
| BUG-028 | Register spacing too small for 64-bit counters | ✅ FIXED | 🔴 CRITICAL | v4.2.3 | Counter 1 overlaps Counter 2 registers |
| BUG-029 | Compare modes use continuous check instead of edge detection | ✅ FIXED | 🔴 CRITICAL | v4.2.4 | Reset-on-read doesn't work, bit4 always set |
| BUG-030 | Compare value not accessible via Modbus | ✅ FIXED | 🔴 CRITICAL | v4.2.4 | Threshold only settable via CLI, not SCADA |
| BUG-031 | Counter write lock ikke brugt af Modbus FC handlers | ✅ FIXED | 🔴 CRITICAL | v4.2.5 | 64-bit counter read kan give korrupt data |
| BUG-032 | Buffer overflow i ST parser (strcpy uden bounds) | ✅ FIXED | 🔴 CRITICAL | v4.2.5 | Stack corruption ved lange variabelnavne |
| BUG-033 | Variable declaration bounds check efter increment | ✅ FIXED | 🔴 CRITICAL | v4.2.5 | Buffer overflow på 33. variable |
| BUG-034 | ISR state læsning uden volatile cast | ✅ FIXED | 🟡 HIGH | v4.2.6 | Sporadisk manglende pulser ved høj frekvens |
| BUG-035 | Overflow flag aldrig clearet automatisk | ✅ FIXED | 🟡 HIGH | v4.2.6 | Sticky overflow kræver manuel reset |
| BUG-036 | SW-ISR underflow wrapper ikke (inkonsistent med SW) | ✅ FIXED | 🟠 MEDIUM | v4.2.5 | DOWN mode stopper ved 0 i ISR mode |
| BUG-037 | Jump patch grænse 512 i stedet for 1024 | ✅ FIXED | 🟠 MEDIUM | v4.2.5 | Store CASE statements kan fejle |
| BUG-038 | ST Logic variable memcpy uden synchronization | ✅ FIXED | 🟡 HIGH | v4.2.6 | Race condition mellem execute og I/O |
| BUG-039 | CLI compare-enabled parameter ikke genkendt | ✅ FIXED | 🟠 MEDIUM | v4.2.7 | Kun "compare:1" virker, ikke "compare-enabled:1" |
| BUG-040 | Compare bruger rå counter værdi i stedet for prescaled | ✅ FIXED | 🟡 HIGH | v4.2.8 | Compare ignorerer prescaler/scale, ukonfigurérbar |
| BUG-041 | Reset-on-read parameter placering og navngivning forvirrende | ✅ FIXED | 🟠 MEDIUM | v4.2.9 | Samme parameter navn for counter og compare reset |
| BUG-042 | normalize_alias() håndterer ikke "auto-load" | ✅ FIXED | 🟡 HIGH | v4.3.0 | "set persist auto-load" ikke genkendt af parser |
| BUG-043 | "set persist enable on" case sensitivity bug | ✅ FIXED | 🟡 HIGH | v4.3.0 | enabled blev altid false → printer "DISABLED" |
| BUG-044 | cli_cmd_set_persist_auto_load() case sensitive strcmp | ✅ FIXED | 🟠 MEDIUM | v4.3.0 | "ENABLE" eller "Enable" ville ikke virke |
| BUG-045 | Upload mode ignorerer brugerens echo setting | ✅ FIXED | 🟡 HIGH | v4.3.0 | "set echo on" har ingen effekt i ST upload mode |
| BUG-046 | ST datatype keywords (INT, REAL) kolliderer med literals | ✅ FIXED | 🔴 CRITICAL | v4.3.1 | REAL/INT variable declarations fejler med "Unknown variable" |
| BUG-047 | Register allocator ikke frigivet ved program delete | ✅ FIXED | 🔴 CRITICAL | v4.3.2 | "Register already allocated" efter delete/recreate |
| BUG-048 | Bind direction parameter ignoreret | ✅ FIXED | 🟡 HIGH | v4.3.3 | "input" parameter ikke brugt, defaults altid til "output" |
| BUG-049 | ST Logic kan ikke læse fra Coils | ✅ FIXED | 🔴 CRITICAL | v4.3.3 | "coil:20 input" læser fra discrete input i stedet for coil |
| BUG-050 | VM aritmetiske operatorer understøtter ikke REAL | ✅ FIXED | 🔴 CRITICAL | v4.3.4 | MUL/ADD/SUB bruger altid int_val, REAL arithmetic giver 0 |
| BUG-051 | Expression chaining fejler for REAL | ✅ FIXED | 🟡 HIGH | v4.3.5 | "a := b * c / d" fejler, men separate statements virker |
| BUG-052 | VM operators mangler type tracking | ✅ FIXED | 🔴 CRITICAL | v4.3.6 | Comparison/logical/bitwise operators bruger st_vm_push() i stedet for st_vm_push_typed() |
| BUG-053 | SHL/SHR operators virker ikke | ✅ FIXED | 🔴 CRITICAL | v4.3.7 | Parser precedence chain mangler SHL/SHR tokens |
| BUG-054 | FOR loop body aldrig eksekveret | ✅ FIXED | 🔴 CRITICAL | v4.3.8 | Compiler bruger GT i stedet for LT i loop condition check |
| BUG-055 | Modbus Master CLI commands ikke virker | ✅ FIXED | 🔴 CRITICAL | v4.4.0 | normalize_alias() mangler parameter entries |
| BUG-056 | Buffer overflow i compiler symbol table | ✅ FIXED | 🔴 CRITICAL | v4.4.3 | strcpy uden bounds check i st_compiler_add_symbol() |
| BUG-057 | Buffer overflow i parser program name | ✅ FIXED | 🟠 MEDIUM | v4.4.3 | strcpy hardcoded string (low risk) |
| BUG-058 | Buffer overflow i compiler bytecode name | ✅ FIXED | 🟠 MEDIUM | v4.4.3 | strcpy program name til bytecode uden bounds check |
| BUG-059 | Comparison operators ignorerer REAL type | ✅ FIXED | 🔴 CRITICAL | v4.4.3 | EQ/NE/LT/GT/LE/GE bruger kun int_val, REAL comparison fejler |
| BUG-060 | NEG operator ignorerer REAL type | ✅ FIXED | 🟠 MEDIUM | v4.4.3 | Unary minus bruger kun int_val, -1.5 bliver til -1 |
| BUG-063 | Function argument overflow validation | ✅ FIXED | 🟡 HIGH | v4.4.3 | Parser bruger break i stedet for return NULL (compilation fejler ikke) |
| BUG-065 | SQRT mangler negative input validation | ✅ FIXED | 🟠 MEDIUM | v4.4.4 | sqrtf(negative) returnerer NaN, crasher beregninger |
| BUG-067 | Lexer strcpy buffer overflow risiko | ✅ FIXED | 🔴 CRITICAL | v4.4.4 | 12× strcpy uden bounds check (token value 256 bytes) |
| BUG-068 | String parsing mangler null terminator | ✅ FIXED | 🟡 HIGH | v4.4.4 | Loop limit 250 men buffer 256, strcpy kan fejle |
| BUG-072 | DUP operator mister type information | ✅ FIXED | 🟠 MEDIUM | v4.4.4 | REAL værdier duplikeres som INT → forkerte beregninger |
| BUG-073 | SHL/SHR mangler shift amount validation | ✅ FIXED | 🟠 MEDIUM | v4.4.4 | Shift >= 32 er undefined behavior på ESP32 |
| BUG-074 | Jump patch silent failure | ✅ FIXED | 🟠 MEDIUM | v4.4.4 | Bounds check returnerer uden fejlmelding → bytecode korruption |
| BUG-069 | INT literal parsing overflow | ✅ FIXED | 🟠 MEDIUM | v4.4.5 | strtol kan overflow uden errno check → undefined values |
| BUG-070 | REAL literal parsing overflow | ✅ FIXED | 🟠 MEDIUM | v4.4.5 | strtof kan overflow uden errno check → undefined values |
| BUG-083 | Modulo INT_MIN overflow | ✅ FIXED | 🔵 LOW | v4.4.5 | INT_MIN % -1 er undefined behavior i C/C++ |
| BUG-084 | Modbus slave_id mangler validation | ✅ FIXED | 🟠 MEDIUM | v4.4.5 | Kan sende requests til invalid slave (0, 248-255) |
| BUG-085 | Modbus address mangler validation | ✅ FIXED | 🟠 MEDIUM | v4.4.5 | Kan sende requests med negative addresser |
| BUG-066 | AST malloc fejl ikke håndteret | ✅ FIXED | 🟡 HIGH | v4.4.5 | 19× ast_node_alloc() uden NULL check → segfault på OOM |
| BUG-087 | NEG operator INT_MIN overflow | ✅ FIXED | 🔵 LOW | v4.4.5 | -INT_MIN er undefined behavior i C/C++ |
| BUG-081 | Memory leak ved parser error | ✅ FIXED | 🟠 MEDIUM | v4.4.5 | Expression parsing chain lækker AST ved fejl |
| BUG-077 | Function return type validation | ✅ FIXED | 🟠 MEDIUM | v4.4.5 | SEL/LIMIT polymorfiske funktioner bruger forkert type |
| BUG-088 | ABS funktion INT_MIN overflow | ✅ FIXED | 🔴 CRITICAL | v4.4.5 | ABS(-2147483648) returnerer -2147483648 (ikke positiv) |
| BUG-089 | ADD/SUB/MUL integer overflow | ✅ FIXED | 🔴 CRITICAL | v4.4.5 | Ingen overflow checks på arithmetic → silent overflow |
| BUG-104 | Function argument NULL pointer | ✅ FIXED | 🟠 MEDIUM | v4.4.5 | parser_parse_expression() NULL ikke håndteret |
| BUG-105 | INT type skal være 16-bit, ikke 32-bit (IEC 61131-3) | ✅ FIXED | 🔴 CRITICAL | v5.0.0 | INT overflow ikke korrekt, mangler DINT/multi-register |
| BUG-106 | Division by zero gemmer gamle værdier | ✅ FIXED | 🔴 CRITICAL | v5.0.0 | Variabler kopieres tilbage fra VM også ved runtime error |
| BUG-107 | CLI bind display viser "HR#X" for coil input | ✅ FIXED | 🔵 LOW | v5.0.0 | Forvirrende CLI output, men funktionalitet virker |
| BUG-108 | CLI mangler `write h-reg value real` kommando | ✅ FIXED | 🔴 CRITICAL | v5.0.0 | Kan ikke skrive REAL værdier korrekt via CLI |
| BUG-109 | Multi-register bindings ikke frigivet korrekt ved delete | ✅ FIXED | 🔴 CRITICAL | v5.0.0 | DINT/REAL bindings frigiver kun 1 register ved sletning |
| BUG-110 | SUM funktion ikke type-aware (returnerer kun første parameter) | ✅ FIXED | 🔴 CRITICAL | v5.0.0 | SUM(5,3) returnerer 5 i stedet for 8 |
| BUG-116 | Modbus Master funktioner ikke registreret i compiler | ✅ FIXED | 🔴 CRITICAL | v5.0.0 | MB_READ_COIL, MB_WRITE_HOLDING osv. kan ikke kompileres |
| BUG-117 | MIN/MAX funktioner ikke type-aware | ✅ FIXED | 🔴 CRITICAL | v5.0.0 | MIN/MAX med REAL værdier giver forkerte resultater |
| BUG-118 | ABS funktion kun INT type | ✅ FIXED | 🟡 HIGH | v5.0.0 | ABS(-1.5) returnerer 1 i stedet for 1.5 |
| BUG-119 | LIMIT funktion ikke type-aware | ✅ FIXED | 🟡 HIGH | v5.0.0 | LIMIT med REAL værdier clampes forkert |
| BUG-120 | SEL return type mangler DINT håndtering | ✅ FIXED | 🟠 MEDIUM | v5.0.0 | SEL(cond, DINT1, DINT2) returnerer INT type |
| BUG-121 | LIMIT return type mangler DINT håndtering | ✅ FIXED | 🟠 MEDIUM | v5.0.0 | LIMIT(DINT_min, val, DINT_max) returnerer INT type |
| BUG-122 | CLI show logic timing og reset logic stats ikke tilgængelige | ✅ FIXED | 🟠 MEDIUM | v5.0.0 | Funktioner implementeret men ikke eksponeret i parser/header |
| BUG-123 | Parser accepterer syntax fejl (reserved keywords i statement position) | ✅ FIXED | 🔴 CRITICAL | v5.0.0 | "THEN THEN", "END_IF x := 5" accepteres uden fejl |
| BUG-124 | Counter 32/64-bit DINT/DWORD register byte order | ✅ FIXED | 🔴 CRITICAL | v5.0.0 | CLI read/write brugte MSW først, counter skriver LSW først |
| BUG-125 | ST Logic multi-register byte order (DINT/DWORD/REAL INPUT/OUTPUT) | ✅ FIXED | 🔴 CRITICAL | v5.0.0 | Variable bindings brugte MSW først, skulle bruge LSW først |
| BUG-126 | st_count redeclaration i cli_show.cpp | ✅ FIXED | 🔵 LOW | v4.4.0 | Variable declared twice in same function, compile error |
| BUG-127 | st_state declaration order (used before declared) | ✅ FIXED | 🔵 LOW | v4.4.0 | Variable used on line 382 but declared on line 415 |
| BUG-128 | normalize_alias() mangler BYTECODE/TIMING keywords | ✅ FIXED | 🟠 MEDIUM | v4.4.0 | `show logic <id> bytecode/timing` kommandoer virker ikke |
| BUG-129 | normalize_alias() returnerer ST-STATS i stedet for STATS | ✅ FIXED | 🟡 HIGH | v4.4.0 | `show logic stats` og `reset logic stats` virker ikke |
| BUG-130 | NVS partition for lille til PersistConfig med ST bindings | ✅ FIXED | 🔴 CRITICAL | v4.5.0 | ESP_ERR_NVS_NOT_ENOUGH_SPACE (4357) ved bind kommandoer |
| BUG-131 | CLI `set id` kommando virker ikke (SLAVE-ID vs ID mismatch) | ✅ FIXED | 🟡 HIGH | v4.5.0 | normalize_alias() returnerer "SLAVE-ID" men parser tjekker "ID" |
| BUG-132 | CLI `set baud` kommando virker ikke (BAUDRATE vs BAUD mismatch) | ✅ FIXED | 🟡 HIGH | v4.5.0 | normalize_alias() returnerer "BAUDRATE" men parser tjekker "BAUD" |
| BUG-133 | Modbus Master request counter reset mangler | ✅ FIXED | 🔴 CRITICAL | v4.5.2 | g_mb_request_count aldrig resettet → system blokerer efter 10 requests. FIX v2 (v7.9.0.2): Reset per-slot i stedet for per-cycle — alle 4 ST slots får nu egen quota |
| BUG-134 | MB_WRITE DINT arguments sender garbage data | ✅ FIXED | 🔴 CRITICAL | v4.6.1 | DINT slave_id/address bruger int_val i stedet for dint_val → garbage validering (Build #919) |
| BUG-135 | MB_WRITE_HOLDING mangler value type validering | ✅ FIXED | 🔴 CRITICAL | v4.6.1 | REAL/DWORD værdier bruger int_val → garbage sendt til remote register (Build #919) |
| BUG-136 | MB_WRITE_COIL mangler value type validering | ✅ FIXED | 🔴 CRITICAL | v4.6.1 | INT værdier bruger bool_val i stedet for konvertering → random coil state (Build #919) |
| BUG-137 | CLI `read h-reg <count> real/dint/dword` ignorerer count parameter | ✅ FIXED | 🟠 MEDIUM | v4.7.1 | Kan ikke læse arrays af multi-register værdier (Build #937) |
| BUG-138 | ST Logic upload error message generisk og ikke informativ | ✅ FIXED | 🔵 LOW | v4.7.1 | Viser kun "Failed to upload" uden detaljer (Build #940) |
| BUG-139 | `show logic stats` skjuler disabled programs med source code | ✅ FIXED | 🟠 MEDIUM | v4.7.1 | Pool total matcher ikke per-program sum (Build #948) |
| BUG-140 | Persistence group_count=255 buffer overflow i show config | ✅ FIXED | 🔴 CRITICAL | v4.7.1 | Out-of-bounds array access → garbage display + crash risk (Build #951 + recovery cmd #953) |
| BUG-141 | Save/load viser var_map_count i stedet for aktive mappings | ✅ FIXED | 🟠 MEDIUM | v4.7.1 | Viser "32 mappings" selvom alle er unused (Build #960) |
| BUG-142 | `set reg STATIC` blokerer HR238-255 fejlagtigt | ✅ FIXED | 🟠 MEDIUM | v4.7.3 | Validation blokerede HR200-299, nu korrigeret til HR200-237 (Build #995) |
| BUG-143 | ST Logic IR variable mapping begrænset til 8 per program | ✅ FIXED | 🟠 MEDIUM | v5.1.0 | Fixed via EXPORT keyword + dynamic IR pool allocation (Build #1032) |
| BUG-144 | Forvirrende CLI: "read reg" læser HR, men ST vars er i IR | ✅ FIXED | 🔵 LOW | v4.7.2 | Brugere forventer "read h-reg 220" viser ST vars, men skal bruge "read i-reg 220" (Build #973-974) |
| BUG-145 | CLI help message mangler "read input-reg" option | ✅ FIXED | 🔵 LOW | v4.7.2 | "read" uden argumenter viste ikke "input-reg" option selvom funktionen findes (Build #973) |
| BUG-146 | Use-after-free i config_save.cpp | ✅ FIXED | 🔴 CRITICAL | v4.7.3 | Memory corruption - debug print brugte frigivet pointer (config_save.cpp:175) (Build #995) |
| BUG-147 | Buffer underflow i modbus_frame.cpp | ✅ FIXED | 🔴 CRITICAL | v4.7.3 | Integer underflow i memcpy size → buffer overflow (modbus_frame.cpp:84,100) (Build #995) |
| BUG-148 | Printf format mismatch i cli_config_regs.cpp | ✅ FIXED | 🟡 HIGH | v4.7.3 | %ld format med int32_t argument - portability issue (cli_config_regs.cpp:398) (Build #995) |
| BUG-149 | Identical condition i modbus_master.cpp | ✅ FIXED | 🟠 MEDIUM | v4.7.3 | Redundant indre if-check altid sand (modbus_master.cpp:181) (Build #995) |
| BUG-150 | CTUD ikke implementeret i VM | ✅ FIXED | 🔴 CRITICAL | v4.8.1 | VM execution handler tilføjet for 5-arg CTUD counter (st_vm.cpp:1047-1059) (Build #1016) |
| BUG-151 | Type stack corruption i AND/OR/XOR/NOT operationer | ✅ FIXED | 🔴 HIGH | v4.8.0 | Legacy st_vm_pop() korrupterer type_stack → downstream type inference fejler (st_vm.cpp:483-527) (Build #1010) |
| BUG-152 | Signal processing antager REAL type uden validering | ✅ FIXED | 🟡 MEDIUM | v4.8.0 | SCALE/HYSTERESIS/BLINK/FILTER bruger direkte .real_val uden type check (st_vm.cpp:1070-1213) (Build #1011) |
| BUG-153 | FILTER hardcoded cycle time (10ms) | ✅ FIXED | 🟠 MEDIUM | v4.8.1 | Cycle time læses nu fra stateful->cycle_time_ms (st_logic_engine.cpp:51-55, st_builtin_signal.cpp:163-169) (Build #1015) |
| BUG-154 | Jump target validation manglede | ✅ FIXED | 🟠 MEDIUM | v4.8.0 | JMP/JMP_IF_FALSE/JMP_IF_TRUE validerer ikke target < instr_count (st_vm.cpp:1275-1329) (Build #1012) |
| BUG-155 | Buffer overflow i st_token_t.value | ✅ FIXED | 🔴 CRITICAL | v4.8.2 | Token buffer kun 256 bytes, lexer kan skrive mere → stack corruption (FIXED Build #1020) |
| BUG-156 | Manglende validation af function argument count | ✅ FIXED | 🔴 CRITICAL | v4.8.2 | Compiler validerer ikke antal argumenter → stack corruption (st_compiler.cpp:335-344) (Build #1018) |
| BUG-157 | Stack overflow risk i parser recursion | ✅ FIXED | 🔴 CRITICAL | v4.8.2 | Rekursiv descent uden depth limit → ESP32 crash (st_parser.h:27, st_parser.cpp:28,353-374) (Build #1018) |
| BUG-158 | NULL pointer dereference i st_vm_exec_call_builtin | ✅ FIXED | 🔴 CRITICAL | v4.8.2 | Stateful check EFTER brug → NULL deref (st_vm.cpp:1000-1272) (Build #1018) |
| BUG-159 | Integer overflow i FOR loop | ✅ MITIGATED | 🟡 HIGH | v4.8.2 | FOR loop overflow beskyttet af max_steps=10000 limit (st_logic_engine.cpp:62) |
| BUG-160 | Missing NaN/INF validation i arithmetic | ✅ FIXED | 🟡 HIGH | v4.8.2 | REAL arithmetik validerer ikke NaN/INF → propagering (st_vm.cpp:284-422) (Build #1018) |
| BUG-161 | Division by zero i SCALE function | ✅ FIXED | 🟡 HIGH | v4.8.2 | Returnerer arbitrær værdi uden error (st_builtin_signal.cpp:28-32) (Build #1018) |
| BUG-162 | Manglende bounds check på bytecode array | ✅ FIXED | 🟡 HIGH | v4.8.2 | target_addr ikke valideret → VM crash (st_compiler.cpp:150-156) (Build #1018) |
| BUG-163 | Memory leak i parser error paths | ✅ VERIFIED | 🟡 HIGH | v4.8.2 | st_ast_node_free rekursivt frigiver alle args (st_parser.cpp:140-145) |
| BUG-164 | Inefficient linear search i symbol lookup | ✅ ACCEPTABLE | 🟡 HIGH | v4.8.2 | O(n) acceptable for max 32 vars (st_compiler.cpp:73-80) |
| BUG-165 | Missing input validation i BLINK function | ✅ FIXED | 🟠 MEDIUM | v4.8.2 | Negative time → huge unsigned (st_builtin_signal.cpp:98-99) (Build #1019) |
| BUG-166 | Race condition i stateful storage access | ✔️ NOT A BUG | 🟠 MEDIUM | v4.8.2 | FALSE POSITIVE - alt kører single-threaded i Arduino loop() (st_logic_engine.cpp:54, st_vm.cpp:1222) |
| BUG-167 | No timeout i lexer comment parsing | ✅ FIXED | 🟠 MEDIUM | v4.8.2 | Unterminated comment scanner til EOF (st_lexer.cpp:50-63) (Build #1019) |
| BUG-168 | Missing validation af CASE branch count | ✅ FIXED | 🟠 MEDIUM | v4.8.2 | Max 16 branches ikke valideret → memory overwrite (st_compiler.cpp:475-574) (Build #1019) |
| BUG-169 | Inefficient memory usage i AST nodes | ✅ ACCEPTABLE | 🔵 LOW | v4.8.2 | Union ~600 bytes per node - acceptable for temporary compilation RAM (st_types.h:270-291) |
| BUG-170 | Missing overflow check i millis() wraparound | ✅ NOT A BUG | 🔵 LOW | v5.1.1 | Unsigned arithmetic handles wraparound correctly - documented (st_builtin_signal.cpp:122-128) (Build #1040) |
| BUG-171 | Suboptimal error messages i compiler | ✅ FIXED | 🔵 LOW | v5.1.1 | Compiler nu inkluderer line number i error messages (st_compiler.h:52, st_compiler.cpp:170-180, 893, 253) (Build #1040) |
| BUG-172 | Missing overflow detection i integer arithmetic | ✅ DOCUMENTED | 🟠 MEDIUM | v5.1.1 | Design choice dokumenteret: wrapping for performance (st_vm.cpp:280-287) (Build #1040) |
| BUG-173 | MOD operation med negative operands | ✅ DOCUMENTED | 🔵 LOW | v5.1.1 | C remainder semantics dokumenteret vs matematik modulo (st_vm.cpp:470-473) (Build #1040) |
| BUG-174 | Missing type validation i binary operations | ✅ FIXED | 🟠 MEDIUM | v5.1.1 | BOOL + BOOL nu valideret - giver type error (st_vm.cpp:273-277, 322-326, 371-375, 420-424, 463-467) (Build #1038) |
| BUG-175 | FILTER function med zero cycle time | ✅ FIXED | 🔵 LOW | v5.1.1 | Fallback dokumenteret med forklaring (st_builtin_signal.cpp:184-189) (Build #1040) |
| BUG-176 | HYSTERESIS function med inverterede thresholds | ✅ FIXED | 🔵 LOW | v4.8.2 | Ingen validation af high > low (st_builtin_signal.cpp:69-76) (Build #1019) |
| BUG-177 | strcpy uden bounds check i lexer operators | ✅ FIXED | 🔵 LOW | v5.1.1 | strcpy → strncpy for 2-char operators (:=, <>, <=, >=, **) (st_lexer.cpp:412-445) (Build #1038) |
| BUG-178 | EXPORT variables ikke skrevet til IR 220-251 | ✅ FIXED | 🔴 CRITICAL | v5.1.1 | EXPORT keyword allokerede pool men skrev aldrig værdier til IR → Modbus read viste altid 0 (ir_pool_manager.cpp:166-236, st_logic_engine.cpp:108-110) (Build #1044) |
| BUG-179 | CLI read i-reg mangler type parameter support | ✅ FIXED | 🟠 MEDIUM | v5.1.2 | "read i-reg 220 int/dint/dword/real" fejlede med "antal skal være større end 0" fordi type blev parset som count → atoi("int")=0 (cli_show.cpp:2687-2960) (Build #1048) |
| BUG-180 | Counter overflow mister ekstra counts ved wraparound | ✅ FIXED | 🟡 HIGH | v5.1.3 | Ved overflow fra 65535 → 0, mistedes ekstra counts. Nu bevares overflow: start_value + (pcnt_value - max_val - 1) (counter_hw.cpp:118-123, counter_sw.cpp:156-161, counter_sw_isr.cpp:190-195) (Build #1052) |
| BUG-181 | DOWN mode underflow wrapper til max_val i stedet for start_value | ✅ FIXED | 🔴 CRITICAL | v5.1.4 | DOWN counting wrapper fejlagtigt til 65535 i stedet for start_value ved underflow (0-1). UP mode: wrap til start_value ✓. DOWN mode: burde wrappe til start_value også! Fixet i alle 3 modes: SW/SW_ISR/HW (counter_sw.cpp:114-122, counter_sw_isr.cpp:39+54-60+77-84+101-107+124-130+236-237, counter_hw.cpp:93-105) (Build #1063) |
| BUG-182 | PCNT signed overflow ved 32768 + atol/atoll signed parsing | ✅ FIXED | 🔴 CRITICAL | v5.1.5 | DOBBELT BUG: (1) PCNT hardware er signed 16-bit (-32768 til 32767), men vi vil have unsigned (0-65535). Hardware events disabled. (2) atol()/atoll() parser signed værdier → 32-bit counter med start_value > 2.1B får negativt tal! Fix: strtoul/strtoull for unsigned parsing (cli_commands.cpp:124-127+171-174, pcnt_driver.cpp:69-99) (Build #1069) |
| BUG-183 | start_value kun uint16_t - begrænser 32/64-bit counters | ✅ FIXED | 🔴 CRITICAL | v5.1.6 | CounterConfig.start_value var uint16_t (0-65535), men counters kan være 32-bit eller 64-bit. CLI brugte strtoul (32-bit). CLI display brugte (unsigned int) cast → trunkering. FIX: start_value → uint64_t, strtoul → strtoull, debug_print_uint → debug_print_ulong (types.h:82, cli_commands.cpp:124-127, cli_show.cpp:172+935+1485-1494+1506) (Build #1077) |
| BUG-184 | Frequency measurement giver forkerte resultater for DOWN counting | ✅ FIXED | 🟡 HIGH | v5.1.7 | Frequency algoritme antog altid UP counting. For DOWN: current_value < last_count er normalt, men koden gik i wrap-around branch og beregnede kæmpe forkert delta. FIX: Direction-aware delta beregning (counter_frequency.cpp:92-138) (Build #1074) |
| BUG-185 | Timer Mode 2 trigger_level parameter ikke brugt | ✔️ DESIGN | 🔵 LOW | v5.1.7 | trigger_level er legacy parameter - Mode 2 (Monostable) triggeres via Modbus ctrl_reg, ikke input-niveau. Dokumenteret som design choice |
| BUG-186 | Timer Mode 1 duration=0 kører hele sekvensen på én iteration | ✔️ DESIGN | 🔵 LOW | v5.1.7 | Hvis alle 3 phase durationer er 0, springer timer igennem alle faser på én loop iteration. Intentional "instant sequence" feature |
| BUG-187 | Timer ctrl_reg ikke initialiseret i defaults | ✅ FIXED | 🟠 MEDIUM | v5.1.7 | timer_config_defaults() satte ikke ctrl_reg, default var 0 → overlap med andre subsystemer. FIX: Smart defaults Timer 1→HR180, Timer 2→HR185, etc. (timer_config.cpp:64-67) (Build #1074) |
| BUG-188 | ISR underflow wrapper vs HW mode inkonsistens | ✔️ DESIGN | 🔵 LOW | v5.1.7 | SW/SW_ISR er edge-triggered (delta=1 altid), HW kan have delta>1. Simpel wrap er korrekt for ISR, kompleks wrap med overflow_amt er korrekt for HW |
| BUG-189 | Timer Mode 4 læser fra COIL i stedet for Discrete Input | ✔️ DESIGN | 🔵 LOW | v5.1.7 | Parameter hedder input_dis men koden læser registers_get_coil(). Bevidst design: tillader Modbus-triggered timer control. Dokumenteret |
| BUG-190 | ST Debug: total_steps_debugged tæller i OFF mode | ✅ FIXED | 🔵 LOW | v5.3.0 | FEAT-008 bugfix: Counter incrementeredes for alle steps, ikke kun debug mode. FIX: Kun tæl når mode != ST_DEBUG_OFF (st_logic_engine.cpp:100-103) (Build #1083) |
| BUG-191 | ST Debug: Ingen snapshot ved halt/error | ✅ FIXED | 🟠 MEDIUM | v5.3.0 | FEAT-008 bugfix: Når program haltede/fejlede under debugging blev ingen snapshot gemt → bruger kunne ikke se final state. FIX: Gem snapshot med REASON_HALT/REASON_ERROR (st_logic_engine.cpp:111-120) (Build #1083) |
| BUG-192 | Dobbelt-close af socket i https_close_fn | ✅ FIXED | 🔴 CRITICAL | v6.0.4 | else-grenen kaldte close(sockfd) men httpd lukker også → heap corruption. FIX: Fjernet close() i else-gren (https_wrapper.c:166) (Build #1126) |
| BUG-193 | Manglende null-terminering i upload buffer ved fuld kapacitet | ✅ FIXED | 🔴 CRITICAL | v6.0.4 | Når cli_upload_buffer_pos >= CLI_UPLOAD_BUFFER_SIZE → strlen() læser ud over buffer. FIX: else-gren null-terminerer ved [SIZE-1] (cli_shell.cpp:232,486) (Build #1126) |
| BUG-194 | URI routing med strstr("/source") giver falsk positiv | ✅ FIXED | 🟡 HIGH | v6.0.4 | strstr matchede /source_backup, /sources etc. FIX: Erstattet med suffix-check strcmp (api_handlers.cpp:708) (Build #1126) |
| BUG-195 | GPIO write API endpoint mangler pin-validering | ✅ FIXED | 🟡 HIGH | v6.0.4 | Kunne skrive til vilkårlige GPIO pins inkl. flash-pins (6-11). FIX: Validerer mod var_maps output-konfiguration (api_handlers.cpp:1240) (Build #1126) |
| BUG-196 | Hardkodede registeradresser i show logic stats | ✅ FIXED | 🟡 HIGH | v6.0.4 | Literal 252/260/268/276/284-292 antog 4 programmer. FIX: Bruger nu ST_LOGIC_*_REG_BASE konstanter (cli_show.cpp:2094-2122) (Build #1126) |
| BUG-197 | wifi_power_save config har ingen effekt | ✅ FIXED | 🟡 HIGH | v6.0.4 | esp_wifi_set_ps() blev aldrig kaldt med config-værdi. FIX: Tilføjet apply i config_apply.cpp (Build #1126) |
| BUG-198 | Manglende defaults for api_enabled og priority | ✅ FIXED | 🟠 MEDIUM | v6.0.4 | Ved første boot/migration var api_enabled=0 (disabled). FIX: Defaults api_enabled=1, priority=1 i config_struct/config_load/network_config (Build #1126) |
| BUG-199 | show config mangler sektionsfiltrering | ✅ FIXED | 🟠 MEDIUM | v6.0.4 | "show config wifi" virkede ikke - ingen section-parameter support. FIX: cli_cmd_show_config(section) med filter for WIFI/MODBUS/COUNTER/etc (cli_show.cpp, cli_parser.cpp) (Build #1126) |
| BUG-200 | Privat TLS-nøgle ikke beskyttet i .gitignore | ✅ FIXED | 🔴 CRITICAL | v6.0.4 | certs/prvtkey.pem kunne committes ved uheld. FIX: Tilføjet certs/prvtkey.pem og certs/*.key til .gitignore (Build #1126) |
| BUG-201 | ESP-IDF middle-wildcard URI routing matcher aldrig | ✅ FIXED | 🔴 CRITICAL | v6.0.5 | `httpd_uri_match_wildcard` understøtter kun `*` i ENDEN af URI. `/api/logic/*/source` matchede aldrig. FIX: Fjernet 8 middle-wildcard URIs, intern suffix-routing i wildcard handlers (http_server.cpp, api_handlers.cpp) (Build #1162) |
| BUG-202 | Source pool entries ikke null-termineret - strlen læser naboer | ✅ FIXED | 🔴 CRITICAL | v6.0.5 | `st_logic_get_source_code()` returnerer pointer i shared pool UDEN null-terminator. `strlen()` læste ind i efterfølgende programmer. FIX: Brug `prog->source_size`, opret null-termineret kopi (api_handlers.cpp) (Build #1162) |
| BUG-203 | /api/config returnerer ufuldstændig konfiguration | ✅ FIXED | 🟡 HIGH | v6.0.5 | Manglede modbus master, counter detaljer, timer detaljer, GPIO, ST Logic, modules, persistence. FIX: Komplet rewrite med alle sektioner matchende `show config` (api_handlers.cpp) (Build #1162) |
| BUG-204 | WWW-Authenticate header tabt pga. httpd response rækkefølge | ✅ FIXED | 🟡 HIGH | v6.0.5 | Header sat FØR `set_type`/`set_status` blev overskrevet. FIX: Flyttet til `api_send_error()` EFTER set_type/set_status men FØR sendstr (api_handlers.cpp) (Build #1162) |
| BUG-205 | API responses cached af browser - manglende Cache-Control | ✅ FIXED | 🟡 HIGH | v6.0.5 | Uden `Cache-Control` header cachede browsere API responses aggressivt. Efter reflash viste browser gammel data. FIX: `Cache-Control: no-store, no-cache, must-revalidate` på alle API svar (api_handlers.cpp) (Build #1162) |
| BUG-206 | /api/ trailing slash returnerer 404 | ✅ FIXED | 🔵 LOW | v6.0.5 | Kun `/api` var registreret, `/api/` gav 404. FIX: Tilføjet separat URI registration for `/api/` (http_server.cpp) (Build #1162) |
| BUG-207 | HTTP server stack_size 4096 for lille til API handlers | ✅ FIXED | 🔴 CRITICAL | v6.0.6 | Plain HTTP task stack kun 4096 bytes → POST `/api/logic/{id}/source` og `/bind` crasher ESP32 (stack overflow). HTTPS brugte 10240. FIX: Øget til 8192 + flyttet handler response buffere fra stack til heap (http_server.cpp:463, api_handlers.cpp) (Build #1196) |
| BUG-208 | GET /api/logic/{id}/stats stack buffer overflow | ✅ FIXED | 🟡 HIGH | v6.0.6 | `char buf[HTTP_JSON_DOC_SIZE]` (1024 bytes) på stack + JsonDocument (~700 bytes) overskrider 4096 stack. FIX: Flyttet buf til heap med malloc (api_handlers.cpp:1589) (Build #1196) |
| BUG-209 | GET /api/logic/{id}/source timeout - delvis data | ✅ FIXED | 🟡 HIGH | v6.0.6 | Content-Length sendes men kun delvis data modtages. Forårsaget af stack overflow (BUG-207). VERIFICERET FIXED efter stack_size øgning til 8192 (Build #1196) |
| BUG-210 | API source upload kompilerer ikke automatisk | ✅ FIXED | 🟡 HIGH | v6.0.6 | POST `/api/logic/{id}/source` kaldte kun `st_logic_upload()` men IKKE `st_logic_compile()`. FIX: Tilføjet `st_logic_compile()` kald efter upload (api_handlers.cpp:1084) (Build #1197) |
| BUG-211 | Parser håndterer ikke FUNCTION/FUNCTION_BLOCK definitioner i PROGRAM body | ✅ FIXED | 🔴 CRITICAL | v6.0.6 | `st_parser_parse_statement()` havde ingen case for FUNCTION/FUNCTION_BLOCK. FIX: (1) `st_parser_parse_program()` parser FUNCTION/FB mellem VAR og BEGIN, (2) valgfri BEGIN i funktionskrop, (3) funktionskald-som-statement support. Alle 11 FEAT-003 tests bestået (st_parser.cpp:1668-1704, 697-747) (Build #1224) |
| BUG-212 | Source pool null-terminering mangler i compiler path | ✅ FIXED | 🔴 CRITICAL | v6.0.6 | `st_logic_get_source_code()` returnerer pointer ind i shared pool UDEN null-terminator. Lexeren bruger '\0' til EOF-detektion → parser læser forbi kildekoden ind i tilstødende pool-data. Symptom: parse fejl med variabelnavne fra andre programmer. FIX: null-termineret kopi i `st_logic_compile()` + memory leak fix i `st_logic_delete()` (st_logic_config.cpp:240-260, 376-380) (Build #1224) |
| BUG-213 | GPIO hardware pins statisk - ignorerer modbus slave/master config | ✅ FIXED | 🟠 MEDIUM | v6.0.7 | `show config`, `show gpio` og `show gpio <pin>` viste hardkodede pins uanset enabled status. Counter pins (GPIO 19/25/27/33) vist selvom counters disabled. FIX: Alle 3 kommandoer bruger nu dynamisk visning baseret på enabled flags (modbus slave/master + counter) + faktisk hw_gpio config (cli_show.cpp:373-411, 2291-2333, 2399-2434) |
| BUG-214 | Backup: ST Logic source korruption pga. manglende null-terminering | ✅ FIXED | 🔴 CRITICAL | v6.0.7 | `api_handler_system_backup()` brugte `st_logic_get_source_code()` pointer direkte i ArduinoJson. Source pool entries er IKKE null-terminerede (BUG-212). ArduinoJson læste forbi program boundary → Program 0 (646 bytes) serialiseret som 5158 bytes (sammenklæbet med program 1-3). Ved restore: pool overflow → program 0+2 source tabt, program 1+3 korrupt. FIX: malloc null-termineret kopi med source_size (api_handlers.cpp:3371) (Build #1241) |
| BUG-215 | Restore: var_maps tabt pga. st_logic_delete() side-effect | ✅ FIXED | 🔴 CRITICAL | v6.0.7 | Under restore blev var_maps restored FØR logic_programs. `st_logic_delete()` (st_logic_config.cpp:411-415) clearer ALLE var_map entries med matchende st_program_id som side-effect. Da logic_programs restore kalder delete+upload, blev alle netop restored var_maps slettet. FIX: Flyttet var_maps restore sektion til EFTER logic_programs restore (api_handlers.cpp) (Build #1241) |
| BUG-216 | Backup: IP-adresser som rå uint32_t i stedet for dotted strings | ✅ FIXED | 🟠 MEDIUM | v6.0.7 | Network felter (static_ip, static_gateway, static_netmask, static_dns) blev serialiseret som rå uint32_t little-endian integers (fx 1677830336 = 192.168.1.100). Ikke menneskelæseligt, platform-afhængigt. FIX: Serialiseres nu som dotted strings + backward-kompatibel `parse_ip_field()` helper der håndterer begge formater ved restore (api_handlers.cpp:3188) (Build #1241) |
| BUG-217 | Backup: Boolean felter inkonsistente (int vs true/false) | ✅ FIXED | 🔵 LOW | v6.0.7 | `wifi_power_save`, `remote_echo`, `gpio2_user_mode` blev serialiseret som integers (0/1) i stedet for JSON boolean (true/false). FIX: Ternary operators for konsistent boolean output + `.as<bool>()` ved restore (api_handlers.cpp) (Build #1241) |
| BUG-218 | W5500 Ethernet boot-loop ved flash overflow | ✅ FIXED | 🔴 CRITICAL | v6.0.8 | `-DETHERNET_W5500_ENABLED` tilføjer ~38 KB → flash 97.1% overfyldt → boot-loop/crash. FIX: Custom partition table (app0 1.5MB vs 1.25MB) + ArduinoJson size flags. Flash nu 80.9% uden Ethernet, 83.3% med (platformio.ini, partitions.csv) |
| BUG-219 | Flash forbrug 97%+ forhindrer nye features | ✅ FIXED | 🔴 CRITICAL | v6.0.8 | Default partition (1.25 MB app) kun 37 KB fri → ingen plads til Ethernet eller andre features. FIX: Aktiveret custom partitions.csv (app0=1.5MB, +262 KB). Kræver `pio run -t erase` ved første upload (platformio.ini) |
| BUG-220 | Ethernet init afhængig af WiFi enabled | ✅ FIXED | 🟡 HIGH | v6.0.9 | `ethernet_driver_init()` kun kaldt fra `network_manager_connect()` som kræver WiFi enabled. Ethernet aldrig initialiseret når WiFi disabled. FIX: Ny `network_manager_start_ethernet()` + `network_manager_start_services()` funktioner, kaldt uafhængigt fra main.cpp. Services (HTTP/Telnet) startes FØR interfaces. W5500 MAC sat fra ESP32 eFuse (network_manager.cpp, main.cpp, ethernet_driver.cpp) |
| BUG-221 | `set wifi disable` blokeret af argc < 2 check | ✅ FIXED | 🟡 HIGH | v6.0.9 | `cli_cmd_set_wifi()` krævede `argc < 2` men `enable`/`disable` er standalone options uden value-parameter. FIX: Ændret til `argc < 1`, value er nu optional med fallback til tom string (cli_commands.cpp) |
| BUG-222 | `set logic interval 100` fejler med "Invalid program ID 0" | ✅ FIXED | 🟡 HIGH | v6.0.9 | Parseren kun understøttede kolon-syntax `set logic interval:100`. Mellemrum-syntax `set logic interval 100` (som `show config` genererer) faldt igennem til program-ID parser → atoi("interval")=0. FIX: Tilføjet mellemrum-variant + `set logic <id> enabled/disabled` + INTERVAL/DISABLED i normalize_alias (cli_parser.cpp) |
| BUG-223 | W5500 Ethernet ~1000ms ping latency | ✅ FIXED | 🔴 CRITICAL | v6.0.10 | ESP-IDF 4.4 W5500 driver RX task (`w5500_tsk`) bruger 1000ms timeout på `ulTaskNotifyTake()`. GPIO34 falling-edge ISR fyrer ikke pålideligt → RX task kun vågner ved timeout (~1s). Ping: 300-1000ms. FIX: `ethernet_driver_loop()` poller GPIO34 hvert loop-cycle, sender `xTaskNotifyGive()` direkte til `w5500_tsk` task når INT er LOW. Ping nu 2-5ms. SPI clock sænket fra 20→8 MHz for signalintegritet (ethernet_driver.cpp, tcp_server.cpp) |
| BUG-224 | Telnet character echo langsom over Ethernet | ✅ FIXED | 🟡 HIGH | v6.0.10 | TCP Nagle algorithm bufferede 1-byte telnet echo pakker i op til 200ms. FIX: `TCP_NODELAY` socket option sat på client accept (tcp_server.cpp:168) |
| BUG-225 | `sh config \| s telnet` viser ingen SET commands | ✅ FIXED | 🟠 MEDIUM | v6.0.10 | Sektionsfilter i `cli_cmd_show_config()` genkendte "NETWORK" og "WIFI" men IKKE "TELNET". Telnet SET commands nested under `show_network` → usynlige ved telnet-filtrering. FIX: Tilføjet `show_section_match(section, "TELNET")` til show_network filter (cli_show.cpp:69) |
| BUG-226 | Telnet config nested under WiFi — usynlig ved WiFi disabled | ✅ FIXED | 🟡 HIGH | v6.1.0 | Telnet SET commands og status var nested under `show_network`/WiFi enabled check. Når WiFi disabled → telnet config helt usynlig i `sh config`. FIX: Standalone `[TELNET]` sektion + `set telnet` kommandoer uafhængig af WiFi (cli_show.cpp, cli_commands.cpp, cli_parser.cpp) |
| BUG-227 | normalize_alias() mangler TELNET keyword | ✅ FIXED | 🟡 HIGH | v6.1.0 | `set telnet pass` → "SET: unknown argument" fordi "telnet" ikke normaliseres til "TELNET". FIX: Tilføjet `if (str_eq_i(s, "TELNET")) return "TELNET"` i normalize_alias() (cli_parser.cpp) |
| BUG-228 | Telnet banner viser "Telnet Server (v3.0)" i stedet for hostname | ✅ FIXED | 🔵 LOW | v6.1.0 | Banner viste hardkodet "=== Telnet Server (v3.0) ===" i stedet for hostname. FIX: Bruger nu `g_persist_config.hostname` + "Telnet Srv" (telnet_server.cpp) |
| BUG-229 | Telnet login bruger startup-kopi af credentials | ✅ FIXED | 🟡 HIGH | v6.1.0 | `telnet_server.cpp` brugte `server->network_config` (kopi fra startup). Credentials ændret via CLI blev aldrig brugt. FIX: Bruger nu `g_persist_config.network` direkte for live credentials (telnet_server.cpp) |
| BUG-230 | `sh config` over telnet trunkeret — kun [SYSTEM] vises | ✅ FIXED | 🔴 CRITICAL | v6.1.0 | Non-blocking TCP socket returnerer EAGAIN ved fuld send-buffer. Data silently droppet → kun første ~200 bytes (SYSTEM sektion) nåede frem. FIX: Retry-loop i `tcp_server_send()` med EAGAIN håndtering (tcp_server.cpp) |
| BUG-231 | TCP send retry blokerer main loop → 1s output bursts | ✅ FIXED | 🔴 CRITICAL | v6.1.0 | `delay(10)` i retry-loop blokerede main loop → `ethernet_driver_loop()` kørte ikke → W5500 RX task kun vågnet på 1000ms timeout → TCP ACKs forsinkede → output i 1-sekunds ryk. FIX: `vTaskDelay(1)` + direkte `xTaskNotifyGive()` til W5500 RX task under retries (tcp_server.cpp) |
| BUG-232 | `sh ethernet` viser "NOT CONNECTED" selvom fysisk link er oppe | ✅ FIXED | 🟡 HIGH | v6.2.0 | `ETHERNET_EVENT_CONNECTED` satte ikke `eth_state.state` → forblev IDLE. State kun sat til CONNECTED i `IP_EVENT_ETH_GOT_IP` (DHCP). FIX: Ny `link_up` flag + `ethernet_driver_has_link()` + `sh ethernet` viser nu Link og IP separat (ethernet_driver.cpp, ethernet_driver.h, cli_show.cpp) |
| BUG-233 | Statisk IP markerer aldrig state CONNECTED | ✅ FIXED | 🔴 CRITICAL | v6.2.0 | `ethernet_driver_set_static_ip()` satte `local_ip` men aldrig `state = CONNECTED`. State kun sat via DHCP `IP_EVENT_ETH_GOT_IP` event. Med DHCP OFF: permanent "Disconnected" trods gyldig statisk IP og link. FIX: Sæt `CONNECTED` i `set_static_ip()` når link er oppe + i `ETHERNET_EVENT_CONNECTED` når statisk IP er konfigureret (ethernet_driver.cpp) |
| BUG-234 | Netmask validering fejler pga. byte order (ntohl mangler) | ✅ FIXED | 🟡 HIGH | v6.2.0 | `network_config_is_valid_netmask()` lavede bit-contiguity check på network byte order (big-endian fra `inet_aton()`). På little-endian ESP32: `255.255.255.0` = `0x00FFFFFF` → `~mask+1` ikke power-of-2 → alle gyldige netmasks afvist. FIX: `ntohl()` konvertering før bit-check (network_config.cpp) |
| BUG-235 | Ethernet statisk IP genaktiveres ikke efter link-flap | ✅ FIXED | 🟡 HIGH | v6.2.0 | `ETHERNET_EVENT_DISCONNECTED` nulstillede `local_ip=0`. `ETHERNET_EVENT_CONNECTED` checkede `local_ip != 0` for statisk IP → aldrig gensat efter disconnect. FIX: Checker `static_ip != 0` og re-applyer fra static config (ethernet_driver.cpp) |
| BUG-236 | Heartbeat POST fanges af GPIO wildcard handler | ✅ FIXED | 🟠 MEDIUM | v6.3.0 | `/api/gpio/2/heartbeat` POST matchede `/api/gpio/*` wildcard fordi gpio_write registreredes FØR heartbeat. FIX: Heartbeat handlers registreres FØR GPIO wildcard i http_server.cpp (Build #1384) |
| BUG-237 | WiFi statisk IP virker ikke — kan ikke ping/API fra remote | ✅ FIXED | 🔴 CRITICAL | v7.1.1 | `wifi_driver_apply_static_ip()` blev aldrig kaldt. `set_static_ip()` gemte kun config, men DHCP blev aldrig stoppet og IP aldrig sat. FIX: Kald `apply_static_ip()` i `WIFI_EVENT_STA_CONNECTED` handler (wifi_driver.cpp) |
| BUG-238 | 74HC165 digital inputs altid HIGH på ES32D26 | ✅ FIXED | 🔴 CRITICAL | v7.1.1 | Pin-mapping i constants.h havde DATA (QH, pin 9) og LOAD (SH/LD, pin 1) byttet om: GPIO0 var defineret som DATA men er LOAD, GPIO15 var defineret som LOAD men er DATA. Koden læste LOAD-pinnen (altid HIGH pga pullup) som data. FIX: Rettet pin-mapping — `PIN_SR_IN_LOAD=0`, `PIN_SR_IN_DATA=15` (constants.h) |
| BUG-239 | Modbus Master runtime config ikke synkroniseret fra NVS ved boot | ✅ FIXED | 🔴 CRITICAL | v7.2.0 | `g_modbus_master_config` initialiseres med `.enabled=false` ved kompilering. `g_persist_config.modbus_master` (NVS) kopieres ALDRIG til runtime config ved boot. `modbus_master_send_request()` returnerer altid `MB_NOT_ENABLED` trods `modbus_mode=master` i NVS. Opdaget via DM56A04 display test: 10704 requests, 0 success, 0 timeout, 0 CRC → alle MB_NOT_ENABLED. FIX: Sync runtime fra persist i `modbus_master_init()` (modbus_master.cpp:48-55) (Build #1540) |
| BUG-240 | ST compiler heap-udmattelse pga oversized AST-noder + fragmentation | ✅ FIXED | 🟡 HIGH | v7.2.0 |
| BUG-241 | ST compile fejler via HTTP API pga heap-fragmentering fra keep-alive | ✅ FIXED | 🔴 CRITICAL | v7.3.0 | HTTP keep-alive connections (3 samtidige fra browser) fragmenterer heap → AST pool `malloc(120KB)` fejler trods 170KB fri. FIX: (1) `heap_caps_get_largest_free_block()` i stedet for `esp_get_free_heap_size()`, (2) try-decreasing allocation (512→256→128→64→32 nodes), (3) `lru_purge_enable=true` for auto-close idle connections, (4) JSON scope-blok i source_post handler frigør ArduinoJson FØR compile (st_parser.cpp, http_server.cpp, api_handlers.cpp) |
| BUG-242 | Tom parse error besked ved AST pool allokerings-fejl | ✅ FIXED | 🟡 HIGH | v7.3.0 | `st_parser_parse_program()` returnerede `NULL` uden at sætte `error_msg` når `ast_pool_init()` fejlede → CLI/API viste "Parse error: " uden forklaring. FIX: Tilføjet `parser_error()` kald ved pool init og program struct malloc fejl (st_parser.cpp:1704-1710) |
| BUG-243 | API /api/system/save gemmer ikke ST Logic programmer | ✅ FIXED | 🔴 CRITICAL | v7.3.0 | `POST /api/system/save` kaldte kun `config_save_to_nvs()` men manglede `st_logic_save_to_persist_config()` og `config_calculate_crc16()`. CLI `save` havde begge. ST programmer uploadet via API/web editor blev IKKE persisteret til NVS. FIX: Tilføjet begge kald i `api_handler_system_save()` (api_handlers.cpp:1195-1198) |
| BUG-244 | API /api/logic response mangler source_size felt | ✅ FIXED | 🟠 MEDIUM | v7.3.0 | `GET /api/logic` returnerede ikke `source_size` per program → web editor kunne ikke afgøre om et slot havde kildekode. FIX: Tilføjet `source_size` felt i logic list response (api_handlers.cpp:863) |
| BUG-245 | normalize_alias() mangler STATUS keyword | ✅ FIXED | 🟡 HIGH | v7.3.0 | `show status` virkede kun med uppercase `sh STATUS`. FIX: Tilføjet `STATUS`/`STAT` i normalize_alias() (cli_parser.cpp) | ST-programmer >~1300 bytes fejlede med "Memory allocation failed" trods 110 KB fri heap. Årsag: (1) `st_ast_node_t` var ~1920 bytes/node (function_def i union), (2) mange små malloc-kald fragmenterede heap, (3) compiler+bytecode duplikerede 18.5 KB. FIX (5 optimeringer): AST node 1920→140 bytes (function_def til pointer), AST pool-allokator (eliminerer fragmentation), direct bytecode output (sparer 10.5 KB malloc), compiler bytecode pointer (sparer 8 KB), parser early-free. Resultat: max 21→34 IF-blokke (+62%), min heap 47→80 KB (+70%). Filer: st_types.h, st_parser.cpp, st_compiler.h, st_compiler.cpp, st_logic_config.cpp (Build #1580) |
| BUG-246 | Web editor login hardcoded "api_user" default | ✅ FIXED | 🟠 MEDIUM | v7.3.1 | Login-formularen i `/editor` havde `value="api_user"` hardcoded — matchede ikke nødvendigvis konfigureret brugernavn. FIX: Tomt felt med placeholder, ingen fejlmeddelelse ved forkert login. Rettet til korrekt auth-test mod `/api/status` før modal lukkes (web_editor.cpp) |
| BUG-247 | Web editor API endpoints ikke registreret i HTTP server | ✅ FIXED | 🔴 CRITICAL | v7.3.1 | `/api/cli`, `/api/bindings`, `/api/bindings/*` var i v1_routes tabel men manglede `httpd_register_uri_handler()` i http_server.cpp → 405 Method Not Allowed. FIX: Tilføjet URI handler structs + registrering (http_server.cpp) |
| BUG-248 | CLI modbus master/slave kommandoer bruger Serial.print direkte | ✅ FIXED | 🟡 HIGH | v7.3.1 | `cli_commands_modbus_master.cpp` (24×) og `cli_commands_modbus_slave.cpp` (36×) brugte `Serial.printf()`/`Serial.println()` i stedet for `debug_printf()`/`debug_println()` → output vistes kun på hardware serial, ikke i web CLI eller telnet. FIX: Alle Serial.print → debug_printf/debug_println |
| BUG-249 | Binding delete bruger forkert index (var_maps vs filtreret liste) | ✅ FIXED | 🟡 HIGH | v7.3.1 | `GET /api/bindings` filtrerede non-ST bindings med `continue` men returnerede ikke globalt `var_maps` index → frontend sendte forkert index til `DELETE /api/bindings/{idx}` → "Not an ST variable binding" fejl. FIX: Tilføjet `index` felt i GET response med globalt var_maps index, fjernet restriktiv source_type check i DELETE (api_handlers.cpp, web_editor.cpp) |
| BUG-253 | SSE events forsinket/tabt pga. manglende TCP_NODELAY | ✅ FIXED | 🔴 CRITICAL | v7.5.0 | Nagle-algoritmen buffedreer små SSE-events (64-384 bytes) og ventede på TCP ACK før afsendelse → events forsinket eller tabt under load fra v7.3+ web features (dashboard, OTA, system). Node-RED viste "connected" men modtog ingen opdateringer. FIX: `TCP_NODELAY` sat på SSE klient-socket (sse_events.cpp:549) (Build #1700) |
| BUG-254 | SSE send timeout for aggressiv (5s) lukker forbindelse under load | ✅ FIXED | 🟡 HIGH | v7.5.0 | `SO_SNDTIMEO=5s` var for kort under belastning fra v7.3+ features (dashboard polling, OTA upload, web system). TCP send-buffer kunne ikke drænes i tide → `send()` returnerede -1 → SSE-forbindelse lukket silently. FIX: Øget SO_SNDTIMEO fra 5s til 30s (sse_events.cpp:553) (Build #1700) |
| BUG-250 | SSE subscribe=all overvåger kun HR 0-15, ignorerer coils/IR/DI | ✅ FIXED | 🔴 CRITICAL | v7.6.1 | `subscribe=all` uden eksplicitte adresse-params (hr/ir/coils/di) defaultede til kun HR 0-15. Coils, IR og DI blev aldrig scannet → ingen register-events for coils/DI. FIX: Ny `watch_all` mode i SseWatchList — allokerer SseWatchAllState (~1.5 KB) og scanner alle 256 HR + 256 IR + 256 coils + 256 DI (sse_events.cpp) |
| BUG-255 | SSE klient-task stack overflow risiko | ✅ FIXED | 🟡 HIGH | v7.5.0 | `init_buf[320]` på stack + `sse_send_event_fd` `buf[384]` = 704 bytes permanent stack-forbrug ved 5120 bytes total stack. Marginalt med dybere callstacks i v7.3+. FIX: `init_buf` flyttet til heap (malloc/free), task stack øget 5120→6144 bytes (sse_events.cpp:370,690) (Build #1700) |
| BUG-256 | SSE sse_sock_send behandler EAGAIN som fatal fejl | ✅ FIXED | 🟠 MEDIUM | v7.5.0 | Når TCP send-buffer var fuld returnerede `send()` -1 med `EAGAIN`, men koden behandlede det som fatal fejl → SSE-forbindelse lukket unødigt. FIX: Retry-loop med max 5 forsøg á 10ms delay ved EAGAIN/EWOULDBLOCK (sse_events.cpp:260-272) (Build #1700) |

## Feature Requests / Enhancements

| Feature ID | Title | Status | Priority | Target Version | Description |
|-----------|-------|--------|----------|----------------|-------------|
| FEAT-001 | `set reg STATIC` multi-register type support | ✅ DONE | 🟠 MEDIUM | v4.7.1 | Add DINT/DWORD/REAL support til persistent register configuration (Build #966) |
| FEAT-002 | ST Logic dynamisk pool allokering (8KB shared) | ✅ DONE | 🟡 HIGH | v4.7.1 | Erstat fixed 4KB arrays med 8KB shared pool - flexibel allokering (Build #944) |
| FEAT-003 | ST Logic custom FUNCTION/FUNCTION_BLOCK support | ✅ DONE | 🟠 MEDIUM | v6.0.6 | IEC 61131-3 FUNCTION/FUNCTION_BLOCK med VAR_INPUT, VAR_OUTPUT, VAR, lokale variable, nested calls, stateful FB instances. Parser, compiler (2-pass), VM (CALL_USER/RETURN/LOAD_PARAM/STORE_LOCAL) fuldt implementeret. 11/11 tests bestået (Build #1224) |
| FEAT-004 | ST Logic ARRAY type support | ✅ DONE | 🟡 HIGH | v7.9.0 | IEC 61131-3 ARRAY: `VAR arr: ARRAY[0..7] OF INT; END_VAR` med `arr[i]` read/write. Lexer ARRAY+`..` tokens, parser array decl+indexing, compiler LOAD_ARRAY/STORE_ARRAY opcodes, VM bounds-checked runtime. Arrays flattened til consecutive variable slots (max 24 elem). (st_types.h, st_lexer.cpp, st_parser.cpp, st_compiler.cpp, st_vm.cpp) |
| FEAT-005 | ST Logic STRING type support | ✅ DONE | 🟠 MEDIUM | v7.9.11.0 | IEC 61131-3 STRING type med `LEN()`/`CONCAT()`/`LEFT()`/`RIGHT()`/`MID()`. **Design**: handle-baseret — `st_value_t` forbliver en ren 4-byte union (nyt `str_ref`-medlem, 2 bits "kind" + 6 bits index, `ST_STR_REF_MAKE/KIND/INDEX`-makroer i st_types.h), INGEN tekst i selve unionen. Tre faste, statisk allokerede arrays (INGEN heap/PSRAM/GC nødvendig, undgår dermed den "kræver GC-overvejelser"-bekymring den oprindelige backlog-post havde): `string_vars[32]` (pr. variabel-slot, persisterer på tværs af cyklusser, kopieres VM↔program som `variables[]` allerede gør), `string_literals[8]` (compile-time-konstant, interneres én gang under kompilering), `string_scratch[8]` i VM'en (round-robin, kun midlertidige udtryks-mellemresultater som CONCAT). Fast kapacitet `ST_MAX_STRING_LEN=32` tegn for ALLE strenge (ingen `STRING[N]`-størrelsessuffiks i denne "ST-Light"-profil — bevidst simplификation). **Sikkerhed**: STRING-variable kan IKKE bindes til Modbus-registre/coils (afvist både i REST `/api/logic/{id}/bind` og CLI `set logic bind` — ville ellers lade en registerskrivning overskrive `str_ref` med vilkårlige bits, en reel hukommelseskorruptionsrisiko) og kan ikke `EXPORT`'es til IR-poolen (ingen meningsfuld 1-2-register-mapping for variabel-længde tekst). **Persistens**: programmer der bruger STRING caches bevidst IKKE til SPIFFS (`st_bytecode_save()` springer dem over) — det etablerede binære format gemmer i dag ikke streng-data, og en genindlæst STRING-variabel ville ellers stille blive tom efter reboot; simplere at altid genkompilere disse fra kildekode ved boot end at udvide formatet nu (ingen anden funktionel forskel, kun marginalt langsommere boot for disse specifikke programmer). Rørte: st_types.h, st_vm.h/.cpp, st_compiler.h/.cpp, st_builtins.h/.cpp, st_lexer.cpp, st_parser.cpp, st_bytecode_persist.cpp, cli_show.cpp, cli_commands_logic.cpp, api_handlers.cpp, web/editor.html, constants.h | **Live-testet v7.9.11.1 på 10.1.1.153 (program slot 3)**: CONCAT/LEN/LEFT/RIGHT/MID/streng-lighed alle bekræftet korrekte end-to-end (lexer→parser→compiler→VM→REST) — se BUG-373 for en fejl i kopiering-tilbage-trinnet fundet og rettet under denne test. **Yderligere grundig test v7.9.13.0** (efter brugerens ønske om ekstra dækning før FEAT-010): copy-semantik bekræftet (`b:=a; a:='ny'` — `b` upåvirket, ægte kopi ikke alias-reference); MID/LEFT/RIGHT grænsetilfælde (start ud over strenglængde, len=0, n>strlen) alle korrekte; CONCAT-afkortning ved 32-tegns-grænsen bekræftet (30+10 tegn → afkortet til præcis 32); EXPORT-afvisning og Modbus-binding-afvisning begge bekræftet aktive (compile fejler hhv. bind-endpoint returnerer 400) |
| FEAT-006 | ST Logic TIME literal support | ✅ DONE | 🟠 MEDIUM | v5.2.0 | Native TIME literals: `T#5s`, `T#100ms`, `T#1h30m`. Lexer parser, gemmes som DINT millisekunder. (st_types.h, st_lexer.cpp, st_parser.cpp) |
| FEAT-007 | ST Logic inter-program variable sharing | ✅ DONE | 🟡 HIGH | v7.9.12.0 | Standalone `GLOBAL_VAR ... END_VAR`-blok (adskilt fra de 4 programmers egen kildekode), delt via nyt `globals[16]` array i `st_logic_engine_state_t` (sibling til `programs[]`) — ALLE 4 programmer kan læse/skrive samme navngivne variabel direkte, uden Modbus-binding-indirektion. **Design**: kun scalar-typer (BOOL/INT/DINT/DWORD/REAL/TIME) — bevidst UDEN STRING (ville kræve sin egen delte string-pulje) og UDEN ARRAY/STRUCT. Nye opcodes `ST_OP_LOAD_GLOBAL`/`ST_OP_STORE_GLOBAL` (var_index peger ind i `state->globals[]`, IKKE programmets eget `variables[]`). Compileren forsøger LOKALT symbolopslag først (`st_compiler_lookup_symbol`) og falder kun tilbage til et globalt opslag (`st_logic_globals_lookup`, via `st_logic_get_state()`) hvis intet lokalt navn matcher — en lokal variabel kan altså bevidst *skygge* en global med samme navn. Kun to compiler-steder ændret for at holde ændringen lille (variabel-læsning + almindelig `navn := udtryk`-assignment) — FB-output-bindings (`Q => globalVar`), array-indeksering og FOR-løkke-tællere understøtter bevidst IKKE globals i v1 (giver en klar "Unknown variable"-fejl i stedet). STORE_GLOBAL genbruger PRÆCIS samme IEC 61131-3-implicit-konverteringsregler som STORE_VAR (udtrukket til en fælles `st_vm_convert_value()`-hjælpefunktion for at undgå at de to kunne drifte fra hinanden). Adgang til `globals[]` beskyttet af det EKSISTERENDE `st_var_spinlock` (via de allerede-offentlige `st_logic_lock_variables()`/`_unlock_variables()`, BUG-038). **Sikkerhedskritisk designpunkt**: en omstrukturering af GLOBAL_VAR-blokken (tilføjet/fjernet/omrokeret variabel) kan skifte hvilket array-indeks et navn er bundet til — ethvert program der allerede er kompileret med den GAMLE indeksering ville ellers stille læse/skrive en FORKERT global efter en sådan ændring. Løst ved at `st_logic_globals_compile()` automatisk genkompilerer ethvert allerede-kompileret program hvis bytecode rent faktisk indeholder LOAD_GLOBAL/STORE_GLOBAL (scannet direkte i dets instruktionsliste) — programmer der IKKE bruger globals røres bevidst slet ikke (se BUG-374 for hvorfor denne afgrænsning er vigtig, ikke kun en optimering). **Persistens**: kun GLOBAL_VAR-blokkens KILDETEKST persisterer på tværs af reboot (ny `/logic_global.dat` i SPIFFS, indlæst+kompileret FØR Logic1-4 ved boot, samme mønster som `/logic_{0-3}.dat`) — de aktuelle VÆRDIER nulstilles altid til 0/FALSE ved enhver (gen)kompilering, samme filosofi som almindelige lokale variable. REST: `GET/POST /api/logic/globals/source`, `GET /api/logic/globals` (ingen nye URI-registreringer — suffix-delegeret under den eksisterende `/api/logic/*`-wildcard). CLI: `show logic globals` (læs-kun; redigering er REST/web-editor-only, samme som programkildekode i praksis). Live-testet v7.9.12.0 på 10.1.1.153 (GLOBAL_VAR + program slot 3 som "writer", slot 4 som "reader") — bekræftet: skrivning i ét program synlig i et andet SAMME cyklus-familie, boolean-propagation, OG korrekt genkompilering/re-binding efter at have omrokeret variabelrækkefølgen i GLOBAL_VAR-blokken midt i kørslen. Slot 2 (brugerens langtids-testprogram) forblev fuldstændig urørt gennem hele testen. Rørte: constants.h, st_types.h, st_logic_config.h/.cpp, st_lexer.cpp, st_parser.h/.cpp, st_compiler.h/.cpp, st_vm.cpp, api_handlers.cpp, http_server.cpp, cli_commands_logic.cpp, cli_parser.cpp, cli_commands_logic.h, docs/manual/08_ST_Logic_Programmering.md, docs/manual/B_REST_API_Reference.md. **Yderligere grundig test v7.9.13.0** (efter brugerens ønske om ekstra dækning før FEAT-010): lokal-skygger-global bekræftet (et program med en LOKAL variabel af samme navn som en global skriver kun til sin egen lokale slot — den globale værdi forblev fuldstændig urørt, verificeret af et TREDJE program uden lokal skygge der læste den ægte globale værdi); type-konvertering på STORE_GLOBAL bekræftet (INT-literal → REAL-global konverteret korrekt til flydende tal, INT→BOOL-global konverteret korrekt via nonzero-regel); tre negative tests (STRING i GLOBAL_VAR, ARRAY i GLOBAL_VAR, dublet-navn) alle korrekt afvist, OG bekræftet at en FEJLET globals-upload IKKE ødelægger den eksisterende gode tilstand (globals-listen og de kørende programmer forblev 100% uændrede efter 3 mislykkede genupload-forsøg) |
| FEAT-008 | ST Logic debugging/single-step mode | ✅ DONE | 🔵 LOW | v5.3.0 | CLI: `set logic <id> debug pause/step/continue`, breakpoints, variable inspection. Build #1082, bugfixes Build #1083 (BUG-190, BUG-191). (st_debug.h, st_debug.cpp) |
| FEAT-009 | ST Logic STRUCT type support | ✅ DONE | 🔵 LOW | v7.9.13.0 | `TYPE Navn : STRUCT felt: type; ... END_STRUCT END_TYPE` parset FØR et programs egen VAR-blok, scoped til DET ENE program (ikke delt på tværs af Logic1-4 som GLOBAL_VAR). **Nøgleindsigt der gjorde dette meget mindre indgribende end oprindeligt planlagt**: felt-adgang (`instans.felt`) er ALTID et kompileringstids-konstant navn (aldrig et runtime-udtryk som array-indeksering), så `.`-adgang opløses 100% på kompileringstidspunktet direkte til et almindeligt `LOAD_VAR`/`STORE_VAR` på det rigtige slot-indeks — INGEN nye VM-opcodes, INGEN ændringer i st_vm.cpp, INGEN ændringer i bytecode-cache-formatet, INGEN ændringer i api_handlers.cpp/cli_show.cpp's variabel-visning var nødvendige (en struct-instans udvides til N sammenhængende `variables[]`-slots med hver sit eget navn "instans.felt1", "instans.felt2" — nøjagtig samme mekanisme ARRAY allerede bruger til "arr[0]", "arr[1]"). Modbus-binding og EXPORT af INDIVIDUELLE felter virker derfor helt transparent (bekræftet ved test — se nedenfor), da hvert felt bare er en almindelig navngivet scalar-variabel for resten af systemet. **V1-scope (bevidst)**: kun scalar-felter (BOOL/INT/DINT/DWORD/REAL/TIME) — INGEN nested STRUCT, INGEN ARRAY-felter, INGEN STRING-felter, INGEN `ARRAY OF <StructType>`, INGEN STRUCT i GLOBAL_VAR (ville kræve at hele felt-udvidelses-mekanismen duplikeres ind i den globale registry), INGEN STRUCT i FUNCTION VAR_INPUT/OUTPUT/lokale (kun program-topniveau). Hele struct-typen kan IKKE `EXPORT`'es som helhed (afvist ved parse-tid — ingen meningsfuld 1-register-mapping for et multi-felt-aggregat, og enkeltfelter er ikke selvstændigt navngivbare ved deklarationstidspunktet). Rørte: constants.h, st_types.h, st_lexer.cpp, st_parser.h/.cpp, st_compiler.h/.cpp, docs/manual/08_ST_Logic_Programmering.md. **Grundigt live-testet v7.9.13.0 på 10.1.1.153** (program slot 3/4, efter brugerens ønske om ekstra test før FEAT-010): to-instans-program (`p1.x/p1.y/p1.active`, `p2.x/p2.y/p2.active`, alle regnestykker korrekte, `sum=70`); 6 negative tests alle korrekt afvist med klar fejlbesked (EXPORT af hel struct, nested STRUCT-felt, ARRAY-felt i STRUCT, STRING-felt i STRUCT, `ARRAY OF <StructType>`, STRUCT i GLOBAL_VAR); ukendt-felt- og bar-struct-reference-fejl bekræftet læsbare (se note om `last_error[64]`-trunkering nedenfor); Modbus-binding af et enkelt felt (`s1.raw` → HR#50) bekræftet virkende 100% transparent uden nogen særkode, register-værdien fulgte korrekt programmets levende tæller. **Kendt, IKKE rettet begrænsning opdaget under test**: `st_logic_program_config_t.last_error[64]` er meget lille, og fejlbeskeder wrappes TO gange ("Parse/Compile error: " + "Parse/Compile error at line N: " + selve beskeden) før de kopieres derind — kun ca. 20-25 tegn af den faktiske besked overlever i praksis. Dette er PRÆ-EKSISTERENDE (rammer også ældre fejlbeskeder, fx STRING's egen EXPORT-afvisning blev også fundet trunkeret under denne tests session) og IKKE unikt for STRUCT — alle nye STRUCT-relaterede fejlbeskeder blev bevidst holdt korte (<20 tegn ekskl. navne) for at overleve budgettet, men en fuld rettelse (udvide `last_error` til fx 128 byte) er UDENFOR scope her |
| FEAT-010 | ST Logic program prioriteter/scheduling | ✅ DONE | 🟡 HIGH | v7.9.14.1 | Per-program `priority` (NORMAL/HIGH) + `interval_ms`, erstatter det ene delte `execution_interval_ms` som scheduling-grundlag. **NORMAL**: uændret eksekveringsvej (kooperativt i `loopTask`/Core 1), men `st_logic_engine_loop()`s scheduler omlagt fra ÉT delt "elapsed<interval"-gate til et per-program forfaldstidspunkts-tjek. **HIGH**: ny dedikeret FreeRTOS-task PINNET TIL CORE 0 (ALDRIG Core 1 — se BUG-336c/BUG-337), vækket af en `esp_timer` (ESP_TIMER_TASK dispatch, ikke ISR) via en semafor — timer-callbacken gør INTET andet end `xSemaphoreGive`, alt arbejde sker i den separate task. Første brug af `esp_timer` i denne kodebase nogensinde. **To research-agenter** undersøgte grundigt før implementering om Modbus Master-kald og register-arrays er sikre på tværs af cores: (1) `MB_READ_*`/`MB_WRITE_*` fra ST Logic går ALLEREDE gennem `mb_async_queue_read/write()` (en allerede trådsikker kø, ikke direkte UART) — ingen ny låsning nødvendig. (2) `holding_regs[]`/`input_regs[]`/`coils[]` var 100% ULÅSTE (0 kritiske sektioner nogen steder) — ny `portMUX_TYPE`-spinlock tilføjet INDENI de 8 get/set-accessor-funktioner i `registers.cpp` (ikke ved de 223 kaldesteder), bevidst AFGRÆNSET til kun selve array-adgangen, IKKE de omkringliggende side-effekt-handlere (`registers_process_st_logic_*`), som kan kalde ind i kompilering/andre låse — samme lektie som BUG-374 om aldrig at lade en kritisk sektion dække blokerende arbejde. (3) `gpio_mapping`s read-before/write-after-synkronisering med hovedloopets kadence kan IKKE uden videre gælde en uafhængig HIGH-task (timing-korrekthedsproblem, ikke hukommelseskorruption) — **designbeslutning: HIGH-programmer kan i v1 IKKE bindes til Modbus-registre/GPIO** (afvist bidirektionelt: bind-oprettelse afviser hvis target er HIGH, prioritets-skift til HIGH afviser hvis programmet har ≥1 binding) — kun lokale variable, GLOBAL_VAR (allerede spinlock-beskyttet fra FEAT-007) og Modbus Master-kald er tilladt. Skærpet sikkerhedsnet for HIGH: lavere `max_steps` (1000 vs. 10000) OG et 5ms wall-clock-loft (tjekket hver 50. instruktion). Selvstændigt fundet og rettet fejl-i-vente: `stateful->cycle_time_ms` (TON/TOF/TP-timere) brugte det GAMLE delte interval uanset hvilket program der kørte — ville have givet forkert beregnet forløbet tid for ethvert program med et andet interval end standarden; rettet til `prog->interval_ms`. **Persistens** undgår bevidst `CONFIG_SCHEMA_VERSION`-bump helt: per-program felterne gemmes i programmets EGEN SPIFFS `/logic_%d.dat`-fil (udvidet header `[magic=0xAA(1)][enabled(1)][priority(1)][interval_ms(2)][source_size(4)]`, hvor magic-byten adskiller det nye format fra det gamle `[enabled(1)][source_size(4)]` — 0xAA kan aldrig forekomme som en gyldig gammel enabled-værdi (kun 0/1) — samme "magic+version"-robusthedsmønster som `st_bytecode_persist.cpp` (jf. BUG-375). CLI: `set logic <id> priority normal\|high`, `set logic <id> interval <ms>` (nye); `set logic interval <ms>` (eksisterende) omdefineret til at sætte alle NORMAL-programmers interval på én gang. REST: `POST /api/logic/{id}/priority`, `POST /api/logic/{id}/interval`, `priority`/`interval_ms` tilføjet til program-detail JSON og `show logic <id>`/`show logic <id> timing` (som begge FEJLAGTIGT viste det gamle delte interval — samme klasse fejl som cycle_time_ms ovenfor, rettet samtidig). **Kendt, accepteret begrænsning**: `g_mb_request_count`/`g_mb_cache_enabled` (Modbus-forespørgsels-kvote pr. cyklus) er proces-globale, ikke pr.-task — en NORMAL- og en HIGH-eksekvering der overlapper i tid kunne teoretisk interleave nulstillinger af denne kvote (værste fald: kvoten er kortvarigt lidt forkert, ingen korruption/crash). Samme forbehold gælder de RÅ pointer-returnerende `registers_get_holding_regs()`-stil funktioner (bruges af RTU-slave-protokollen til bulk-adgang) — disse omgår den nye spinlock, som kun dækker enkelt-register get/set-stien. **BUG fundet OG rettet under egen live-test**: `ST_LOGIC_HIGH_TASK_STACK` sat til 4096 byte (samme som `mb_async`) crashede/genstartede enheden næsten øjeblikkeligt ved eksekvering af ethvert program via HIGH-tasken — rodårsag: `st_vm_t` (indeholder `string_vars[32][33]`, `string_scratch[8][33]`, `stack[64]`, `variables[32]` m.m., ~2,7KB alene) allokeres som en STACK-lokal variabel i `st_logic_execute_program()`, hvilket levner for lidt margin på kun 4096 byte når kaldt fra en anden tasks stack-ramme. Rettet ved at fordoble til 8192 byte — genverificeret efter fix: en bevidst uendelig `WHILE TRUE DO...END_WHILE`-testprogram som HIGH-program stoppede nu korrekt og forudsigeligt med "Max steps exceeded (1000)" på HVER cyklus (100% fejlrate, som forventet for et reelt uendeligt program), UDEN at genstarte enheden, mens dashboard/HTTP forblev fuldt responsivt (60-80ms svartider) og Logic1 (NORMAL, Core 1) fortsatte upåvirket. **Live-testet grundigt v7.9.14.0-1 på 10.1.1.153**: Logic1 (brugerens rigtige program) 100% upåvirket gennem hele testforløbet (tusindvis af eksekveringer, 0 fejl, korrekt NORMAL/10ms genindlæst fra dets GAMLE .dat-format via magic-byte-fallback); et velformet HIGH-program (lokal tæller + GLOBAL_VAR-skrivning, 5ms interval) kørt i en ~4,25 minutters soak-test PARALLELT med Logic1: **52.409 HIGH-eksekveringer, 0 fejl, 0 overruns**; Logic1 samtidig **32.221 eksekveringer, 0 fejl**; GLOBAL_VAR `shared_counter` fulgte korrekt med (52.516); heap fuldstændig stabil gennem hele testen (115.088→114.896 byte, ingen lækage over 84.000+ kombinerede eksekveringer). Binding-afvisning bekræftet bidirektionelt (bind→HIGH afvist, HIGH→bind afvist). **v7.9.15.0**: web-GUI-eksponering tilføjet (var kun CLI/REST) — ny tabel i ST Editor'ens Settings-fane (Program-prioritet & interval) viser og redigerer alle 4 programmers priority/interval_ms/binding_count, samme mønster som den eksisterende globale interval-indstilling; `GET /api/logic`-oversigten udvidet med de samme tre felter så GUI'en kan vise dem uden 4 separate kald. Rørte: constants.h, st_logic_config.h/.cpp, st_logic_engine.cpp, registers.cpp, cli_commands_logic.cpp/.h, cli_parser.cpp, api_handlers.cpp, config_apply.cpp, main.cpp |
| FEAT-011 | HTTP REST API v6.0.0 | ✅ DONE | 🟡 HIGH | v6.0.0 | REST API med 20+ endpoints for counters, timers, logic, registers, GPIO, system control. (api_handlers.cpp) (Build #1108) |
| FEAT-012 | HTTPS/TLS support | ✅ DONE | 🟠 MEDIUM | v6.0.4 | esp_https_server component (httpd_ssl_start) med ECDSA P-256 certifikat embedded i flash. (https_wrapper.c) (Build #1126) |
| FEAT-013 | Dynamisk parser/compiler RAM-allokering | ✅ DONE | 🟡 HIGH | v6.0.4 | Parser/compiler malloc'd under kompilering, frigivet efter. Sparer ~12KB permanent RAM. Upload-buffer også dynamisk (~5KB). (st_logic_config.cpp, cli_shell.cpp) (Build #1126) |
| FEAT-014 | ST_LOGIC_MAX_PROGRAMS refactoring | ✅ DONE | 🟠 MEDIUM | v6.0.4 | Alle hardkodede 4-værdier erstattet med konstant. Module enable/disable flags. (constants.h, 10+ filer) (Build #1126) |
| FEAT-015 | Telnet IAC negotiation + ANSI-kompatibel history | ✅ DONE | 🟠 MEDIUM | v6.0.4 | Korrekt IAC WILL ECHO/SUPPRESS-GO-AHEAD ved connection. ANSI-fri line clearing for alle terminaler. (telnet_server.cpp) (Build #1126) |
| FEAT-016 | Show config sektionsfiltrering | ✅ DONE | 🟠 MEDIUM | v6.0.4 | "show config wifi/modbus/counters/http/..." viser kun relevant sektion. (cli_show.cpp, cli_parser.cpp) (Build #1126) |
| FEAT-017 | Config Backup/Restore via HTTP API + CLI | ✅ DONE | 🟡 HIGH | v6.0.7 | GET /api/system/backup download fuld JSON config inkl. passwords + ST Logic source. POST /api/system/restore upload JSON for fuld restore. CLI: show backup. (api_handlers.cpp, http_server.cpp, cli_show.cpp, cli_parser.cpp) |
| FEAT-018 | CLI ping kommando | ✅ DONE | 🟠 MEDIUM | v6.2.0 | `ping <ip> [count]` kommando i CLI. esp_ping session-baseret async med semaphore. Viser RTT per ping + statistik (sent/received/loss/min/avg/max). Virker over WiFi og Ethernet (wifi_driver.cpp, cli_commands.cpp, cli_parser.cpp) |
| FEAT-019 | Telnet Configuration API endpoint | ✅ DONE+TESTET | 🟡 HIGH | v6.3.0 | `GET/POST /api/telnet` — Telnet konfiguration via REST API. Testet 4/4 PASS (Build #1384) |
| FEAT-020 | ST Logic Debug API endpoints | ✅ DONE+TESTET | 🟡 HIGH | v6.3.0 | `POST /api/logic/{id}/debug/pause\|continue\|step\|breakpoint\|stop` + `GET .../debug/state` — Fuld debug kontrol via API. Testet 8/8 PASS (Build #1384) |
| FEAT-021 | Bulk Register Operations API | ✅ DONE+TESTET | 🟡 HIGH | v6.3.0 | `GET /api/registers/hr?start=0&count=100` + `POST /api/registers/hr/bulk` + IR/coils/DI bulk read/write. Testet 12/12 PASS (Build #1384) |
| FEAT-022 | Persistence Group Management API | ✅ DONE | 🟠 MEDIUM | v7.1.0 | `GET/POST/DELETE /api/persist/groups/{name}` + `/api/persist/save` + `/api/persist/restore` — fuld REST API for persistence groups |
| FEAT-023 | SSE Real-Time Events | ✅ DONE+TESTET | 🟠 MEDIUM | v7.0.3 | Raw TCP multi-klient SSE med CLI management. `set sse`/`show sse` config+status. Klient-registry med IP-tracking, `disconnect all\|<slot>`. Konfigurerbar watch HR/IR/Coils/DI. NVS-persisteret config (schema 12) |
| FEAT-024 | Hostname API endpoint | ✅ DONE+TESTET | 🟠 MEDIUM | v6.3.0 | `GET/POST /api/hostname` — Hostname ændring via REST API. Testet 3/3 PASS (Build #1384) |
| FEAT-025 | Watchdog Status API endpoint | ✅ DONE+TESTET | 🟠 MEDIUM | v6.3.0 | `GET /api/system/watchdog` — Reboot count, reset reason, heap stats, uptime. Testet 2/2 PASS (Build #1384) |
| FEAT-026 | GPIO2 Heartbeat Control API | ✅ DONE+TESTET | 🔵 LOW | v6.3.0 | `GET/POST /api/gpio/2/heartbeat` — Enable/disable heartbeat LED. BUG-236 fixed. Testet 3/3 PASS (Build #1384) |
| FEAT-027 | CORS Headers support | ✅ DONE+TESTET | 🟠 MEDIUM | v6.3.0 | `Access-Control-Allow-Origin: *` på alle API responses + OPTIONS preflight. Testet 3/3 PASS (Build #1384) |
| FEAT-028 | Request Rate Limiting | ✅ DONE | 🟠 MEDIUM | v7.1.0 | Token bucket rate limiter per klient IP (30 burst, 10/sec refill). Returnerer 429 Too Many Requests ved overbelastning |
| FEAT-029 | OpenAPI/Swagger Schema endpoint | ✅ DONE | 🔵 LOW | v7.9.10.42 | `GET /api/schema` — genererer et gyldigt OpenAPI 3.0-dokument (chunked respons) fra en ny delt `API_ROUTES[]`-tabel i api_handlers.cpp, som `GET /api/`s endpoint-discovery NU OGSÅ bruger (rettede samtidig en opdaget stalenes-bug: `/api/`s tidligere håndskrevne liste havde kun 82 entries mod 105 reelt registrerede routes — begge endpoints deler nu ÉN kilde så de ikke kan drifte fra hinanden igen). Kun sti/metode/kort-beskrivelse pr. endpoint (ingen struktureret parameter-/svar-skema-sporing findes i projektet i dag) — nok til automatisk klient-kodegenerering af selve rute-/metode-overfladen. Grupperer eksplicit efter normaliseret sti (ikke naboskab i tabellen, da fx `/api/counters/{1-4}`'s DELETE ikke står ved siden af sit GET/POST) — en `seen_paths[]`-hjælpetabel (~103×64 = 6.6KB) allokeres bevidst på HEAP, ikke stak, givet enhedens httpd-worker kun har 8KB stak (samme forsigtighed som BUG-364/369-lækagen krævede). **v7.9.10.41-hotfix (v7.9.10.42)**: `/api/`s JSON-header-buffer (`head[80]`) var 1-2 byte for lille — `snprintf` afkortede den lige inden det afsluttende `[`, hvilket ødelagde hele JSON-strukturen (bekræftet af bruger: `SyntaxError: JSON.parse: expected double-quoted property name`). Bufferen sat til 128 byte med reel margin (src/api_handlers.cpp) |
| FEAT-030 | API Versioning | ✅ DONE+TESTET | 🔵 LOW | v7.0.0 | `GET /api/version` + `/api/v1/*` dispatcher med URI rewriting. Alle eksisterende endpoints tilgængelige via v1 prefix. Backward-kompatibelt. Testet 32/32 PASS (Build #1389) |
| FEAT-031 | Firmware OTA via API | ✅ DONE | 🟡 HIGH | v7.5.0 | `POST /api/system/ota` chunked firmware upload, `GET /api/system/ota/status` progress polling, `POST /api/system/ota/rollback` rollback. Dual OTA partitions (ota_0/ota_1 á 1.625MB), boot validation via `esp_ota_mark_app_valid_cancel_rollback()`. Web UI på `/ota` med drag-drop upload, progress bar, rollback knap. Auth + rate limit beskyttet (Build #1699) |
| FEAT-032 | Prometheus Metrics endpoint | ✅ DONE | 🔵 LOW | v7.1.0 | `GET /api/metrics` i Prometheus text format — eksponerer: uptime, heap, HTTP stats, Modbus slave/master stats, SSE clients, WiFi/Ethernet, counters, timers, watchdog, firmware info |
| FEAT-033 | Request Audit Log | ✅ DONE | 🔵 LOW | v7.9.10.41 | `GET /api/system/logs?limit=N` + `POST /api/system/logs/clear` — 100-entry PSRAM-ringbuffer (nyt `api_audit_log.h`/`.cpp`, kopierer 1:1 mønsteret fra system_log.h/.cpp: PSRAM-first `heap_caps_malloc` m. DRAM-fallback, portMUX-spinlock, head/count cirkulær indeksering). **Design-afvejning**: fyldes IKKE fra hver enkelt af de ~108 handler-funktioner (ville kræve at ændre `http_server_stat_request()`s signatur + ~108 kaldested-edits) — i stedet logges fra de to allerede-centrale respons-funktioner `api_send_error()`/`api_send_json()` (api_handlers.cpp), som praktisk talt alle handlers i sidste ende sender deres svar igennem. Daekker dermed IKKE de fåtal handlers der streamer chunked JSON direkte (fx `/api/syslog`, `/api/modbus/activity`, sig selv) — en bevidst, dokumenteret afgrænsning for at holde ændringen til 2 kaldesteder i stedet for ~108. IP+brugernavn genbruger eksisterende `http_get_client_info()`/401-fejlstiens allerede-udtrukne data. **v7.9.10.43**: tilføjet GUI — ny `/logs`-side (5. knap i topnavigationen ved siden af Monitor/ST Editor/CLI/System, alle 5 sider linker nu til hinanden), viser loggen i en tabel med opdater/ryd-knapper og en limit-vælger (25/50/100). Genbruger samme `queuedFetch()`-samtidigheds-kø-mønster som resten af sitet (BUG-369c) (src/api_handlers.cpp, include/api_audit_log.h, src/api_audit_log.cpp, src/main.cpp, src/http_server.cpp, web/logs.html (ny), include/web_logs.h (ny), src/web_logs.cpp (ny), scripts/gzip_web_assets.py, web/dashboard.html, web/editor.html, web/cli.html, web/system.html, web/ota.html) |
| FEAT-034 | ES32D26 Analog spændings-input (0-10V) | ✅ DONE | 🟡 HIGH | v7.9.10.2 | 4x 0-10V analog input, Vi1-4 (GPIO14/33/27/32). Nyt drivlag `analog_driver.h/.cpp` (LAYER 0, mirrorer gpio_driver.cpp's poll/cache-mønster) læser `analogReadMilliVolts()` (eFuse-kalibreret) ind i cache hver `loop()`-iteration, FØR ST Logic. Vi1/Vi3 er ADC2 — som forudset ubrugelige mens WiFi er tilsluttet; disse to kanaler springes over (beholder sidste gyldige registerværdi) i stedet for at skrive stoej, med tydelig markering i `show analog`/`/api/analog` (`wifi_blocked`). Kalibrering: brugerjusterbar `scale`+`offset` (samme princip som `CounterConfig.scale_factor`) med et startgaet (fuld ADC-skala=10,00V) — firmware kender ikke boardets praecise delerforhold, saa reel noejagtighed kraever kalibrering mod en kendt kilde efter installation (analog_driver.h/.cpp) |
| FEAT-035 | ES32D26 Analog strøm-input (4-20mA) | ✅ DONE | 🟡 HIGH | v7.9.10.2 | 4x 4-20mA input, Ii1-4 (GPIO34/39/36/35), alle ADC1 (upaavirket af WiFi). Samme drivlag/kalibreringsprincip som FEAT-034, med default nulpunkt-offset saa 0mV raa-laesning => 4,00mA (loopets fysiske nulpunkt), fuld ADC-skala => 20,00mA (analog_driver.h/.cpp) |
| FEAT-036 | ES32D26 Analog output (DAC 0-10V/4-20mA) | ✅ DONE | 🟠 MEDIUM | v7.9.10.2 | 2x analog udgang, AO1-2 (GPIO25/26, ESP32's indbyggede 8-bit DAC — bemaerk: kun 256 trin opløsning, en reel HW-begraensning). `analog_driver_flush_outputs()` laeser setpoint-registret (×100 fixed-point) og skriver `dacWrite()` skaleret efter kanalens `scale`/`offset`. Her faar `ao1_mode`/`ao2_mode` (FEAT-039, klar siden v7.2.0 men uden forbruger indtil nu) endelig en reel forbruger — CLI'ens `set ao1/ao2 mode`-kommando advarer nu om at tjekke kalibrering ved mode-skift (voltage/current har vidt forskellige skalerings-omraader) (analog_driver.h/.cpp, cli_parser.cpp) |
| FEAT-037 | ES32D26 Analog Modbus register mapping | ✅ DONE | 🟡 HIGH | v7.9.10.2 | Alle 10 kanaler eksponeret som HOLDING registre (HR 0-17: Vi1-4 raw+value HR0-7, Ii1-4 raw+value HR8-15, AO1-2 setpoint HR16-17) — bevidst IKKE Input Registers som oprindeligt skitseret: undersoegelse af den eksisterende kodebase viste at counters/timers OGSAA bruger HR for beregnede/read-mostly vaerdier (counter_engine.cpp), saa AI/AO foelger samme etablerede konvention i stedet for at introducere et nyt moenster. Registrene er FASTE i v1 (ikke bruger-omkonfigurerbare som counters/timers er) for at holde omfanget overskueligt — allokeret via `register_allocator_allocate()` ved boot (register_allocator.cpp, ny `REG_OWNER_ANALOG`) saa fremtidige counter/timer/ST-var-tildelinger ikke kan kollidere. CLI (`set analog`, `show analog`), REST (`GET`/`POST /api/analog`), Prometheus-metrics (`analog_input_value`, `analog_output_setpoint`) og et nyt "Analog I/O"-dashboardkort (skjules automatisk uden aktiverede kanaler, kan saette AO-setpoint direkte fra UI). **Vigtig arkitekturbeslutning:** nye felter i `PersistConfig` (schema 19→20) er bevidst tilfoejet HELT SIDST i strukturen (lige foer `crc16`), IKKE ved siden af det beslaegtede `ao1_mode`/`ao2_mode` — en indsaettelse midt i structen ville forskyde byte-positionen for RBAC/NTP/dashboard-felter/CRC for enhver bruger der opgraderer fra en aeldre schema-version, med usikker `nvs_get_blob`-haandtering af den forskydning (se BUG-339-erfaringen). At tilfoeje til sidst er den eneste maade at vaere 100% sikker paa at intet eksisterende felt paavirkes (types.h, config_load.cpp, cli_commands_analog.h/.cpp, api_handlers.h/.cpp, http_server.cpp, web_dashboard.cpp, MODBUS_REGISTER_MAP.md, docs/manual) |
| FEAT-038 | Modbus mode refaktor (single-transceiver) | ✅ DONE | 🟡 HIGH | v7.2.0 | ES32D26 deler én RS485 transceiver (GPIO1/3/21) mellem slave og master. `set modbus mode slave\|master\|off`. Bidirektionel sync med enabled flags. Conditional compilation via `MODBUS_SINGLE_TRANSCEIVER`. Master bruger `uart1_*` abstraktion på ES32D26 |
| FEAT-039 | AO mode config (DIP switch SW1) | ✅ DONE | 🟠 MEDIUM | v7.2.0 | ES32D26 AO1/AO2 hardware DIP switch mode konfiguration. `set ao1\|ao2 mode voltage\|current`. Config-infrastruktur for fremtidig DAC driver (FEAT-036) |
| FEAT-040 | Runtime UART selection | ✅ DONE | 🟡 HIGH | v7.2.0 | `set modbus slave\|master uart uart0\|uart1\|uart2` — runtime-konfigurerbar UART perifer for Modbus slave/master. Board-afhængige defaults (ES32D26: UART2, andre: UART1) |
| FEAT-041 | Hardware modul config (RS485 pins + Ethernet) | ✅ DONE | 🟡 HIGH | v7.2.0 | `set modul rs485 uart1\|uart2 tx <pin> rx <pin> dir <pin>` — konfigurerbare UART pins via ESP32 GPIO matrix. `set modul ethernet enable\|disable`. Schema 14 med pin config felter. Backup/restore support |
| FEAT-042 | Web-baseret ST Logic Editor | ✅ DONE | 🟡 HIGH | v7.3.0 | Embedded HTML/CSS/JS editor served at `/editor`. Program slot selector (1-4), code editor med line numbers, Tab/Ctrl+S, pool usage meter, ST keyword reference, compile error feedback, enable/disable/delete/save. Basic Auth login. Zero runtime RAM (PROGMEM). Bruger eksisterende `/api/logic/*` endpoints |
| FEAT-043 | Web CLI Console | ✅ DONE | 🟡 HIGH | v7.3.1 | Web-baseret CLI terminal i `/editor` (CLI panel). `POST /api/cli` — fanger CLI output i buffer-console og returnerer som JSON. Kommandohistorik (pil op/ned), monospace output, blokerer farlige kommandoer (reboot/defaults). Samme funktionalitet som telnet |
| FEAT-044 | Web Bindings Panel | ✅ DONE | 🟡 HIGH | v7.3.1 | Bindings-panel i `/editor` til oprettelse/sletning af ST variabel→Modbus register bindinger. Dropdown med kompilerede variabler, retning (input/output), registertype (HR/Coil/DI), adresse. `GET /api/bindings` + `DELETE /api/bindings/{idx}` endpoints |
| FEAT-045 | Web Runtime Monitor | ✅ DONE | 🟡 HIGH | v7.3.1 | Runtime monitor-panel i `/editor` med 1.5s auto-refresh. Viser execution count, timing (last/min/max µs), fejl, overruns + live variabel-tabel med aktuelle værdier. Auto-stop polling ved panel-skift |
| FEAT-046 | BIT_SET/BIT_CLR/BIT_TST ST Logic funktioner | ✅ DONE | 🟠 MEDIUM | v7.3.1 | Bit-manipulation builtins: `BIT_SET(value, bit)` → INT (sæt bit N), `BIT_CLR(value, bit)` → INT (clear bit N), `BIT_TST(value, bit)` → BOOL (test bit N). Bit position 0-15 for INT. Registreret i compiler, VM dispatcher og web editor sidebar |
| FEAT-047 | Web Dashboard med live Prometheus metrics | ✅ DONE | 🟡 HIGH | v7.3.1 | Dashboard forside served at `/` uden login. Parser `/api/metrics` Prometheus text format. 8 kort: System (uptime/heap/PSRAM), Netværk (WiFi/Ethernet/Telnet), Modbus Slave/Master stats, HTTP API stats, Tællere, Timere, ST Logic programmer. Auto-refresh 3s. Catppuccin Mocha dark theme. Navigation til `/editor` |
| FEAT-048 | Bindings redigering i Web Editor | ✅ DONE | 🟠 MEDIUM | v7.3.1 | Redigér-knap på hver binding i Bindings-panelet. Pre-udfylder formularen med eksisterende data (variabel, retning, register type, adresse). "Tilføj" skifter til "Opdatér" mode. Implementeret som delete+re-create via eksisterende API |
| FEAT-049 | Syntax highlighting i ST Editor | ✅ DONE | 🟠 MEDIUM | v7.3.1 | Farvelægning af ST keywords, kommentarer, strenge, tal og typer i code editoren via transparent textarea + farvet overlay |
| FEAT-050 | Auto-complete/IntelliSense i ST Editor | ✅ DONE | 🟠 MEDIUM | v7.3.1 | Popup med ST keywords, builtin funktioner og program-variabler mens man skriver. Tab/Enter til at indsætte, Esc til at lukke |
| FEAT-051 | Sparkline grafer i Runtime Monitor | ✅ DONE | 🟠 MEDIUM | v7.3.1 | Mini SVG-grafer der viser variabel-historik over tid i monitor-panelet. Ringbuffer med seneste 60 datapunkter per variabel |
| FEAT-052 | Single-step execution i ST Logic | ✅ DONE | 🟡 HIGH | v7.3.1 | Pause/Step/Resume kontrol i monitor-panelet. `POST /api/logic/{id}/step` endpoint. Kør ét cycle ad gangen til debugging |
| FEAT-053 | Register viewer i Dashboard | ✅ DONE | 🟡 HIGH | v7.3.1 | Grid-visning af alle Modbus holding registers og coils med live opdatering. Viser allokerings-ejer (counter/timer/ST/manual) |
| FEAT-054 | Historik-grafer i Dashboard | ✅ DONE | 🟠 MEDIUM | v7.3.1 | SVG trend-grafer med ringbuffer for heap, uptime, Modbus requests over seneste 5/15/60 min |
| FEAT-055 | Alarm/notifikationer i Dashboard | ✅ DONE | 🟠 MEDIUM | v7.3.1 | Konfigurerbare tærskler (heap < 30KB, CRC errors stigende) med visuel alarm-bar. Blinkende indikator ved overskridelse |
| FEAT-056 | Modbus register map i Dashboard | ✅ DONE | 🟡 HIGH | v7.3.1 | Visuel oversigt over alle allokerede Modbus registre — hvem ejer dem (counter/timer/ST/manual), address range og konflikter |
| FEAT-057 | Program backup/download | ✅ DONE | 🔵 LOW | v7.3.1 | Download ST kildekode som .st fil direkte fra browseren. Knap i editor toolbar |
| FEAT-058 | PLC Ladder diagram visning | ✅ DONE | 🟠 MEDIUM | v7.3.1 | Read-only ladder diagram genereret fra ST kildekode. Viser kontakter, coils, assignments og IF/THEN som visuelle ladder rungs |
| FEAT-059 | RBAC Multi-User System | ✅ DONE | 🟡 HIGH | v7.6.2 | Role-Based Access Control med op til 8 brugere. Roller: api, cli, editor, monitor (bitmask). Privilegier: read, write, read/write. Auth for HTTP API, SSE, CLI og Web UI. Schema migration v14→v15 med legacy single-user auto-migration. CLI: `set user`, `show users`, `delete user`, `show config rbac`. Backup/restore inkluderer RBAC data. Virtual admin (idx 99) for backward-kompatibilitet |
| FEAT-060 | Standalone Web CLI side | ✅ DONE | 🟠 MEDIUM | v7.6.1.1 | CLI udskilt fra ST Editor til selvstændig side på `/cli`. Fuld terminal med login, kommandohistorik (ArrowUp/Down), statusbar. Konsistent topnav: Monitor \| ST Editor \| CLI \| System |
| FEAT-061 | SSE subscribe=all watch_all mode | ✅ DONE | 🟠 MEDIUM | v7.6.1 | `subscribe=all` scanner nu alle 256 HR + 256 IR + 256 coils + 256 DI. Heap-allokeret SseWatchAllState (~1.5 KB pr. klient). BUG-260 fix |
| FEAT-062 | SSE klient monitor udvidelse | ✅ DONE | 🔵 LOW | v7.6.2 | `show sse` viser nu username, IP, subscribed topics og formateret uptime per klient. SseClientInfo udvidet med username[24] og topics felter |
| BUG-260 | SSE subscribe=all overvågede kun HR 0-15 | ✅ FIXED | 🟡 HIGH | v7.6.1 | `subscribe=all` uden eksplicitte adresser defaultede til HR 0-15 — coils, IR og DI blev ignoreret. FIX: watch_all mode med fuld-range scanning |
| BUG-261 | show config RBAC viste tom sektion | ✅ FIXED | 🔵 LOW | v7.6.2 | `show config rbac` matchede ikke noget filter — RBAC sektionen brugte `show_http` guard. FIX: Selvstændig `show_rbac` filter med match på "RBAC" og "USER" |
| BUG-262 | set RBAC kommando ikke genkendt | ✅ FIXED | 🔵 LOW | v7.6.2 | `set rbac ?` returnerede "unknown argument". FIX: Tilføjet RBAC alias til normalize_alias() → mapper til USER |
| FEAT-063 | Web User Badge + Login/Logout | ✅ DONE | 🟠 MEDIUM | v7.6.2.2 | User badge i topnav højre hjørne på alle 4 web-sider. Viser brugernavn, grøn/rød status-dot, dropdown med roller, privilegier, auth mode. Log ind/ud funktionalitet. Dashboard: login modal (skjult default) med "Log ind" knap. CLI/Editor/System: updateUserBadge() efter login. `/api/user/me` endpoint + `show user` CLI kommando |
| FEAT-064 | /api/user/me API endpoint | ✅ DONE | 🟠 MEDIUM | v7.6.2.1 | REST endpoint returnerer authenticated status, username, roles, privilege, mode (legacy/rbac). Bruges af web user badge til at vise brugerinfo |
| FEAT-065 | show user CLI kommando | ✅ DONE | 🔵 LOW | v7.6.2.1 | `show user` viser aktuel session info: interface, RBAC status, antal brugere, auth mode |
| FEAT-066 | SSE klient management i web System | ✅ DONE | 🟠 MEDIUM | v7.6.2.3 | SSE sektion på `/system` side: tabel med slot, IP, bruger, topics, uptime. Afbryd individuelle klienter eller alle. API: `GET /api/events/clients`, `POST /api/events/disconnect` med `{slot: N}` eller `{slot: -1}` for alle |
| FEAT-069 | SSE konfiguration i backup/restore | ✅ DONE | 🟠 MEDIUM | v7.6.2.7 | Backup eksporterer SSE indstillinger (enabled, port, max_clients, check_interval_ms, heartbeat_ms) som selvstændig `sse` JSON sektion. Restore gendanner SSE config fra backup fil |
| FEAT-070 | Async Modbus Master (non-blocking) | ✅ DONE | 🔴 CRITICAL | v7.7.0 | Dedikeret FreeRTOS task (Core 0, 4KB stack) til Modbus UART I/O. ST builtins er nu non-blocking: reads returnerer cached værdi og køer refresh, writes køes i baggrunden. Nye builtins: `MB_SUCCESS()`, `MB_BUSY()`, `MB_ERROR()`. 32-entry cache, 16-deep queue, request deduplication. Eliminerer overruns ved Modbus-operationer i ST Logic |
| FEAT-072 | Web Monitor: Modbus RTU trafikmonitor | ✅ DONE | 🟡 HIGH | v7.8.0 | Live req/3s rate, success rate, CRC fejl, timeouts, exceptions, sparkline grafer for slave+master |
| FEAT-073 | Web Monitor: Modbus Master status | ✅ DONE | 🟡 HIGH | v7.8.0 | Async cache stats (hits/misses/entries/hit rate), per-slave cache status tabel, queue full count |
| FEAT-074 | Web Monitor: Ethernet/WiFi signalkvalitet | 🟠 DELVIST | 🟠 MEDIUM | v7.9.10.3 | RSSI-trend tilføjet (sparkline, genbruger eksisterende `drawSparkline()`-infrastruktur — kun mens WiFi er forbundet, for at undgå et fladt "hul" i grafen ved disconnect). **Mangler stadig:** link speed (Ethernet), pakketab, reconnect-historik over tid (reconnect-*tæller* vises allerede, men ikke som trend) (web_dashboard.cpp) |
| FEAT-075 | Web Monitor: TCP forbindelsesmonitor | ✅ DONE | 🔵 LOW | v7.9.6.7 | Dashboard kort: SSE klient-tabel (IP/bruger/topics/uptime), Telnet IP+bruger+uptime via Prometheus, HTTP aggregate stats. TcpClient.connected_ms tilføjet |
| FEAT-076 | Web Monitor: Bandwidth monitor | ❌ OPEN | 🔵 LOW | v8.0.0 | Bytes sendt/modtaget over tid (Modbus + HTTP + SSE) |
| FEAT-077 | Web Monitor: Heap fragmentation graf | ✅ DONE | 🟡 HIGH | v7.9.10.3 | Frag%-tallet blev allerede beregnet hvert pollingkald (FEAT-078), men kun vist som et enkelt tal+bar — ingen historik. Tilføjet `history.fragPct[]` + sparkline (genbruger `drawSparkline()`, samme moenster som heap-trend). Fanger nu gradvis fragmentering over tid, ikke kun et snapshot (web_dashboard.cpp) |
| FEAT-078 | Web Monitor: FreeRTOS task monitor | ✅ DONE | 🟠 MEDIUM | v7.8.0 | Task count, heap fragmentation (largest block vs free), stack HWM for kendte tasks (loopTask, mb_async, IDLE0/1) |
| FEAT-079 | Web Monitor: FreeRTOS task monitor | 🟠 DELVIST (CLI ja, web nej) | 🟠 MEDIUM | v8.0.0 | CLI-delen findes som `show tasks` (BUG-343, v7.9.9.5): tilstand (RUNNING/READY/BLOCKED/SUSPEND), core-tilknytning, prioritet og resterende stak. **Mangler stadig:** visning i web-dashboardet. NB: `uxTaskGetSystemState()` kan ikke bruges i denne framework-build (`CONFIG_FREERTOS_USE_TRACE_FACILITY` er slaaet fra → undefined reference ved link), saa en web-udgave skal ligesom CLI-udgaven slaa tasks op ved NAVN via `xTaskGetHandle()` — dvs. kun kendte tasks kan vises, ikke en komplet liste |
| FEAT-080 | Web Monitor: Boot-historik | 🟠 DELVIST | 🟡 HIGH | v7.9.10.3 | Reboot-taeller og seneste reset-aarsag er nu vist i web-dashboardets System-kort (var allerede sporet/NVS-persisteret/API-eksponeret, blot aldrig vist — ny client-side `fmtResetReason()`-mapping mirrorer `watchdog_get_reset_reason_str()` i watchdog_monitor.cpp 1:1, verificeret mod `esp_reset_reason_t`s faktiske ESP-IDF-enum-vaerdier). **Mangler stadig:** historik over de sidste 10 boot-tidspunkter — `WatchdogState` (types.h) har kun skalar-felter for SENESTE reboot, ingen ringbuffer. Kraever ny NVS-ringbuffer, ikke bare eksponering (web_dashboard.cpp) |
| FEAT-081 | Web Monitor: NVS brug | ✅ DONE | 🟠 MEDIUM | v7.9.10.4 | `nvs_get_stats(NULL, &stats)` (ESP-IDF, `nvs.h`) giver `used_entries`/`free_entries`/`total_entries`/`namespace_count` — vist som entries (NVS' egen 32-bytes/entry-granularitet), IKKE et gaettet byte-tal, for ikke at fremstaa mere praecist end det er. Nyt Prometheus-blok + System-kort raekke m. procent-bar (samme bar-moenster som heap/frag). **Ikke inkluderet:** write-count/flash wear-indikator — `nvs_get_stats()` eksponerer det ikke, kraever separat instrumentering (api_handlers.cpp, web_dashboard.cpp) |
| FEAT-082 | Web Monitor: SPIFFS/LittleFS brug | ✅ DONE | 🔵 LOW | v7.9.10.4 | `SPIFFS.usedBytes()`/`totalBytes()` (allerede monteret ved boot — bruges til ST Logic bytecode/kildekode-persistens). Nyt Prometheus-blok + System-kort raekke m. procent-bar. **Ikke inkluderet:** filantal (kraever at iterere SPIFFS-mappen, ikke tilfoejet — lav vaerdi paa et system med kun to faste filtyper) (api_handlers.cpp, web_dashboard.cpp) |
| FEAT-083 | Web Monitor: ESP32 intern temperatur | 🚫 HARDWARE-BLOKERET | 🔵 LOW | - | Undersoegt v7.9.10.4: `driver/temp_sensor.h` findes i denne framework-installation KUN for ESP32-S2/S3/C3, ikke almindelig ESP32 (Xtensa LX6 — den chip dette projekt bygger til, "ESP32-WROVER"). Det er ikke en software-mangel — chippen har fysisk ingen intern temperatursensor. Kan kun implementeres hvis/naar boardet skifter til en S2/S3/C3-baseret variant |
| FEAT-084 | Web Monitor: Spændingsmonitor | 🚫 DELVIST HARDWARE-BLOKERET | 🔵 LOW | v7.9.10.4 | Undersøgt: almindelig ESP32 (denne chip) har ingen indbygget VCC/3.3V-raill-læse-API (i modsætning til fx ESP8266's `ESP.getVcc()`) — kræver en ekstern spændingsdeler-kreds ind på en ADC-pin, som der ikke er belæg for findes på ES32D26 uden at kende boardets skematik. **Brownout-DELEN er derimod allerede dækket**: `ESP_RST_BROWNOUT` er én af de reset-årsager der nu vises i dashboardet (FEAT-080, v7.9.10.3, "Sidste reset: Brownout") — dog kun seneste hændelse, ikke en løbende log over brownout-events over tid |
| FEAT-085 | Web Monitor: Alarm-historik/log | ✅ DONE | 🟡 HIGH | v7.8.0 | 32-entry ringbuffer med auto-detection (heap, CRC, timeout, auth, overrun). API: GET /api/alarms + POST /api/alarms/ack. Dashboard kort med severity-farver og kvittér-knap |
| FEAT-086 | Web Monitor: Event log viewer | ✅ DONE | 🟠 MEDIUM | v7.9.10.6 | Nyt delt modul `system_log.h/.cpp` (PSRAM ringbuffer, 200 entries, samme moenster som `mb_activity_log.cpp`) — logger config-gem (NVS), reboot (REST/CLI/OTA — 3 kaldesteder), login-fejl (401/403 m. IP+brugernavn) og boot. Login-**succes** logges bevidst IKKE (stateless Basic Auth ville flode loggen). Dashboard-kort "Hændelseslog" med kategori-/kilde-filter, CSV-eksport, start/stop (samme moenster som FEAT-153). API: `GET /api/syslog` (chunked, `?category=`/`?limit=`), `POST /api/syslog/{clear,start,stop}`. **Delt modul med FEAT-089** (ét kategori-felt) — bevidst afvigelse fra alarmlog/aktivitetslog-praecedensen for at spare flash (91,9% brugt foer denne feature). **Ikke daekket i v1:** `config_apply()`- og persist-group-restore-events (nedprioriteret, se FEAT-089s afgraensning) |
| FEAT-087 | Web Monitor: Watchdog-statistik | 🟠 DELVIST | 🟠 MEDIUM | v7.9.10.3 | Reboot-taeller + seneste reset-aarsag vises nu i web-dashboardet (se FEAT-080). **Mangler stadig:** hvilken task der udloeste — `watchdog_track_modbus_rx/st_logic/heartbeat()` er alle eksplicitte no-op-stubs i dag (watchdog_monitor.cpp: "Future: ... For now: No-op") — og tid siden sidste feed. Kraever reel implementering af de eksisterende stub-funktioner, ikke kun eksponering |
| FEAT-088 | Web Monitor: Threshold-alarmer | ❌ OPEN | 🟠 MEDIUM | v8.0.0 | Konfigurerbare grænseværdier for vilkårlige registre med visuel alarm |
| FEAT-089 | Web Monitor: Register change-log | ✅ DONE | 🟠 MEDIUM | v7.9.10.6 | Samme delte `system_log`-modul som FEAT-086 (kategori `regchange`). Logger gammel/ny vaerdi, adresse, coil/register-type, tidsstempel, samt "hvem" (RBAC-brugernavn+klient-IP ved REST, "ekstern master" ved Modbus Slave). **Bevidst afgraenset til REST API + Modbus Slave (ekstern master)** — de to fjern-aktoer-kilder hvor et audit-spor loeser et reelt problem. **CLI-skrivninger og ST Logics periodiske output-binding logges IKKE i v1**: CLI er lav audit-vaerdi (lokal, synlig handling ved konsollen); ST Logics periodiske binding kraever en ny per-mapping forrige-vaerdi-cache (findes ikke i dag) for ikke at oversvoemme loggen hver scan-cyklus — en selvstaendig, stoerre tilfoejelse. Kendt hul: `modbus_handle_reset_on_read()`s auto-clear-bivirkning ved FC03-laesning rammer bevidst IKKE loggen (logges ved hoejniveau-kaldesteder, ikke i registers.cpp) — korrekt, det er ikke en bruger-initieret aendring |
| FEAT-090 | Web Monitor: Modbus request log / sniffer | ✅ DONE (som FEAT-149/153) | 🟡 HIGH | v7.9.8.0 | Leveret af **FEAT-149** (Modbus Aktivitetslog, v7.9.8.0): wire-level ringbuffer over BAADE Master- og Slave-trafik med rolle, kilde (ST Logic/CLI/dashboard/ekstern master), slave-ID, function code, adresse, antal, vaerdi og status — praecis den "live visning af Modbus requests" der efterspurgtes, blot bredere (ogsaa udgaaende Master-trafik). Udvidet i **FEAT-153** (v7.9.10.0) med start/stop, CSV-eksport, 500 poster i PSRAM og NTP-tidsstempler. Posten stod faelagtigt som OPEN indtil v7.9.10.1 |
| FEAT-091 | Web Monitor: ST Logic profiler | 🟠 DELVIST | 🟠 MEDIUM | v8.0.0 | Min/last/max exec-tid, exec-count, fejl-count og overrun-count pr. program vises allerede i web-dashboardet (`$('stBody')`-tabellen, web_dashboard.cpp), ikke kun via CLI/Prometheus. **Mangler stadig:** cycle-time histogram/fordeling, og variabel-trace over tid (ingen sampling/lagring af ST-variabelvaerdier findes noget sted i kodebasen) |
| FEAT-092 | Web Monitor: GPIO pulse monitor | ❌ OPEN | 🔵 LOW | v8.0.0 | Pulsbredde, frekvens, duty cycle for digitale inputs |
| FEAT-093 | Web Monitor: Uptime-graf | ❌ OPEN | 🔵 LOW | v8.0.0 | Visuelt overblik over oppetid med markering af nedetid/reboot |
| FEAT-094 | Web Monitor: Analog input trending | ✅ DONE | 🟡 HIGH | v7.9.10.3 | 0-10V (Vi1-4) og 4-20mA (Ii1-4) inputs faar nu en sparkline + min/maks/snit pr. AKTIVERET kanal i Analog I/O-kortet, beregnet client-side fra samme 120-sample/6-min rullende vindue som resten af dashboardet (`history.analog{}`, ny per-kanal dictionary siden antal aktive kanaler er dynamisk). Teknisk detalje: tabellen genopbygges via `innerHTML` hver polling, saa `<svg>`-elementerne findes foerst NAAR innerHTML er sat — `drawSparkline()` kaldes derfor i en separat loekke EFTER innerHTML, ikke inline i byggeloekken (web_dashboard.cpp) |
| FEAT-095 | Web Monitor: Digital I/O dashboard | ✅ DONE | 🟠 MEDIUM | v7.8.0 | Visuelle LED-indikatorer for IN1-IN8 (input) og CH1-CH8 (output) med klik-toggle på outputs. Auto-skjules uden shift register HW |
| FEAT-096 | Web Monitor: RS-485 bus health | ✅ DONE | 🟠 MEDIUM | v7.9.20.0 | **Rodårsag-fund undervejs**: `g_persist_config.modbus_slave.{total_requests,successful_requests,crc_errors,exception_errors}` (`include/types.h`) fandtes allerede i struct'en og blev allerede vist i CLI/REST/Prometheus — men var ALDRIG blevet inkrementeret noget sted i kildekoden, saa de altid viste 0 i praksis, uanset reel Modbus-slave-trafik. Wired op i `src/modbus_server.cpp`: `MODBUS_STATE_RX`'s `MODBUS_RX_ERROR`-gren (daekker BÅDE ægte CRC-mismatch og for-kort-frame, jf. `modbus_rx.cpp` — de to skelnes ikke i returvaerdien) inkrementerer `crc_errors`; `MODBUS_STATE_PROCESS` (efter `modbus_dispatch_function_code()`, hvor frame'et allerede er CRC-valideret og adresseret til os/broadcast) inkrementerer `total_requests` altid plus `successful_requests`/`exception_errors` afhaengigt af dispatch-resultatet. **Collision detection**: `g_modbus_bus_busy_errors` (mutex-timeout paa det delte enkelt-transceiver, FEAT-038 — praktisk "kollisions"-proxy for denne halv-dupleks arkitektur) fandtes ALLEREDE i Prometheus-metrics men blev aldrig vist i GUI'en — tilfoejet som "Bus busy (kontention)"-raekke paa Modbus Master-dashboardkortet. **Bus idle/busy ratio**: ingen byte-niveau UART-instrumentering findes i firmwaren (bevidst IKKE tilfoejet — ville kraeve at instrumentere real-time-foelsom RTU-timingkode, en unoedvendig risiko for en "nice to have"-metrik) — i stedet et rent CLIENT-SIDE ESTIMAT i `web/dashboard.html` beregnet ud fra allerede-kendt request-rate × en dokumenteret antaget gennemsnitlig frame-stoerrelse (~30 byte) × standard RTU-bit-formel (11 bit/byte) / baudrate, tydeligt maerket "(estimeret)" i GUI'en med en forklarende tekst der siger praecis hvad det IKKE er (en maalt vaerdi). **Nyt dashboardkort** "RS-485 Bus Health" (`bushealth`, tilfoejet SIDST i `_allCardIds` per BUG-365s indeks-stabilitets-regel) kombinerer Slave+Master-fejl til én samlet fejlrate, viser bus busy/kontention, og det estimerede belastningstal. **Live-testet v7.9.20.0 paa 10.1.1.153**: build+boot rent, nyt kort+alle nye element-id'er bekraeftet til stede i den servrede dashboard-side, Master-siden (aktiv rolle paa denne enhed — transceiver er sat til "master") viste reelle, ikke-nul tal (157 requests, 139 success) som den nye kombinerede fejlrate-udregning korrekt kan bruge. **IKKE live-verificeret**: selve slave-tælleren-wiring, da enhedens transceiver lige nu er konfigureret som Master, ikke Slave — ingen ekstern master poller den, saa der er ingen reel indgaaende RTU-trafik til at trigge `total_requests`/`crc_errors` osv. i praksis lige nu; koden er verificeret ved grundig laesning (samme moenster som den allerede-virkende master-side-taelling), men en fysisk ekstern-master-test er ikke udfoert. Logic1 upaavirket (src/modbus_server.cpp, web/dashboard.html) |
| FEAT-097 | Web Monitor: Customizable dashboard | ✅ DONE | 🔵 LOW | v7.9.6.8 | Leveret ad to omgange: kort-raekkefoelge (`dashboard_card_order`, schema 17, v7.8.4.2) samt fane-tildeling og skjulte kort (`dashboard_card_tabs`/`dashboard_card_hidden`, schema 18, v7.9.6.8). Gemmes i NVS som oenskset, med `GET`/`POST /api/dashboard/layout` (bevidst uden auth — ikke-foelsom UI-praeference). Posten stod faelagtigt som OPEN indtil v7.9.10.1 |
| FEAT-098 | Web Monitor: Register-map visualizer | ✅ DONE | 🟠 MEDIUM | v7.8.4.2 | Fandtes allerede: "Register Allokering"-siden (`pageRegmap`, web_dashboard.cpp) viser grafisk oversigt over HR/IR/Coil/DI (0-255 hver) med farvekodet legend (Counter/Timer/ST Logic/Manual/Ledig), hover-tooltips og en samlet allokeringstabel — opdateres hvert pollingkald. FEAT-111 (v7.8.4.2) beskriver allerede en senere udvidelse (sammenlagt sticky header) af netop denne feature, hvilket afsloerede at basisfeaturet fejlagtigt stod som OPEN. Posten stod fejlagtigt som OPEN indtil v7.9.10.2 |
| FEAT-099 | Web Monitor: Trend recorder med CSV-eksport | ✅ DONE | 🟡 HIGH | v7.9.21.0 | Helt ny, selvstændig ringbuffer-baseret optager (`trend_recorder.h/.cpp`) — undersøgt først om noget eksisterende kunne genbruges: `system_log.h`s regchange-log er hændelses-drevet ved faktiske SKRIV-operationer, ikke periodisk sampling af uændrede værdier, og kunne ikke genbruges direkte. **RAM/PSRAM-only, IKKE NVS-persisteret** — matcher værktøjets natur ("optag en session, eksportér, evt. forfra"), undgår enhver schema-version-komplikation; konfiguration+data nulstilles ved reboot. Op til 8 samtidige målepunkter (HR/IR/Coil/DI, hver med egen adresse), ringbuffer-dybde 720 samples (~26KB i PSRAM ved 8 punkter — trivielt, samme "PSRAM er næsten helt frit"-præmis som `mb_activity_log.h` allerede dokumenterer), interval konfigurerbart 500-60000ms. En rekonfiguration (tilføj/fjern punkt, skift interval) stopper og rydder altid eksisterende data bevidst — simplest at ræsonnere om, matcher værktøjets "start en frisk session"-natur. **Genbruger etablerede mønstre 1:1**: PSRAM-first ringbuffer-allokering med heap-fallback (samme som `mb_activity_log.cpp`/`system_log.cpp`/`api_audit_log.cpp`), samme spinlock-per-ringbuffer-mønster (skriver fra `loop()`, læses fra httpd-task på evt. anden core), samme chunked-JSON-afsendelse som `/api/modbus/activity` (BUG-332-lektien: aldrig én stor buffer for et svar hvis størrelse skalerer med datamængde), og samme client-side CSV-eksport-mønster (fetch hele datasættet → byg CSV-streng → Blob-download) som `exportSyslog()`/`exportMbActivity()`. **Nye REST-endpoints**: `GET/POST /api/trend/config`, `POST /api/trend/start\|stop\|clear`, `GET /api/trend/data` (URI-suffix-dispatch på `/api/trend/*`, samme mønster som `/api/syslog/*`). **Nyt dashboardkort** "Trend Recorder" (`trendrec`, tilføjet SIDST i `_allCardIds` per BUG-365s indeks-stabilitets-regel, tab `app` sammen med Tællere/Timere/ST Logic) — tabelvisning (ikke graf, for at undgå en ny charting-library-afhængighed med flash allerede ved 90,5%), toolbar til at tilføje/fjerne målepunkter og styre optagelse. **Live-testet v7.9.21.0 på 10.1.1.153**: konfigurerede HR#0+Coil#0 ved 1000ms interval, startede optagelse, bekræftede korrekte værdier og PRÆCISE 1000ms-intervaller over 11 samples (18242,19242,...28242ms); ændrede HR#0 fra 4242→9999 og coil 0→1 midt i optagelsen, bekræftede næste samples fangede ændringen korrekt; stop/clear/config-persistens-gennem-clear alle bekræftet; valideringsfejl bekræftet (ugyldig type-streng, >8 punkter afvist med klare fejlbeskeder); ryddet til tom state og nulstillet testregistrene til 0 for en ren overlevering. Logic1 upåvirket gennem hele test-forløbet (4300+ eksekveringer, 0 fejl). Rørte: include/trend_recorder.h (ny), src/trend_recorder.cpp (ny), src/main.cpp, src/api_handlers.cpp, include/api_handlers.h, src/http_server.cpp, web/dashboard.html |
| FEAT-100 | Web Monitor: Mini-SCADA view | ❌ OPEN | 🔵 LOW | v8.0.0 | Simpel process-visualisering med symboler for pumper, ventiler, motorer |
| FEAT-101 | NTP tidssynkronisering | ✅ DONE | 🟡 HIGH | v7.8.1 | ESP-IDF SNTP klient med konfigurerbar server, POSIX tidszone, sync interval. CLI: `set ntp`, `show ntp`/`show time`. API: `GET/POST /api/ntp`. Prometheus metrics: ntp_synced, ntp_epoch, ntp_sync_count. Dashboard NTP kort med live tid. Alarm-log med real-time tidsstempler. Schema 15→16 |
| BUG-263 | Web user badge viste altid "Ikke logget ind" | ✅ FIXED | 🟡 HIGH | v7.6.2.3 | Tre separate fejl: (1) `/api/user/me` manglede direkte httpd URI handler — var kun i v1_routes, så `fetch('/api/user/me')` gav 404 (kun `/api/v1/user/me` virkede). (2) Dashboard manglede login modal. (3) `updateUserBadge()` blev ikke kaldt efter login. FIX: Tilføjet `uri_user_me` httpd registrering, login modal på dashboard, updateUserBadge() kald efter login i alle 4 sider |
| BUG-264 | SSE afviste brugere med API rolle (403) | ✅ FIXED | 🟡 HIGH | v7.6.2.4 | SSE rolle-check krævede kun `ROLE_MONITOR` (0x08). Brugere med `api` rolle (0x01) fik 403 "MONITOR role required" selvom de var autentificeret korrekt. FIX: Ændret check til `ROLE_MONITOR \| ROLE_API` — begge roller giver SSE-adgang. Tilføjet RBAC Authentication sektion i `show sse` output |
| BUG-265 | API write endpoints tjekker ikke privilege | ✅ FIXED | 🔴 CRITICAL | v7.6.2.4 | Alle 42 POST/DELETE endpoints brugte `CHECK_AUTH` som kun tjekker authentication — ikke privilege. Read-only brugere (`privilege read`) kunne lave skrive-operationer (reboot, save, delete, config-ændringer). FIX: 42 write-handlers ændret til `CHECK_AUTH_WRITE` som returnerer 403 "Write privilege required" |
| BUG-266 | rbac_parse_privilege("write") returnerede PRIV_RW | ✅ FIXED | 🟠 MEDIUM | v7.6.2.4 |
| BUG-267 | Web system write-knapper viste ikke fejl ved 403 | ✅ FIXED | 🟠 MEDIUM | v7.6.2.5 |
| BUG-268 | Confirm dialog callback blev aldrig kaldt | ✅ FIXED | 🔴 CRITICAL | v7.6.2.6 | `confirmAction()` kaldte `closeConfirm()` som satte `pendingConfirmFn=null` FØR callback-tjekket. Alle handlinger bag bekræftelsesdialog (Fabriksindstillinger, Genstart uden gem, Afbryd alle SSE, Restore config) viste dialogen men gjorde ingenting ved "Bekræft". FIX: Gem fn i lokal variabel før closeConfirm() | `doReboot()` brugte `.catch(()=>{})` — slugte fejl silently. Brugeren så "Genstarter..." men intet skete ved manglende privilege. Alle write-funktioner (reboot, save, load, defaults) viser nu 403 "Ingen skriveadgang" fejl korrekt | `strcasestr(str, "write")` matchede i `rbac_parse_privilege()` før "read/write" blev tjekket — så `privilege write` blev til `PRIV_RW` (read+write). FIX: Tjek "read/write" og "rw" først, derefter "write" alene → `PRIV_WRITE` |
| BUG-250 | PSRAM detection mangler i CLI og metrics | ✅ FIXED | 🔵 LOW | v7.3.1 | `show version` og `show status` viste ikke PSRAM info. `/api/metrics` manglede `esp32_psram_total_bytes` og `esp32_psram_free_bytes`. FIX: Tilføjet PSRAM detection med `ESP.getPsramSize()`/`ESP.getFreePsram()` til begge CLI kommandoer og Prometheus endpoint |
| BUG-251 | `/api/metrics` kræver unødvendig auth | ✅ FIXED | 🟠 MEDIUM | v7.3.1 | Metrics endpoint krævede Basic Auth → dashboard og Prometheus scrapers kunne ikke hente data uden credentials. FIX: Fjernet `CHECK_AUTH()`, beholdt `CHECK_API_ENABLED()` og rate limit. Read-only data, ingen sikkerhedsrisiko |
| BUG-252 | Web editor/system login tabt ved navigation mellem moduler | ✅ FIXED | 🟡 HIGH | v7.3.2 | `AUTH` variabel (in-memory) nulstillet ved hvert page load → brugeren skulle logge ind igen ved skift mellem `/editor` og `/system`. FIX: Auth token gemt i `sessionStorage` (`hfplc_auth` key), auto-verificeret ved page load. Ryddes ved 401 eller tab-lukning (web_editor.cpp, web_system.cpp) |
| BUG-290 | VAR initial values ignoreret af compiler | ✅ FIXED | 🔴 CRITICAL | v7.7.1.1 | `INPUTTIMER : INT := 120` blev altid 0. Compiler satte `variables[i].int_val = 0` hardkodet. FIX: Symbol table bærer nu `initial_value` fra parser → compiler → bytecode. (st_compiler.h, st_compiler.cpp) |
| BUG-291 | VAR initial values tabt ved boot (bytecode cache) | ✅ FIXED | 🔴 CRITICAL | v7.7.1.1 | Cached bytecode (`.bc` filer) gemte ikke initial values. Ved boot satte `st_bytecode_load()` alle variabler til 0. FIX: Bytecode persist format v2 — gemmer/loader `var_initial[]` per variabel. Version mismatch trigger automatisk recompile. (st_bytecode_persist.cpp, st_types.h) |
| BUG-292 | Telnet auth bruger ikke RBAC | ✅ FIXED | 🟡 HIGH | v7.7.1.1 | Telnet brugte kun lokale telnet credentials, ikke RBAC brugerdatabase. FIX: Når `rbac.enabled=true` bruger telnet `rbac_authenticate()`. Fallback til lokal user/pass når RBAC disabled. `show config telnet` viser auth mode. (telnet_server.cpp, cli_show.cpp) |
| BUG-293 | mb_async_init() crash på ES32D26 i SLAVE mode | ✅ FIXED | 🔴 CRITICAL | v7.7.1.1 | `g_modbus_master_config.enabled` var true i NVS selvom master ikke initialiseret (SLAVE mode). Guard checkede forkert flag → crash ved "Sub". FIX: `#if MODBUS_SINGLE_TRANSCEIVER` checker nu `mb_mode == MODBUS_MODE_MASTER` direkte. (main.cpp) |
| BUG-294 | Web editor manglede BEGIN/END/EXPORT keywords | ✅ FIXED | 🟠 MEDIUM | v7.7.1.1 | Autocomplete kendte ikke BEGIN, END, EXPORT. Modbus Master funktioner ufuldstændige (manglede MB_READ_COIL, MB_READ_INPUT_REG, MB_SUCCESS, MB_BUSY, MB_ERROR). FIX: Opdateret ST_KW og ST_FN lister + tilføjet Modbus Master hjælpe-sektion. (web_editor.cpp) |
| BUG-295 | Version+build ikke vist i web System/Administration | ✅ FIXED | 🟠 MEDIUM | v7.7.1.1 | `/api/status` manglede `firmware` felt. Web UI overskrev firmware med bare version. OTA status brugte ESP-IDF app version. FIX: Tilføjet `firmware: "v7.7.1.1.XXXX"` i API + konsistent visning i web UI og OTA. (api_handlers.cpp, web_system.cpp, ota_handler.cpp) |
| BUG-296 | ST parser max 4 function arguments — CTUD/SCALE/CNT fejler | ✅ FIXED | 🔴 CRITICAL | v7.7.2 | AST struct `args[4]` og parser `< 4` check begrænsede alle funktionskald til max 4 args. CTUD(5 args), SCALE(5 args) og nye CNT_SETUP(6 args) kunne aldrig kompilere. FIX: `args[4]→args[8]`, parser limit 4→8 (st_types.h:229, st_parser.cpp:367+385+761+768) |
| BUG-297 | Web dashboard register map viser forkerte counter-adresser | ✅ FIXED | 🟡 HIGH | v7.7.2 | Register allokering brugte `(id-1)*4` fra HR0 (Counter 1→HR0-3). Korrekt er base=100+(id-1)*20 stride 20. Counter 1 er HR100-114, ikke HR0-3. FIX: Korrekt base+offset for value/raw/freq/ctrl/compare (web_dashboard.cpp:569-580) |
| FEAT-102 | Web Monitor: Udvidet alarm historik | ✅ DONE | 🟡 HIGH | v7.8.3.1 | Alarm-kort spænder 2 kolonner, 25 entries (op fra 15), severity-kolonne (KRIT/ADV/INFO), beriget Kilde/Detaljer med farvekodede IP/brugernavn (auth) og Modbus kommunikationsfejl med slave ID + register adresse. Scrollbar max 320px. Alarm-beskeder: Master `TX -> ID:X @Y`, Slave `RX <- ID:X`. Backend tracker `last_error_slave_id`/`last_error_address` per fejlet request (types.h, modbus_master.cpp, api_handlers.cpp, web_dashboard.cpp) |
| FEAT-103 | Web Monitor: Modbus Master/Slave interface info | ✅ DONE | 🟡 HIGH | v7.8.3.1 | Interface-række i card body: "ID:1 Baud:115K Data:8 Parity:None Stop:1" (slave) / "Baud:115K Data:8 Parity:None Stop:1" (master). Badge viser Deaktiveret/Aktiv/Idle. Nye Prometheus metrics: modbus_slave_config_enabled/id/baudrate/parity/stopbits, modbus_master_config_enabled/baudrate/parity/stopbits (api_handlers.cpp, web_dashboard.cpp) |
| FEAT-104 | Web Monitor: NTP tid følger ESP32 tidszone | ✅ DONE | 🟠 MEDIUM | v7.8.3.1 | Dashboard NTP-kort bruger nu `local_time` fra ESP32 `/api/ntp` i stedet for browserens tidszone via `new Date()`. Tiden følger NTP timezone-konfigurationen (fx CEST). (web_dashboard.cpp) |
| FEAT-105 | NTP Tidszone dokumentation | ✅ DONE | 🔵 LOW | v7.8.3.1 | `docs/NTP_TIMEZONE_GUIDE.md` — POSIX TZ-format forklaring, kopiér-og-indsæt tidszoner for 30+ lande, NTP servere, fejlfinding, CLI+API eksempler |
| BUG-298 | Alarm historik auth failures viste 0.0.0.0 som klient-IP | ✅ FIXED | 🟡 HIGH | v7.8.3.2 | `getpeername()` blev kaldt EFTER `httpd_resp_sendstr()` — socket kunne være lukket/genbrugt → 0.0.0.0. Derudover brugte koden `sockaddr_in` men ESP-IDF lwIP kan bruge IPv6-kompatible sockets (IPv4-mapped `::ffff:x.x.x.x`). FIX: IP+username opslag flyttet til FØR response sendes + håndtering af både AF_INET og AF_INET6 (api_handlers.cpp:282-326) |
| BUG-299 | Alarm historik opdaterings-interval for langsomt (10s) | ✅ FIXED | 🔵 LOW | v7.8.3.2 | `fetchAlarms()` kørte hvert 10. sekund mens metrics opdateredes hvert 3. sekund → alarmer virkede forsinkede. FIX: Sænket interval fra 10s til 5s (web_dashboard.cpp) |
| FEAT-106 | Web Monitor: Udvidet Netværk kort med IP/DNS/gateway | ✅ DONE | 🟡 HIGH | v7.8.4.1 | Netværk-kort viser nu hostname, WiFi IP/mask/gateway/DNS/SSID, Ethernet IP/mask/gateway/DNS/MAC/speed. Henter data fra `/api/hostname`, `/api/wifi`, `/api/ethernet` hvert 10. sekund. SSE info flyttet til HTTP API kort (web_dashboard.cpp) |
| FEAT-107 | Web Monitor: SSE info sammenlagt med HTTP API kort | ✅ DONE | 🟠 MEDIUM | v7.8.4.1 | SSE server status (aktiv/deaktiveret, port, klienter, max klienter, heartbeat) flyttet fra Netværk-kort til HTTP API kort som separat sektion. Henter data fra `/api/events/status` (web_dashboard.cpp) |
| FEAT-108 | Web Monitor: Drag-and-drop kort-sortering med NVS persistens | ✅ DONE | 🟠 MEDIUM | v7.8.4.2 | Cards i monitor dashboard kan trækkes og omarrangeres via drag-and-drop. Rækkefølge gemmes på ESP32 via `POST /api/dashboard/layout` og hentes ved load via `GET /api/dashboard/layout`. Persisteres i NVS som del af PersistConfig (schema 17). Hvert card har drag-håndtag (≡) i overskriften (types.h, config_load.cpp, api_handlers.cpp, web_dashboard.cpp) |
| FEAT-109 | Global Save-knap i topnav på alle sider | ✅ DONE | 🟡 HIGH | v7.8.4.2 | Grøn "💾 Save"-knap i top-navigation på ALLE 4 sider (Monitor, ST Editor, CLI, System). Kalder `POST /api/system/save` for at gemme al konfiguration til NVS inkl. dashboard kort-rækkefølge. Visuelt feedback: grøn→orange (gemmer)→checkmark (gemt)/rød (fejl). (web_dashboard.cpp, web_system.cpp, web_editor.cpp, web_cli.cpp) |
| FEAT-110 | SSE server mættet alarm + Write denied alarm | ✅ DONE | 🟠 MEDIUM | v7.8.4.2 | Alarm (ADV) når SSE server mættet + alarm (ADV) ved 403 "Write privilege denied" med klient-IP/brugernavn. `docs/ALARM_SYSTEM_GUIDE.md` dokumentation (api_handlers.cpp) |
| FEAT-111 | Register Map: Sticky allokerings-reference | ✅ DONE | 🟡 HIGH | v7.8.4.2 | Register Allokering + Allokerings-tabel slået sammen i sticky header der følger med ved scroll i register-vinduer. Legende + tabel altid synlig som reference (web_dashboard.cpp) |
| BUG-300 | Dashboard kort-rækkefølge tabt ved reboot | ✅ FIXED | 🟡 HIGH | v7.8.4.2 | `GET /api/dashboard/layout` krævede auth (CHECK_AUTH) men restoreCardOrder() sendte ingen auth ved page load. FIX: Ændret til CHECK_API_ENABLED (ingen auth krævet for UI-præference) + robust tom-streng håndtering i JS (api_handlers.cpp, web_dashboard.cpp) |
| BUG-301 | Telnet RBAC: Mangler ROLE_CLI check ved login | ✅ FIXED | 🔴 CRITICAL | v7.8.4.4 | Bruger uden CLI-rolle kunne logge ind via Telnet. FIX: `rbac_has_role(uid, ROLE_CLI)` check efter autentificering, afviser med "User does not have CLI role" (telnet_server.cpp) |
| BUG-302 | Telnet RBAC: Mangler rbac_cli_allowed() ved kommando | ✅ FIXED | 🔴 CRITICAL | v7.8.4.4 | Read-only Telnet-bruger kunne køre alle kommandoer (set, reboot, etc.). FIX: `rbac_cli_allowed()` check før `cli_shell_execute_command()` — read-only brugere begrænset til show/help/ping/who (telnet_server.cpp) |
| BUG-303 | Web CLI: Mangler ROLE_CLI check | ✅ FIXED | 🟡 HIGH | v7.8.4.4 | `POST /api/cli` brugte kun `CHECK_AUTH_WRITE` — bruger med api+write men uden cli-rolle kunne køre CLI-kommandoer. FIX: Ændret til `CHECK_AUTH_ROLE(req, ROLE_CLI)` + separat write-privilege check (api_handlers.cpp) |
| BUG-304 | show config: Inkonsistent RBAC-visning på tværs af interfaces | ✅ FIXED | 🟠 MEDIUM | v7.8.4.4 | HTTP viste "RBAC active" men Telnet viste altid legacy credentials og SSE manglede auth-info helt. FIX: Ensrettet alle 3 sektioner i både table- og script-format med samme stil: `auth: RBAC (N users)` + `auth-mode: RBAC (multi-user)` (cli_show.cpp) |
| FEAT-112 | ST Logic Cold Restart (Reinit) | ✅ DONE | 🟡 HIGH | v7.8.4.5 | IEC 61131-3 cold restart: nulstiller program-variabler til kompilerede VAR-initialværdier, resetter stateful builtins (TON/TOF, R_TRIG/F_TRIG, CTU/CTD), FB-instanser og statistik. CLI: `set logic <id> reinit`, API: `POST /api/logic/{1-4}/reinit`, Web: orange "Reinit"-knap i ST Editor toolbar. Bruger eksisterende `var_initial[32]` array fra bytecoden. (st_logic_config.cpp, cli_commands_logic.cpp, cli_parser.cpp, api_handlers.cpp, web_editor.cpp) |
| FEAT-113 | Modbus baudrate 2400/4800 support | ✅ DONE | 🟠 MEDIUM | v7.8.4.5 | Tilføjet 2400 og 4800 baud til både master og slave baudrate-validering. Før: kun 9600-115200. Nu: 2400, 4800, 9600, 19200, 38400, 57600, 115200. t3.5 inter-frame delay beregnes korrekt for lave baudrater. (cli_commands_modbus_master.cpp, cli_commands_modbus_slave.cpp) |
| FEAT-114 | Modbus Master cache age human-readable | ✅ DONE | 🔵 LOW | v7.8.4.5 | Cache entry alder vises nu som `60ms`, `23.0s`, `1.5m` i stedet for rå millisekunder. CLI: Age-kolonne i `show modbus master`. Web dashboard: ny "Age" kolonne i cache-tabel via `age_ms` label i Prometheus-metrik. (cli_commands_modbus_master.cpp, api_handlers.cpp, web_dashboard.cpp) |
| BUG-305 | ARRAY base-variabel vises som `CH` i stedet for `CH[0]` | ✅ FIXED | 🟠 MEDIUM | v7.9.0 | FEAT-004 array base-symbol beholdt rå navn (`CH`) i bytecode var_names display. Monitor viste `CH` for element 0 men `CH[1]`..`CH[7]` for resten. FIX: Compiler omdøber base-symbol til `CH[0]` format i bytecode var_names copy-loop med `snprintf("%s[%d]", name, lower_bound)`. Lookup i symbol table bruger stadig `CH`. (st_compiler.cpp) |
| BUG-306 | ES32D26 Master mode: USB serial dør ved boot | ✅ FIXED | 🔴 CRITICAL | v7.9.1.0 | `modbus_master_init()` kaldte `modbus_master_reconfigure()` som overtog GPIO1/3 (USB serial) til RS485 **FØR** WiFi/Telnet var startet → ingen konsol tilgængelig. FIX: Udskudt UART-overtagelse til efter netværksservices er startet + 5 sek interaktiv boot-prompt (tryk MELLEMRUM for at beholde USB console). Ny `modbus_master_activate_uart()` funktion. (modbus_master.cpp, modbus_master.h, main.cpp) |
| BUG-307 | `esp_wifi_set_ps()` kaldt før WiFi-stak initialiseret | ✅ FIXED | 🟡 HIGH | v7.9.1.0 | `config_apply()` kører på linje 147 i main.cpp, men WiFi init er først linje 173. Kaldte `esp_wifi_set_ps()` på uinitialiseret WiFi-stak → potentiel crash/ESP_ERR_WIFI_NOT_INIT. FIX: Guard med `cfg->network.enabled` check + `esp_err_to_name()` fejlhåndtering, springer over hvis WiFi er disabled. (config_apply.cpp) |
| BUG-133 | Modbus Master: Kun slot 0 kører MB operationer | ✅ FIXED (v2) | 🔴 CRITICAL | v7.9.1.0 | `g_mb_request_count` blev nulstillet én gang per cycle (før for-loop), så slot 0 brugte alle requests. FIX v2: Reset per slot + `g_mb_cache_enabled = true` reset per slot. (st_logic_engine.cpp) |
| FEAT-115 | MB_CACHE(BOOL) builtin til per-program cache kontrol | ✅ DONE | 🟠 MEDIUM | v7.9.1.0 | ST builtin `MB_CACHE(FALSE/TRUE)` styrer om MB_READ/MB_WRITE bruger cache-dedup. Returnerer forrige tilstand. Reset til TRUE per slot. (st_builtin_modbus.cpp, st_builtins.cpp, st_compiler.cpp, st_builtins.h, st_builtin_modbus.h) |
| FEAT-116 | Modbus Master Cache TTL konfiguration | ✅ DONE | 🟠 MEDIUM | v7.9.1.0 | `set modbus-master cache-ttl <ms>` — 0=aldrig expire, >0=ms. Expired entries markeret [EXP] i show. cache_entry_expired() helper i alle 4 read builtins. NVS persistent. CLI + API + backup/restore. (cli_commands_modbus_master.cpp, cli_parser.cpp, api_handlers.cpp, types.h, config_load.cpp, modbus_master.cpp) |
| FEAT-117 | Cache hits/misses tracking | ✅ DONE | 🔵 LOW | v7.9.1.0 | `cache_hits` og `cache_misses` felter i `mb_async_state_t` blev aldrig inkrementeret. FIX: Tæller nu i `mb_cache_get_or_create()`. (mb_async.cpp) |
| BUG-308 | Web editor: syntax-highlight usynlig efter scroll | ✅ FIXED | 🟠 MEDIUM | v7.9.1.1 | `#highlight` div havde `height:100%` + `overflow:hidden`, så content udover container-højden blev klippet bort — transform `translate(0, -scrollTop)` kunne aldrig afsløre det. Tekst var kun synlig ved markering (via textarea). FIX: `min-height:100%` + `overflow:visible` (parent `.editor-container` klipper stadig). (web_editor.cpp) |
| BUG-309 | Modbus Master cache list: alle ops vises som FC3 | ✅ FIXED | 🟠 MEDIUM | v7.9.1.1 | Cache nøgles på read-FC (FC01-FC04); writes (FC05/FC06) merger ind i read-cache entry via `WRITE_HOLDING→READ_HOLDING`-mapping, så `key.req_type` altid viste FC3 for holding registers. FIX: Nyt `last_fc` felt på `mb_cache_entry_t` der opdateres med faktisk request-type ved hver completion. CLI `show modbus master` + web dashboard + Prometheus-metrik viser nu reel FC (FC01-FC06). Writes til aldrig-læste adresser opretter nu også cache-entry så de er synlige. (mb_async.h, mb_async.cpp, api_handlers.cpp, cli_commands_modbus_master.cpp) |
| BUG-310 | `set modbus-master cache-ttl` giver "unknown parameter" | ✅ FIXED | 🟠 MEDIUM | v7.9.1.1 | `normalize_alias()` manglede `cache-ttl` mapping, så user input forblev lowercase og matchede aldrig `strcmp(param, "CACHE-TTL")`. Parameteren var også udeladt fra help-teksten. FIX: Tilføjet alias `CACHE-TTL`/`CACHETTL`/`CACHE_TTL`/`TTL` i `normalize_alias()` + dokumenteret i `print_modbus_master_help()` og kort usage-liste. (cli_parser.cpp) |
| FEAT-118 | `show modbus-master` viser konfig-kommandoer | ✅ DONE | 🔵 LOW | v7.9.1.2 | Tilføjet "Configuration commands" sektion i bunden af `show modbus-master` output med komplet liste over alle `set modbus-master` parametre inkl. `cache-ttl`. Brugere kan nu se tilgængelige kommandoer direkte i status-output. (cli_commands_modbus_master.cpp) |
| FEAT-119 | Web dashboard: Cache TTL + [EXP] markør | ✅ DONE | 🟠 MEDIUM | v7.9.1.2 | Web dashboard Modbus Master-kort viser nu: (1) ny "Cache TTL" række (`0 (aldrig expire)` eller `N ms`), (2) `[EXP]` markør i status-kolonnen når `age_ms >= cache_ttl_ms` (expired entries vises i orange val-warn). Ny Prometheus-metric `modbus_master_cache_ttl_ms` eksponerer TTL-værdien. (api_handlers.cpp, web_dashboard.cpp) |
| FEAT-120 | ST Logic Multi-Register Modbus Master (FC03/FC16) med ARRAY | ✅ DONE | 🟡 HIGH | v7.9.2.0 | `regs := MB_READ_HOLDINGS(slave, addr, count)` læser op til 16 reg via FC03 direkte ind i ARRAY OF INT. `MB_WRITE_HOLDINGS(slave, addr, count) := regs` skriver fra ARRAY via FC16. Konsistent assignment-syntax med eksisterende single-register ops. Compiler resolver array-variabel til base_index ved compile-time, VM kopierer til/fra variable slots. Cache opdateres per-register. Protokol: `modbus_master_read_holdings()`+`write_holdings()`. (modbus_master.h/cpp, mb_async.h/cpp, st_builtin_modbus.h/cpp, st_builtins.h/cpp, st_compiler.cpp, st_vm.cpp, st_parser.cpp, st_types.h) |
| OPT-001 | ST Compiler heap optimering | ✅ DONE | 🟡 HIGH | v7.9.3.0 | AST node 156->84 bytes (46% reduktion): char[64]->char[32] i union + CASE branches heap-allokeret. var_names[32][32]->[32][16] (-2KB RAM). Dynamisk bytecode buffer: start 256, realloc i 256-blokke, max 2048 (var fast 1024=8KB). Function registry functions[64]->[32]. Bytecode persist format v3. Resultat: peak heap 39KB->22KB, largest_block 41KB->49KB. (st_types.h, st_parser.cpp, st_compiler.cpp, st_compiler.h, st_bytecode_persist.h/cpp, constants.h) |
| FEAT-127 | Realtime compiler ressource-info i web editor | ✅ DONE | 🟠 MEDIUM | v7.9.3.1 | `/api/logic` returnerer `resources{}` med heap_free, largest_block, min_free, pool_total/used/free, ast_node_size, max_ast_nodes. Web editor viser Heap-bar ved siden af Pool-bar med storste blok i KB + max AST nodes. Auto-poll 5s. Farvekoder: gron >35KB, orange 20-35KB, rod <20KB. Tooltip med detaljeret info. (api_handlers.cpp, web_editor.cpp) |
| OPT-002 | Modbus Master cache + request optimering | ✅ DONE | 🟡 HIGH | v7.9.3.2 | mb_async_request_t 41→10 bytes (ring-buffer pool for FC16 values). LRU eviction når cache fuld (ældste non-PENDING entry). Write dedup: skipper queue hvis cache allerede har samme værdi. Queue memory: 656→160 bytes (-75%). (mb_async.h/cpp) |
| OPT-003 | Modbus FC16 response timeout fix | ✅ DONE | 🔴 CRITICAL | v7.9.3.2 | FC16 (0x10) manglede i response parser switch — alle FC16 writes ventede fuld timeout (500ms) selvom 8-byte response var modtaget. Tilføjet `function_code == 0x10` case med `bytes_received >= 8` check. Dual-phase timeout: inter-character timeout beregnet fra baudrate (T3.5), fuld timeout kun for første byte. (modbus_master.cpp) |
| FEAT-128 | Intelligent per-slave adaptive backoff | ✅ DONE | 🟡 HIGH | v7.9.3.2 | Exponential backoff ved timeouts (50ms→2000ms cap), lineær decay ved success (-100ms). Op til 8 slaves tracked. Forhindrer bus-flooding mod offline slaves. Synlig i CLI `show modbus-master` + web dashboard + Prometheus metric `modbus_master_slave_backoff`. (mb_async.h/cpp, cli_commands_modbus_master.cpp, web_dashboard.cpp, api_handlers.cpp) |
| FEAT-129 | Modbus Master stats reset med tidspunkt | ✅ DONE | 🟠 MEDIUM | v7.9.3.2 | `POST /api/modbus/master/reset-stats` nulstiller alle master+cache statistik-tællere og adaptive backoff. Web dashboard viser "Statistik siden: Xt Ym siden" med `modbus_master_stats_age_ms` metric. Nulstil-knap i Modbus Master kort. (mb_async.h/cpp, types.h, api_handlers.cpp, web_dashboard.cpp, modbus_master.cpp) |
| FEAT-121 | ST Logic TIME datatype | ✅ DONE | 🟡 HIGH | v7.9.4.0 | IEC 61131-3 TIME datatype: `T#5s`, `T#100ms`, `T#1m30s`, `T#2h`. ST_TYPE_TIME enum, TIME keyword i VAR declarations, T# literals som native TIME type. 32-bit millisekunder via DWORD push. (st_types.h, st_lexer.cpp, st_parser.cpp, st_compiler.cpp, st_vm.cpp) |
| FEAT-122 | ST Logic TON timer (On-delay) | ✅ DONE | 🟡 HIGH | v7.9.4.0 | IEC 61131-3 TON function block med named-parameter syntax: `TON(IN := trigger, PT := T#5s, Q => output, ET => elapsed)`. Output bindings via `=>` operator og ny ST_OP_LOAD_FB_FIELD opcode. Backward-kompatibel med positionel syntax. (st_parser.cpp, st_compiler.cpp, st_vm.cpp) |
| FEAT-123 | ST Logic TOF timer (Off-delay) | ✅ DONE | 🟡 HIGH | v7.9.4.0 | IEC 61131-3 TOF function block med named-parameter syntax: `TOF(IN := trigger, PT := T#3s, Q => output, ET => elapsed)`. Deler parser/compiler infrastruktur med TON. |
| FEAT-124 | ST Logic CTU counter (Count Up) | ✅ DONE | 🟡 HIGH | v7.9.4.0 | IEC 61131-3 CTU function block med named-parameter syntax: `CTU(CU := pulse, RESET := rst, PV := 100, Q => done, CV => count)`. Edge-triggered, output bindings via LOAD_FB_FIELD. |
| FEAT-125 | ST Logic CTD counter (Count Down) | ✅ DONE | 🟡 HIGH | v7.9.4.0 | IEC 61131-3 CTD function block med named-parameter syntax: `CTD(CD := pulse, LOAD := ld, PV := 100, Q => done, CV => count)`. Edge-triggered, deler counter infrastruktur med CTU. |
| FEAT-126 | ST Logic CTUD counter (Up/Down) | ✅ DONE | 🟡 HIGH | v7.9.4.0 | IEC 61131-3 CTUD function block med named-parameter syntax: `CTUD(CU := up, CD := down, RESET := rst, LOAD := ld, PV := 100, QU => at_max, QD => at_zero, CV => count)`. 3 output bindings (QU, QD, CV). |
| BUG-311 | Function call statement ikke kompileret | ✅ FIXED | 🔴 CRITICAL | v7.9.4.1 | `ST_AST_FUNCTION_CALL` manglede i compiler statement-switch → `default: break` ignorerede hele FB-kaldet. TON/TOF/CTU etc. som statement (ikke assignment) genererede 0 instrukser. FIX: Tilføjet case med compile_expr + POP (st_compiler.cpp) |
| BUG-312 | TIME variabel vist som int16 i REST API | ✅ FIXED | 🟡 HIGH | v7.9.4.1 | `ST_TYPE_TIME` manglede i API handler value-switch → faldt til `default: val.int_val` (16-bit). T#60s=60000 vistes som -5536. FIX: Tilføjet TIME case med dint_val (api_handlers.cpp) |
| BUG-313 | DINT literal >32767 crasher ESP32 | ✅ FIXED | 🔴 CRITICAL | v7.9.4.1 | DINT brugte PUSH_INT (16-bit int_arg). Værdier >32767 overfyldte int16 og crashede VM'en. FIX: DINT literals bruger nu PUSH_DWORD (32-bit) ligesom TIME (st_compiler.cpp) |
| BUG-314 | Integer literal auto-promote til DINT | ✅ FIXED | 🟡 HIGH | v7.9.4.1 | Parser afviste alle integers >32767 med "INT overflow" fejl. DINT variabler kunne ikke initialiseres med store tal. FIX: Auto-promote literal til ST_TYPE_DINT når værdi > INT16_MAX eller < INT16_MIN (st_parser.cpp) |
| BUG-315 | Modbus Master parity/stop-bits ignoreret på ES32D26 | ✅ FIXED | 🔴 CRITICAL | v7.9.5.1 | `modbus_master_reconfigure()` kaldte `uart1_init(baudrate)` i ES32D26 (shared transceiver) grenen, men `uart1_init()` hardcoded SERIAL_8N1. Master parity/stop-bits config blev silent droppet → frames blev sendt med forkert framing, slaver kasserede dem. FIX: Ny `uart1_init_ex(baudrate, config)` + build korrekt UART config fra `g_modbus_master_config.parity`/`stop_bits` (uart_driver.cpp, uart_driver.h, modbus_master.cpp) |
| BUG-316 | Modbus Master post-TX DE-release for hurtig → sidste byte afskåret | ✅ FIXED | 🔴 CRITICAL | v7.9.5.1 | `modbus_master_send_request()` ventede kun 50µs efter `flush()` før DE blev sat LOW. Ved 9600 baud er 1 byte ~1040µs — hvis `HardwareSerial::flush()` ikke venter på shift-register empty (version-afhængig semantik i Arduino ESP32 core), blev sidste byte (typisk CRC hi) afskåret midt i transmissionen. Slave modtog korrupt frame, ignorerede den, ingen svar → timeout. Symptom: pulser synlige på oscilloskop men ingen slave svarede. FIX: Delay nu beregnet fra baudrate som 1 char-tid (11 bit) + 100µs margin — fx 1246µs @ 9600 baud (modbus_master.cpp) |
| BUG-323 | Login modal flash ved sideskift i web GUI | ✅ FIXED | 🟡 HIGH | v7.9.6.5 | Editor/CLI/System sider startede med login modal synlig (`class="show"`), så brugeren blev promptet for login ved hvert sideskift selvom `sessionStorage` havde gyldigt auth token. FIX: Modal starter nu skjult, vises kun hvis auth mangler eller validering fejler (web_editor.cpp, web_cli.cpp, web_system.cpp) |
| FEAT-130 | SSE-drevet dashboard + editor monitor | ✅ DONE | 🟡 HIGH | v7.9.6.0 | Web dashboard (/) og editor monitor (/editor) abonnerer nu på `/api/events?subscribe=all\|registers` via EventSource i stedet for konstant polling. Dashboard polling slowed 3s→5s ved SSE connect, editor monTimer 1500→2500ms + debounced refresh (500ms) ved register-events. SSE indicator (●) viser live-status, auto-reconnect efter 5s ved fejl. Register events opdaterer regHR/regmapHR/regmapIR/regCoils/regmapCoils/regmapDI grids instant. visibilitychange handler pauser polling når tab er hidden. (web_dashboard.cpp, web_editor.cpp) |
| FEAT-131 | Inline kompileringsfejl i web editor | ✅ DONE | 🟡 HIGH | v7.9.6.0 | Parser `compile_error` response med regex `/(?:at\s+line\|line)\s+(\d+)/i` for at udtrække linjenummer. Rød baggrund (`.line-nums .errln`) markerer fejl-linjen i line-numbers gutter, auto-scroll via `scrollToLine()` centrerer textarea på fejl-linjen. Ryddes ved slot-skift eller successful compile. (web_editor.cpp) |
| FEAT-132 | Upload .st-fil i web editor | ✅ DONE | 🟢 MEDIUM | v7.9.6.0 | Upload-knap + skjult `<input type=file accept=".st,.txt">` + FileReader text-read. 16KB max størrelse-check, indsætter indhold i textarea og markerer slot som modified. (web_editor.cpp) |
| FEAT-133 | Find/Replace i web editor (Ctrl+F/Ctrl+H) | ✅ DONE | 🟢 MEDIUM | v7.9.6.0 | Find-bar panel med find/replace inputs, count display, Next/Prev/Replace/ReplaceAll knapper. Global Ctrl+F / Ctrl+H keydown handler åbner panel og fokuserer inputs. Case-sensitive string search på textarea value. (web_editor.cpp) |
| FEAT-134 | Alarm historik filter | ✅ DONE | 🟢 MEDIUM | v7.9.6.0 | Client-side filter bar på alarm-card: severity (info/warn/error/critical), ack status (all/acked/unacked), search text (message contains), limit input, reset-knap. `fetchAlarms()` cacher rå liste, `renderAlarms()` applicerer filter ved hvert render + ved filter-change. (web_dashboard.cpp) |
| FEAT-135 | Modbus Master mb read/write mini-form i dashboard | ✅ DONE | 🟢 MEDIUM | v7.9.6.0 | Live widget i Master-card: dropdown (read hr/ir/coil/di, write hr/coil), slave/addr/val inputs, Udfør-knap. Poster til `/api/cli` med `mb <op> <slave> <addr> <val>` og viser response i `<pre>`. Ingen CLI-session nødvendig. (web_dashboard.cpp) |
| BUG-317 | Dashboard DIO toggle brugte forkerte JSON-felter | ✅ FIXED | 🟢 MEDIUM | v7.9.6.0 | `toggleDO(pin)` læste `d.level` og postede `{level: newVal}` — men `/api/gpio/{pin}` returnerer og forventer `{value: 0/1}`. Toggle var halvt implementeret (CSS hover hint fandtes, men kald failede silent). FIX: Brug `d.value` + `{value: newVal}` body + eksplicit error-alert ved HTTP fejl. (web_dashboard.cpp) |
| FEAT-136 | CLI `set rbac enable/disable` | ✅ DONE | 🟡 HIGH | v7.9.6.1 | RBAC-flaget kunne kun sættes via REST API (`/api/rbac`) eller migration — CLI manglede helt. Nu: `set rbac <enable\|disable>` + aliases (on/off/1/0/true/false). Viser current status når kaldt uden argument. Advarer hvis RBAC enables før der er oprettet brugere (lock-out risk). Fjernet `RBAC→USER` alias som shadow-mappede kommandoen til user-add. (cli_parser.cpp) |
| BUG-318 | SSE fejlede med 401 fra web monitor/editor når auth aktiv | ✅ FIXED | 🔴 CRITICAL | v7.9.6.2 | `EventSource` API tillader ikke custom headers, og browsere deler ikke Basic Auth credentials cross-port (dashboard på :80, SSE på :81 = forskellige origins). Resultat: alle SSE-forbindelser fra web UI fik 401, `show sse` viste 0 clients, dashboard og editor fallback'ede til ren polling. FIX: Query-string token auth — `api_handler_sse_status` udsteder et 5-min reusable token bundet til authenticated caller, dashboard+editor sender det som `?token=XXX` i EventSource URL. SSE accept-loop validerer token før fallback til Authorization header. Token-tabel med 8 slots, `portMUX_TYPE` lås, evict-oldest ved overflow. (sse_events.cpp, web_dashboard.cpp, web_editor.cpp) |
| BUG-319 | mb mini-form i dashboard brugte forkerte type-navne | ✅ FIXED | 🟡 HIGH | v7.9.6.2 | Dropdown brugte `hr`, `ir`, `di` som CLI aldrig har accepteret — CLI'en forventer `holding`, `input-reg`, `input` (og `coil`). Desuden sendte formen altid en value-arg, selv for read coil/input/input-reg som CLI afviser. Og `write coil` fik tal-input men CLI forventer `0/1/on/off`. FIX: Opdateret dropdown med korrekte CLI-navne + FC-numre som label, `updateMbForm()` disabler value-feltet hvor det ikke bruges, `doMbCmd()` bygger nu kommandoen præcist som CLI-hjælpen viser. (web_dashboard.cpp) |
| BUG-320 | SSE-events opdaterede kun register-grids, resten af dashboardet ventede på 5s polling | ✅ FIXED | 🟡 HIGH | v7.9.6.3 | `sseApplyCounterEvent`/`sseApplyTimerEvent` var no-ops (satte kun `sseLastEventMs`). DIO LEDs, counter-widgets, timer-widgets, master-stats og system-info ventede stadig på `fetchMetrics()` som kører hver 5 sekunder når SSE er aktiv. Resultat: bruger så ingen forskel i opdaterings-hastighed. FIX: Ny `sseKickMetrics()` debounced til max 1 fetch per 150ms — trigges ved counter/timer/coil/di events så hele UI reagerer inden for ~150ms efter aktivitet. Register-events opdaterer stadig grid-celler direkte uden round-trip. (web_dashboard.cpp) |
| BUG-321 | Editor monitor SSE brugte 500ms debounce + kun register-topic | ✅ FIXED | 🟡 HIGH | v7.9.6.3 | `editorSseConnect` abonnerede kun på `subscribe=registers` og debouncede refreshMonitor 500ms. Counter/timer ændringer fra ST-kode viste sig langsomt, og første event kom først efter 500ms delay. FIX: Ændret til `subscribe=all`, leading-edge dispatch (første event triggrer refreshMonitor med det samme hvis >100ms siden sidste kick), subsequent events coalesces til én refresh per 100ms vindue. Abonnerer nu på register, counter OG timer events. (web_editor.cpp) |
| FEAT-137 | Monitor trend-kurver forbedret med oscilloskop-gitter | ✅ DONE | 🟢 MEDIUM | v7.9.6.4 | Trend SVG 320×40 med oscilloskop-stil gitterlinjer (4 horisontale + 8 vertikale), mørk baggrund (#11111b). Min/max labels til venstre for kurve, tid til højre. Valgbar kurvefarve (blå/grøn/gul/rød/lilla/orange/cyan/hvid) på kontrollinjen. (web_editor.cpp) |
| FEAT-138 | Monitor hastigheds- og historik-kontrol | ✅ DONE | 🟢 MEDIUM | v7.9.6.4 | Opdateringshastighed dropdown (500ms–10s) styrer polling-interval korrekt. Historiklængde (30/60/120/240) trimmer eksisterende data-buffer. SSE events udløser ikke længere direkte refresh — kun polling-timeren styrer rate for jævn opdatering. Overlap-guard (monBusy) forhindrer stacked API-kald. (web_editor.cpp) |
| FEAT-139 | Debug-knapper omdøbt til klarere funktionsbeskrivelser | ✅ DONE | 🟢 MEDIUM | v7.9.6.4 | Pause→"Pause Execute", Step→"Single Step", Continue→"Single Cycle", Stop Debug→"Normal Execute". Tooltips tilføjet. Vertikal separator mellem knapper og statusbadge. PC-info integreret i badge-tekst ("Paused \| PC=5"). Kompakt tabel-padding for flere synlige variabler. (web_editor.cpp) |
| FEAT-140 | Modbus Master mini-form labels i dashboard | ✅ DONE | 🟢 MEDIUM | v7.9.6.5 | Tilføjet statiske labels (FC Type, Slave ID, Adresse, Antal/Værdi) over hvert input-felt i Read/Write sektionen. Label for felt 4 skifter dynamisk mellem "Antal" (read) og "Værdi" (write). Slave ID felt gjort bredere (55px, samme som Adresse/Værdi). (web_dashboard.cpp) |
| BUG-322 | Monitor opdatering uregelmæssig — SSE bypass'ede polling-interval | ✅ FIXED | 🟡 HIGH | v7.9.6.4 | SSE `kickMonitor()` kaldte `refreshMonitor()` direkte med kun 100ms throttle, uanset brugerens valgte opdateringshastighed (500ms–10s). Resulterede i uregelmæssige ryk i kurver og ignorering af hastighedsvalg. FIX: SSE events trigger ikke længere refresh direkte — kun `setInterval` polling-timeren styrer opdateringsraten. `monBusy` flag forhindrer overlappende API-kald. (web_editor.cpp) |
| FEAT-141 | Masonry dashboard layout med dynamisk kort-stacking | ✅ DONE | 🟢 MEDIUM | v7.9.6.6 | Dashboard grid brugte `grid-auto-rows: auto` → alle kort i samme række fik højden fra det største kort. Små kort (NTP, Counters) spildte plads ved siden af store kort (Modbus Master). FIX: Masonry layout med `grid-auto-rows: 8px` + JS `autoSizeCards()` der måler `scrollHeight` og sætter `grid-row: span N`. Bred/normal toggle (⇔ knap) per kort med `localStorage` persistence. Mobile fallback til enkelt-kolonne. Auto-resize ved metrics-fetch, alarm-render og window resize. Scroll-position bevares via `minHeight` freeze under resize. (web_dashboard.cpp) |
| FEAT-142 | Modbus Master mini-form bruger async kø + cache | ✅ DONE | 🟡 HIGH | v7.9.6.6 | Mini-form brugte `/api/cli` med synkron `mb read` → blokerede HTTP-tråd + UART-konflikt med async-task → hyppige timeouts. Nyt `/api/modbus/master/rw` endpoint der bruger `mb_cache_find()` for reads (returnerer cached værdi instant) og `mb_async_queue_write()` for writes. Polling (max 3s) for pending reads. Output er nu selekterbart (`user-select:text`). (api_handlers.cpp, web_dashboard.cpp) |
| FEAT-143 | Modbus Master priority queue + forbedret cache statistik | ✅ DONE | 🟡 HIGH | v7.9.7.1 | FreeRTOS FIFO queue erstattet med custom 3-level priority queue: Write (prio 0) > Read-fresh (prio 1) > Read-refresh (prio 2). Ved fuld kø droppes nyeste entry med laveste prioritet (eviction). Mutex + counting semaphore for thread-safety. Nye Prometheus metrics: queue_depth, queue_hwm, priority_drops, cache_hit_rate, cache_utilization. Dashboard Modbus Master kort udvidet med queue depth/HWM, priority drops, cache utilization. (mb_async.h, mb_async.cpp, api_handlers.cpp, web_dashboard.cpp, cli_commands_modbus_master.cpp) |
| FEAT-144 | Konfigurerbar cache/kø-størrelse via CLI + NVS | ✅ DONE | 🟢 MEDIUM | v7.9.7.2 | `set modbus-master cache-size <1-32>` og `queue-size <4-32>` med runtime limits (compile-time max som øvre grænse). Schema 18→19 migration. Alias-normalisering (cache-size/cachesize/cache_size). Vist i `show config`, `show running-config` og `mb stats`. (types.h, config_load.cpp, modbus_master.cpp, cli_parser.cpp, cli_commands_modbus_master.cpp, cli_show.cpp, mb_async.cpp) |
| FEAT-145 | ES32D26 opgraderet til ESP32-WROVER (4 MB PSRAM) | ✅ DONE | 🟡 HIGH | v7.9.7.3 | ES32D26 board hardware-revideret: WROOM-32 modul erstattet med pin-kompatibelt ESP32-WROVER med 4 MB SPI PSRAM onboard. `platformio.ini` env:es32d26: `board = esp-wrover-kit`, nye build-flags `-DBOARD_HAS_PSRAM` + `-mfix-esp32-psram-cache-issue`, `board_build.arduino.memory_type = qio_qspi`. Eksisterende `ESP.getPsramSize()`/`getFreePsram()` detection (BUG-250) aktiveres automatisk — dashboard PSRAM kort, `show version`, `show status` og `/api/metrics esp32_psram_{total,free}_bytes` rapporterer nu ~4 MB i stedet for 0. Ingen kode-ændringer — kun build config + docs. (platformio.ini, docs/ES32D26_BOARD_GUIDE.md, include/constants.h) |
| FEAT-146 | Flash-optimering: LTO + dead code elimination + repartition | ✅ DONE | 🔴 CRITICAL | v7.9.7.4 | Flash overskred OTA-partitionen efter WROVER-skift (101.3% = 1.726 MB / 1.625 MB). 3-trins fix: (1) Link Time Optimization aktiveret (`-flto` + `-ffunction-sections -fdata-sections -Wl,--gc-sections` + `build_unflags = -fno-lto`) → sparede 25 KB ved cross-TU inlining og dead-code stripping. (2) `CORE_DEBUG_LEVEL` sænket fra INFO (3) til WARNING (2) — Arduino/ESP-IDF INFO-log format strings fjernet, egen `debug_println()`-wrapper uændret. (3) OTA partitioner forstørret 1.625 MB → 1.812 MB ved at skrumpe SPIFFS 640 KB → 256 KB (SPIFFS bruges kun til max ~96 KB ST-bytecode/source, 256 KB er 2.6× margin). Resultat: 89.5% flash (1.701 MB / 1.900 MB) med 199 KB headroom. **⚠ FØRSTE FLASH KRÆVER `pio run -t erase && pio run -t upload`** — partition layout ændret. (platformio.ini, partitions.csv, include/constants.h) |
| FEAT-148 | ST program pool 8× forstørret via PSRAM (64 KB) | ✅ DONE | 🟡 HIGH | v7.9.7.6 | ST Logic source code pool flyttet fra statisk DRAM-array til dynamisk PSRAM-allokering (heap_caps_malloc med MALLOC_CAP_SPIRAM + fallback til regular heap). `ST_LOGIC_POOL_SIZE` conditional: 65536 bytes (64 KB) når `BOARD_HAS_PSRAM` defineret, ellers 8000 bytes. Resultat: (1) et enkelt ST program kan være op til 64 KB source code (fra 8 KB), (2) 8 KB DRAM frigivet — BSS-forbrug ned fra 81 KB til 73 KB. Alle pool-operationer (`st_logic_pool_allocate/free`, `st_logic_get_source_code`) har nu null-check på source_pool pointer. Web editor pool-bar viser automatisk ny størrelse via eksisterende `ST_LOGIC_POOL_SIZE` reference. (include/st_logic_config.h, src/st_logic_config.cpp) |
| FEAT-147 | Flash chip info synlig i CLI + /api/metrics | ✅ DONE | 🔵 LOW | v7.9.7.5 | `show version` viser nu Flash størrelse/frekvens/mode (fx `Flash: 4 MB @ 80 MHz (DIO)`) og `Target:` afspejler WROVER (`ESP32-WROVER (PSRAM)`) når `BOARD_HAS_PSRAM` er defineret, ellers WROOM-32. `show status` kortudvidet med `Flash: 4 MB @ 80 MHz`. `/api/metrics` eksponerer nu `esp32_flash_total_bytes` + `esp32_flash_speed_hz` gauges (bruges af Prometheus/dashboard). Bruger `ESP.getFlashChipSize()/Speed()/Mode()` fra Arduino-ESP32 API. (cli_show.cpp, api_handlers.cpp) |
| BUG-324 | MB_WRITE_HOLDINGS/MB_READ_HOLDINGS count ikke clampet for INT-type | ✅ FIXED | 🔴 CRITICAL | v7.9.7.7 | Se SECURITY_INDEX.md #4. Count-arg blev kun clampet til 1-16 for DINT/DWORD (og selv der ikke på negative værdier) — plain INT (default-typen i ST) passerede helt uklampet igennem, og gather/scatter-loopet kunne skrive op til 16 ekstra uint16_t (32 bytes) ud over det 16-element globale `g_mb_multi_reg_buf`. FIX: Begge bounds (1-16) håndhævet for alle 3 typer (st_vm.cpp:1273-1284) |
| BUG-325 | ST parser rekursionsdybde-guard dækkede ikke parenteser/statement-nesting | ✅ FIXED | 🔴 CRITICAL | v7.9.7.7 | Se SECURITY_INDEX.md #5. BUG-157's oprindelige fix tjekkede kun `recursion_depth` i `parser_parse_unary` (unær-operator-kæder) — parentes-udtryk (`parser_parse_primary`'s LPAREN-gren), CASE-branch bodies (`st_parser_parse_statements_for_case`) og nested IF/FOR/WHILE/REPEAT-bodies (`st_parser_parse_statements`) kaldte rekursivt uden nogen grænse. Dybt nestede parenteser/statements uploadet via API/CLI gav ubegrænset C-stack-vækst → stack overflow i main-loop-tasken. FIX: Samme `recursion_depth`-tæller nu håndhævet ved indgang til `parser_parse_expression()`, `st_parser_parse_statements()` og `st_parser_parse_statements_for_case()` (st_parser.cpp) |
| BUG-326 | LOAD() og CLI `load registers` stack-allokerede ~30KB PersistConfig | ✅ FIXED | 🔴 CRITICAL | v7.9.7.7 | Se SECURITY_INDEX.md #6. `PersistConfig temp_config;` som lokal stack-variabel i `st_builtin_persist_load()` samt i `cli_cmd_load_registers()` (begge grene) — samme struct som `config_save.cpp` bevidst heap-allokerer med kommentaren "too large for ESP32 stack". Ethvert ST-program med blot `LOAD(0);`, eller CLI-kommandoen `load registers all`/`load registers group <navn>`, overskred deterministisk main-loop-stakken (~8 KB) → crash/reboot. FIX: Heap-allokeret med malloc/free, matcher config_save.cpp's mønster (st_builtin_persist.cpp:104, cli_commands.cpp:1406+1437) |
| BUG-327 | Telnet brute-force lockout omgået ved disconnect/reconnect | ✅ FIXED | 🟡 HIGH | v7.9.7.7 | Se SECURITY_INDEX.md #8. `auth_attempts`/`auth_lockout_time` blev nulstillet ubetinget både når TCP-forbindelsen døde og ved ny forbindelse — en angriber kunne omgå den ellers korrekte 3-forsøgs/30-sek lockout ved blot at afbryde og genoprette forbindelsen efter 1-2 fejlede gæt. FIX: Lockout-state nulstilles ikke længere ved (re)connect — kun ved naturlig udløb (`telnet_handle_auth_input`) eller succesfuldt login (telnet_server.cpp) |
| BUG-328 | Backup-endpoint lækkede credentials til read-only brugere + auth deaktiveret som fabriksdefault | ✅ FIXED | 🔴 CRITICAL | v7.9.7.7 | Se SECURITY_INDEX.md #1 + #3. `GET /api/system/backup` brugte kun `CHECK_AUTH` (enhver gyldig login, inkl. read-only) og returnerede WiFi/telnet/HTTP/RBAC-passwords i klartekst. Samtidig var `http.auth_enabled = 0` fabriksdefault ("no auth by default (for ease of testing)") — et nulstillet device har fuldt åbent REST API inkl. dette endpoint. FIX: (1) Backup kræver nu skriverettighed (`CHECK_AUTH_WRITE`) (api_handlers.cpp:4058), (2) `auth_enabled` default ændret til 1 (network_config.cpp:54) |
| BUG-329 | ST Logic Modbus Master builtins ignorerer "Modbus Master enabled"-flag → phantom aktivitet i monitor | ✅ FIXED | 🟡 HIGH | v7.9.7.7 | `validate_slave_addr()` i st_builtin_modbus.cpp tjekkede kun om async-subsystemet var initialiseret (`pq_mutex != NULL`), ikke om `g_modbus_master_config.enabled` var sat. På ES32D26 (single transceiver) startes `mb_async_init()` ved boot baseret på UART-rolle (`mb_mode==MASTER`), uafhængigt af "modbus-master enabled"-toggle. Resultat: dashboard viste Master som deaktiveret, men ST-scripts med MB_*-kald blev stadig queued, behandlet af async-task'en (total_requests/total_errors talt op, cache markeret ERROR, inter-frame delay ventet) — kun selve UART-transmissionen blev til sidst blokeret nede i `modbus_master_send_request()`. FIX: `validate_slave_addr()` tjekker nu også `g_modbus_master_config.enabled` og afviser requests med det samme, før de queues (st_builtin_modbus.cpp:46-58) |
| BUG-330 | OTA-upload afviser gyldig firmware med "Fil for stor (max 1.625MB)" | ✅ FIXED | 🔴 CRITICAL | v7.9.7.8 | FEAT-146 (v7.9.7.3) forstørrede OTA-partitionen fra 1.625 MB til 1.8125 MB (0x1A0000→0x1D0000 i partitions.csv), men `OTA_MAX_FIRMWARE_SIZE` i constants.h + de to duplikerede client-side JS size-checks (web_system.cpp, web_ota.cpp) blev aldrig opdateret — blokerede alle firmwares mellem 1.625MB og 1.8125MB, inkl. den aktuelle build (1.63MB). FIX: Alle 4 forekomster (constants.h:495, ota_handler.cpp:109, web_system.cpp:568, web_ota.cpp:163) opdateret til 0x1D0000/1.8125MB |
| BUG-334 | ST Logic kører videre men når aldrig bussen — RS485-aktivering afbrudt af bus-trafik ved boot | ✅ FIXED | 🔴 CRITICAL | v7.9.8.5 | **Rodårsag til "ST program eksekverer men variabler fryser efter dagevis".** På ES32D26 deler USB-konsollen og RS485-transceiveren GPIO1/3. Ved boot i MASTER-mode gav firmwaren et 5-sekunders vindue hvor ét enkelt mellemrum (0x20) på konsollen afbrød RS485-aktiveringen og satte `g_modbus_master_config.enabled = false` — **kun i RAM**, mens den gemte config fortsat sagde `enabled on`. Men alt hvad andre enheder sender på bussen i det vindue læses som konsol-input, og en `0x20` optræder let i et Modbus-telegram (slave-ID 32, adresse- eller data-byte). Efter en spontan genstart (watchdog/brownout) kunne bus-trafik derfor slå masteren fra for hele sessionen. ST-programmet kørte videre — eksekveringstælleren steg, nul fejl — men hvert `MB_*`-kald blev blokeret af `validate_slave_addr()` (BUG-329), så tilstandsmaskinen frøs i sin vente-tilstand med alle værdier på 0. `stop`/`start`/`reinit` rørte ikke RAM-flaget; kun en ny boot hjalp. FIX: (1) RX-bufferen tømmes før nedtællingen så trafik fra før boot ikke tæller, (2) afbrydelse kræver nu **3 mellemrum i træk** og enhver anden byte nulstiller tælleren, så binær bus-trafik ikke kan udløse den, (3) boot-beskeden oplyser nu at `set modbus-master enabled on` genaktiverer uden reboot, (4) nyt RAM-flag `g_modbus_master_boot_aborted` gør tilstanden synlig i `show modbus-master` i stedet for et gådefuldt DISABLED (main.cpp, modbus_master.cpp/.h, cli_commands_modbus_master.cpp) |
| BUG-333 | Priority-eviction efterlader cache-entry i PENDING for evigt → adressen dør indtil reboot | ✅ FIXED | 🔴 CRITICAL | v7.9.8.5 | `mb_async_queue_read()` markerer cache-entryen `MB_CACHE_PENDING` inden requesten køes. Når køen er fuld overskriver `mb_pq_insert()` offeret på plads (`pq_buf[victim] = *req`) og tæller `priority_drops++` — men **ingen rullede offerets cache-entry tilbage**. ST-læsningerne genindkøer kun når `status != MB_CACHE_PENDING`, og med `cache-ttl 0` ("never expire") er `expired` permanent falsk, så adressen blev aldrig forespurgt igen og `g_mb_success` forblev false — indtil reboot. Ét enkelt eviction-ramt multi-register-kald kunne låse op til 16 adresser. FIX: (1) offerets PENDING-mærke ryddes nu når det smides ud af køen, (2) nyt `pending_since_ms` pr. entry + periodisk sweeper i baggrundstasken der efter 5× timeout (min. 3s) frigør entries der hænger i PENDING — uafhængigt af `cache-ttl`, så enhver fremtidig vej til en tabt request også selvheler. Tælleren `stale_pending_recovered` viser hvor ofte det sker (mb_async.h/.cpp, st_builtin_modbus.cpp) |
| FEAT-152 | Fastlåst (sticky) header i scrollbare log-tabeller | ✅ DONE | 🔵 LOW | v7.9.8.4 | Header-rækken i Modbus Aktivitetslog og Alarm Historik scrollede ud af billedet når man rullede i loggen, så kolonnerne ikke længere kunne aflæses. FIX: `position:sticky;top:0` på `th` i `#mbActivityBody`/`#alarmBody` med opak card-baggrund (#181825) + `box-shadow:inset 0 -1px 0` i stedet for `border-bottom` (borderen ville ellers scrolle væk pga. `border-collapse:collapse`) (web_dashboard.cpp) |
| FEAT-150 | `show telnet` og `show modbus` CLI-genveje | ✅ DONE | 🔵 LOW | v7.9.8.3 | Kun `show modbus-slave`/`show modbus-master` og `show config telnet`/`show config modbus` fandtes — ingen bare `show telnet`/`show modbus` top-niveau genvej (virker naturligvis også via `sh`/`s` alias). FIX: To nye dispatch-grene i SHOW-kommandoen der delegerer til den allerede eksisterende `cli_cmd_show_config("TELNET"/"MODBUS")` sektionsfilter (samme output som `show config telnet`/`show config modbus`, "modbus" viser slave+master samlet) (cli_parser.cpp) |
| BUG-336 | `mb scan` (og enhver ubesvaret Modbus-forespørgsel) blokerer resten af systemet — dashboard/HTTP hænger under scan | ✅ FIXED (v2: taskYIELD utilstraekkelig, se nedenfor) | 🔴 CRITICAL | v7.9.8.7 | `modbus_master_send_request()`s modtage-løkke er et **fuldstændig unyielding busy-loop** (kun `millis()`+`available()`-polling, intet `delay()`/OS-yield) mens den venter på FØRSTE byte af et svar — op til `timeout_ms` (default 1000ms) pr. ubesvaret slave. For et enkelt kald er det et kort, næppe mærkbart hak; for `mb scan` gentages det uafbrudt for hver tom adresse i rækken, og monopoliserer sin core så længe scanningen kører — observeret som at webdashboard/HTTP går i knæ under scanning (samme rodårsag som BUG-335, opdaget da brugeren rapporterede GUI-hæng under scan). FIX: `taskYIELD()` indsat specifikt i "venter stadig på byte #1"-grenen — bevidst **ikke** i inter-character-timing-grenen (`interchar_ms`, et par ms, gælder kun når en frame allerede er i gang, `bytes_received>0`), så Modbus RTU's timingkritiske frame-modtagelse er fuldstændig upåvirket (modbus_master.cpp) |
| BUG-337 | `mb scan` og ST Logic's async Modbus-trafik kan kollidere paa den fysiske RS485-bus — falske "fundne" slaver | ✅ FIXED | 🔴 CRITICAL | v7.9.8.9 | **Bekraeftet af bruger paa reelt anlaeg med kun 2 fysiske Modbus-noder:** en fuld `mb scan 1 247` "fandt" 75 slaver, med kun to gentagne vaerdier (228 og 32768) i et regelmaessigt moenster hele vejen igennem — signaturen paa at scanningen opsnappede svar tilhoerende ST Logic's SAMTIDIGE, uafhaengige Modbus-trafik og fejlagtigt tilskrev dem den adresse den lige spurgte om (CRC16 er kun 16-bit, saa et fejlplaceret men i sig selv gyldigt svar bestod tjekket). Rodaarsag: CLI's `mb read`/`mb write`/`mb scan` kalder `modbus_master_send_request()` direkte og synkront fra loopTask (Core 1), mens den asynkrone Modbus Master-task (ST Logic/dashboard-trafik) kalder PRAECIS SAMME funktion fra sin egen task paa Core 0 — helt uden indbyrdes koordinering. To transaktioner kunne saette DE/RE hoejt SAMTIDIGT (elektrisk kollision paa bussen) og laese hinandens bytes ind i egen buffer. FIX: ny mutex (`g_modbus_uart_mutex`) om hele UART-transaktionen i `modbus_master_send_request()`, taget via en RAII-guard (`ModbusUartLock`, samme moenster som `MbTempBaud`) der garanterer frigivelse paa ALLE returnerings-veje — enhver kalder (scan, CLI read/write, async-task) venter nu blot sin tur (op til 2s begraenset ventetid, herefter MB_TIMEOUT) i stedet for at kollidere paa bussen. Overvejede at "pause ST Logic under scan" (brugerens foerste forslag) i stedet, men forkastede det: en laas om selve UART-transaktionen loeser problemet komplet og for alle nuvaerende/fremtidige kaldere, mens pause af ST Logic stadig ville efterlade allerede-koeede async-requests i race og unoedigt stoppe ST'ens ikke-Modbus-relaterede arbejde (modbus_master.cpp) |
| BUG-336c | v7.9.8.7's `vTaskDelay(1)`-fix *også* utilstraekkeligt — dashboard forblev totalt ukontaktbart under `mb scan` | ✅ FIXED | 🔴 CRITICAL | v7.9.8.8 | Brugeren bekraeftede empirisk at heller ikke v2-fixet (vTaskDelay) laante dashboardet fra at gaa fuldstaendigt i knaeb. To forsoeg paa at yield'e sig ud af problemet inde i `modbus_master_send_request()` var altsaa ikke den rigtige loesningsstrategi — problemet er strukturelt: HTTP(S)-serveren, SSE-tasken og `loopTask` (CLI, `mb scan`) kunne alle dele **samme fysiske CPU-core**. Plain-HTTP's `HTTPD_DEFAULT_CONFIG()` lod `core_id` staa paa `tskNO_AFFINITY` (scheduleren fri til at placere den paa Core 1 — samme core som loopTask), og HTTPS-wrapperen var eksplicit sat til Core 1 (formentlig en vilkaarlig default, ingen dokumenteret grund til praecis dét valg). SSE's to tasks (`sse_client_task`, `sse_accept_task`) brugte `xTaskCreate()` uden affinitet af samme grund. FIX: alle fire — plain HTTP, HTTPS, SSE accept-task, SSE client-task — pinnet eksplicit til **Core 0**, fysisk adskilt fra `loopTask` paa Core 1, saa de ikke laengere KAN dele core med CLI/scan uanset praecis hvilken schedulings-mekanisme der var den egentlige aarsag. Samtidig rettet HTTPS' `max_uri_handlers` fra 64 til 96 (matcher de faktiske 92 registrerede handlers — var en kendt, adskilt fejl fundet under HTTPS/SSH-undersoegelsen, se plans/FEAT-151) (http_server.cpp, sse_events.cpp) |
| BUG-336b | v7.9.8.6's `taskYIELD()`-fix var utilstraekkeligt — dashboard forblev ukontaktbart under `mb scan` | ✅ FIXED | 🔴 CRITICAL | v7.9.8.7 | Brugeren bekraeftede empirisk at dashboardet stadig var ukontaktbart under scan efter v7.9.8.6. `taskYIELD()` reschedulerer kun HVIS en opgave med samme-eller-hoejere prioritet allerede staar klar i praecis det oejeblik — httpd's default `task_priority` (tskIDLE_PRIORITY+5) er ganske vist hoejere end `loopTask`s (1), saa simpel prioritets-baseret sult var ikke hele forklaringen; noget andet paa Core 1 (WiFi/lwIP-timing, flash/cache-arbitrering e.l.) blev stadig forsinket naar intet konkret var "ready" til at overtage praecis i yield-oejeblikket. FIX: `taskYIELD()` → `vTaskDelay(1)` i samme gren (kun foer foerste byte af et svar er modtaget, samme begrundelse som foer for hvorfor Modbus RTU-timing er upaavirket) — `vTaskDelay` tager tasken fuldstaendigt UD af ready-koeen for et helt OS-tick (~1ms), hvilket tvinger et reelt context-skift uanset prioritet eller hvad der ellers er klar. Aendrer ikke scanningens samlede varighed (stadig bundet af `timeout_ms`, blot samplet hvert ~1ms i stedet for kontinuerligt) (modbus_master.cpp) |
| BUG-335 | `mb scan` kan trigge 30-sek. watchdog og genstarte enheden midt i scan | ✅ FIXED | 🔴 CRITICAL | v7.9.8.6 | `cli_cmd_mb_scan()` var en fuldt synkron løkke der blokerer main loop() — `watchdog_feed()` kaldes normalt kun 1×/loop()-iteration, så en scan der ikke returnerer inden 30 sek. sulter watchdoggen. Ved default timeout (1000ms) + 10ms pause udløste blot **~28 tomme adresser** (28×1010ms>30000ms) allerede genstart — en fuld `mb scan` (1-247) ville altid ramme det, uden fejlbesked, midt i scanningen. Fandt via brugerønske om fremdriftsvisning + afbrydelsesmulighed, som afslørede at kommandoen reelt kunne blokere i op til 4+ minutter uden nogen feedback. FIX: (1) `watchdog_feed()` kaldes nu hver iteration, (2) fremdrift vises hvert 10. forsøg, (3) et tastetryk (seriel/telnet, via eksisterende `Console`-abstraktion og `cli_shell_get_debug_console()`) afbryder scanningen med det samme — web-CLI kan pr. design (enkelt request/response) ikke afbrydes midt i, men rammer heller ikke watchdog-bugget længere (cli_commands_modbus_master.cpp) |
| BUG-332 | Modbus Aktivitetslog stopper med at opdatere når loggen bliver fuld | ✅ FIXED | 🔴 CRITICAL | v7.9.8.2 | `api_handler_modbus_activity_get()` estimerede JSON-output til 128 bytes/entry, men en reel entry serialiserer til ~145+ bytes. Ved fuld log (40 entries) blev `serializeJson()`'s output-buffer for lille → JSON afkortet midt i et objekt → ugyldig JSON → browserens `r.json()` kastede en fejl → `fetchMbActivity()`'s `catch(e){}` fejede den under gulvtæppet uden at opdatere `_mbActivityLog` → dashboardet fremstod frosset ("stopper når fuld", bekræftet af bruger som data-overrun). FIX: output-bufferen allokeres nu eksakt via `measureJson(doc)+1` i stedet for et gættet per-entry-budget, og dokument-kapaciteten hævet til fast 10240 bytes (api_handlers.cpp:6368,6395-6399) |
| BUG-331 | Modbus Aktivitetslog (FEAT-149) viste aldrig noget — wildcard-route shadowede endpoint | ✅ FIXED | 🔴 CRITICAL | v7.9.8.1 | `/api/modbus/activity` og `/api/modbus/activity/clear` blev registreret som separate `httpd_uri_t`, men wildcard-routen `/api/modbus/*` (uri_modbus_get/uri_modbus_post) var registreret FØR dem — ESP-IDF's httpd matcher ruter til den først-registrerede matchende URI, så `api_handler_modbus_get()` fangede requesten, matchede hverken "/slave" eller "/master" i URI'en, og returnerede 400 FØR den dedikerede activity-handler nogensinde blev kaldt. Samme fejlklasse som BUG-201. Backend-loggingen (mb_activity_log.cpp + hooks i modbus_master.cpp/modbus_server.cpp) var korrekt hele tiden — kun API-laget var uopnåeligt. FIX: fjernet de separate route-registreringer, `api_handler_modbus_get()`/`_post()` delegerer nu til `api_handler_modbus_activity_get()`/`_clear()` via URI-suffix-tjek FØR egen stat/auth-håndtering (samme mønster som eksisterende `/master/rw`-routing) (api_handlers.cpp, http_server.cpp) |
| FEAT-149 | Modbus Aktivitetslog — wire-level Master+Slave sniffer, RAM-only, med source-attribution | ✅ DONE | 🟡 HIGH | v7.9.8.0 | Nyt modul `mb_activity_log.h/.cpp`: fælles ringbuffer (40 entries, RAM-only) for BÅDE Modbus Master (afsendt/modtaget til andre enheder) og Modbus Slave (modtaget fra eksterne mastere) trafik. Hooket helt tæt på UART: Master-siden logger inde i `modbus_master_send_request()` (den ENESTE funktion al Master-trafik funnel'er igennem — fanger både async-kø fra ST Logic/dashboard OG direkte synkrone CLI-kald), Slave-siden logger i `modbus_server_loop()`'s PROCESS-state lige efter `modbus_dispatch_function_code()`. Hver entry har et `source`-felt (ST_LOGIC/CLI/DASHBOARD/EXTERNAL) så en wire-transaktion kan spores tilbage til den high-level kommando der udløste den — snapshottet via `g_mb_activity_next_source`→`mb_async_request_t.source`→`g_mb_activity_current_source` for at overleve den asynkrone kø (source ville ellers være forældet når request'et rent faktisk sendes). Nyt API: `GET /api/modbus/activity`, `POST /api/modbus/activity/clear`. Nyt dashboard-kort "Modbus Aktivitetslog" (card-wide, modbus-tab) med badge, rolle/kilde-filter og tabel. Cross-core thread-safe via egen spinlock (Master-task kører Core 0, Slave-loop kører Core 1). (mb_activity_log.h/.cpp, modbus_master.cpp, modbus_server.cpp, st_builtin_modbus.cpp, mb_async.h/.cpp, cli_commands_modbus_master.cpp, api_handlers.cpp, http_server.cpp, web_dashboard.cpp) |
| BUG-338 | BUG-337's mutex loeste kollisionen, men `mb scan` finder nu 0 slaver paa en bus med 2 kendte enheder | ✅ FIXED | 🔴 CRITICAL | v7.9.9.0 | **Bekraeftet af bruger paa samme reelle anlaeg som BUG-337:** efter mutex-fixet (v7.9.8.9) fandt en fuld `mb scan 1 247` 0 slaver — vaerre end de 75 falske positiver foer, for nu blev end ikke de 2 rigtige enheder fundet. Rodaarsag: BUG-337's `g_modbus_uart_mutex` fjernede kollisionen korrekt, men skabte samtidig ny **ressource-sult**: den asynkrone Modbus Master-task (ST Logic/dashboard-trafik, Core 0) genindtager mutex'en langt hyppigere end scanningens 247 enkeltstaaende forsoeg (loopTask, Core 1) statistisk naar at vinde imod — hvert scan-forsoeg tabte konsekvent laase-koebet inden for det 2-sekunders begraensede ventevindue og returnerede `MB_TIMEOUT` (nu `MB_BUS_BUSY`) **uden nogensinde at have sendt en eneste byte paa bussen**, identisk med en tom adresse. BUG-337-notatet overvejede og forkastede dengang "pause ST Logic under scan" til fordel for laasen alene — det viste sig utilstraekkeligt: en korrekt, ikke-korrumperende laas forhindrer kollision, men garanterer intet om FAIRNESS naar den ene side er langt mere aktiv end den anden. FIX: (1) ny fejlkode `MB_BUS_BUSY` (adskilt fra reelt `MB_TIMEOUT`) saa laase-svigt kan skelnes fra et reelt ubesvaret opkald, med egen tæller `bus_busy_errors` (vist i `show modbus-master`, `/api/modbus/master`, Prometheus-eksport), (2) ny **kooperativ** pause af den asynkrone koe (`mb_async_pause()`/`mb_async_unpause()`) — til forskel fra den allerede eksisterende, men farlige `mb_async_suspend()` (raa `vTaskSuspend`, kunne ramme tasken MIDT i en transaktion og laase mutex'en permanent), tjekkes pause-flaget kun oeverst i den asynkrone tasks loop, aldrig midt i en transaktion, saa den garanteret aldrig fanges mens den holder UART-mutex'en. `mb scan`, `mb read` og `mb write` pauser nu koeen (RAII-guard `MbAsyncPauseGuard`, samme moenster som `MbTempBaud`) for deres varighed — ST Logic's eksekvering fortsaetter uaendret, kun dens Modbus-forespoergsler venter i koeen til pausen ophaeves (types.h, mb_async.h/.cpp, modbus_master.cpp, cli_commands_modbus_master.cpp, api_handlers.cpp) |
| BUG-339 | v7.9.9.0 (BUG-338-fix) nulstillede HELE configen — inkl. WiFi — til fabriksdefault ved naeste boot | ✅ FIXED | 🔴 CRITICAL | v7.9.9.1 | **Selvforskyldt: bruger mistede WiFi-forbindelsen umiddelbart efter at have flashet v7.9.9.0.** Rodaarsag: BUG-338-fixet tilfoejede et nyt felt (`bus_busy_errors`) direkte til `modbus_master_config_t` — men denne struct er indlejret i `PersistConfig` (types.h:530), som gemmes RAAT til NVS som én blob med et `schema_version`-felt + CRC16-tjek (config_load.cpp). Et nyt felt AENDRER `sizeof(PersistConfig)` uden en tilsvarende schema-migration (skulle vaere fulgt moensteret fra de 12 eksisterende migrationsblokke, `CONFIG_SCHEMA_VERSION` blev IKKE bumpet). Ved naeste boot laeser `nvs_get_blob()` den gamle (mindre) gemte blob ind i den nu stoerre struct — `schema_version` matcher stadig (ingen migration triggres), saa CRC beregnes over en delvist ulaest/forskudt struct og matcher aldrig den gemte CRC → `config_load_from_nvs()` afviser den som korrupt og kalder `config_init_defaults()`, som nulstiller ALT inkl. WiFi SSID/password (network_config_init_defaults()) til fabriksvaerdier. Ingen data var fysisk overskrevet i NVS (main.cpp gemmer ikke automatisk efter et fejlet load) — kun IN-RAM defaults blev brugt for den boot-session, saa en ren fix uden datatab var mulig. FIX: `bus_busy_errors` flyttet HELT UD af `modbus_master_config_t` til en selvstaendig runtime-only global `g_modbus_bus_busy_errors` (modbus_master.h/.cpp, samme moenster som allerede-eksisterende `g_modbus_master_boot_aborted`) — `PersistConfig`s layout er dermed byte-for-byte identisk med foer BUG-338, saa brugerens oprindelige NVS-blob (WiFi m.m.) laeses korrekt igen ved naeste boot, forudsat intet er blevet gemt/aendret imens v7.9.9.0 kørte. **Laering for fremtidige runtime-taellere/flag: ALDRIG tilfoej et felt til en struct der er indlejret i `PersistConfig` uden enten (a) at holde det uden for enhver persisteret struct, eller (b) bumpe `CONFIG_SCHEMA_VERSION` og tilfoeje en migrationsblok efter det etablerede moenster i config_load.cpp** (types.h, modbus_master.h/.cpp, mb_async.cpp, cli_commands_modbus_master.cpp, api_handlers.cpp) |

| BUG-340 | `set modbus-master enabled on` gjorde IKKE Master reelt funktionsdygtig hvis boardet bootede i SLAVE/OFF-mode — mutex NULL + async-task staar for evigt STOPPED | ✅ FIXED | 🔴 CRITICAL | v7.9.9.2 | **Fandt under gendannelse efter BUG-339:** brugeren fulgte den anbefalede rute (`set modbus mode master` → `set modbus-master enabled on` → `save` → `reboot`), men `show modbus-master` viste stadig `Task: STOPPED` og `mb scan` gav `BUS BUSY` paa samtlige 247 adresser — og `show modbus` bekraeftede at `Mode: MASTER` rent faktisk VAR korrekt persisteret. Rodaarsag: `modbus_master_set_enabled(true)` (kaldt af `set modbus-master enabled on`) saetter kun `g_modbus_master_config.enabled=true` + rekonfigurerer UART'en — den antager at `modbus_master_init()` (opretter `g_modbus_uart_mutex`) og `mb_async_init()` (starter baggrundstasken) allerede er koert, hvilket kun sker ved et boot der reelt starter i MASTER-mode (main.cpp). Enhver vej hvor boardet bootede i SLAVE/OFF (fabriksdefault efter BUG-339, ELLER et boot-tids RS485-vindue afbrudt af stoej paa konsollen, BUG-334) og brugeren derefter manuelt genaktiverer via CLI UDEN et efterfoelgende reelt boot i MASTER-mode, efterlod systemet i en "halv" tilstand: `enabled=true` (bestod `mb scan`s indledende tjek), men mutex'en NULL (`ModbusUartLock` fejler ALTID => `MB_BUS_BUSY` paa selv en fysisk tom bus, uden nogensinde at sende en byte) og async-tasken aldrig startet (ST Logic's `MB_*`-koeer akkumulerer men behandles aldrig — `Task: STOPPED`, alle async-taellere paa 0 uanset hvor mange ST-kald der er lavet). FIX: `modbus_master_set_enabled(true)` opretter nu selv mutex'en (`if (!g_modbus_uart_mutex) ...`) og starter async-tasken (`if (!mb_async_get_state()->task_handle) mb_async_init();`) hvis de mangler — begge idempotente tjek, rammer aldrig en allerede koerende opsaetning, saa funktionen er sikker at kalde uanset hvordan systemet naaede den tilstand den er i (modbus_master.cpp) |

| BUG-341 | Dashboard/GUI forblev ukontaktbart under `mb scan` — 4. forsoeg, nu isoleret til ren CPU-monopolisering (ikke laase/kontention) | ✅ FIXED | 🔴 CRITICAL | v7.9.9.3 | **Bekraeftet af bruger EFTER BUG-338/340: async-tasken er nu fuldstaendig paused under en scan (bevisligt ingen mutex-kontention tilbage), men GUI frøs stadig.** Det isolerer endegyldigt aarsagen vaek fra laase/scheduling-prioritet (BUG-336/336b/336c's teorier) til ren **CPU-tids-monopolisering via for hyppig polling**: en ikke-svarende slave sidder i modtage-loopet i op til `timeout_ms` (500-1000ms), og vaagner hvert eneste OS-tick (~1ms, BUG-336b's `vTaskDelay(1)`) for at tjekke `uart1_available()` foer den straks sover igen. Hvert opvaagn koster et reelt context-switch, og de resulterende ~1ms-vinduer er for korte og for hyppige til at httpd/WiFi/lwIP kan naa reel, brugbar fremdrift paa en request (TCP-handshake, header-parsing, svar-afsendelse) selv naar de teknisk set er "fri" til at koere imellem dem — et scheduling-thrash-moenster, ikke sult. FIX: pollér langt sjaeldnere i stedet for at "nedee" smartere — Modbus RTU har intet sub-millisekund-krav til at opdage FOERSTE byte af et svar (rigtige slaver' turnaround er typisk enkelt-cifrede til lave to-cifrede ms, og UART-hardwarens FIFO holder enhver byte der ankommer uanset hvor prompte softwaren tjekker for den), saa intet gaar tabt ved sjaeldnere tjek. `vTaskDelay(1)` -> `vTaskDelay(pdMS_TO_TICKS(10))` skaerer antallet af opvaagninger (og dermed context-switches) ned med ~10x, hvilket giver httpd/lwIP vinduer en stoerrelsesorden laengere imellem denne tasks korte afbrydelser. Fortsat kun i "intet svar modtaget endnu"-grenen — inter-character-timing (`interchar_ms`, bytes_received>0-stien) er fuldstaendig uroert, saa RTU-frame-timing midt i et svar er upaavirket (modbus_master.cpp) |

| BUG-342 | RODAARSAG til "dashboard/GUI dødt under mb scan" fundet: `loop()` staar HELT stille i hele scan-varigheden -> Wi-Fi-genopkobling koerer aldrig | ✅ FIXED | 🔴 CRITICAL | v7.9.9.4 | **Brugeren efterspurgte en reel dybdeanalyse efter FIRE forgaeves fix-forsoeg (BUG-336/336b/336c/341) alle rettet mod CPU-scheduling — ingen af dem havde nogen effekt, hvilket i sig selv beviste at scheduling aldrig var aarsagen.** Faktisk rodaarsag, fundet ved at spore hele kaldekaeden: `cli_cmd_mb_scan()` koeres SYNKRONT fra `loop()` (main.cpp) via `cli_shell_loop()` -> `cli_parser_execute()` — saa i HELE scanningens varighed (op til flere minutter for et fuldt 1-247-scan) koerer `loop()` reelt slet ikke, uanset hvor mange `vTaskDelay()`'er der ligger inde i selve scan-funktionen. `wifi_driver.cpp`s event-drevne `wifi_event_handler()` (koerer paa ESP-IDF's egen event-task, uafhaengig af `loop()`) saetter KUN `wifi_state.state=DISCONNECTED` ved et Wi-Fi-drop — selve genopkoblingskaldet (`esp_wifi_connect()`) sker udelukkende inde i `wifi_driver_loop()`, som UDELUKKENDE kaldes fra `network_manager_loop()` i `loop()`. Med `loop()` blokeret kan ETHVERT Wi-Fi-drop under scanningen (helt normalt paa et rigtigt netvaerk over flere minutter — én tabt beacon er nok) derfor ALDRIG genoprettes foer scanningen er faerdig: enheden forsvinder simpelthen fra nettet i mellemtiden, hvilket rammer dashboard, web-CLI og SSE SAMTIDIGT (alle over IP) — fuldstaendig uafhaengigt af CPU-core eller schedulings-granularitet, hvilket forklarer hvorfor alle fire tidligere forsoeg var virkningsloese. FIX: `cli_cmd_mb_scan()` kalder nu selv `network_manager_loop()` (Wi-Fi-genopkobling + Ethernet + Telnet-server events) og `cli_remote_loop()` (Telnet-kanal-state) i hver iteration af scan-loopet — samme kadence som den allerede eksisterende `watchdog_feed()` (BUG-335) — som kompensation for den housekeeping `loop()` ellers ville have lavet. Bevidst IKKE udvidet til `st_logic_engine_loop()`: brugeren har selv tidligere foreslaaet at ST-programmer gerne maa pause under en bevidst, manuel scan, saa kun netvaerks-tilgaengelighed (dashboard/CLI) er den kritiske ting at bevare her (cli_commands_modbus_master.cpp) |

| BUG-343 | Ny diagnostik `show tasks` — indfoert fordi FEM fix-forsoeg paa GUI-frysningen alle var gaet, uden at kunne SE hvilken task der haenger | 🔧 DIAGNOSTIK | 🟡 HIGH | v7.9.9.5 | Efter BUG-336/336b/336c/341/342 (taskYIELD, vTaskDelay(1), Core-0-pinning, 10ms-polling, network_manager_loop i scan-loopet) var problemet stadig uaendret. Empirisk indsnaevring gav: **ping virker hele tiden** (netvaerkslaget er oppe), **selv en helt frisk browser-session kan ikke hente SIDEN** (ikke bare API-data), og **selv `mb scan 1 5` (5 adresser, faa sekunder) udloeser det** (altsaa oejeblikkeligt, ikke kumulativt). Det udelukker baade Wi-Fi-tab (BUG-342), én enkelt langsom API-handler, og alt der akkumulerer over mange iterationer — tilbage staar httpd-tasken selv, som enten er BLOCKED (venter paa noget) eller READY men sultet for CPU. De to kraever modsatte loesninger, og der fandtes ingen maade at se forskel paa dem. FIX (diagnostik, ikke funktionsaendring): ny read-only CLI-kommando `show tasks` der viser tilstand (RUNNING/READY/BLOCKED/SUSPEND), core-tilknytning, prioritet og resterende stak for de relevante tasks (httpd, loopTask, mb_async, sse_accept, tiT/lwIP, wifi, sys_evt, IDLE0/1). Koeres fra seriel/telnet MENS problemet staar paa. NB: `uxTaskGetSystemState()` kunne ikke bruges (CONFIG_FREERTOS_USE_TRACE_FACILITY er slaaet fra i denne Arduino-ESP32 framework-build → undefined reference ved link), og `xTaskGetCoreID()` findes ikke i denne version (brug `xTaskGetAffinity()`) — derfor opslag ved tasknavn via `xTaskGetHandle()` + `eTaskGetState()` i stedet (cli_show.h/.cpp, cli_parser.cpp) |

| BUG-344 | REGRESSION fra BUG-342: `mb scan` startede sig selv rekursivt og draebte telnet-sessionen | ✅ FIXED | 🔴 CRITICAL | v7.9.9.6 | **Selvforskyldt regression indfoert i v7.9.9.4.** Brugerens output viste scan-headeren printet FEM gange i traek uden en eneste testet adresse imellem, hvorefter telnet-sessionen doede. Rodaarsag: BUG-342 tilfoejede `network_manager_loop()` inde i scan-loopet. Den kaede ender i `telnet_server_loop()`, som **selv eksekverer CLI-kommandoer** (telnet_server.cpp:984, `cli_shell_execute_command()`) — og som foerst nulstiller `input_ready` EFTER kommandoen returnerer (telnet_server.cpp:1008). Men den koerende kommando VAR `mb scan`, som tager minutter: det indlejrede kald saa derfor den samme kommando som stadig "klar" og startede en ny scanning inden i den igangvaerende. Hver runde forbrugte mere stak indtil tasken doede. FIX: (1) BUG-342's `network_manager_loop()`/`cli_remote_loop()`-kald FJERNET fra scan-loopet igen — praemissen var i forvejen afkraeftet af brugerens ping-test (ping svarer uafbrudt under hele scanningen, saa Wi-Fi mistes aldrig), (2) nyt reentrans-vaern i `cli_shell_execute_command()`: kun én CLI-kommando ad gangen, ellers svares "OPTAGET". Vaernet lukker samtidig et latent kapløb paa tvaers af tasks — web-CLI'ens handler kalder samme funktion fra httpd-taskens kontekst og ville kunne kapre den GLOBALE `g_debug_console` midt i en igangvaerende seriel/telnet-kommando. **Generel laering: kald aldrig loop()-housekeeping inde fra en CLI-kommando — kommando-dispatch ligger nede i den kaede, saa det bliver reentrant** (cli_commands_modbus_master.cpp, cli_shell.cpp) |

| BUG-345 | `mb scan` afbroed sig selv oejeblikkeligt ved slave 1 — uden at brugeren havde roert tastaturet | ✅ FIXED | 🟡 HIGH | v7.9.9.7 | Efter BUG-344-rettelsen afbroed enhver `mb scan` fra telnet med det samme: "Afbrudt af bruger ved slave 1 (0/247 testet)". Rodaarsag: BUG-335's afbryd-funktion tjekker `console_available()` i hver iteration, men kommandolinjen selv efterlader tegn i socket-bufferen — "mb scan" afsluttes med CR+LF, og telnet-laget forbruger kun det der skal til for at danne linjen; resten (typisk '\n') ligger stadig og venter naar scanningen starter. Foerste gennemloeb laeste det som et tastetryk. Bugget havde ligget der siden BUG-335 (v7.9.8.6), men var **maskeret** af BUG-342's `network_manager_loop()`-kald, som tilfaeldigvis naaede at forbruge tegnet foerst — det blev derfor foerst synligt da BUG-342 blev rullet tilbage. Praecis samme fejlklasse som BUG-334 (én efterladt byte i RX-bufferen afbroed RS485-aktiveringen ved boot). FIX: ventende input toemmes (max 64 tegn, saa en snakkende forbindelse ikke kan holde os fast) FOER afbryd-overvaagningen begynder (cli_commands_modbus_master.cpp) |

| BUG-346 | `tcp_server_available()` var en stub der ALTID svarede "der er data" → enhver `mb scan` fra telnet afbroed sig selv oejeblikkeligt | ✅ FIXED | 🔴 CRITICAL | v7.9.9.8 | **Den egentlige rodaarsag bag BUG-345's symptom** (BUG-345's toemning hjalp ikke, fordi tjekket den byggede paa loej). `tcp_server_available()` (tcp_server.cpp:405) tjekkede aldrig socket'en — den returnerede ubetinget `256` med kommentaren *"For non-blocking sockets, we'd need to peek or use select / For now, return simple estimate / Optimistic: assume data might be available"*. Alt der spurgte "ligger der input?" fik derfor JA, ogsaa naar bufferen var tom: `console_available()` -> `telnet_has_input()` -> `telnet_server_available()` -> denne stub. `mb scan`s afbryd-tjek (BUG-335) ramte dermed paa allerfoerste gennemloeb og afbroed hver eneste scanning fra telnet med "Afbrudt af bruger ved slave 1 (0/N testet)" uden at nogen havde roert tastaturet. Fejlen havde ligget latent siden afbryd-funktionen blev indfoert (v7.9.8.6) men var usynlig, fordi scans indtil nu blev koert fra web-CLI, hvis `has_input` pr. design altid svarer "ingen input" — og BUG-345's toemme-loop kunne heller ikke slippe ud, da betingelsen aldrig blev falsk (den loeb blot sine 64 runder og gav op). FIX: rigtigt opslag via `ioctl(sock, FIONREAD, &bytes)` — giver antal bytes der kan laeses uden at blokere, uden at forbruge dem. Kan opslaget ikke lade sig goere, svares 0 ("ingen data"): at gaette den anden vej var praecis det der skabte fejlen (tcp_server.cpp) |

| BUG-347 | **RODAARSAG (endelig)**: web-CLI eksekverer kommandoen paa httpd's EGEN task → en `mb scan` derfra blokerer hele webserveren | ✅ FIXED | 🔴 CRITICAL | v7.9.9.9 | **Efter FEM forgaeves fix-forsoeg mod det forkerte lag** (BUG-336 taskYIELD, BUG-336b vTaskDelay(1), BUG-336c Core-0-pinning af httpd/SSE, BUG-341 10ms-polling, BUG-342 network_manager_loop i scan-loopet) leverede brugeren det afgoerende eksperiment: koert fra **telnet** er der intet problem overhovedet — dashboardet opdaterer, Modbus-loggen loeber, og man kan betjene UI'et imens — mens **praecis samme kommando fra GUI'ens CLI** fryser alt som foer. Rodaarsag: `api_handler_cli_exec()` (api_handlers.cpp) kalder `cli_shell_execute_command()` SYNKRONT, dvs. paa httpd's egen task. ESP-IDF's httpd behandler requests én ad gangen, saa mens kommandoen koerer kan webserveren ikke svare paa NOGET — hverken dashboardets polling, SSE eller en frisk sideindlaesning. En `mb scan 1 247` holder dermed hele web-UI'et nede i minutter. Bekraeftet af `show tasks` (BUG-343) taget MENS en telnet-scan koerte: `httpd BLOCKED` (normal hviletilstand, venter paa requests) og `loopTask RUNNING` — httpd var altsaa aldrig sultet eller laast, den var helt enkelt *optaget af scanningen selv* naar den blev startet fra web. Alle fem tidligere teorier (scheduling-granularitet, core-deling, Wi-Fi-genopkobling, mutex-kontention) var dermed irrelevante. FIX: `mb scan` afvises nu fra web-CLI med HTTP 409 og en forklarende besked om at koere den fra telnet/seriel i stedet. NB: dette er en afgraensning, ikke en fuld loesning — aegte asynkron eksekvering af lange web-CLI-kommandoer (egen worker-task + polling af output) er den rigtige langsigtede vej, hvis kommandoen skal kunne startes fra GUI'en (api_handlers.cpp) |

| FEAT-153 | Modbus Aktivitetslog: start/stop, CSV-eksport, 500 poster i PSRAM, NTP-tidsstempler | ✅ DONE | 🟡 HIGH | v7.9.10.0 | Brugeroenske. (1) **Start/stop**: ny `mb_activity_log_set_enabled()` + `POST /api/modbus/activity/start|stop` og knap i dashboardet — fryser opsamlingen UDEN at rydde indholdet, saa man kan naa at laese/eksportere et oejebliksbillede foer det ruller videre. Tilstanden bevares bevidst naar man rydder loggen (at rydde skal ikke starte logningen igen bag om brugeren) og nulstilles kun ved reboot. (2) **CSV-eksport**: knap der henter HELE loggen (ikke kun det viste udsnit) og gemmer den klientside som semikolon-separeret CSV med baade klokkeslaet, uptime og Unix-tid. (3) **Kapacitet 40 -> 500**: ringbufferen allokeres nu i PSRAM (`heap_caps_malloc(MALLOC_CAP_SPIRAM)`, ~11 KB — ingenting af de 4 MB, og tager ikke af den knappe interne DRAM), med fallback til almindelig heap og til sidst "log deaktiveret" frem for NULL-dereference. `count`/`get` skiftet fra uint8_t til uint16_t da 500 > 255. (4) **NTP-tidsstempler**: nyt `epoch_s`-felt pr. post, sat fra `ntp_driver_get_epoch()` naar uret er synkroniseret (0 ellers). `millis()` bevares ved siden af, fordi den er monoton og virker fra boot — dashboardet viser rigtigt klokkeslaet naar det findes, ellers uptime. Epoch hentes UDEN for spinlock'en, saa den kritiske sektion forbliver kort. (5) **Ydelse**: GET understoetter nu `?limit=N` (nyeste N), og dashboardet henter kun det viste udsnit ved sin 3-sekunders polling — uden det ville 500 poster (~70 KB) blive traukket over WiFi 20 gange i minuttet. Svaret sendes desuden **chunked** i stedet for at blive samlet i én stor buffer, hvilket helt fjerner den fejlklasse BUG-332 tilhoerte (fast buffer for lille -> afkortet JSON -> dashboardet holdt op med at opdatere) og goer stoerrelsen ligegyldig. Default visningsantal 25 -> 100. Manual opdateret (kap. 4 + Appendiks B) (mb_activity_log.h/.cpp, api_handlers.h/.cpp, web_dashboard.cpp, docs/manual) |

| FEAT-154 | Alarmlog + CLI-historik flyttet til PSRAM — 7 KB intern DRAM frigjort | ✅ DONE | 🔵 LOW | v7.9.10.1 | Opfoelgning paa FEAT-153: hvad kan ellers med fordel ligge i PSRAM? **Vigtigste konstatering: det meste sker allerede automatisk.** Arduino-frameworket har `CONFIG_SPIRAM_USE_MALLOC=y` med `CONFIG_SPIRAM_MALLOC_ALWAYSINTERNAL=4096`, saa enhver `malloc()` over 4 KB havner af sig selv i PSRAM — web-CLI'ens 12+24 KB buffere, backup-JSON (12 KB) og metrics (10 KB) brugte det altsaa i forvejen. Statiske/globale arrays gaar derimod ALTID i intern DRAM uanset den indstilling, og `EXT_RAM_BSS_ATTR` er ikke tilgaengelig her (`CONFIG_SPIRAM_ALLOW_BSS_SEG_EXTERNAL_MEMORY` er slaaet fra) — derfor eksplicit `heap_caps_malloc(MALLOC_CAP_SPIRAM)` med fallback til intern heap. Maalt med `xtensa-esp32-elf-nm --size-sort` paa firmware.elf. FLYTTET: `alarm_log` (4.480 B — skrives kun ved alarmhaendelser, laeses af dashboard-polling hvert 5. sek) og `cli_history_buffer` (2.560 B — roeres kun naar et menneske taster). BEVIDST IKKE FLYTTET: `g_logic_state` (6.080 B) og `g_persist_config` (3.543 B), som begge laeses i hver `loop()`-iteration — PSRAM er SPI-tilgaaet og langsommere, saa det ville koste PLC-ydelse; og `g_mb_async` (1.824 B), som tilgaas under spinlock fra begge cores (PSRAM-adgang med interrupts slaaet fra er en kendt faldgrube). `dashboard_html` (100 KB) ligger allerede i flash, ikke RAM. Begge flyttede buffere allokeres ved foerste brug og springes stille over hvis allokeringen fejler, frem for at dereferere NULL — inkl. et ekstra vaern paa alarm-ack-loekken, som er den eneste der ikke er afgraenset af `alarm_log_count`. **Resultat: intern DRAM 74.152 -> 66.312 bytes (-7.840 B totalt inkl. FEAT-153)** (api_handlers.cpp, cli_history.cpp) |

| BUG-348 | ST-program med tilstandsmaskine der venter paa `MB_SUCCESS()` efter et write kan laase sig fast permanent efter lang tids drift — IKKE en firmware-fejl | ✅ FORKLARET (bruger-program) | 🔴 CRITICAL | dokumentation | **Bekraeftet af bruger:** "modbus_plc" (den kendte `CASE step OF`-tilstandsmaskine fra tidligere i sessionen) stoppede med at opdatere efter ca. et doegns drift — praecis samme signatur som BUG-333/334 (`Udfoerelser` stiger, intet opdateres), men denne gang hjalp `Reinit` (mens `Stop`+`Start` ikke gjorde), hvilket peger vaek fra Modbus-master-laget og ind mod ST-programmets EGEN variabel-/tilstandsdata. Rodaarsag fundet ved at laese `st_builtin_modbus.cpp`: `MB_SUCCESS()` betyder noget FORSKELLIGT efter READ vs. WRITE. Efter `MB_READ_*` afspejler den cache-status (aegte succes/fejl, `st_builtin_modbus.cpp:131,164,197,230`). Efter `MB_WRITE_*` saettes den derimod KUN ud fra om skrivningen blev lagt i kø (`g_mb_success = queued;`, `st_builtin_modbus.cpp:262,292,326,355`) — IKKE om den faktisk blev udfoert paa bussen — og aendres aldrig igen efter det ene write-kald. Brugerens trin 4 ventede ubegraenset paa `MB_SUCCESS()` efter et `MB_WRITE_HOLDING()` uden nogen timeout/fallback — er koeen (16-32 pladser) fuld i netop det oejeblik (bustravlhed, eller en `mb scan` der har koeen paa pause, BUG-338), forbliver flaget falsk for evigt, og tilstandsmaskinen (variablen `step`) sidder fast i trin 4 til evig tid, medmindre noget eksplicit nulstiller `step` — praecis hvad `Reinit` goer (nulstiller ALLE variabler til deres compilerede startvaerdi), mens `Stop`+`Start` kun taeler en enabled-flag uden at roere variabelvaerdier. Symptomet kraever typisk lang tids drift foer det udloeses, fordi det afhaenger af at koeen tilfaeldigvis er fuld i netop det ene oejeblik et write forsoeges — matcher "efter et doegns drift" perfekt. **Sekundaert afklaret i samme omgang:** den tidligere mistaenkte `ELSE;`-syntaks (semikolon direkte efter ELSE i samme program) blev grundigt verificeret IKKE at vaere en bug — parseren (`st_parser.cpp:1657-1658`) returnerer en tom-sætning-NULL uden at saette `error_count`, saa genopretningslogikken i `st_parser_parse_statements` (linje 1743-1757) blot springer det tomme semikolon over og fortsaetter korrekt til den reelle sætning derefter. `ELSE; counter := 0;` opfoerer sig 100% identisk med `ELSE counter := 0;`. FIX (ingen firmwaeaendring — dette er korrekt, dokumenteret adfaerd, blot utilstraekkeligt dokumenteret foer nu): (1) `docs/manual/08_ST_Logic_Programmering.md` §8.9 udvidet med en eksplicit advarsel + korrigeret kodeeksempel (taeller-baseret timeout i stedet for ubegraenset ventetid), (2) `docs/manual/13_Fejlfinding.md` §13.3 faaet et nyt "aarsag C" der specifikt bruger "hjaelper Reinit?" som differentialdiagnose mellem dette (C, reinit hjaelper) og de tidligere kendte aarsager A/B (Modbus-master-lag-problemer, hvor hverken stop/start eller reinit hjaelper). Brugeren fik en konkret rettet ST-kodeeksempel til deres eget program (docs/manual/08_ST_Logic_Programmering.md, docs/manual/13_Fejlfinding.md) |

| BUG-349 | ST Logic-editor føltes ekstremt langsom ved scroll — genberegnede syntax-highlighting for HELE dokumentet ved hver scroll-event | ✅ FIXED | 🟡 HIGH | v7.9.10.5 | Bruger rapporterede scroll op/ned i editoren var "virkelig langsomt", som om der blev "hentet over API'et for hver linje" — antagelsen om et netvaerks-/API-problem var forkert, men symptomet var reelt. `syncHighlight()` (`web_editor.cpp`) koerte SYV sekventielle regex-erstatninger (`highlightST()`: kommentarer, strenge, I/O-adgang, tal, typer, funktioner, noegleord, operatorer) over HELE editor-tekstens indhold (`ed.value`, ikke kun de synlige linjer) — og blev kaldt fra `scroll`-eventlisteneren, som fyrer utallige gange i sekundet under en scroll-gestus. For et program paa flere hundrede linjer betoed det gentagen fuld-dokument-regex-genberegning dusinvis af gange pr. scroll, foelt som konstant lag. FIX: splittet i to funktioner — `renderHighlight()` (den dyre regex-genberegning, uaendret, stadig kaldt fra alle 9 tekst-aendrings-steder: input, programindlaesning, o.lign.) og `syncScrollPos()` (billig, blot flytter highlight-overlay'ets CSS-transform til at foelge scroll-positionen). Scroll-handleren skiftede til KUN at kalde `syncScrollPos()` — highlighting genberegnes nu udelukkende naar teksten reelt aendrer sig, ikke naar man blot ruller i et uaendret dokument. `syncHighlight()` selv bevaret som en tynd wrapper (kalder begge) saa de 9 andre kaldesteder er fuldstaendig uroerte (web_editor.cpp) |

| FEAT-155 | Flash-optimering: web-UI'et gzip-komprimeret ved build — 174 KB frigjort (92,6% -> 83,5% flash) | ✅ DONE | 🟡 HIGH | v7.9.10.7 | Flash naaede 92,6% efter FEAT-086/089 — undersoegt med `xtensa-esp32-elf-nm --size-sort` paa firmware.elf for at finde de reelle stoerste poster. **De 5 indlejrede dashboard/editor/system/cli/ota-sider stod alene for 236 KB** (117+61+34+12+11 KB), ukomprimeret ren HTML/CSS/JS i `.flash.rodata` — langt over naest-stoerste post (`cli_parser_execute`, 26 KB). Ingen af de 5 handlers goer runtime-templating/substitution paa siden foer send (verificeret: hver handler er ren `httpd_resp_send(statisk_buffer, strlen(...))`), saa build-tids gzip er risikofrit. **Struktur-aendring**: HTML/CSS/JS-kilden flyttet fra C++ raw-string-literals (`R"rawhtml(...)rawhtml"` i web_*.cpp) til rigtige `.html`-filer under `web/` (dashboard.html, editor.html, system.html, cli.html, ota.html) — redigeres DER fremover, ikke i .cpp'erne. Nyt pre-build-script `scripts/gzip_web_assets.py` (samme moenster som eksisterende `generate_build_info.py`) gzip-komprimerer hver side ved hver build til en genereret header i `include/generated_web/*_gz.h` (`static const uint8_t X_html_gz[]` + `_len`, UTF-8-encoded eksplicit — en implicit Windows cp1252-encoding i det foerste udkast korrumperede em-dash-tegn i header-kommentaren, fanget ved en gzip-decompress-roundtrip-verifikation foer commit). Hver handler faar `httpd_resp_set_hdr(req,"Content-Encoding","gzip")` + sender byte-arrayet direkte med eksplicit laengde (ikke `strlen()`, da gzip-data kan indeholde NUL-bytes). Browseren dekomprimerer automatisk — ingen synlig aendring for brugeren. **Resultat pr. side:** dashboard 120→29 KB, editor 63→18 KB, system 37→10 KB, cli 12→4 KB, ota 11→4 KB (samlet 243→64 KB, 178 KB sparet). Verificeret med en decompress-roundtrip mod kildefilerne foer build (byte-for-byte match), samt `node --check` paa den udtrukne JS fra alle 5 nye `.html`-filer (scripts/gzip_web_assets.py, web/*.html, web_dashboard.cpp, web_editor.cpp, web_system.cpp, web_cli.cpp, web_ota.cpp, platformio.ini) |

| FEAT-156 | HTTPS/TLS til/fra-knap tilfoejet i web-GUI'en (System-siden) — samme betingelser som CLI | ✅ DONE | 🔵 LOW | v7.9.10.7 | FEAT-012 (v6.0.4) leverede selve HTTPS-serveren og `set http tls on\|off` i CLI, men GUI'en havde intet sted at styre det — backend (`POST /api/http` med `{"tls_enabled":bool}`, `GET /api/config`s `http.tls_enabled`) fandtes allerede fuldt ud, kun UI-elementet manglede. Nyt kort "HTTPS / TLS" paa System-siden (`/system`) med status-visning + checkbox — **samme betingelser som CLI**: rent til/fra-flag, INTET certifikat-upload (cert/nøgle er stadig kun build-time-indlejret via `board_build.embed_txtfiles`), aendringen skrives kun til RAM og kraever eksplicit "Gem & Genstart" (samme to-trins-moenster som CLI's `set http tls on` + `save`-hint) for reelt at traede i kraft (web_system.cpp) |

| BUG-350 | HTTPS delte portnummer med HTTP (begge `network.http.port`, default 80) — aktivering af TLS gjorde port 80 om til en TLS-only-lytter uden varsel, floedede loggen med "bad ClientHello" | ✅ FIXED | 🔴 CRITICAL | v7.9.10.8 | **Fundet dagen efter FEAT-156 blev skibet**, da brugeren rent faktisk aktiverede den nye GUI-knap: boot-loggen floedede oejeblikkeligt med `E esp-tls-mbedtls: mbedtls_ssl_handshake returned -0x7900` / `esp_https_server: esp_tls_create_server_session failed`, dusinvis af gange i sekundet, kontinuerligt. `-0x7900` = `MBEDTLS_ERR_SSL_BAD_HS_CLIENT_HELLO` (verificeret i den vendorede `mbedtls/ssl.h`) — IKKE et certifikat-/mbedtls-konfigurationsproblem, men et **protokol-mismatch**: serveren modtog almindelig (ukrypteret) HTTP paa en port den nu forventede TLS paa. Rodaarsag: `http_server_start()` (http_server.cpp) brugte `config->port` (samme `network.http.port`-felt, default 80) til BAADE HTTP- og HTTPS-tilstand — der fandtes intet separat portfelt. Enhver klient der stadig sendte `http://<ip>/` mod port 80 (browser-bogmaerker, Node-RED-flows, aabne dashboard-faner der pollede `/api/metrics` hvert 3. sek.) blev fejltolket som en ugyldig TLS-handshake af serveren. Og `https://<ip>` uden eksplicit portnummer ramte slet ikke serveren, fordi browsere antager port 443 for https-skemaet — kun `https://<ip>:80` ville rent faktisk have virket, hvilket ingen bruger ville gaette uden dokumentation. FIX: nyt dedikeret `https_port`-felt i `PersistConfig` (schema 20→21, migration saetter default 443 for eksisterende installationer — se BUG-339 for hvorfor dette ALTID kraever en migrationsblok naar noget indlejret i PersistConfig aendres), placeret **helt til sidst** foer `crc16` (samme byte-offset-sikkerhedsbegrundelse som analog I/O-felterne, FEAT-034/035/036) da det semantisk hoerer hjemme i `HttpConfig` men INDSAETTELSE der ville forskyde alt efter). `http_server_start()` laeser nu `g_persist_config.https_port` (default `HTTPS_SERVER_PORT`=443) i HTTPS-grenen i stedet for `config->port`. Ny CLI-kommando `set http https-port <port>` (mirror af `set http port`). REST: `GET /api/config`/`GET /api/system/backup` eksponerer nu `http.https_port`, `POST /api/http` og `POST /api/system/restore` accepterer den. Alle steder der byggede en fuld URL ud fra `tls_enabled ? "https" : "http"` (cli_show.cpp, 2 steder) rettet til at vaelge det rigtige portfelt ud fra samme flag — de brugte foer ubetinget `network.http.port` uanset protokol, hvilket ville have vist en forkert/vildledende URL selv efter portadskillelsen. `show http` (kort + udvidet) viser nu BEGGE porte eksplicit, mærket. Web-GUI'ens HTTPS/TLS-kort (FEAT-156) udvidet med et eksplicit HTTPS-port-felt + en live-opdateret "Tilgå via: https://<host>:<port>/"-URL-hjælpetekst, netop for at forhindre denne forvirring i at opstå igen. **Ingen data tabt** — brugeren kom tilbage paa HTTP via Telnet (`set http tls off; save; reboot`) mens fixet blev udviklet (types.h, constants.h, config_load.cpp, http_server.cpp, cli_commands.cpp, cli_parser.cpp, cli_show.cpp, api_handlers.cpp, web/system.html, docs/manual) |

| BUG-351 | **KRITISK, forudgaaende (ikke skibet endnu):** enhver schema-migration der aendrer PersistConfig nulstillede HELE configen (WiFi, RBAC, alt) til fabriksdefault paa naeste boot | ✅ FIXED (foer produktion) | 🔴 CRITICAL | v7.9.10.8 | **Opdaget under implementeringen af BUG-350** (schema 20→21) ved at spore `nvs_get_blob`s faktiske semantik heilt ned i ESP-IDFs kildekode (`nvs_api.cpp:501`, `nvs_get_str_or_blob`): naar destinationsbufferen (nu-kompileret `sizeof(PersistConfig)`) er stoerre end den lagrede blob (gammelt schema), kopieres KUN de faktisk lagrede bytes ind — resten af bufferen (herunder det NYE felt og — kritisk — `crc16`-feltets NYE, forskudte position) forbliver UROERT (nul/garbage). `config_load_from_nvs()`s migrationsstige opdaterede korrekt `schema_version` og satte fornuftige standardvaerdier for nye felter — men det efterfoelgende CRC-tjek sammenlignede den nu-migrerede (AENDREDE) data mod `stored_crc` (den GAMLE, foer-migration CRC-vaerdi, laest direkte fra den delvist tomme `crc16`-plads) — disse to matcher per design ALDRIG efter en reel migration (samme byte-aendring, ny beregning vs. gammel/urelateret vaerdi, ~1/65536 chance for tilfaeldigt match). Konsekvens: `stored_crc != calculated_crc` → "CONFIG CORRUPTED, REJECTING" → `config_init_defaults()` → total nulstilling. En kommentar ("CRC will be invalid, but we'll recalculate on next save") har staaet ved migrationskoden siden schema 7→8 — hensigten var tydeligvis at det skulle taales, men det blev ALDRIG faktisk implementeret som en undtagelse i CRC-tjekket. **Paavirker enhver historisk schema-migration** (7→8 op til 19→20), ikke kun BUG-350s 20→21 — reelt omfang paa tidligere brugere ukendt (ingen entydig rapportering, men forklarer plausibelt "mystiske" konfigurationstab efter firmwareopdateringer). FIX: ny `bool migrated`-flag sat naar migrationsstigen reelt koerer; naar sand SPRINGES det stale CRC-tjek over, `out->crc16` genberegnes friskt paa den migrerede struct, og resultatet gemmes STRAKS til NVS (`config_save_to_nvs()`) saa naeste boot igen validerer normalt — i stedet for at afvise data der er korrekt, blot endnu ikke gemt i sin nye form. Verificeret sikkert paa tvaers af alle 5 kaldesteder af `config_load_from_nvs()` (boot, REST `/api/system/load`, CLI `load`, samt to scratch-buffer-kald i persist-gruppe-kode) — funktionen opererer udelukkende paa den overgivne pointer, saa auto-gem paa en scratch-buffer gemmer kun dens egen (korrekt migrerede) data, aldrig krydskontaminering med `g_persist_config`. **Fanget FOER det ramte brugerens enhed** via en grundig gennemgang af den faktiske ESP-IDF NVS-kildekode i stedet for at stole paa antagelser — ingen reelt datatab denne gang (config_load.cpp) |

| FEAT-157 | Modbus Aktivitetslog: filtrering på status (OK/fejlede) og slave-adresse | ✅ DONE | 🔵 LOW | v7.9.10.8 | Brugeroensket. To nye filtre i toolbaren ved siden af de eksisterende rolle-/kilde-filtre (FEAT-149/153): en status-dropdown (OK+Fejlede/Kun OK/Kun fejlede, matcher entry'ens `success`-felt) og et slave-adresse-talfelt (tomt = alle). Rent klientside-filtrering paa den allerede hentede log, samme moenster som de eksisterende filtre — ingen aendring i REST API'et eller backend-loggen (web_dashboard.cpp) |

| BUG-352 | REST API-auth-modernisering, fase 1: HTTP/RBAC-passwords laa i klartekst i NVS — nu SHA-256+salt hashet | ✅ FIXED | 🔴 CRITICAL | v7.9.10.9 | Brugeren spurgte hvad der kunne goere REST API-auth "tidssvarende" — undersoegelsen fandt `strcmp(u->password, password)` paa raa klartekst, baade for RBAC-brugere og det legacy single-user HTTP-password. En NVS-backup-eksport eller flash-dump afsloerede dermed credentials direkte. FIX: **hash genbruger eksisterende byteplads** — `RbacUser.password[32]` og `HttpConfig.password[64]` er begge ≥32 bytes, en SHA-256-digest er PRAECIS 32 bytes, saa selve hashen kraever ingen struct-vaekst. Kun saltet er nyt: `rbac_salt[RBAC_MAX_USERS][16]` og `http_legacy_salt[16]`, tilfoejet HELT TIL SIDST i `PersistConfig` (samme sikre moenster som analog I/O/https_port — schema 21→22, migration hasher hvad der staar i felterne NU, med et frisk salt hver). **Denne migration er den foerste reelle test af BUG-351-fixet** (fundet dagen foer, under BUG-350-arbejdet) — uden det ville schema-bumpet have nulstillet HELE configen. Nye hjaelpefunktioner i rbac.cpp (`rbac_generate_salt()` via `esp_fill_random()`, `rbac_hash_password()` = `SHA256(salt \|\| password)`, `rbac_hash_equal()` konstant-tid-sammenligning) bruges IDENTISK paa baade skrive- og laesevejen (`rbac_set_user()`, `rbac_hash_and_store_legacy_password()`, `rbac_authenticate()`, `rbac_legacy_auth()`) — elimininerer strukturelt enhver risiko for salt-raekkefoelge-mismatch mellem de to. Backup/restore-format aendret: `password`-feltet (klartekst) erstattet af hex-encoded `password_hash`+`password_salt`-par (raa hash-bytes kan ikke sendes direkte som JSON-string); restore skriver dem raat tilbage (ingen gen-hashing) men accepterer stadig det GAMLE klartekst-format for bagudkompatibilitet med aeldre backup-filer. **Bevidst UDENFOR scope:** WiFi-password (skal vaere klartekst for WPA2-haandtryk) og Telnet-password (separat credential-system) — sidstnaevnte bevidst UBERoeRT som en garanteret fallback-adgangsvej hvis noget skulle gaa galt med HTTP/RBAC-login efter opdateringen. Eksisterende credentials fortsaetter med at virke transparent — hashingen sker automatisk ved foerste boot efter opdateringen, ingen handling kraevet af brugeren. Session-tokens (naeste fase i auth-moderniseringen) er IKKE del af denne aendring (types.h, constants.h, rbac.h/.cpp, config_load.cpp, cli_commands.cpp, api_handlers.cpp, docs/manual) |

| BUG-353 | REST API-auth-modernisering, fase 2: session-tokens — web-UI'et gensender ikke laengere brugernavn/kodeord paa hvert request | ✅ DONE | 🟡 HIGH | v7.9.10.10 | Fase 1 (BUG-352) hashede passwords ved lagring; dette er fase 2, som brugeren bad om bagefter. **Genbrugte et allerede eksisterende, fungerende moenster** i stedet for at opfinde noget nyt: `sse_events.cpp`s `sse_token_issue()`/`sse_token_check()` (bygget til EventSource paa tvaers af porte) — samme arkitektur (fast slot-array, spinlock, `esp_random()`-hex-token, TTL+GC, evict-oldest-ved-fuld), men egen tabel i rbac.cpp med anden levetid: SLIDENDE 30-min inaktivitets-timeout (forlaenges ved hvert gyldigt kald) i stedet for SSE-tokenets faste 5-min bro-TTL. **Étpunkts-integration**: `rbac_check_http()` tjekker `Bearer `-praefiks FOeRST, falder ellers tilbage til den UAeNDREDE Basic Auth-logik — fordi dette er choke-point for ALLE `CHECK_AUTH*`-makroer, daekker den ene aendring automatisk samtlige ~90 eksisterende endpoints (inkl. web-CLI, OTA-upload) uden at roere dem selv. Nye endpoints: `POST /api/login` (verificerer via den SAMME Basic Auth-header-parsing som alt andet, udsteder token, svar mirror'er `/api/user/me` plus et `token`-felt via en delt `build_user_info_json()`-helper der ogsaa `/api/user/me` nu bruger), `POST /api/logout` (invaliderer token'et med det samme — en reel server-side-invalidering, findes ikke tidligere). **Fejl fundet og rettet under implementeringen**: foerste udkast af login-fejl-stien duplikerede IP/brugernavn-indfangning manuelt via `http_get_client_info()` — men den funktion henter kun brugernavnet ved en ALLEREDE lykkedes auth (ubrugelig ved en fejlet login-forsoeg) OG `api_send_error(401)` gjorde det HELE allerede, korrekt, via sin egen manuelle header-dekodning (fanger det FORSOeGTE brugernavn selv ved forkert password) — duplikeringen fjernet, laent paa den eksisterende, bedre mekanisme. Som en direkte foelge blev FEAT-086s tidligere "login-succes logges bevidst ikke" (stateless Basic Auth ville flode loggen) forkastet for selve `/api/login`-kaldet specifikt: et login er nu en diskret, sjaelden handling, ikke et per-request-tjek, saa succesfulde logins logges nu ogsaa. **Frontend, 5 filer** (web/dashboard.html, editor.html, system.html, cli.html, ota.html): kun `doLogin()`/`doLogout()` aendret i hver — resten af hver sides fetch-kald laeser allerede blot en faerdig `Authorization`-header-vaerdi fra `sessionStorage['hfplc_auth']` og videresender den blindt, skema-agnostisk, saa de forbliver fuldstaendig uroerte. `web/ota.html` var en undtagelse (gemte kun den rene base64-del uden skema-praefiks, prependede selv `'Basic '` to steder) — ombygget til samme "gem en komplet header-vaerdi"-konvention som de andre 4, inkl. et nyt `loginErr`-element i dens login-modal (fandtes slet ikke foer — siden havde ingen fejlvisning overhovedet). **Bagudkompatibelt for evigt**: almindelig Basic Auth virker uaendret side om side — Node-RED/curl/scripts der taler direkte REST paavirkes slet ikke. Bevidst UDENFOR scope: dedikeret brute-force-lockout paa `/api/login` (kun den generelle rate-limiter gaelder) (rbac.h/.cpp, api_handlers.h/.cpp, http_server.cpp, web/*.html, docs/manual) |

| BUG-354 | **REGRESSION fra BUG-353:** `/cli`- og `/ota`-siderne holdt op med at virke ("This URI does not exist") — `max_uri_handlers` (96) sprunget af de 2 nye login/logout-routes | ✅ FIXED | 🔴 CRITICAL | v7.9.10.11 | Brugeren rapporterede live: "gui kan ikke tilgå CLI tap mere". `httpd_register_uri_handler()`s returvaerdi tjekkes IKKE ved nogen af de 98 kaldesteder i `http_server.cpp` — naar det faktiske antal registreringer overstiger `max_uri_handlers`, fejler de SIDSTE registreringer i rækkefølgen STILLE, uden boot-fejl. BUG-336c satte oprindeligt grænsen til 96 for at daekke de daværende 92 handlers — praecis nok margin til at BUG-353s to nye routes (`/api/login`, `/api/logout`) skubbede totalen til 98 og fortraengte de to SIDST registrerede: `uri_ota_page` (`/ota`) og `uri_cli_page` (`/cli`, registreringsnummer 98 — den absolut sidste i listen). Kun `/cli` blev rapporteret, men `/ota` var samtidig ramt af nøjagtig samme aarsag. FIX: `max_uri_handlers` haevet 96→128 (baade plain-HTTP-config i `http_server.cpp` og `https_wrapper_start()`s parameter i samme fil) — denne gang med reel margin, ikke praecis nok til det nuvaerende antal. Tilfoejet en tydelig kommentar ved registreringsblokkens start der forklarer fejlklassen og instruerer i at koere `grep -c "httpd_register_uri_handler(http_state.server" src/http_server.cpp` efter fremtidige route-tilfoejelser, saa denne praecise fejl (ramt to gange nu: BUG-336c, BUG-354) forhaabentlig ikke sker en tredje gang (http_server.cpp) |

| BUG-355 | Sikkerheds-oprydning, fase A1+A2: 6 af 9 aabne SECURITY_INDEX-fund rettet, 2 dokumenteret som bevidst fravalg, 1 (SSE TLS) bevidst udskudt | ✅ DONE | 🔴 CRITICAL | v7.9.10.12 | Brugeren bad om at faa styr paa HELE SECURITY_INDEX.md-listen. **Rettet:** #2 OTA kraevede kun login, ikke skriverettighed — `CHECK_AUTH_OTA` mirror'er nu `CHECK_AUTH_WRITE` praecist (ota_handler.cpp). #7 rate-limit blev tjekket EFTER auth i alle fire auth-makroer (`CHECK_AUTH`/`CHECK_AUTH_WRITE`/`CHECK_AUTH_ROLE`, `CHECK_AUTH_OTA`), saa et forkert login altid ramte 401 foer 429 kunne naas — rate-limit-tjek flyttet foerst. #9 Telnet kunne DoS'es permanent (`TELNET_READ_TIMEOUT_MS=0`, kun ét klient-slot) — sat til 30 min, samme filosofi som BUG-353s glidende session-token-timeout. #10 stack buffer overflow i `set coil/holding-reg DYNAMIC` (`strncpy` uden loft mod 32-byte buffer) — clampet i begge identiske forekomster (cli_config_coils.cpp, cli_config_regs.cpp). #12 `/api/alarms` (GET) manglede auth helt, lækkede IP/brugernavne fra fejlede logins — `CHECK_AUTH` tilfoejet. #16 off-by-one i `set persist group add` (ubounded `strcat`) — samme bounded `strncat`-moenster som naboloekken. **Dokumenteret som bevidst fravalg (ingen kodeaendring):** #14 (RTU-bussens fysiske lag har i sagens natur ingen adgangskontrol) og #11 (ST-scripts' uindskraenkede Modbus-slave-adgang — en angriber med skriverettighed til at uploade ST-programmer har allerede tilsvarende adgang via `mb write` i CLI'en, saa en allowlist ville ikke reelt begraense truslen) — begge forklaret i `docs/manual/06_Modbus_Interface.md` §6.2. **Bevidst udskudt:** #13 SSE understoetter aldrig TLS — sse_events.cpp er en haandrullet raw-TCP-socket-server, IKKE bygget paa ESP-IDFs `esp_http_server`/`httpd_ssl`, saa TLS kraever manuel mbedtls_ssl-integration i en allerede kompleks, langtlevende per-klient-task-loekke — for risikabelt at presse ind sammen med 8 andre samtidige aendringer paa en produktionsenhed, udskudt til en dedikeret, separat omgang. #15 (cleartext credentials) var allerede delvist fixet af BUG-352 — SECURITY_INDEX.md's post opdateret til at afspejle det (kun WiFi/Telnet stadig klartekst, af dokumenterede grunde). SECURITY_INDEX.md's status-tabel og "Aabne punkter"-sektion opdateret til at afspejle det hele (cli_config_coils.cpp, cli_config_regs.cpp, cli_commands.cpp, api_handlers.cpp, ota_handler.cpp, include/constants.h, docs/manual, SECURITY_INDEX.md) |

| FEAT-158 | Alarm-bjaelken (dashboard-top) kan nu afstemmes — forsvandt foer ALDRIG af sig selv | ✅ DONE | 🟡 HIGH | v7.9.10.13 | Brugeroensket: "ALARM: Modbus Master timeouts: 384 \| HTTP auth failures: 194" stod fast i toppen uden nogen maade at rydde den. Rodaarsag: denne bjaelke (`checkAlarms()`) er IKKE det samme som "Alarm Historik"-kortet — den genberegnes client-side fra RAA, monotont stigende Prometheus-taellere (fejl-COUNTS, ikke events) ved hver 3-sekunders poll, saa den forsvandt aldrig af sig selv efter en taeller foerst havde krydset sin graense, kun ved reboot (som nulstiller taellerne). FIX: ny "Afstem ✕"-knap i bjaelken gemmer et snapshot af den AKTUELLE vaerdi pr. alarmtype i `sessionStorage`; bjaelken forbliver skjult indtil vaerdien reelt bliver VAERRE end snapshottet (ikke bare stadig over graensen) — retningen afhaenger af alarmtypen (`lowerIsWorse`: heap-alarmer er vaerre ved LAVERE vaerdi, faelt-taellere er vaerre ved HOEJERE). Ingen backend-aendring — rent client-side, samme moenster som andre UI-praeferencer i dashboardet (web/dashboard.html) |

| FEAT-159 | Kort-bredde (bred/normal) gemmes nu pr. skaermstoerrelse — mobil og desktop deler ikke laengere samme "faste ramme" | 🔄 SUPERSEDED af FEAT-160 | 🟠 MEDIUM | v7.9.10.13 | Brugeroensket: en layout tunet paa desktop blev tvunget ned over mobilvisningen (og omvendt), da `hfplc_card_sizes` (localStorage) var ÉN faelles noegle uanset skaermstoerrelse. FIX (denne version): noeglen fik et oploesnings-suffiks. **Erstattet allerede i NAESTE runde (FEAT-160)** af et rigere fri-2D-placerings-system, samme opløsnings-scoping-idé, men uden det binaere bred/normal-loft |
| FEAT-160 | Fri 2D kort-placering ("ramme-system") — kort kan flyttes og resizes frit i et 12-kolonners grid, ikke kun bred/normal | ✅ DONE | 🟡 HIGH | v7.9.10.14 | Brugeroensket, praeciseret via spoergsmaal: "Fri 2D-placering (dashboard-builder)", som Grafana/iPad-hjemmeskaerm — erstatter FEAT-159s binaere bred/normal-toggle. Hvert kort faar en gemt `{x,y,w,h}` (x=kolonne 0-11, w=kolonne-span, y=raekke-start, h=raekke-span) sat som inline `grid-column`/`grid-row` i et fast 12-kolonners grid — browserens auto-placering (tidligere `repeat(auto-fill,minmax(340px,1fr))`, masonry-agtig content-hoejde-drevet) bruges ikke laengere paa desktop/tablet. **Flyt**: museknap-baseret (ikke native HTML5 drag-and-drop — giver praecis, live pixel-til-grid-celle-feedback), samme ☰-haandtag som foer. **Resize**: nyt haandtag i nederste hoejre hjoerne. **Kollision, bevidst simpel** (ingen Gridstack-agtig kaskade-compaction): flytter man oven i et andet kort, BYTTER de plads; resizer man ind i et nabokort, skubbes DET kort ned. Ingen ekstern grid-biblotek (Gridstack e.l.) — enheden er en LAN-only/potentielt offline industriel controller (CDN ikke en mulighed) og flash er allerede i 84%, saa haandrullet vanilla JS. Persisteres i localStorage pr. opløsnings-klasse (`hfplc_grid_layout_<mobile\|tablet\|desktop>`, genbruger FEAT-159s `_sizeBreakpoint()`). Foerste besoeg (eller et nyt kort uden gemt position) faar et beregnet standard-layout (shortest-column 2-spalte bin-packing efter reelt renderet hoejde — `card-wide`-klassen, stadig statisk i HTML'en paa mbactivity/alarms/syslog, bruges nu kun som et data-hint om fuld-bredde-default, ikke som en CSS-regel laengere). **Race-condition fanget og rettet under implementeringen**: standard-positioner blev foerst beregnet FOeR `restoreCardOrder()`s NVS-hentning af skjulte kort/fane-tildeling var landet — en for-tidlig beregning (baseret paa midlertidigt forkert synlighed) kunne laase sig fast som en PERMANENT forkert gemt standard-layout, fordi allerede-tildelte positioner ikke genberegnes. FIX: nyt flag `_cardVisibilityReady` (sat af `restoreCardOrder()` naar NVS-svaret er landet, success ELLER fejl) — standard-positioner beregnes og GEMMES kun naar flaget er sandt; foer da vises kort uden gemt position blot midlertidigt via browserens auto-placering (selvkorrigerende, ufarligt). Mobil (≤768px) uaendret: CSS tvinger auto-flow enkelt-kolonne via `!important` (inline grid-column/row vinder ellers altid over media queries), traek-/resize-haandtag skjules. Nyt "Nulstil layout"-knap paa Indstillinger-siden (pr. skaermstoerrelse) — samme sikkerhedsventil-princip som resten af sessionen. Server-side `card_order`/`card_tabs`/`card_hidden` (NVS) UAeNDRET — styrer stadig synlighed/fane, ikke placering (web/dashboard.html, docs/manual) |

| BUG-356 | **REGRESSION fra FEAT-160:** kort med indbygget scroll-flade (Modbus Aktivitetslog, Alarm Historik, Hændelseslog, Modbus manuel Read/Write) mistede scroll-funktion — standard-hoejden blev laast for lille foer data naaede at loade | ✅ FIXED | 🟡 HIGH | v7.9.10.15 | Brugeren rapporterede: "de badge som man kunne scroole i før har miste funktion nu" + risiko for at saette et kort for lille til sit eget indhold, uden nogen maade at se det. **Rodaarsag**: fire kort har en indre `<div style="max-height:320px (eller 100px);overflow-y:auto">`-flade (mbActivityBody, alarmBody, syslogBody, mbResult). FoeR FEAT-160 voksede det YDRE kort automatisk til at passe indholdet (`autoSizeCards()`, fjernet i FEAT-160). NU har kortet en FAST, brugerstyret hoejde — og standard-hoejden (`computeDefaultLayout()`) blev beregnet ud fra `card.scrollHeight` PAA DET TIDSPUNKT siden loadede, hvor disse tabeller typisk viser en kort placeholder ("Ingen aktivitet endnu"), ikke deres fulde 320px-flade. Naar de rigtige data ankom bagefter (async fetch), var kortets faste hoejde allerede laast for lille — den indre scrollflade blev klippet af det for-lille ydre kort. FIX: ny `naturalCardHeightPx()`/`naturalCardRows()`-hjaelper — finder en indre `[style*="max-height"]`-flade og erstatter dens VARIABLE del (nuvaerende `scrollHeight`, som afhaenger af hvor meget data der er) med dens FASTE loft (`max-height`), lagt sammen med resten af kortets maalte "overhead" (header, filterbar, padding) — giver et paalideligt "hvis fladen var fyldt op"-maal, uanset hvornaar det beregnes. Bruges tre steder: (1) `computeDefaultLayout()` for nye kort, (2) `initCardResize()` som en HAARD GULV brugeren ikke kan resize under (med live visuel feedback — kortets ramme bliver roed naar man raemmer minimum), (3) en ny "selvhelbred"-passage i `applyGridLayout()` der bumper allerede-gemte, for-smaa layouts op (retter ogsaa dem der naaede at blive gemt forkert under v1 af FEAT-160, uden at brugeren skal bruge "Nulstil layout"). Ny CSS-backstop: `.card{overflow:auto}` (scrollbar i stedet for overlap mod nabokort, hvis noget alligevel gaar galt). Denne generelle, selektor-baserede tilgang (`querySelector('[style*="max-height"]')`) fangede ogsaa Modbus Master-kortets `#mbResult`-boks, som ikke var specifikt identificeret under analysen, men reelt havde samme svaghed (web/dashboard.html) |

| FEAT-162 | Kort i monitor kan nu flyttes ved venstreklik VILKAARLIGT paa kortet — ikke kun i ☰-haandtaget | ✅ DONE | 🟢 LOW | v7.9.10.16 | Brugeroensket: "hvis man venstre klikker på en badge et vikkåligt sted så flytter man på den". `initCardDrag()` lytter nu ogsaa paa `mousedown` for HELE kortet, ikke kun `.drag-handle`-elementet — men kun naar klikket ikke rammer et element der selv skal reagere paa klik: knapper, felter, `<select>`, `<textarea>`, links, resize-haandtaget eller ☰-haandtaget (som allerede har sin egen listener, ellers ville et klik paa den starte to samtidige flyt-forloeb). Ny helper `_dragBlockedTarget()` bruger `element.closest(...)` til at teste dette. ☰-haandtaget virker uaendret som foer (web/dashboard.html) |
| BUG-357 | Digital I/O-kortet kunne slet ikke flyttes — manglede ☰-traek-haandtaget, som eneste kort paa siden | ✅ FIXED | 🟡 HIGH | v7.9.10.16 | Fundet ifm. FEAT-162: brugeren rapporterede at "Digital IO" ikke kunne flyttes og manglede "venstre hjoerne knap". **Rodaarsag**: kortets aabne-tag manglede simpelthen den afsluttende `>` — `<div class="card" data-card-id="dio" data-tab="app" id="cardDio"` fortsatte direkte ind i `<h2>Digital I/O</h2>` paa naeste linje uden lukning, saa browserens HTML-parser aldrig dannede et rigtigt `<h2>`-element for dette kort. `initDragDrop()`s `card.querySelector('h2')`-opslag (som er stedet ☰-haandtaget indsaettes) fandt derfor intet — og da hele kortets indhold ellers udelukkende bestaar af til/fra-knapper (nu korrekt udelukket fra FEAT-162s klik-for-at-flytte), var der intet sted tilbage overhovedet at starte et traek fra. FIX: tilfoejet den manglende `>` (web/dashboard.html) |

| BUG-358 | NTP-badge (og potentielt Alarm/Syslog/Firmware/Watchdog-status) viste forkert i dashboardet ("Deaktiveret") selvom CLI korrekt viste NTP som aktiveret | ✅ FIXED | 🟡 HIGH | v7.9.10.17 | Brugeren rapporterede: "NTP badger viser ikke status korekt, stå som deaktiveret i gui når den stå som aktiv i CLI". **Rodaarsag**: `/api/metrics` (Prometheus-tekst, poll'et hvert 3. sek. af dashboardet) skriver til en FAST 12KB buffer via `PROM_APPEND`-makroen, som stille DROPPER enhver linje der ikke er plads til — uden fejl, uden log. De to register-dump-loekker (`modbus_holding_register`/`modbus_input_register`, 256+256 registre) laa placeret FOeR NTP/alarm/syslog/firmware-metrics i funktionen, og kan alene fylde op mod ~21KB hvis alle registre er non-zero (helt normalt paa et aktivt system med mange taellere/registre i brug) — hvilket fyldte bufferen op INDEN den naaede frem til at skrive `ntp_enabled`/`ntp_synced`, saa dashboardet parsede disse som fravaerende (=0/false) og viste "Deaktiveret", mens CLI's `show ntp` laeser `g_persist_config.ntp.enabled` direkte og altid var korrekt. Samme bug-klasse som BUG-336c/BUG-354 (fast graense uden margin, overskredet stille). FIX: (1) bufferen oeget til 32KB (reel margin over det teoretiske ~21KB+ vaerste-fald), (2) register-dump-loekkerne flyttet til SIDST i funktionen, saa alle smaa/faste status-metrics (som dashboardets badges bruger) altid skrives foerst og er sikre uanset hvor mange registre der er non-zero — kun selve den ubegraensede raa-register-liste (bruges ikke af dashboardet, kun eksterne Prometheus/Grafana-forbrugere) er nu i farezonen ved ekstreme registerantal. Kommentar tilfoejet ved buffer-allokeringen og ved det flyttede register-dump, med instruks om ALDRIG at tilfoeje nye ubegraensede loekker foer register-dumpet (src/api_handlers.cpp) |

| FEAT-163 | Modbus Slave/Master og RTU Trafik-badges viser nu reelt req/s (ikke antaget "/3s") samt min/max over den synlige historik | ✅ DONE | 🟢 LOW | v7.9.10.18 | Brugeroensket: "i alle badge hvor vi har en form for historis gengivelse af request rate skal vi kunne se hvad de faktiske min og max er samttidig hvad faktoele rate er" + "i Modbus RTU trafik badge skal vi have aendret den der /3Sek til hvad er rate i sekundet i steddet for". **Rodaarsag for "/3s"**: RTU Trafik-kortet viste raa delta-per-poll (antog stiltiende et fast 3-sekunders poll-interval), som reelt varierer (3s/5s afhaengig af tilstand, plus browser-throttling af baggrundsfaner) — saa tallet var ikke en sand rate. FIX: ny faelles `_lastPollTs`/`_dtSec` i `updateDashboard()` maaler det FAKTISKE forloebne tidsrum siden sidste poll (`Date.now()`-diff, ikke en antagelse), og alle fire request-rate-historikker (Modbus Slave "Requests", Modbus Master "Requests", RTU Slave, RTU Master) gemmer nu req/s-vaerdier (delta/dtSec) i `history.*`-arrayet i stedet for raa delta-per-poll — goer dem sammenlignelige paa tvaers af varierende poll-intervaller. Ny `fmtRate()`/`histMinMax()`-hjaelper viser den aktuelle rate ("X.X/s") plus "min X.X/s / max X.X/s" over den synlige 120-samples sparkline-historik, under hver af de fire sparklines. RTU-kortets rae-tekster aendret fra "Slave/Master req/3s" til "Slave/Master rate". Ingen backend-aendring — rent client-side (web/dashboard.html) |

| FEAT-164 | ST Logic Editor har nu en "Settings"-sektion med de globale motor-indstillinger der foer kun fandtes i CLI (eksekveringsinterval + motor til/fra) | ✅ DONE | 🟢 LOW | v7.9.10.19 | Brugeroensket: "i ST editor skal vi have en Settings seksion med alle de config settings som vi har i cli som er relevante at kunne justere på i GUI". Gennemgang af `set logic ...`-kommandoerne viste at alt PER-PROGRAM allerede er i GUI'en (upload/kompilér/enable/reinit/slet via vaerktoejslinjen, bindings via Bindings-fanen, debug pause/step/breakpoints via Monitor-fanen) — kun de TO GLOBALE motor-indstillinger (`set logic interval:X`, gaelder alle 4 programmer) og motorens til/fra-modul-flag (allerede tilgaengeligt for andre moduler via `/api/modules`, men ikke for st_logic i noget GUI) manglede. Ny fjerde view-knap "Settings" (ved siden af Bindings/Monitor) viser: (1) checkbox for motor-enable (POST /api/modules, `st_logic`-feltet), (2) dropdown for interval 2/5/10/20/25/50/75/100ms (POST /api/logic/settings, `interval_ms`) — begge endpoints eksisterede allerede (bruges internt af `/api/config`), saa dette er ren GUI-forbrug, ingen backend-aendring. Motorens interne trace-debug (`set logic debug:true|false`, bytecode-udskrift til seriel/telnet) er bevidst UDELADT — ingen effekt synlig i web-GUI'et, kun paa en tilkoblet CLI-session (web/editor.html, docs/manual) |
| — | Editor-knappen flyttet op ved siden af de 4 program-faner (Logic1-4), i stedet for nede i vaerktoejslinjen sammen med Bindings/Monitor/Settings | ✅ DONE | 🟢 LOW | v7.9.10.19 | Brugeroensket UI-justering: "editor knap skal flyttes op ved siden af de 4 ST logic knapper og være samme størrelse som dem som min." Knappen bevarer sin eksisterende `vbtn`-klasse (saa `setView()`s aktiv-tilstand-styring virker uaendret uanset placering i DOM'et) men faar en ny `.vbtn-editor`-modifikator der matcher `.tab`s stoerre padding/font-size, saa den visuelt passer ind i faneraekken den nu sidder i (web/editor.html) |
| FEAT-165 | Ny "⊞ Auto-organisér"-knap direkte i Metrics-fanelinjen (Monitor-siden) — samme funktion som "Nulstil layout" paa Indstillinger-siden, men tilgaengelig uden at skifte side | ✅ DONE | 🟢 LOW | v7.9.10.19 | Brugeroensket: "der skal være en auto orginations knap i monitor vindue man kan bruge til at få basel orginiseret indhold i vindue til en start". Kalder den eksisterende `resetGridLayout()` (samme som Indstillinger-siden knappen) — ingen ny logik, blot en mere synlig indgang til en allerede eksisterende funktion, med samme bekraeftelses-dialog (web/dashboard.html) |
| BUG-359 | Kort-flyt/resize var svaert at styre praecist, fordi kortet kunne "snappe" tilbage til sin gamle position midt i traekket naar en periodisk dataopdatering landede | ✅ FIXED | 🟡 HIGH | v7.9.10.19 | Brugeren rapporterede: "det er et stort problem at navigere rundt med badge i monitor vindue når de samtiddig vil opdatere på indhold". **Rodaarsag**: `applyGridLayout()` genanvender ALLE kort-positioner fra `gridLayout`-state efter hver `fetchMetrics()`-poll (hvert 3. sek.) — men det kort man er ved at traekke/resize har kun sin NYE position i `card._pendingPos`/`_pendingSize` (midlertidig, indtil mouseup), saa en poll der lander midt i traekket overskrev kortets live drag-position med dets GAMLE, gemte position, og andre kort kunne samtidig hoppe i indhold/stoerrelse fra de oevrige periodiske opdateringer (alarmer, aktivitetslog, syslog, forbindelser, bindings). FIX: ny global `_dragOrResizeActive`-flag saettes ved mousedown paa enten drag- eller resize-haandtaget og ryddes ved mouseup — alle 7 periodiske fetch-funktioner (`fetchMetrics`, `fetchAlarms`, `fetchMbActivity`, `fetchSyslog`, `fetchConnections`, `fetchNetworkInfo`, `fetchBindings`) springer stille over deres poll mens flaget er sandt og genoptager automatisk ved naeste normale interval efter mouseup (web/dashboard.html, docs/manual) |

| BUG-360 | Flere kort voksede ukontrolleret store i monitor-vinduet — nogle op til 10x vinduets egen størrelse | ✅ FIXED | 🔴 CRITICAL | v7.9.10.20 | Brugeren rapporterede: "flere af badge fyler alt for meget i monitor vindue, noget fyller 10 gang så maget plads som vindue er stort". **Rodaarsag, en runaway-feedback-fejl**: `.card` er et grid-item og straekkes (default `align-items:stretch`, ingen override i `.grid`) til at fylde sin tildelte `grid-row`-span — saa `card.scrollHeight` afspejler den AKTUELT STRAKTE hoejde, ikke indholdets reelle behov, saa snart et kort er blot en smule hoejere end noedvendigt. `naturalCardHeightPx()`s formel (`card.scrollHeight - scrollable.scrollHeight`, indfoert i BUG-356) brugte netop dette upaalidelige tal. `applyGridLayout()`s "selvhelbred"-passage (FEAT-161) koerte denne maaling efter HVER metrics-poll (~3 sek.) og bumper kun opad — en let opblaest maaling blev permanent gemt, hvilket gjorde NAESTE maaling endnu mere opblaest: en kompoundérende runaway uden loft. FIX (to lag): (1) `naturalCardHeightPx()` maaler nu "overhead" via `scrollable.offsetTop` (position relativt til `.card`, som er `position:relative`) — ren dokument-flow-position, upaavirket af hvor strakt selve grid-cellen p.t. er; (2) selvhelbred-passet gates nu til at koere KUN ÉN GANG pr. side-indlaesning (nyt flag `_selfHealDone`) i stedet for efter hver poll — det var altid kun tiltaenkt som en engangs-reparation af gamle, forkert gemte layouts (BUG-356), ikke en loebende korrektion, saa selv en fremtidig upraecis maaling kan ikke laengere kompoundere. Samme selvhelbred-passage reparerer nu OGSAA allerede-opblaeste kort fra selve runaway-fejlen (hvis gemt `h` > 4x det reelt noedvendige, sættes den ned til det noedvendige) — bevidst med generøs margin (4x), saa et kort en bruger selv har gjort stort af visuelle grunde IKKE tvinges tilbage (web/dashboard.html) |

| FEAT-166 | System-siden fik en fuld settings-audit: ny "Brugerstyring" (RBAC opret/redigér/slet + til/fra) og GUI for 9 tidligere CLI/REST-only konfigurationsgrupper | ✅ DONE | 🟡 HIGH | v7.9.10.21 | Brugeroensket: "få alle relevant settings med system seksion også og lave en seksion til users styrring samt opret af users". Samme princip som FEAT-164 (ST Editor Settings): gennemgik CLI'ens `set ...`-kommandotraeer og fandt hvad der reelt var konfigurerbart, men KUN via CLI. **Ny backend** (eneste del der ikke allerede havde et REST-endpoint): `GET/POST /api/rbac` (RBAC til/fra + brugerliste UDEN password/hash/salt) og `POST /api/rbac/users`/`DELETE /api/rbac/users/{navn}` (opret/opdatér/slet), alle bag `CHECK_AUTH_WRITE` — bevidst SAMME graense som CLI'ens `rbac_cli_allowed()` allerede har i dag (en CLI-rolle+skriverettigheds-bruger kan allerede eskalere/oprette admin-konti via `set user`), dokumenteret i SECURITY_INDEX.md #18 som en parity-beslutning, ikke et nyt hul. Genbruger eksisterende `rbac_set_user()`/`rbac_delete_user()`/`rbac_parse_roles()`/`rbac_parse_privilege()`/`rbac_roles_to_str()` (rbac.cpp) — ingen ny parse-logik. **Resten var ren GUI-forbrug af allerede-fungerende endpoints**: WiFi (`/api/wifi`), Ethernet (`/api/ethernet`), Hostname (`/api/hostname`), Modbus Slave+Master seriel-config (`/api/modbus/slave`+`/master`), HTTP Legacy Auth (`/api/http`, plus `http.username` tilfoejet til den eksisterende `/api/config`-dump da den kun manglede dét ene felt), Telnet (`/api/telnet`), NTP (`/api/ntp`), Analog I/O-kalibrering (`/api/analog`, kun ES32D26 — kortet skjuler sig selv ved 404 paa andre boards). Ingen af disse fik nye endpoints, kun nye HTML-formularer der GET'er ved side-load og POST'er ved "Gem". **Bevidst UDENFOR scope** (ville kraeve ny, endnu ikke designet backend): SSE-config (`set sse`), Rate-limit-config (`set rate-limit`), fuld GPIO-mapping-redigering — ingen af disse har noget REST-endpoint i dag. Watchdog har ingen konfigurerbare settings overhovedet (kun status), saa intet at tilfoeje der. Ryddet to forældede sikkerheds-doc-passager undervejs (OTA-auth-forbehold i `11_Backup_Restore_og_Firmware.md` var stadig beskrevet som uafhjulpet selvom BUG-355 fixede det; endpoint-taeller i `B_REST_API_Reference.md` var 94 mod det faktiske tal) (include/api_handlers.h, src/api_handlers.cpp, src/http_server.cpp, web/system.html, docs/manual, SECURITY_INDEX.md) |

| BUG-360b | "Auto-organisér" fixede stadig ikke allerede-opblæste kort, fordi målingen genbrugte den samme upålidelige `card.scrollHeight` for alle kort UDEN en scrollflade | ✅ FIXED | 🔴 CRITICAL | v7.9.10.22 | Brugeren rapporterede: "vi har samme problem med at nogen monitor vindue badge er alt for store og at auto organiser ikke løser der problem". Rodårsagen fra BUG-360 var kun halvt rettet: `naturalCardHeightPx()`s FøRSTE fix (v7.9.10.20) brugte `scrollable.offsetTop` — men KUN for de fire kort med en indre scrollflade. Alle andre kort (System, Netværk, Modbus Slave/Master, Tællere osv.) faldt stadig tilbage til `return card.scrollHeight`, som er PRÆCIS lige så upålideligt (samme grid-stretch-problem) — så "Auto-organisér" (som nulstiller og genberegner alle positioner) målte det ALLEREDE opblæste kort og fik samme opblæste tal igen. FIX: ny, generel metode — fjerner MIDLERTIDIGT kortets grid-strækning (`card.style.alignSelf='start'`) lige før måling, saa `scrollHeight` altid afspejler den REELLE, unstrakte indholdshøjde uanset kortets nuværende størrelse eller hvor dybt en evt. scrollflade er nestet, gendanner straks bagefter. Dækker nu ALLE kort ensartet, ikke kun de fire med scrollflade — og "Auto-organisér" reducerer nu korrekt et opblæst kort ned til nøjagtigt det antal rækkeenheder dets indhold reelt kræver (afrundet op til naermeste hele række-enhed, ikke mere) (web/dashboard.html) |
| BUG-361 | Trend-graf-badges (sparklines) skalerede ikke med, når man gjorde et kort bredere — grafen blev i en fast lille kasse i venstre side | ✅ FIXED | 🟠 MEDIUM | v7.9.10.22 | Brugeren rapporterede: "alle de badge kort som har noget med en trend graf de scanere ikke op sammen når man gør badge kort breder". **Rodårsag**: `<svg width="200" height="24">` uden `viewBox` giver SVG'en en FAST intrinsic størrelse — CSS/layout kan ikke stroekke selve tegne-koordinatsystemet, kun elementets boks (som der heller ikke var nogen CSS-regel til at goere responsiv i forvejen). FIX: tilfoejet `viewBox="0 0 200 24" preserveAspectRatio="none"` til alle 7 sparkline-SVG'er (Heap/RSSI/Frag-trend, Modbus Slave/Master Requests, RTU Slave/Master rate) plus en ny CSS-regel (`.spark-row svg{flex:1;width:100%;...}`) der lader SVG-ELEMENTETS boks fylde resten af raekken — viewBox soerger saa for at `drawSparkline()`s eksisterende 200x24-koordinat-tegning automatisk skalerer til at fylde den nye, variable boks-stoerrelse. Ingen JS-aendring noedvendig. Fangede samtidig en beslaegtet, hidtil upaagtet fejl: Analog I/O-tabellens mini-sparklines (`width="80" height="20"`, ogsaa uden viewBox) blev reelt beskaaret/klippet af (tegnet i 0-200/0-24-koordinater ind i en alt for lille 80x20-kasse) — fik samme viewBox-fix (web/dashboard.html) |

| FEAT-167 | Kort-positioner er nu fane-scoped (ikke laengere delt paa tvaers af Alle/Overblik/Modbus/Forbindelser/Applikation) + ny uafhaengig "Custom"-fane med selvvalgte kort | ✅ DONE | 🟡 HIGH | v7.9.10.23 | Brugeren rapporterede: "badge på monitor vindue deler alle sammen posision på alle kort vinduer" og bad om "en 'curstom' fane hvor man selv kan bestemme hvad for badge som skal være på". **Del 1 (fane-scoped positioner):** `_gridLayoutKey()` inkluderer nu `_activeSubTab` (`hfplc_grid_layout_<opløsning>_<fane>` i stedet for kun `<opløsning>`) — flytter man et kort mens "Modbus"-fanen er aktiv, paavirker det ikke laengere kortets position paa "Alle" eller "Overblik". `gridLayout` genindlaeses ved hvert fane-skift (`showSubTab()`), ellers uaendrede funktioner (`applyGridLayout`/`findCardAt`/drag/resize kender kun til "det aktuelle gridLayout-objekt", ikke hvordan noeglen er sat sammen). Selvhelbred-passet (BUG-360) spores nu pr. noegle (`_selfHealedKeys`, tidligere ét globalt flag), saa hver fane stadig faar sin egen engangs-heling foerste gang den besoeges. Ingen automatisk sammenkobling af det gamle, fane-loese layout — hver fane starter forfra, MED ÉN undtagelse: "Alle"-fanen seedes én gang fra den gamle noegle hvis den findes, saa det man allerede har indrettet ikke bare forsvinder. Sidegevinst: "Auto-organisér" nulstiller nu naturligt kun den fane man rent faktisk kigger paa. **Del 2 (Custom-fane):** ny, HELT UAFHAENGIG medlemsskabsliste (`_customMembers`, adskilt fra det eksisterende ene-fane-pr-kort `_customTabs`) — et kort tilfoejet til Custom via Indstillinger-sidens nye checkbox-kolonne ("Vis i Custom") BEVARER sin normale fane-tilhoerighed, det forsvinder ikke derfra (bekraeftet valg via spoergsmaal: "vises begge steder"). Ny sub-tab-knap "Custom" + `applySubTab()`-gren der filtrerer paa `_customMembers` i stedet for `getCardTab()`. **Ny backend**: `dashboard_card_custom[80]` tilfoejet til `PersistConfig` (helt til sidst, foer `crc16`, efter `http_legacy_salt` — samme velkendte byte-offset-sikre placering som analog-/salt-felterne), `CONFIG_SCHEMA_VERSION` 22→23 + migrationsblok (`src/config_load.cpp`), `GET/POST /api/dashboard/layout` udvidet med `card_custom`-feltet (samme "id,id,..."-format og noeglenspring som det eksisterende `card_hidden`) (include/types.h, include/constants.h, src/config_load.cpp, src/api_handlers.cpp, web/dashboard.html, docs/manual) |

| BUG-362a | ST Editor Settings: gem af eksekveringsinterval fejlede med "Invalid logic program ID" (så ud som en uvedkommende fejl for brugeren) | ✅ FIXED | 🟡 HIGH | v7.9.10.24 | Brugeren rapporterede fejlen `HTTP 400: {"error":"Invalid logic program ID","status":400}` ved forsøg på at gemme interval — først mistænkt for at være en "SPI-fejl", men uden relation til SPI/SPIFFS overhovedet. **Rodårsag**: klassisk wildcard-shadowing (samme bug-klasse som allerede kendt/håndteret for `/api/modbus/activity`) — `/api/logic/*` (wildcard, POST) registreres i `http_server.cpp` FØR den mere specifikke, exacte rute `/api/logic/settings` (POST), og ESP-IDF's httpd matcher registrerede URI'er i registrerings-rækkefølge. Enhver POST til `/api/logic/settings` landede derfor i `api_handler_logic_single()` (til `/api/logic/{id}`), som forsøgte at parse "settings" som et numerisk program-ID, fejlede, og returnerede fejlen. FIX: samme etablerede moenster som modbus/activity — `api_handler_logic_single()` tjekker nu eksplicit for `/settings`-suffikset og delegerer til `api_handler_logic_settings_post()` FØR ID-parsingen naas (src/api_handlers.cpp) |
| BUG-362b | ST Logic "motor aktiveret"-knappen (system.html/editor.html) havde ingen effekt — motoren blev ved med at køre efter deaktivering | ✅ FIXED | 🔴 CRITICAL | v7.9.10.24 | Brugeren rapporterede: "den her funktion har ikke nogen vikning, ST motor køre vider kan jeg se" — checkboxen viste "ST Logic-motor deaktiveret", men programmerne blev ved med at eksekvere. **Rodårsag**: `module_flags`-bitten (`MODULE_FLAG_ST_LOGIC_DISABLED`, sat via `POST /api/modules`) blev ALDRIG konsulteret noget sted — hverken ved boot eller runtime. Den faktiske eksekverings-løkke (`st_logic_engine_loop()`) tjekker en HELT ANDEN, separat runtime-variabel (`st_logic_get_state()->enabled`), som `st_logic_init()` ubetinget satte til 1 ved hver boot, uanset `module_flags`. Et forudgående, allerede-eksisterende hul (GAP-28 blev tilsyneladende aldrig færdiggjort for st_logic) — blot aldrig opdaget før nu, da der aldrig før har været en dedikeret GUI-kontrol for det. FIX (to steder, samme princip som `st_logic_interval_ms`s eksisterende "apply persisted config to runtime state"-mønster i `config_apply.cpp`): (1) `config_apply()` sætter nu `state->enabled` ud fra `module_flags` ved hver boot; (2) `api_handler_modules_post()` opdaterer OGSÅ den levende `state->enabled` med det samme når flaget ændres via REST, saa det virker uden reboot (samme "aendringer traeder i kraft straks"-princip som intervallet). **Bemærk**: `counters`- og `timers`-modul-flagene har det identiske, endnu ikke rettede hul (heller ingen konsument nogen steder i `counter_engine.cpp`/`timer_engine.cpp`) — ikke rettet her, da ingen GUI eksponerer dem endnu og brugerens rapport specifikt gjaldt ST Logic (src/config_apply.cpp, src/api_handlers.cpp) |

| BUG-363 | ST Editor Runtime Monitor: trend-kurver "skalerede om" (klemte sig sammen) hele tiden i stedet for at rulle ved en fast bredde, indtil historik-bufferen naaede sin fulde konfigurerede laengde | ✅ FIXED | 🟢 LOW | v7.9.10.25 | Brugeren rapporterede: "trend kuver [kan ikke] rulle fra højre til venste i det faste [antal] som er valgt i historik 30,60,120,240... trend vindue ikke som nu scanner[skalere] heletid til det rammer den settings for historik". **Rodårsag**: `sparkSVG()`s x-akse blev udregnet fra `t0`=det AeLDSTE punkts tidsstempel og `tRange`=spaend til det NYESTE — begge baseret på hvad der reelt var i bufferen LIGE NU, ikke den konfigurerede fulde laengde. Lige efter fane-skift/nulstilling (faa punkter, lille reelt tidsspaend) blev grafen dermed strakt ud over hele bredden alligevel, og for hvert nyt punkt der ankom, voksede `tRange`, hvilket klemte de eksisterende punkter taettere sammen — en synlig "omskalering" der først stoppede naar bufferen naaede sin fulde `HIST_LEN` og reelt begyndte at skubbe gamle punkter ud (`.shift()`). FIX: `tRange` er nu et FAST tidsvindue (`HIST_LEN*monInterval` — den fulde konfigurerede historik-laengde gange opdaterings-intervallet), og `t0` udregnes bagud derfra (`tEnd-tRange`), ikke fra det reelt aeldste punkt. Grafen ser dermed ud til at rulle fra hoejre mod venstre fra allerfoerste datapunkt — nye punkter fylder ind fra hoejre kant, resten af vinduet er tomt indtil der reelt er nok historik, i stedet for at hele grafen skaleres om for hver måling (web/editor.html) |

| — | ST Editor: "Settings"-knappen flyttet op ved siden af "Editor" og de 4 program-faner (Logic1-4), samt ensartet 2px-mellemrum sikret hele vejen igennem raekken | ✅ DONE | 🟢 LOW | v7.9.10.26 | Brugeroensket: "settings badge i ST fanen skal op ved siden af editor også da det er en global settings for alle ST logic programmer" — samme begrundelse som tidligere flytning af Editor-knappen: Settings gaelder alle 4 programmer globalt, i modsaetning til Bindings/Monitor som er specifikke for det valgte slot og derfor forbliver i vaerktoejslinjen. Desuden: "lave ligt afstand imellem logic1-4 knapper og editor knap" — `#tabs`-div'en (som JS fylder de 4 program-faner ind i) fik `display:contents`, saa dens boern bliver DIREKTE flex-boern af den ydre `.tabs`-container i stedet for at sidde i deres eget nestede flex-lag — garanterer samme `gap:2px` mellem ALLE knapper i raekken (Logic1-4, Editor, Settings), uden at kunne afvige pga. en nestet flex-kontainers egen box-model (web/editor.html) |

| FEAT-168 | ST Editor: individuel trend-farve pr. variabel (i stedet for kun én global farve for alle) | ✅ DONE | 🟢 LOW | v7.9.10.27 | Brugeroensket: "de farver man kan tilvælge i ST fane under monitor for trend kunne nu være fedt hvis de var muligt at sætte induviduelt på variablerne". Ny lille "Std/Blå/Grøn/.../Hvid"-dropdown direkte i hver variabels trend-celle (`varColorSelectHtml()`) — "Std" betyder "brug det globale standardvalg" (det eksisterende `monColor`-dropdown, nu omdoebt til "Standardfarve" for at afspejle den nye rolle som fallback). Valg gemmes i `localStorage` (`hfplc_st_trend_colors`), noeglet paa `SLOT+':'+variabelnavn` (samme variabelnavn kan optraede i flere program-slots med hver sin farve). `sparkSVG()` tager nu et eksplicit farve-argument i stedet for kun at laese den globale `trendColor` (web/editor.html) |
| — | ST Editor: Logic1-4-fanerne faar nu groen baggrund naar programmet reelt koerer (enabled+kompileret) og roed naar det ikke goer | ✅ DONE | 🟢 LOW | v7.9.10.27 | Brugeroensket: "farvel man har på logic1-4 knapperne må godt have en baggrund som er grøn for run og en en som er rød når logic block ikke køre". Ny `.tab-running`/`.tab-stopped`-klasse i `updateTabs()`, betingelse `p.enabled && p.compiled` — matcher praecist `st_logic_engine_loop()`s egen exekverings-betingelse (api_handlers.cpp), saa farven altid afspejler den reelle koerselstilstand. Den valgte/aabne fane (`.active`) beholder sin egen moerke baggrund uanset koerselstilstand (`.tab.active.tab-running`/`.tab.active.tab-stopped`-override), saa man stadig altid kan se HVILKEN fane man har aabent (web/editor.html) |

| FEAT-169 | GitHub Releases-baseret OTA-opdatering (manuelt "Tjek for opdatering"/"Installér" på system-siden) + fast release-publiceringsprocedure + system-siden opdelt i sektioner | ✅ DONE | 🟡 HIGH | v7.9.10.28 | Brugeroensket: "i system settings skal vi have organiseret ting lidt feder samt vi skal have opdatering muligt fra github under OTA, hvikket også betyder at vi skal have opsat en procedure for commit af fireware til github release version". Bekraeftet via spoergsmaal: kun MANUELT tjek (ingen baggrunds-polling — enheden kan vaere en LAN-only/offline industriel controller) og repoet forbliver OFFENTLIGT (intet gemt personal access token). **Ny netvaerks-kapabilitet**: enheden har ALDRIG foer optraadt som HTTP/HTTPS-KLIENT (kun server + Modbus RTU/TCP master/slave) — `grep` for `WiFiClientSecure`/`HTTPClient` i `src/` gav nul resultater foer denne aendring. Genbruger 1:1 den eksisterende `esp_ota_begin/write/end/set_boot_partition`-flash-logik fra `api_handler_ota_upload` (src/ota_handler.cpp) — kun byte-KILDEN skiftes fra en indkommende browser-upload til en udgaaende `HTTPClient`-stream. Nye endpoints `GET /api/system/ota/github-check` (slaar op mod `api.github.com/repos/.../releases/latest`, sammenligner tag mod PROJECT_VERSION) og `POST /api/system/ota/github-install` (downloader+flasher det cachede resultat fra check-kaldet), begge bag samme `CHECK_AUTH_OTA` som eksisterende upload/rollback (skriverettighed kraevet). **TLS**: rigtige rod-CA'er bundlet (`certs/github_ca_bundle.pem`, embeddet via `board_build.embed_txtfiles` ligesom `certs/servercert.pem` allerede goer — USERTrust ECC CA + ISRG Root X1, begge selv-signerede rod-ankre med lang gyldighed, verificeret live via `openssl s_client -showcerts` mod alle tre involverede domaener), IKKE `setInsecure()` — se SECURITY_INDEX.md #19 for den fulde afvejning (dokumenteret, accepteret cert-rotations-risiko, manuel upload er altid en fungerende fallback). Flash-forbrug steg fra 84,9% til 87,9% (WiFiClientSecure/HTTPClient/mbedtls-TLS-klient-kode) — stadig komfortabel margin (~230KB). **Release-procedure**: nyt `scripts/publish_release.sh` (bygger, tjekker for ukommiterede aendringer, opretter+pusher git-tag `v<PROJECT_VERSION>`, publicerer via `gh release create` med asset navngivet PRAeCIS `firmware.bin` — matcher hvad `github-check` leder efter) + `docs/RELEASE_PROCEDURE.md` (hvornaar/hvordan). Ingen kildekode committet/pushet/publiceret som en del af DENNE opgave — kun mekanismen er bygget, den faktiske foerste udgivelse er en separat, brugerbekraeftet handling. **System-siden**: de nu ~20 kort grupperet under 7 tydelige sektionsoverskrifter (Netvaerk, Modbus, Sikkerhed & Brugere, Tid, Analog I/O, Backup & Firmware, Vedligeholdelse) via en ny `.section-hdr`-CSS-klasse — ren omrokering af eksisterende kort, ingen ny JS-tilstand (include/ota_handler.h, src/ota_handler.cpp, src/http_server.cpp, web/system.html, certs/github_ca_bundle.pem, scripts/publish_release.sh, docs/RELEASE_PROCEDURE.md, SECURITY_INDEX.md, docs/manual) |

| BUG-364 | GitHub-OTA "Tjek for opdatering" gav "NetworkError when attempting to fetch resource" i browseren — reelt et stack overflow-crash/reboot på enheden | ✅ FIXED | 🔴 CRITICAL | v7.9.10.29 | Brugeren rapporterede: "Kunne ikke tjekke GitHub: NetworkError when attempting to fetch resource" — en raa browser-fetch-fejl (forbindelsen faldt, ikke et rent HTTP-fejlsvar), hvilket peger paa at ESP32'en selv crashede/genstartede midt i requesten, ikke en almindelig netvaerks-/DNS-fejl (som `api_handler_ota_github_check()` allerede haandterer paent med et rigtigt JSON-fejlsvar). **Rodårsag**: FEAT-169s GitHub-OTA-handlers koerer et KLIENT-side TLS-handshake (`WiFiClientSecure`/`HTTPClient`/mbedtls) INDE FRA den almindelige HTTP-API-servers httpd worker-task, som kun havde `stack_size=8192` (8 KB). Denne PRAeCISE stoerrelse er allerede kendt utilstraekkelig i dette projekt — `https_wrapper_start()` (server-side TLS, samme fil) bruger allerede 10240 med kommentaren "TLS handshake needs ~8-10KB", saa 8192 var i forvejen UNDER det behov, foer man laegger selve handlerens egne lokale variabler (JsonDocument, buffere) oveni. Stack overflow her korrumperer hukommelse og giver et reboot, som i browseren viser sig som netop denne generiske netvaerksfejl i stedet for en brugbar fejlbesked. FIX: `httpd_config.stack_size` for den almindelige HTTP-server saat op fra 8192 til 16384 — reel margin ud over de kendte ~10KB, samme "bump med reel margin"-princip som BUG-354 (max_uri_handlers) (src/http_server.cpp) |

| BUG-365 | "Gem indstillinger" på Indstillinger-siden gemte reelt aldrig Custom-fane-valg hvis mere end nogle få kort blev markeret — og succesbeskeden gjorde det uklart om NVS-persistering krævede et separat "Save" | ✅ FIXED | 🟡 HIGH | v7.9.10.30 | Brugeren rapporterede: "der er også fejl i gem funktion under monitor/metrics/custom en gem saver aldri og det er uklar om en gem her også gemmer i NVS gem mode". **Rodårsag**: `card_custom` blev gemt som en komma-separeret liste af FULDE kort-navne ("modbusslave,modbusmaster,mbactivity,rtutrafik,..."), som nemt kunne overstige det 80-byte NVS-felt (`dashboard_card_custom`, `include/types.h`) allerede ved 6-8 valgte kort — server-siden (`api_handler_dashboard_layout_post`, api_handlers.cpp) tjekker `strlen(custom) < sizeof(...)` og DROPPER stille opdateringen (uden fejlsvar) hvis for lang, saa browseren viste "Indstillinger gemt" selvom intet reelt blev gemt. FIX: kodningen er nu INDEKS-baseret ("0,4,7,12" i stedet for de fulde navne) — en ren client-side aendring (serveren behandler feltet som en opak streng, ingen backend-/schema-aendring noedvendig), som gør ALLE ~16-20 kort komfortabelt kan vaelges samtidig uden at ramme 80-byte-graensen. `_allCardIds`s raekkefoelge maa fremover kun UDVIDES (nye kort tilfoejes sidst), ellers peger gemte indekser paa forkerte kort. Samtidig gjort eksplicit i succesbeskeden at denne knap kun opdaterer RAM ("Indstillinger anvendt (RAM). Husk Save/Gem Config for at overleve en genstart") — matcher hvordan resten af systemets indstillinger allerede kommunikerer dette, og fjerner den rapporterede uklarhed (web/dashboard.html) |

| BUG-366 | GitHub-tjek/-install CRASHER enheden (bekræftet af bruger) — IKKE bekræftet løst, kun forbedret | 🟠 UNDER UNDERSØGELSE | 🔴 CRITICAL | v7.9.10.32 | v7.9.10.31 tilføjede link-tjek/timeouts/logging, men brugeren bekræftede at enheden reelt CRASHER (reboot) ved forsøg — ikke blot en netværksfejl. Mest sandsynlige årsag: `WiFiClientSecure`/mbedTLS-TLS-håndtryk kræver ét stort SAMMENHÆNGENDE stykke INTERN (ikke PSRAM) heap (typisk ~32KB+ til ESP-IDFs default in+out SSL-buffere), og enheden har `BOARD_HAS_PSRAM` aktiveret — store allokeringer omdirigeres normalt automatisk til ekstern SPI-RAM, men mbedTLS's TLS-buffere allokeres typisk STADIG fra intern heap. Er intern heap fragmenteret nok (sandsynligt efter lang oppetid med web/Modbus/ST Logic aktivt), kan mbedTLS's allokering fejle på en måde HTTPClient ikke fanger pænt, og crashe hele enheden i stedet. v7.9.10.32 tilføjer et eksplicit forhåndstjek (`heap_caps_get_largest_free_block(MALLOC_CAP_INTERNAL\|MALLOC_CAP_8BIT)` ≥ 48KB) FØR TLS-forsøget i begge GitHub-OTA-handlers — er der ikke nok sammenhængende intern hukommelse, fejles der PÆNT (HTTP 503) i stedet for at forsøge og risikere et crash. Logger også selve blok-størrelsen ved hvert forsøg. **Stadig ikke bekræftet fikset** — kræver enten et vellykket forsøg, eller konsol-output (seriel/telnet) fra et evt. fortsat crash for at kunne lukkes endeligt (src/ota_handler.cpp) |
| BUG-367 | GitHub-tjek blokerede HELE web-serveren permanent (bekræftet: ping virkede, browser fik intet svar til NOGET, ikke kun GitHub-endepunktet) | ✅ FIXED (ny arkitektur, ikke testet på hardware) | 🔴 CRITICAL | v7.9.10.33 | Root cause fundet: `esp_http_server` kører HER som ÉN eneste singletrådet task for al HTTP-trafik. `http.setConnectTimeout()`/`setTimeout()` begrænser kun TCP-connect/dataoverførsel — IKKE selve DNS-opslaget (lwIP `gethostbyname`/`getaddrinfo`), som kan blokere langt ud over enhver af de satte timeouts (fx uendeligt ved forkert/uopnåeligt DNS-server-IP efter et netværksskift — matcher at brugeren netop havde lavet WiFi-setup om igen). Blokerer DNS-opslaget, blokerer dermed HELE httpd-tasken for evigt — inkl. dashboard/API/alt andet HTTP, ikke kun GitHub-kaldet — hvilket forklarer at porten stadig "lyttede" (TCP-niveau upåvirket) men intet nogensinde svarede, mens ping (håndteret af netværksstakken selv, ikke af httpd-tasken) fortsat virkede. **Fix**: selve HTTPS-arbejdet (DNS+TLS+GET+parse/download) i begge GitHub-OTA-handlers er flyttet til en DEDIKERET FreeRTOS-baggrundstask (egen 16KB stak, pinnet til core 0); den oprindelige httpd-handler venter kun på den via en semaphore MED HÅRD ØVRE GRÆNSE (20s for check, 90s for install) — timer den ud, svarer handleren STRAKS med en fejlbesked og frigiver httpd-tasken, uanset om baggrundstasken stadig hænger. En evt. hængende baggrundstask forbliver isoleret (egen stak/ressource) og blokerer ikke længere andre requests. Kan ikke testes reelt uden hardware — kræver bekræftelse fra brugeren efter flash. **Akut handling ved allerede-hængt enhed**: et rent power-cycle/reboot (evt. via telnet/seriel CLI's `reboot`-kommando, som kører i en helt separat task og derfor forventes upåvirket) frigiver med det samme, uden at vente på denne fix (src/ota_handler.cpp) |
| BUG-368 | v7.9.10.33's BUG-367-fix LÆKKEDE selv permanent intern hukommelse pr. GitHub-forsøg — bekræftet af bruger via CLI-log fyldt med `mbedtls_ssl_setup returned -0x7F00`/`ESP_ERR_MBEDTLS_SSL_SETUP_FAILED` (enhedens EGEN HTTPS-server kunne ikke længere oprette TLS-sessions til browser-klienter) | ✅ FIXED | 🔴 CRITICAL | v7.9.10.34 | `vTaskDelete(NULL)` er en FreeRTOS-primitiv, IKKE et normalt C++ return — den frigør tasksens stak som en rå hukommelsesblok UDEN at køre C++-destruktorer for objekter der stadig er i scope. De to nye baggrundstasks fra BUG-367 (`github_check_worker`/`github_install_worker`) kaldte `vTaskDelete(NULL)` direkte fra funktioner der stadig havde en levende `WiFiClientSecure client`/`HTTPClient http` lokalt — mbedTLS's TLS-kontekst/buffere (typisk 20-40KB pr. forsøg) blev dermed ALDRIG frigivet, en permanent lækage for hvert eneste klik på "Tjek for opdatering"/"Installér". Efter blot et par forsøg var intern heap (PSRAM hjælper IKKE her — mbedTLS's SSL-buffere allokeres som udgangspunkt fra INTERN DRAM, ikke ekstern SPI-RAM, uanset at enheden har rigeligt PSRAM til rådighed) så opbrugt at enhedens egen HTTPS-server ikke længere kunne oprette nye TLS-sessions overhovedet. **Fix**: selve arbejdet er flyttet til separate `*_do_work()`-funktioner der returnerer NORMALT — `client`/`http`/`doc` destrueres dermed korrekt via almindelig C++ stack-unwinding FØR den ydre task-entry-funktion kalder `vTaskDelete()`. **Vigtigt**: allerede-lækket hukommelse fra forsøg med v7.9.10.33 frigives IKKE af sig selv — enheden skal genstartes (reboot) for at få den tilbage; det nye BUG-366-heap-forhåndstjek (≥48KB sammenhængende intern hukommelse) vil ellers blive ved med at afvise nye GitHub-forsøg indtil da (src/ota_handler.cpp) |
| BUG-369 | Enhedens EGEN HTTPS-server (port 443, browser-adgang) løber tør for intern heap i et selvforstærkende forløb, HELT UAFHÆNGIGT af GitHub-OTA — bekræftet af bruger: `show status` viste 132.392 bytes fri heap, 54 sekunder senere (efter en burst på 19 fejlede TLS-forsøg inden for <1s i CLI-loggen) kun 52.708 bytes (heap min 36.296 bytes). Reproduceret IGEN på en frisk boot: 132.356 bytes lige efter boot → heap min 35.800 bytes blot ~3½ minut senere, EFTER at have tilgået dashboardet (ingen GitHub-OTA involveret) | ✅ FIXED (reel udløser fundet og fjernet) | 🔴 CRITICAL | v7.9.10.36 | Root cause for SELVE lækagen: ESP-IDF's egen `esp_https_server`/`esp-tls`-komponent (https_wrapper.c — bevidst valgt fremfor custom open_fn/close_fn netop for at undgå denne slags heap-problemer, se filens egen kommentar) rydder tilsyneladende ikke fuldt op når `mbedtls_ssl_setup()` fejler pga. lav heap — det er en del af det vendorede Arduino-ESP32-framework og kan ikke rettes direkte i projektets kode. Men **den reelle UDLØSER er fundet og ER rettet**: `web/dashboard.html`s `init()` fyrede historisk 7 fetch-kald (`fetchBindings/fetchMetrics/fetchAlarms/fetchMbActivity/fetchSyslog/fetchNetworkInfo/fetchConnections`) plus `updateUserBadge()` plus `sseConnect()`s eget statustjek — op til 9 SAMTIDIGE HTTPS-forbindelsesforsøg ved hver eneste side-indlæsning/genindlæsning — mod en HTTPS-server konfigureret med kun `max_open_sockets=3` (https_wrapper.c). Hver TLS-session kræver ~32-40KB intern heap (`CONFIG_MBEDTLS_SSL_MAX_CONTENT_LEN=16384`, ingen `MBEDTLS_SSL_VARIABLE_BUFFER_LENGTH`, bekræftet i den vendorede sdkconfig.h for `qio_qspi`-varianten) — 3 samtidige sessioner æder allerede ~100-120KB af den ~130KB der er til rådighed lige efter boot, så resten af de 9 forsøg rammer en næsten tom heap og fejler deres TLS-opsætning, hvilket udløser frameworkets lækage-ved-fejl gentagne gange på én gang. **Fix**: `init()` sender nu kun `fetchMetrics()` med det samme; de øvrige 7 kald er spredt ud over ~120-960ms via `setTimeout()`, så der aldrig er mere end 1-2 nye TLS-handshakes i gang samtidig — hver session når at afslutte og frigive sin heap før den næste starter. Den dobbelte `updateUserBadge()`-kaldelse (var kaldt BÅDE i `init()`-sekvensen og separat helt til sidst i scriptet) er samlet til ét, forsinket kald. Løser ikke frameworkets underliggende lækage-ved-fejl (kan i teorien stadig udløses af andre kilder til mange samtidige forbindelser), men fjerner den identificerede, reproducerbare udløser fuldstændigt (web/dashboard.html) |
| BUG-369b | v7.9.10.36s fix var UTILSTRÆKKELIG — bekræftet af bruger: samme heap-styrtdyk (92KB→52KB, min ned til 34KB) og samme burst af `mbedtls_ssl_setup returned -0x7F00`/`esp_tls_create_server_session failed` gentog sig med web-session aktiv, selv efter kun side-indlæsningen blev spredt ud | ✅ FIXED | 🔴 CRITICAL | v7.9.10.37 | v7.9.10.36 spredte kun den ENGANGS-burst der sker ved side-indlæsning (`init()`) — men dashboardets 7 uafhængige `setInterval`-cadencer (3s/3s/5s/5s/5s/10s/15s) fortsætter uændret bagefter og kan periodisk overlappe hinanden igen (fx når 3s- og 5s-cadencerne rammer samtidig hvert 15. sekund). Derudover blev det opdaget at `fetchNetworkInfo()` ALENE fyrer 4 fetch-kald (hostname/wifi/ethernet/sse-status) parallelt uden at vente på hinanden — noget der ikke var synligt før. Reelt kan dashboardet altså udløse langt flere end de oprindeligt antagede "9 samtidige" forbindelser under normal, vedvarende brug, ikke kun ved side-indlæsning. **Fix**: ny `dashFetch(url,opts)`-wrapper (web/dashboard.html) — en simpel FIFO-kø med et hårdt loft på `DASH_FETCH_MAX_CONCURRENT=2` samtidige forbindelser, delt af ALLE 10 automatiske polling-fetch-kald (fetchMetrics/fetchAlarms/fetchMbActivity/fetchSyslog/fetchBindings/fetchConnections + de 4 inde i fetchNetworkInfo). Uanset hvor mange timere der tilfældigvis rammer samme øjeblik, kan dashboardet nu ALDRIG selv skabe mere end 2 samtidige HTTPS-forbindelser til enheden — komfortabelt under `max_open_sockets=3`-grænsen, med plads til andre samtidige klienter/faner. Bruger-udløste engangshandlinger (gem indstillinger, kvittér alarm, ryd log, GPIO-skift) er bevidst IKKE lagt i køen — de er sjældne/interaktive, ikke en del af det tilbagevendende polling-mønster der forårsager problemet (web/dashboard.html) |
| BUG-369c | v7.9.10.37s fix dækkede KUN dashboard.html — bekræftet stadig fejlende (bruger: "jeg browser rundt i web GUI", samme mbedtls-burst + heap ned til 33.144 bytes) fordi de andre sider slet ikke var beskyttet | ✅ FIXED | 🔴 CRITICAL | v7.9.10.38 | Opdaget ved gennemgang: `web/system.html`s `loadAllSettings()` fyrer alene **10 parallelle** `api()`-kald ved side-indlæsning (RBAC/WiFi/Ethernet/Hostname/Modbus Slave+Master/HTTP Auth/Telnet/NTP/Analog) — helt ubeskyttet af BUG-369b's dashboard-specifikke fix, som kun rørte `dashboard.html`. Editor (`editor.html`), OTA-siden (`ota.html`) og CLI-siden (`cli.html`) havde samme blinde vinkel — hver har sin egen centrale `api()`-hjælpefunktion, men ingen samtidigheds-begrænsning. Navigerer man rundt mellem siderne (dashboard→system→editor osv.), rammer hver side-indlæsning enheden med sin egen, ubegrænsede byge af forbindelser, uanset at dashboard.html selv var rettet. **Fix**: samme `queuedFetch()`-kø-princip (hårdt loft på `API_FETCH_MAX_CONCURRENT=2` samtidige forbindelser) tilføjet i alle fire resterende sider (`system.html`, `editor.html`, `ota.html`, `cli.html`) — hver sides eksisterende `api()`-funktion kalder nu `queuedFetch()` i stedet for `fetch()` direkte, uændret ydre kontrakt for alle eksisterende kald. Alle fem web-sider (inkl. dashboard.html fra BUG-369b) er dermed nu dækket (web/system.html, web/editor.html, web/ota.html, web/cli.html) |
| BUG-369d | v7.9.10.38 (alle 5 sider queued) løste det STADIG IKKE — bekræftet: burst på ~35 fejl inden for ét sekund (hver ~20-40ms), for tæt til at et klient-side kø-loft på 2 kan bremse (en fejlet forbindelse fejler på millisekunder, så køen tømmes øjeblikkeligt). MEN: en helt ren test (reboot, INGEN browser/web-GUI rørt overhovedet) viste 132KB fri heap, 100% STABIL over 1+ minuts oppetid — beviser endegyldigt at problemet er 100% web-GUI-udløst, intet andet (ikke boot, WiFi/NTP, Modbus/ST Logic) | 🟠 UNDER UNDERSØGELSE (yderligere afhjælpning, ikke bekræftet løst) | 🔴 CRITICAL | v7.9.10.39 | Da en fejlet TLS-opsætning fejler næsten instant (millisekunder), hjælper et klient-side samtidigheds-loft ikke reelt — det forsinker ikke raten af NYE forsøg naar hvert forsøg fejler saa hurtigt. Reel analyse: 132KB fri heap / max_open_sockets=3 giver kun ~44KB pr. session — meget tæt paa mbedTLS's eget ~32-40KB minimumsbehov (CONFIG_MBEDTLS_SSL_MAX_CONTENT_LEN=16384, ingen dynamisk buffer-størrelse). Ramt kapacitetsgrænsen evicter esp_https_server (lru_purge_enable=true) den ældste session for at gøre plads til en ny — men den nye session ser ikke ud til at få nok "frisk" heap i praksis, formentlig fordi selve oprydningen efter den evictede session ikke er færdig (fuldt synkron) inden den nye forsøges. **Fix-forsøg**: `max_open_sockets`/`backlog_conn` sat ned fra 3 til 2 (https_wrapper.c) — giver hver tilbageværende session markant mere luft (~66KB i stedet for ~44KB), på bekostning af at kun 2 samtidige HTTPS-klienter/-faner nu understøttes i stedet for 3. **Ikke bekræftet løst** — dette er endnu et forsøg på at afhjælpe et problem hvis rod ligger i ESP-IDF/esp_https_server's interne sessions-livscyklus-håndtering, som ikke kan rettes direkte i projektets egen kode. Kræver ny test med aktiv web-GUI-brug (src/https_wrapper.c) |
| BUG-370 | ST Editor (og dens "Monitor"-visning) beder unødvendigt om login, selvom serveren ikke kræver det / brugeren allerede er logget ind andre steder | ✅ FIXED | 🟠 MEDIUM | v7.9.10.40 | `web/editor.html` viste UBETINGET login-modalen på side-indlæsning hvis der ikke allerede lå et cached token i `sessionStorage` (`if(AUTH){...tjek...}else{tving login-modal}`) — uden nogensinde selv at spørge serveren om login reelt er nødvendigt. `dashboard.html` gør det modsat: den prøver bare handlinger og reagerer kun på et faktisk 401-svar. Ramte netop nu fordi brugeren slog HTTPS fra (`set http tls off`) — skift af protokol (https→http) er en ANDEN browser-origin, med sin egen tomme `sessionStorage`, så et evt. eksisterende login-token fra før skiftet var utilgængeligt, og editor.html tvang login-modalen frem uden at tjekke om RBAC/legacy-auth overhovedet er aktiveret (er den ikke, returnerer `rbac_check_http()` en "virtual admin"-bruger uden credentials, og `/api/status` ville svare 200 OK helt uden login). **Fix**: side-indlæsningen kalder nu altid `/api/status` (med eller uden Authorization-header), og viser kun login-modalen hvis serveren FAKTISK svarer med noget andet end 200 OK — samme mønster som dashboard.html (web/editor.html) |
| BUG-372 | Audit-loggen (FEAT-033) viste ALTID `username:"-"`, selv for gyldige, autentificerede requests — fundet ved LIVE test mod rigtig hardware (10.1.1.153) med `curl` | ✅ FIXED | 🟠 MEDIUM | v7.9.10.44 | Rodårsag: `api_send_json()`s audit-log-hook kaldte `http_get_client_info()` (som læser `Authorization`-headeren for at slå RBAC-brugeren op) EFTER `httpd_resp_sendstr()` — bekræftet ved test at `httpd_req_get_hdr_value_str("Authorization")` stille fejler når den kaldes efter selve svaret er sendt (headeren kan ikke længere læses), så brugernavn-opslaget altid ramte det tomme fallback. IP virkede fint (rå socket-fd via `getpeername()` forbliver gyldig efter send) — kun brugernavnet var ramt. Den eksisterende 401/403-fejlsti i `api_send_error()` overholdt allerede denne regel korrekt (med kommentaren "capture ... BEFORE sending response") — min nye kode i `api_send_json()` fulgte ikke samme regel. **Fix**: IP+brugernavn indhentes nu FØR `httpd_resp_sendstr()` begge steder; `api_audit_log_add()`s signatur udvidet til at modtage IP som parameter (i stedet for selv at kalde `getpeername()` igen bagefter), for konsistens og robusthed (include/api_audit_log.h, src/api_audit_log.cpp, src/api_handlers.cpp) |
| BUG-371 | Ændring af hostname (CLI `set hostname`, `/api/hostname`, `/system`-siden) havde INGEN reel netværkseffekt — kun kosmetisk (telnet-banner + REST-svar) | ✅ FIXED | 🟠 MEDIUM | v7.9.10.43 | Brugeren rapporterede: "hvis jeg ændre hostname så se det ikke på '/api/'". Undersøgelse viste at `g_persist_config.hostname` (og den separate `g_persist_config.network.ethernet.hostname`, sat via `/api/ethernet`) ALDRIG blev anvendt på nogen netværks-interface — ingen `esp_netif_set_hostname()`/`WiFi.setHostname()`-kald fandtes noget sted i kodebasen. Enheden broadcastede dermed altid Arduino-ESP32-kernens standard-hostname i DHCP-requests, uanset hvad brugeren konfigurerede. **Bekræftet: ingen mDNS-responder findes heller** (intet `MDNS.begin()`) — `<hostname>.local`-opløsning har derfor ALDRIG virket, uanset hostname-værdi. **Fix**: nye `wifi_driver_set_hostname()`/`ethernet_driver_set_hostname()` (kalder `esp_netif_set_hostname()` på de gemte STA-/Ethernet-netif-handles) — anvendt (1) ved boot i main.cpp, EFTER `network_manager_init()` opretter netif'erne men FØR forbindelse startes (config_apply.cpp kører for tidligt til dette — netif'erne findes ikke endnu der), og (2) straks ved enhver CLI/REST-ændring. Ethernets separate hostname-felt respekteres (falder kun tilbage til det globale felt hvis Ethernets eget er tomt), matcher den allerede-dokumenterede adskillelse i `docs/manual/12_Netvaerkskonfiguration.md` §12.5. Selve DHCP-broadcastet opdateres først ved næste lease/reconnect (ikke øjeblikkeligt på en allerede-aktiv forbindelse) — dokumenteret i både REST-svaret og manualen. mDNS-responder er bevidst UDENFOR scope her (separat, større feature) (include/wifi_driver.h, src/wifi_driver.cpp, include/ethernet_driver.h, src/ethernet_driver.cpp, src/main.cpp, src/api_handlers.cpp, src/cli_commands.cpp, docs/manual/12_Netvaerkskonfiguration.md) |
| BUG-374 | Første live-test af FEAT-007 (`POST /api/logic/globals/source`) crashede/genstartede enheden (10.1.1.153) på første forsøg, og hang derefter i 30+ sekunder uden svar på andet forsøg | ✅ FIXED (før shipping — fundet under egen live-hardware-test, aldrig rapporteret af brugeren) | 🔴 CRITICAL | v7.9.12.0 | To selvstændige designfejl fundet ved test, begge rettet i samme omgang: **(1)** `st_logic_globals_compile()` allokerede `st_variable_decl_t vars[ST_MAX_GLOBAL_VARS]` (~1,3KB) på STACKEN — samme klasse fejl som BUG-364/369 allerede lærte projektet (den almindelige HTTP-worker-task har kun 8KB stack), og som FEAT-029's `seen_paths[]`-array bevidst undgik via `malloc()` — denne nye kode fulgte ikke sin egen tidligere lektie. Rettet: `vars` er nu heap-allokeret (`malloc`/`free`). **(2)** Enhver GLOBAL_VAR-ændring udløste en "genkompilér alle allerede-kompilerede programmer"-cascade UDEN at tjekke om programmet overhovedet BRUGTE en global variabel — det betød (a) brugerens eksplicit fredede langtids-testprogram i slot 2 ville være blevet genkompileret (nulstillet statistik/tilstand) af en ÆNDRING DER INTET HAVDE MED DET AT GØRE, og (b) at genkompilere+gemme-til-SPIFFS 3-4 programmer synkront inden i én enkelt HTTP-requesthandler var langsomt nok til at forårsage den observerede hængning. Rettet: cascaden scanner nu hvert allerede-kompileret programs bytecode-instruktionsliste for rent faktisk at indeholde `ST_OP_LOAD_GLOBAL`/`ST_OP_STORE_GLOBAL` FØR det genkompileres — et program der ikke bruger nogen global røres slet ikke. Desuden flyttet et `free()`-kald til UDENFOR den kritiske sektion (`st_var_spinlock`) hvor det ved en fejl oprindeligt stod. Genverificeret efter fix: samme upload nu besvaret på 74-155ms, ingen genstart, slot 2 fuldstændig urørt gennem hele testforløbet (src/st_logic_config.cpp) |
| BUG-375 | Logic1 ("ModbusPLC", brugerens rigtige langtids-testprogram — brugeren rettede undervejs at det er slot 1, ikke slot 2, der skal friholdes) fejlede med `"last_error":"Stack underflow"` på ALLE 1176 eksekveringer siden seneste reboot (10.1.1.153) | ✅ FIXED (rettet efter brugerens godkendelse) | 🔴 CRITICAL | v7.9.13.0 | Opdaget ved rutinetjek af alle 4 slots før STRUCT-arbejde (FEAT-009). **Rodårsag bekræftet IKKE relateret til dagens ændringer** (STRING/GLOBAL_VAR/STRUCT): `st_bytecode_load()` validerer en cache-fil mod BÅDE `ST_BYTECODE_VERSION` OG kildekodens CRC32 før den bruges — ingen af delene er ændret i dag, så den (defekte) cache blev genindlæst uændret ved hver reboot, uafhængigt af al kompilator/parser-kode skrevet i dag. Bekræftet empirisk: upload af IDENTISK, ubearbejdet kildekode til en tom slot (4) og tvinget genkompilering gav **0 fejl over 226+ eksekveringer** med fornuftige værdier (CH[0]=218, counter/step fremadskridende) — beviser fejlen sad i selve den PERSISTEREDE `.bc`-cachefil for slot 1, ikke i kildekoden, kompilatoren eller VM'en. Nøjagtig årsag til hvordan cachen blev korrupt/forældet er IKKE identificeret (mulige kandidater: en afbrudt SPIFFS-skrivning på et tidligere tidspunkt, eller en ældre firmware-version med en siden rettet kompilatorfejl — ingen af delene undersøgt yderligere, da den praktiske løsning ikke kræver det). **Fix**: genuploadede den 100% identiske kildekode til slot 1 via `POST /api/logic/1/source`, hvilket tvinger en frisk kompilering og (som automatisk sideeffekt af enhver vellykket kompilering, `st_bytecode_save()`) overskriver den defekte cache-fil med en ny, korrekt en. Genverificeret: 6188+ eksekveringer, 0 fejl, `init_done`/`ok`/`CH[]`/`counter`/`step` alle fremadskridende korrekt. **Bemærkning**: under selve fix-processen oplevede enheden ÉN uventet genstart (curl-timeout på selve upload-kaldet, uptime faldt fra ~172s til ~30s) — årsagen hertil er ikke undersøgt (kunne være urelateret timing/heap-spidsbelastning fra flere hurtige REST-kald i træk), men enheden genstartede rent og selvstændigt, og den efterfølgende tilstand var øjeblikkeligt sund og stabil (heap ~124KB, uændret) |
| BUG-373 | STRING-variables tekstværdi vises altid tomt (`""`) i REST-svar for et KØRENDE program, selvom `CONCAT`/`LEN`/`LEFT`/`RIGHT`/`MID`/`=` alle beregner korrekt internt — fundet ved LIVE test af FEAT-005 mod rigtig hardware (10.1.1.153, program slot 3) | ✅ FIXED | 🟠 MEDIUM | v7.9.11.1 | Rodårsag: `st_logic_engine_loop()`s efter-eksekvering-kopiering (`memcpy(prog->bytecode.variables, vm.variables, ...)`, src/st_logic_engine.cpp) kopierede kun selve `st_value_t`-unionen (dvs. `str_ref`-handle'et) tilbage fra VM til programmet — men den FAKTISKE strengtekst bor i et separat array (`vm.string_vars[32]` vs. `prog->bytecode.string_vars[32]`), som IKKE blev kopieret tilbage. API'ets variabel-serialisering læser `prog->bytecode.string_vars[i]` direkte (da et STRING-variabel-slot altid selv-refererer sit eget indeks), som derfor for evigt forblev tomt efter compile (kun nulstillet, aldrig opdateret). Bekræftet ved test: `msg_len` (INT, fra `LEN(full_msg)`) og `is_equal` (BOOL, fra strengsammenligning) viste KORREKTE beregnede værdier — kun selve tekstværdien manglede, hvilket beviste at VM'ens interne strengberegning var korrekt hele tiden, og fejlen var isoleret til kopiering-tilbage-trinnet. **Fix**: tilføjet `memcpy(prog->bytecode.string_vars, vm.string_vars, sizeof(...))` lige efter den eksisterende `variables[]`-kopiering, inden for samme `st_var_spinlock`-kritiske sektion (src/st_logic_engine.cpp). Verificeret på hardware efter fix: `greeting="Hello"`, `who="World"`, `full_msg="HelloWorld"` (CONCAT), `msg_len=10` (LEN), `first3="Hel"` (LEFT), `last3="rld"` (RIGHT), `mid_part="lloW"` (MID, 1-baseret IEC-61131-3-indeksering), `is_equal=true` — alle korrekte, ~350 eksekveringscyklusser uden fejl/heap-lækage (125492 bytes frit, uændret) |
| FEAT-170 | GUI-oprydning: system.html reorganiseret (Fase A) + manglende CLI-indstillinger tilføjet til web-GUI | ✅ DONE | 🟠 MEDIUM | v7.9.16.0 | Brugerønsket: "se på hvad mangler i gui som vi har i cli og organisere settings i gui det er en rode butik". Todelt: (A) en Explore-agent kortlagde hele CLI `set`-overfladen mod web/system.html/dashboard.html/editor.html, (B) hver reelt manglende REST-forbindelse blev verificeret ved selv at læse den relevante handler-kode i api_handlers.cpp (ikke kun agent-rapporten) — flere "huller" viste sig allerede at have fuld backend-understøttelse (Persist-gruppe-CRUD, HTTP generelle felter), kun frontend manglede. **Ny informationsarkitektur** (samme `.section-hdr`+`.card`-mønster som hele siden allerede brugte — selve markup'et var aldrig rodet, kun grupperingen): Telnet flyttet fra "Sikkerhed" til "Netværk" (det er en netværkstjeneste, ikke et auth-system); "Sikkerhed & Brugere" omdøbt "HTTP & Sikkerhed" og fik et nyt "Modbus Transceiver"-kort (mode slave/master/off + UART-valg) i Modbus-sektionen; HTTP Legacy Auth-kortet udvidet til også at rumme de generelle serverfelter (enabled/port/api_enabled — fandtes kun i CLI/REST før); ny "Rate-Limit"-kort (bevidst ikke NVS-persisteret, matcher CLI'ens egen runtime-only opførsel); ny "Realtidsopdateringer"-sektion med et nyt SSE-serverindstillinger-kort (enabled/port/max_clients/check_interval/heartbeat) ved siden af det eksisterende SSE Klienter-overvågningskort (flyttet hertil fra "Vedligeholdelse"); Analog I/O-tabellen udvidet med en Mode-kolonne (spænding/strøm) pr. AO-kanal; ny "Persistens"-sektion med fuld gruppe-CRUD-UI (opret/tilføj-registre/fjern-registre/slet, samt to nye enabled/auto-load-toggles — flyttet hertil fra "Vedligeholdelse", som nu er omdøbt "System" og kun rummer Fabriksindstillinger/Reboot/System-Information. **Backend-tilføjelser** (alle små, API_ROUTES[]-skema holdt opdateret): `api_handler_analog_post()` fik `mode`-håndtering for ao1/ao2 (felterne `ao1_mode`/`ao2_mode` fandtes allerede i `g_persist_config`, manglede kun en REST-vej udenom fuld config-restore); `api_handler_config_get()`+`api_handler_http_config_post()` fik en ny `"sse"`-JSON-sektion (samme `g_persist_config.network.http`-struct som HTTP-felterne, ingen ny endpoint); `/api/modbus/slave`+`/master`s GET/POST fik en ny `"transceiver"`-sektion; helt ny `GET/POST /api/system/rate-limit` (kalder eksisterende `http_rate_limit_set_enabled()`, bevidst ikke persisteret); helt ny `POST /api/persist/config` (`enabled`/`auto_load_enabled` — disse to felter fandtes kun i GET-svaret og fuld config-restore før, ingen dedikeret skrive-vej). `web/dashboard.html`s forældede kommentar ved Analog I/O-kortet ("kun CLI/REST") rettet til at pege på system-siden. **Grundigt live-testet v7.9.16.0 på 10.1.1.153** (efter OTA-flash, ren reboot, build #2237): alle 6 nye/udvidede endpoints round-trip-testet direkte (rate-limit til/fra, AO1 mode current↔voltage, persist-gruppe opret+tilføj-registre+slet, persist-config auto-load til/fra) — alle svarede korrekt; `/api/analog`s `ao[]`-array bekræftet at indeholde det nye `mode`-felt; `/api/config`s nye `sse{}`-blok og `/api/modbus/slave`s nye `transceiver{}`-blok begge bekræftet til stede med korrekte værdier (transceiver var allerede sat til "master" fra tidligere konfiguration, viste korrekt igennem). Den servrede system.html-side selv hentet og verificeret: alle 5 nye/omdøbte sektionsoverskrifter til stede ("Modbus Transceiver", "Rate-Limit", "Realtidsopdateringer", "SSE Server-indstillinger", "Persistens"), OG ingen dubletter tilbage (Telnet-kort optræder nu kun 1 gang, "Vedligeholdelse"-overskriften væk, "SSE Klienter" kun 1 gang, gamle "Persist Grupper"-korttitel væk). Logic1 (brugerens beskyttede langtids-testprogram, slot 1) bekræftet 100% upåvirket gennem hele OTA-flash+reboot-cyklussen (5906 eksekveringer, 0 fejl, uændret NORMAL/10ms). `node --check` kørt på hele det udtrukne `<script>`-indhold (47.620 tegn) før flash — ingen syntaksfejl. **Fase B** (Counter/Timer/GPIO-mapping-konfiguration i GUI) bevidst UDENFOR scope her, givet omfanget (sammenlignelig størrelse med hele FEAT-010) — foreslås som selvstændig opfølgningsopgave. Rørte: web/system.html, web/dashboard.html, src/api_handlers.cpp, include/api_handlers.h, src/http_server.cpp |
| BUG-376 | `api_send_error()`s HTTP-statuslinje viste "400 Bad Request" for ENHVER statuskode uden for {400,401,403,404,500} — bl.a. 409, 429, 502, 503, 504, alle brugt bredt i api_handlers.cpp/ota_handler.cpp — selvom JSON-brødteksten korrekt viste den tiltænkte kode. Fundet under undersøgelse af brugerrapporteret GitHub-OTA-krasch (se nedenfor) | ✅ FIXED | 🟠 MEDIUM | v7.9.16.4 | `httpd_resp_set_status()` blev kaldt med en haardkodet ternary-kaede der kun daekkede 5 koder — alt andet faldt igennem til default-grenen `"400 Bad Request"`. Client-siden saa dermed ALTID HTTP 400 paa selve status-linjen for disse endpoints (bekræftet ved `curl -v` mod GitHub-check-endpointet: `HTTP/1.1 400 Bad Request` selvom koden kalder `api_send_error(req, 502, ...)` og JSON-kroppen korrekt siger `"status":502`). Uafhaengig af selve GitHub-OTA-krascet (som stadig IKKE er løst, se nedenfor) — ren kosmetisk/protokol-korrekthedsfejl, ingen sikkerheds- eller stabilitetsimpact i sig selv, men kunne forvirre enhver klient der læser HTTP-statuskoden i stedet for JSON-feltet. **Fix**: udvidet til en fuld status→tekst-mapning (400/401/403/404/409/429/500/502/503/504 + "Error"-fallback), bygget i en `static char status_line[40]` (IKKE stack-lokal — `httpd_resp_set_status()` gemmer kun en POINTER, ikke en kopi, saa strengen skal leve til selve send-kaldet; `static` er sikkert her fordi denne httpd-instans er enkelt-traadet, jf. BUG-367). Verificeret på hardware: 404- og 400-svar viser nu korrekt status-linje (uændret opførsel for disse), ingen regression (src/api_handlers.cpp) |
| BUG-377 | Brugerrapporteret: enheden crasher (genstarter) ved "Tjek for opdatering" fra GitHub-OTA (system-siden/GUI) | 🟠 UNDER UNDERSØGELSE (IKKE løst — 6 hypoteser afkræftet, se detaljer) | 🔴 KRITISK | v7.9.16.1-7.9.17.0 | Fortsættelse af BUG-364/366/367/368/369-serien (FEAT-169), forladt "UNDER UNDERSØGELSE" ved v7.9.10.32-39 uden nogensinde at blive bekræftet løst. **Reproduceret 100% pålideligt DIREKTE via API** (`curl .../api/system/ota/github-check`, INGEN browser/GUI involveret) — udelukker enhver GUI-specifik teori. **Diagnostik uden serial-adgang**: et "sidst nåede trin"-breadcrumb i `RTC_NOINIT_ATTR`-hukommelse (overlever panic-genstart) sat ved hvert risikofyldt undertrin i `github_check_do_work()`/`github_check_worker()`/REST-handleren, læsbart via et midlertidigt `GET /api/system/ota/github-debug`-endpoint (bevidst LADT I KODEN, markeret "MIDLERTIDIG DIAGNOSTIK"). **SEKS hypoteser afprøvet og AFKRÆFTET, hver via reel genstart på hardware**: (1) Utilstrækkelig stack — `gh_check`/`gh_install` doblet 16384→32768 byte, krascher identisk. (2) Lav/fragmenteret heap — 114KB+ fri ved hvert forsøg (langt over BUG-366s 48KB-grænse), udelukket. (3) Core-affinitet/prioritet — mistanke om at en CPU-bundet TLS-håndtryk pinnet til Core 0 med prioritet 5 sulter IDLE-tasken og udløser Task-Watchdog-panic; ændret til `tskNO_AFFINITY`+prioritet 1, krascher identisk. (4) `lru_purge_enable`/socket-udsultning — mistanke om at ESP-IDFs LRU-forbindelses-oprydning lukker vores egen, stadig-i-brug forbindelse mens handleren venter flere sekunder på baggrundstasken; `max_open_sockets` øget 7→10, krascher identisk, OGSÅ i fuld isolation uden nogen samtidige forbindelser. (5) **Arkitektur-omlægning** (den mest substantielle ændring denne omgang): begge endpoints (`github-check`, `github-install`) omlagt fra at BLOKERE http-forbindelsen i op til hhv. 20s/90s mens en baggrundstask arbejder, til et start+poll-mønster identisk med det allerede eksisterende firmware-upload-flow (`POST` starter og svarer ØJEBLIKKELIGT, `GET` (samme URI, ny metode) poller resultatet uden nogensinde at blokere; install genbruger den allerede eksisterende `GET /api/system/ota/status`-progress-polling i stedet for en ny endpoint) — elegant og korrekt i sig selv, men krascher STADIG, nu blot hurtigere (typisk allerede under selve baggrundstaskens `http.GET()`-forsøg, bekræftet ved at et rent `POST`-kald med frisk login-token umiddelbart efter viser `reboot_count` steget og uptime på få sekunder) — beviser at krascet ALDRIG havde noget med hvor længe http-forbindelsen holdes åben at gøre. (6) **CA-bundle/certifikatkæde** — en grundig `openssl s_client -showcerts`-undersøgelse fandt initialt at en simpel bar-leaf-verificering fejlede ("unable to get local issuer certificate") for BÅDE api.github.com (Sectigo-kæden) og objects.githubusercontent.com (Let's Encrypt "YR1"/"Root YR"-kæden, en NYERE kæde end da FEAT-169 oprindeligt blev bygget) — men en KORREKT ombygget verificering (med de FULDE 3-certifikat-kæder serveren faktisk sender under handshake, ikke kun bar-leaf) viste at begge domæner rent faktisk verificerer OK mod den EKSISTERENDE, ÆNDREDE `certs/github_ca_bundle.pem` (begge nyere mellemled er krydssignerede tilbage til de allerede bundlede rod-ankre — Sectigo Root E46 er signeret af USERTrust ECC CA, og Let's Encrypts nye "Root YR" er signeret af ISRG Root X1). Den oprindelige bar-leaf-test var en fejlbehæftet testmetode (ingen ægte klient, inkl. mbedTLS på ESP32, verificerer NOGENSINDE kun bar leaf uden serverens medsendte mellemled) — CA-bundlen er dermed BEKRÆFTET KORREKT, ingen ændring foretaget i `certs/github_ca_bundle.pem`. **Konklusion efter alle 6 udelukkelser**: årsagen er hverken stack, heap, scheduling, http-forbindelses-levetid, eller certifikatkæden — noget dybere i selve TLS-handshake-forsøget (WiFiClientSecure/mbedTLS) crasher konsekvent, uafhængigt af hvor lang tid det tager eller hvordan det udløses. **IKKE løst** — kræver enten (a) rigtig serial/JTAG-konsol-adgang til et reelt backtrace (findes ikke i dette miljø: kun COM1 lokalt tilgængeligt, ikke forbundet til netværksenheden; ingen coredump-partition i `partitions.csv`), eller (b) yderligere bisection direkte i mbedTLS-laget (kræver flere reelle genstarter — 17+ er allerede påført enheden under hele denne undersøgelse). Manuel `.bin`-firmware-upload er UPÅVIRKET og fortsat en fungerende opdateringsvej i mellemtiden. Rørte: src/ota_handler.cpp, include/ota_handler.h, src/http_server.cpp, web/system.html |
| FEAT-171 | Fase B: ny web-GUI-side `/io` for Counters, Timers og GPIO Statisk Mapping — hidtil kun CLI/REST | ✅ DONE | 🟡 HIGH | v7.9.18.2 | Opfølgning på FEAT-170s bevidst udskudte "Fase B". Forud gik et arkitektur-spørgsmål fra brugeren: er de selvstændige Counter/Timer-motorer stadig relevante nu ST Logic har sine egne `TON`/`TOF`/`TP`? Undersøgt: **Counters er komplementære** — `COUNTER_HW_PCNT` bruger ESP32's dedikerede hardware-pulstæller-periferi (noget ST's scan-cyklus-baserede software aldrig kan efterligne ved høje pulsfrekvenser), og tællerværdien skrives allerede til Modbus-registre som ST frit kan læse. **Timers er ren software** (`millis()`-polling fra hovedloopet) — mekanisk identisk med ST's egne `TON`/`TOF`/`TP`, bare i en separat konfigurationsoverflade; en reel konsolideringskandidat, men brugeren bad eksplicit om at implementere begge uændret nu og tage relevans-spørgsmålet op senere. **Ny side** `web/io.html` (+ `include/web_io.h`/`src/web_io.cpp`, samme minimale gzip-servering-mønster som `web_logs.*`), registreret på `/io`, tilføjet til topnav på alle 6 eksisterende sider. Eksplicit HTML for hver af de 4 counter/timer-instanser (samme mønster som editor.html's Logic1-4), delte ID-parametriserede JS-funktioner. Mode-afhængige felter vises/skjules via `data-mode`-attributter. Register-felter (value_reg/raw_reg/freq_reg/ctrl_reg) vises bevidst READ-ONLY — matcher CLI'ens egen blokering af manuel register-override (`cli_commands.cpp:100-127`) og undgår at REST-stien (som ikke kalder `register_allocator_check()`) kan skabe kollisioner via GUI'en. GPIO-pin-picker er en grupperet dropdown (virtuelle DI 101-108/DO 201-208/frie GPIO 4,5,16,17,18,19 m. Ethernet-delings-advarsel) i stedet for fritekst, hardkodet efter ES32D26-boardets faktiske pin-reservationer. Ingen UI bygget for bekræftet dødt-kodede felter (`trigger_level` — aldrig læst af `mode_monostable()`; `mode_enable` — duplikat af `enabled`, ikke læst af motorens enable-tjek). **Backend-udvidelser** (alle i `src/api_handlers.cpp`, verificeret præcise linjenumre før ændring): Counter POST fik `compare_source`/`reset_on_read` (fandtes i struct+motor, kun tilgængelige før via fuld backup/restore); Timer POST fik `phase1/2/3_output_state`/`trigger_edge`; ny `POST /api/timers/{id}/control` (start/stop/reset, mirror af counters' tilsvarende, dispatchet via samme URI-suffix-mønster som `/reset`,`/start`,`/stop`,`/control` allerede bruger for counters — `api_handler_timer_config_post()` tjekker nu for `/control`-suffix FØR sit exact-match-afvisning). **To selvstændigt fundne, rettede GET-huller** (opdaget da GUI'en forsøgte at INDLÆSE eksisterende konfiguration og fik tomme felter tilbage): (1) `GET /api/counters/{id}` returnerede hidtil KUN status/live-værdier (id/enabled/mode/value/raw/frequency/running/overflow/compare_triggered) — INGEN af de faktiske konfigurationsfelter (edge_type/prescaler/bit_width/scale_factor/hw_gpio/compare_*/register-adresser osv.), så en GUI kunne skrive blindt via POST men aldrig vise den gemte tilstand; alle manglende felter tilføjet. (2) Samme for `GET /api/timers/{id}` manglede `ctrl_reg`. **Selvstændigt fundet OG rettet krasch-uafhængig korrekthedsfejl**: `POST /api/counters/{id}/control`s `{"running":true/false}` skrev hidtil kun de TRANSIENTE bit1(start)/bit2(stop)-kommandobits — disse FÅR rent faktisk counteren til at starte/stoppe (bekræftet), men rører ALDRIG bit7, som er den ENESTE bit `counter_engine_handle_control()` bruger som vedvarende running-status (`counter_engine.cpp:328-365`) og som GET's (nu rettede) `running`-felt læser. Et REST-startet counter viste dermed FOR EVIGT `running:false` uanset faktisk tilstand — opdaget under egen live-test (startede Counter 4 i PCNT-mode, læste status tilbage, så `false`). Rettet: endpointet sætter/rydder nu bit7 direkte (samme bit CLI'ens `set counter <id> control running:on` allerede bruger) — motoren tjekker bit7 hver loop-tick og starter/stopper selv. Timer-siden havde IKKE samme fejl (Timer Bit0/1 sætter `is_active` direkte i `timer_engine_handle_control()`, ingen separat persistent bit eksisterer for timere). **Grundigt live-testet v7.9.18.2 på 10.1.1.153**: alle 3 counter hw_modes (SW/SW_ISR/PCNT) og alle 4 timer modes (ONESHOT/MONOSTABLE/ASTABLE/INPUT_TRIGGERED) round-trip-verificeret (konfigurer→læs tilbage→felter matcher); Counter running-status bekræftet korrekt FØR OG EFTER bit7-fixet (false→sand fejl reproduceret, derefter bekræftet rettet: start→`running:true`, stop→`running:false`); Timer running/current_phase bekræftet at følge et faktisk kørende ASTABLE-forløb (fase 0→1, output skiftede); GPIO-mapping opret/list/slet verificeret (virtuel DI, korrekt `gpios[]`-nøgle — IKKE `gpio`/`mappings` som først antaget); `/io`-siden selv hentet og bekræftet at indeholde alle 3 sektioner; nav-link `/io` bekræftet til stede på både system- og dashboard-siden. `node --check` kørt på io.html's udtrukne script (2 omgange, efter GET-hul-rettelserne). Logic1 (brugerens beskyttede program) bekræftet upåvirket gennem hele test-forløbet (4000+ eksekveringer, 0 fejl). Rørte: web/io.html (ny), include/web_io.h (ny), src/web_io.cpp (ny), scripts/gzip_web_assets.py, src/http_server.cpp, src/api_handlers.cpp, web/dashboard.html, web/editor.html, web/cli.html, web/system.html, web/logs.html, web/ota.html |
| FEAT-172 | `/logs`-siden omlagt til én samlet fuld-side visning af BÅDE API Audit Log (FEAT-033) OG System Hændelseslog (FEAT-086/089) — sidstnævnte fandtes hidtil kun som et lille indlejret `.card-wide`-kort på dashboard.html (`max-height:320px`, scrollbart), ikke sin egen side | ✅ DONE | 🟠 MEDIUM | v7.9.19.0 | Brugerønsket: "vi skal have lavet en samlet side for audit og system logs og den må godt bruge hele siden og ikkke som nu kun en frame på den lig side". `web/logs.html` genopbygget med samme layout-mønster som system.html/io.html (topnav, full-height content, `.card`) i stedet for sin ældre, smallere `nav`+`max-width:1000px`-container-stil — bringer siden visuelt i tråd med resten af FEAT-170/171-omlægningen. To faner (`API Audit Log`/`System Hændelseslog`), hver bruger FULD sidebredde/-højde når aktiv, i stedet for at klemme begge logtyper sammen. Hændelseslog-fanens funktionalitet (kategori/kilde-filtre, start/stop-logning, CSV-eksport, ryd log) er 1:1 portet fra dashboard.html's gamle `syslog`-kort-JS (samme funktionsnavne/-logik, kun tilpasset det nye sides `api()`/`queuedFetch`-mønster i stedet for dashboardets `dashFetch()`). **Bevidst IKKE fjernet fra dashboard.html**: det gamle `syslog`-kort og dets polling (`setTimeout(fetchSyslog,480)`/`setInterval(fetchSyslog,5000)`) er efterladt urørt — `_allCardIds`-arrayets rækkefølge er INDEKS-baseret i brugernes gemte dashboard-layout-præferencer (jf. BUG-365: "`_allCardIds`s rækkefølge må fremover kun UDVIDES... ellers peger gemte indekser på forkerte kort"), så at fjerne `'syslog'` fra midten af arrayet ville have forskudt alle senere kort-indekser og korrumperet enhver brugers allerede gemte tilpassede kort-valg. Brugere der ikke ønsker det lille dashboard-kort kan allerede skjule det via dashboardets egen eksisterende kort-synlighedsindstilling — ingen kodeændring nødvendig for det. **Live-testet v7.9.19.0 på 10.1.1.153**: begge faner bekræftet til stede i den servrede side; `/api/syslog`- og `/api/system/logs`-endpoints begge bekræftet at returnere reelle poster; syslog start/stop-toggle rundtur-testet (`logging:true`→`false`→`true`); `node --check` på det udtrukne script. Logic1 upåvirket. Rørte: web/logs.html |
| FEAT-397h | Brugerønsket, opfølgning på en lang session om browser-baseret Basic-Auth-cache (BUG-387→397g): et valg i Settings mellem tre HTTP auth-metoder (None/Basic/Bearer-only), i stedet for at Basic-Auth fjernes unilateralt | ✅ DONE, live-verificeret | 🟡 HIGH | v7.9.42.0 | **Baggrund**: Firefox cacher HTTP Basic-Auth-headere permanent i en intern credential-butik adskilt fra password manager'en (bekræftet via websøgning, ingen JS-API til at rydde den) — det er DERFOR en browser forbliver logget ind efter en reboot, selvom serverens session-cookie korrekt invalideres (`rbac_check_http()` tjekker Authorization-header FØR cookien, saa en cachet Basic-header vinder altid og viser aldrig login-boksen). Brugerens valg (via AskUserQuestion): Bearer-tilstand skal være REN (afviser Basic aktivt, ikke bare foretrække Bearer), og en opgradering af den EKSISTERENDE enhed skal gå direkte til Bearer (ikke bevare Basic som "sikker default"). **Design**: nyt felt `auth_mode` (0=BASIC/1=BEARER, kun meningsfuldt naar `auth_enabled=1` — "None" er uændret den allerede eksisterende `auth_enabled=0`-fald-tilbage, BUG-370). `rbac_check_http()` afviser nu Basic-Auth-headeren aktivt i Bearer-tilstand paa alle endpoints UNDTAGEN `/api/login` selv (som SKAL blive ved med at acceptere Basic-encodede credentials — det ER selve login-kaldets formaal at bytte dem til et Bearer-token). `rbac_check_sse()` bevidst urørt (separat, allerede kendt begrænsning, SECURITY_INDEX #13). CLI: `set http auth-mode basic\|bearer`. REST: `auth_mode`-felt i GET/POST `/api/http` og `/api/config` + full-restore. Web-GUI: ny dropdown i "HTTP & Sikkerhed"-kortet (system.html). **KRITISK selvfundet-og-rettet fejl FØR bygning**: det oprindelige udkast (skrevet i en tidligere, nu komprimeret sessionsdel) placerede `auth_mode` SOM FELT I `HttpConfig`, lige efter `auth_enabled` — men `HttpConfig` sidder nær STARTEN af `NetworkConfig`/`PersistConfig`, saa et nyt felt der ville have forskudt byte-offsettet for naesten HELE resten af den persisterede struct (counters, timere, register-mappinger, var_maps, RBAC, NTP, dashboard, analog, https_port, salte, crc16 — alt) for enhver allerede-konfigureret enhed, nøjagtig samme klasse fejl som `https_port`/`rbac_salt`/`dashboard_card_custom` allerede har en dokumenteret advarsel imod i types.h. Fanget under en efterfølgende gennemgang (udløst af den beslægtede var_maps-kapacitetsopgave, se FEAT-397i) FØR koden nogensinde blev bygget eller flashet — rettet ved i stedet at flytte feltet til `PersistConfig.http_auth_mode`, HELT TIL SIDST (foer crc16), samme etablerede placeringsmønster som de øvrige "sidst"-felter. **Live-verificeret v7.9.42.0 på 10.1.1.153**: efter flash gik enheden automatisk i Bearer-tilstand (schema-migration 23→24, brugerens "Bearer med det samme"-valg) — bekræftet: Basic-Auth paa et ALMINDELIGT endpoint (`/api/logic/1`) giver nu 401; `POST /api/login` MED Basic-Auth giver stadig 200 og udsteder et token (login-undtagelsen virker); det udstedte token virker som `Authorization: Bearer` paa det samme endpoint; ingen auth overhovedet giver ogsaa korrekt 401 (uaendret baseline). `GET /api/config` viser `"auth_mode":"bearer"`. Logic1 (disabled, urørt) og Logic2 (17 bindinger, 5033+ eksekveringer, 0 fejl) begge bekræftet upåvirket gennem to reboots. Rørte: include/types.h, include/constants.h, src/rbac.cpp, src/network_config.cpp, src/config_load.cpp, src/cli_commands.cpp, src/cli_show.cpp, src/api_handlers.cpp, web/system.html |
| FEAT-397i | Brugerrapporteret live-fejl: `HTTP 500 "Maximum GPIO mappings (32) reached"` med kun 15 GPIO-mappinger konfigureret via `/io`-siden — undersøgt og forklaret (IKKE en bug: det delte `var_maps[]`-array, 32 pladser, var reelt fuldt: 15 GPIO + 17 ST-Logic-bindinger fra brugerens Logic2-testprogram), hvorefter brugeren bad om at hæve loftet til 64 | ✅ DONE, live-verificeret | 🟡 HIGH | v7.9.42.0 | **Baggrund**: `PersistConfig.var_maps[]` deles mellem GPIO-statiske mappinger og ST Logic-variabelbindinger (`VariableMapping.source_type`) — arrayet var OPRINDELIGT 64 lang, men blev på et tidspunkt før denne sessions historik reduceret til 32 "for at spare NVS-plads" (levn-kommentar i types.h); `cli_commands_logic.cpp`s egen kapacitets-check var aldrig opdateret til det nye loft og checkede stadig fejlagtigt mod 64 — et selvstændigt fundet, hidtil ufarligt (fordi ingen bruger nogensinde havde nået 32) men reelt out-of-bounds-skriverisiko, nu rettet af samme omgang. Ny navngivet konstant `MAX_VAR_MAPPINGS=64` (constants.h), alle 6+ hårdkodede `32`-grænsetjek (api_handlers.cpp, cli_commands.cpp, config_load.cpp, config_struct.cpp) opdateret til at bruge den. **KRITISK designudfordring**: `var_maps[]` sidder MIDT i `PersistConfig` (efterfulgt af `gpio2_user_mode`, `persist_regs`, `modbus_master`, RBAC, NTP, dashboard-felter, analog I/O, `https_port`, password-salte, crc16) — dette er FØRSTE GANG i denne struct's historie at en udvidelse sker midt i structen i stedet for at blive tilføjet HELT TIL SIDST (den etablerede, hidtil eneste brugte migrationsstrategi, jf. de eksisterende kommentarer ved https_port/rbac_salt/dashboard_card_custom). En simpel schema-bump alene er UTILSTRÆKKELIG her: `nvs_get_blob()` kopierer en gammel, kortere blob RAAT ind i den nye, større struct — alt fra og med `gpio2_user_mode` ville lande 32×13=416 bytes for TIDLIGT (midt inde i det nye `var_maps[32..63]`), og de RIGTIGE nye positioner ville forblive tomme, for enhver allerede-konfigureret enhed (WiFi, RBAC-brugere, Modbus Master-indstillinger, analog-kalibrering — alt). **Løsning**: en dedikeret "VAR_MAPS RE-POSITIONERING"-migration i `config_load.cpp`, kørt ÉN GANG, FØR den almindelige schema_version-kæde overhovedet starter (ikke som et normalt kædetrin) — genlæser den samme raa NVS-blob ind i en ny `PersistConfig_v24_shadow_t` (byte-for-byte kopi af schema ≤24's layout, `var_maps[32]`), kopierer `var_maps[0..31]` uændret, initialiserer de 32 nye slots eksplicit som "unused" (`source_type=0xff` — IKKE nul, som er `MAPPING_SOURCE_GPIO` og ville have vist fantom-GPIO-mappinger på pin 0), og kopierer resten af structen ("halen" fra `gpio2_user_mode` til `http_auth_mode`) som én sammenhængende memcpy til dens korrekte NYE byte-position. Falder eksplicit tilbage til fabriksdefault (med tydelig fejlbesked) hvis gen-læsningen af den raa blob mod forventning skulle fejle, fremfor at fortsætte med potentielt korrupt data. Schema-version bumpet 24→25 (`CONFIG_SCHEMA_VERSION`). **Live-verificeret v7.9.42.0 på 10.1.1.153** (RAM 23.5%/Flash 91.7%): den levende enheds reelle schema-23-config (15 GPIO + 17 ST-bindinger = 32/32 fuldt) migreret gennem 23→24→25 i én boot. Fuld backup-JSON sammenlignet felt-for-felt (Python) FØR/EFTER flash: samtlige 30 top-niveau-nøgler identiske UNDTAGEN de forventede (`build`/`firmware_version`/`schema_version`/nyt `http.auth_mode`-felt) — `var_maps` (alle 32 entries), `rbac` (alle 3 brugeres hash+salt+roller+privilegium), `network` (WiFi), `analog`, `ntp`, `persist_regs` osv. 100% byte-identiske, INGEN korruption. Verificeret STABIL over en ANDEN efterfølgende reboot (ren "ingen migration nødvendig"-sti, schema forbliver 25). Selve kapacitetsudvidelsen bekræftet funktionelt: tilføjede en 33. mapping (virtuel GPIO 208/DO8) — lykkedes (count 32→33), hvor den FØR denne fix ville have fejlet med netop brugerens oprindelige fejlbesked; fjernet igen og gemt til NVS for at genoprette enhedens præcise oprindelige tilstand. Logic1 (disabled, urørt) og Logic2 (17 bindinger, kørende, 0 fejl) bekræftet upåvirket gennem to reboots + en efterfølgende save-cyklus. Rørte: include/types.h, include/constants.h, src/config_load.cpp, src/config_struct.cpp, src/cli_commands.cpp, src/cli_commands_logic.cpp, src/api_handlers.cpp, docs/GPIO_MAPPING_GUIDE.md |
| FEAT-399 | Brugerønsket: en IP Access Control List (ACL) i Settings — "hvem må hvad fra hvad ip adr. til hvad services", plus (brugerens eget, selvstændige krav) en lockout-recovery-mekanisme: en management-påvirkende ACL-ændring skal anvendes midlertidigt/testbart, ikke persisteres med det samme, og automatisk rulles tilbage hvis ikke bekræftet via en FRISK login inden for en timeout | ✅ DONE, live-verificeret | 🔴 CRITICAL (lockout-risiko) | v7.9.45.0 | **Design** (fuldt planlagt via en dedikeret Plan-review-agent, der læste ESP-IDF's egen `esp_https_server`-kildekode før implementering — se planfilen): default-tillad, kun blokeringsregler (`AclRule{network_addr,prefix_len,service,enabled}`, fuld CIDR /0-/32), dækker HTTP/HTTPS (Web-GUI+REST+OTA), Telnet og SSE (IKKE Modbus RTU, intet IP-koncept der). Ny `include/types.h`-struct `AclRule` + `PersistConfig.acl_enabled/acl_rule_count/acl_rules[32]`, tilføjet HELT TIL SIDST (efter `http_auth_mode`, før `crc16`) — schema 25→26. **Fire håndhævelses-hooks, tre forskellige mekanismer**: (1) plain HTTP via `httpd_config.open_fn` (src/http_server.cpp) — bekræftet 100% sikkert i ESP-IDF's `httpd_sess.c`-kilde, rammen lukker selv socket'en korrekt ved non-ESP_OK. (2) **HTTPS bevidst IKKE på samme lag** — verificeret direkte i `esp_https_server`s kildekode at `httpd_ssl_open()` udfører HELE TLS-håndtrykket FØR den kalder et evt. chainet open_fn og IGNORERER dets returværdi fuldstændigt; et forsøg på selv at `close(sockfd)` der ville frigive fd'et til OS'et mens `esp_tls`-konteksten stadig tror forbindelsen lever — præcis den brug-efter-frigivelse-fejlklasse `https_wrapper.c`s eget filhoved allerede advarer imod. HTTPS håndhæves i stedet på REQUEST-laget (`CHECK_AUTH`/`CHECK_AUTH_WRITE`/`CHECK_AUTH_ROLE`-makroerne, api_handlers.cpp, + eksplicit i `api_handler_login()`/`api_handler_user_me()` som ikke bruger disse makroer) — kendt, dokumenteret restbegrænsning: en blokeret IP kan stadig gennemføre selve TCP+TLS-håndtrykket før afvisning. (3) Telnet: `tcp_server_accept()` (src/tcp_server.cpp), lige efter `accept()` men FØR den ene klientplads optages. (4) SSE: `sse_accept_task()` (src/sse_events.cpp), lige efter `accept()`, før ressourceallokering — begge sidstnævnte har `client_addr` direkte fra `accept()`, intet ekstra `getpeername()` nødvendigt. **Lockout-recovery (den kritiske del)**: en "gated" ændring (tilføjelse/genaktivering af en aktiv HTTP/TELNET/ALL-regel, eller `acl_enabled 0→1` mens en sådan regel findes) lander IKKE i `g_persist_config` men i en HELT SEPARAT RAM-only staging-struct i det nye `src/ip_acl.cpp` — bevidst designvalg fundet af Plan-agenten: kodebasen har ~15 uafhængige `config_save_to_nvs()`-kaldesteder (REST/CLI/ST Logic), og havde den ventende regel ligget i `g_persist_config`, kunne EN HVILKEN SOM HELST anden, urelateret "Gem"-handling andetsteds i UI'et have persisteret den utestede, potentielt selv-udelukkende regel til NVS FØR bekræftelse. Staging håndhæves STRAKS af `ip_acl_check()` (reelt testbar), men persisteres først ved eksplicit `POST /api/acl/confirm`/`confirm acl` inden for `ACL_PENDING_CONFIRM_TIMEOUT_MS` (5 min) — ellers kasseret automatisk af et periodisk `ip_acl_tick()`-kald (main.cpp's `loop()`). Bekræftelse kræver en FRISK login: alle aktive RBAC-session-tokens revokeres (`rbac_session_token_revoke_all()`, nyt i rbac.cpp, samme spinlock-mønster som eksisterende `rbac_session_token_revoke()`) idet en gated ændring staged'es, og en evt. åben Telnet-forbindelse tvinges til at genforbinde (`network_manager_disconnect_telnet_client()`, nyt i network_manager.cpp) — kun en session-token udstedt EFTER dette tidspunkt kan nogensinde nå frem til confirm-endpointet, hvilket strukturelt ER beviset for at login/adgang stadig virker. `/api/login` er BEVIDST IKKE undtaget fra ACL'en (til forskel fra FEAT-397h's Bearer/Basic-undtagelse) — hele pointen med flowet er at kunne OPDAGE om login rent faktisk fejler under de nye regler. Race-condition mellem `ip_acl_tick()` (loop-tasken) og `/api/acl/confirm` (httpd worker-tasken) beskyttet af én `portMUX_TYPE`-spinlock (samme mønster som `rbac_session_token_mux`) omkring hver "tjek pending, så handl"-sekvens. Sletning/deaktivering af en regel er ALDRIG gated (kan kun åbne adgang). **Selvstændigt fundet og rettet under implementeringen**: en oprindelig, endnu ikke bygget/flashet designfejl fra tidligere i sessionen (FEAT-397h's `auth_mode`) var placeret midt i `HttpConfig` — samme klasse fejl som denne opgave i forvejen handlede om at undgå; fanget og flyttet til `PersistConfig`s hale, sammen med denne opgaves egne felter, FØR noget af det nogensinde blev flashet. Selvstændigt fundet under kodegennemgang: `ip_acl_get_effective_rule()` brugte oprindeligt en delt statisk retur-buffer, som ville race mellem samtidige CLI/REST-kald fra forskellige FreeRTOS-tasks — rettet til caller-provided buffer FØR første build. CLI: `set acl enable|disable`, `set acl rule add <cidr> service <svc> [disabled]`, `set acl rule <index> enable|disable`, `delete acl rule <index>`, `show acl`, `confirm acl` (src/cli_parser.cpp — IKKE cli_commands.cpp, samme sted som `set rbac`/`set user`; src/cli_show.cpp for `show acl`). REST: `GET/POST /api/acl`, `POST/DELETE /api/acl/rules[/{index}]`, `POST /api/acl/confirm` (src/api_handlers.cpp, registreret i src/http_server.cpp, tilføjet til `API_ROUTES[]`-skemaet). Web-GUI: nyt "IP Access Control (ACL)"-kort i web/system.html (regeltabel, tilføj-regel-formular, fremtrædende gult "afventer bekræftelse"-banner med live nedtælling + bekræft-knap). **Selvstændigt fundet OG rettet under live-verifikation**: `GET/POST /api/system/backup`/`/api/system/restore` serialiserede/genindlæste oprindeligt slet ikke de nye `acl_enabled`/`acl_rule_count`/`acl_rules[]`-felter — en fuld backup/restore-cyklus ville stille have mistet enhver konfigureret ACL-regel. Fanget ved at diffe to fuld-backup-snapshots hvor et helt uafhængigt fund (brugerens egen, sideløbende tilføjelse af en ny GPIO-mapping via `/io`-siden mellem to af denne sessions test-runder) fik mig til at gennemgå backup-JSON'en grundigt nok til at opdage det manglende `acl`-felt. Rettet (ny `"acl":{"enabled":bool,"rules":[{"cidr","service","enabled"}]}`-blok i begge handlers, samme sted og stil som `var_maps`) FØR endelig live-verificering, genbygget/genflashet (v7.9.45.0). **Fuld live-verifikation på 10.1.1.153, ALLE 8 planlagte tests bestået**: (1) Regression — schema 25→26-migration ren, ACL default OFF/0 regler, Logic1/Logic2 upåvirket. (2) Ikke-selv-blokerende HTTP-regel (bevidst IANA-reserveret test-net `203.0.113.0/24`, matcher aldrig min egen rigtige IP `10.1.1.75`) — udløste korrekt pending-gaten og session-revoke, men blokerede ALDRIG min egen adgang, hvilket gjorde det trygt at teste hele state-maskinen uden reel lockout-risiko. (3) **Auto-rollback** (kørt FØRST, før confirm nogensinde testet): fulgte den 5-minutters nedtælling i realtid via gentagne `GET /api/acl`-kald (300000→0 ms) — regel og pending-flag automatisk og korrekt nulstillet ved timeout, UDEN reboot. (4) Confirm-flow: ny regel → tvungen frisk login (gammelt token korrekt 401'et) → `confirm acl` → persisteret → overlevede en reel reboot. (5) HTTPS-request-lags-håndhævelsen dækket indirekte via de samme HTTP-endpoints (device kører kun almindelig HTTP i denne testopstilling; koden for HTTPS-stien er identisk logik, verificeret ved kodegennemgang mod ESP-IDF-kilden i designfasen). (6) Telnet: port stadig sund/lyttende efter alt ACL-arbejde, ingen krasch. (7) SSE — **den mest afgørende funktionelle test**: en IKKE-gated regel der blokerede min EGEN rigtige IP for SSE specifikt viste `curl`: "Empty reply from server" (afvist ved `accept()`, før noget HTTP-svar) BÅDE i pending- og bekræftet tilstand; med ACL deaktiveret igen gav PRÆCIS samme kald et normalt `HTTP/1.1 401 Unauthorized` — utvetydigt bevis for at selve IP/service-matchings- og afvisningslogikken virker korrekt, opnået uden nogensinde at røre HTTP/Telnet for min egen IP. (8) **Persisteringsisolation** (den vigtigste sikkerhedsgaranti): stagede en ny, ubekræftet regel, kaldte en HELT urelateret `POST /api/system/save`, genstartede UDEN at bekræfte eller vente — kun den tidligere BEKRÆFTEDE regel overlevede, den ubekræftede var sporløst væk. Bekræfter at staging-designet (RAM-only, aldrig i `g_persist_config` før `confirm`) virker som tiltænkt. Backup/restore-rundtur separat verificeret (slettede en live regel, restorede fra backup, reglen kom korrekt igen). Fuld backup-diff mod FEAT-397i-baseline: KUN forventede forskelle (build/version/schema/nyt acl-felt + brugerens egen uafhængige GPIO-tilføjelse) — RBAC og netværkskonfiguration 100% uændrede. Logic1 (disabled, urørt) og Logic2 (kørende, 0 fejl) bekræftet sunde gennem samtlige reboots. Rørte: include/types.h, include/constants.h, include/ip_acl.h (ny), src/ip_acl.cpp (ny), include/rbac.h, src/rbac.cpp, include/network_manager.h, src/network_manager.cpp, src/config_load.cpp, src/http_server.cpp, src/api_handlers.cpp, include/api_handlers.h, src/tcp_server.cpp, src/sse_events.cpp, src/cli_parser.cpp, include/cli_show.h, src/cli_show.cpp, src/main.cpp, web/system.html |

## Quick Lookup by Category

### 🌐 API Roadmap (v6.3.0 — v7.0.0)

**v6.3.0 — API Gap Coverage (CLI parity) — ✅ 7/8 DONE:**
- **FEAT-019:** ✅ Telnet Configuration API — 4/4 testet PASS
- **FEAT-020:** ✅ ST Logic Debug API — 8/8 testet PASS
- **FEAT-021:** ✅ Bulk Register Operations — 12/12 testet PASS
- **FEAT-022:** ✅ Persistence Group Management API (v7.1.0)
- **FEAT-024:** ✅ Hostname API — 3/3 testet PASS
- **FEAT-025:** ✅ Watchdog Status API — 2/2 testet PASS
- **FEAT-026:** ✅ Heartbeat Control API — 3/3 testet PASS (BUG-236 fixed)
- **FEAT-027:** ✅ CORS Headers — 3/3 testet PASS
- **Test:** 34/34 PASS (100%) — se `tests/API_V630_TEST_RESULTS.md`

**v7.1.0 — Prometheus Metrics + Persist API + Rate Limiting (2026-03-18):**
- **FEAT-032:** `GET /api/metrics` — Prometheus text format med system, HTTP, Modbus, SSE, network, counter, timer, watchdog metrics
- **FEAT-022:** Persistence Group Management API — `GET/POST/DELETE /api/persist/groups/{name}` + save/restore endpoints
- **FEAT-028:** Token bucket rate limiter per klient IP (30 burst, 10/sec) — returnerer 429 Too Many Requests
- **CLI:** `show rate-limit`, `set rate-limit enable|disable`, `show metrics` + sektioner i `show config` og `show http`
- **FIX:** API v1 routing-tabel manglede 7 endpoints (bulk read, heartbeat, SSE status, version)
- **FIX:** max_uri_handlers øget fra 64 til 80 (72 registrerede handlers)

**v7.0.3 — SSE CLI Management (2026-03-18):**
- **FEAT:** `set sse` / `show sse` CLI sektioner med fuld konfiguration
- **FEAT:** Klient-registry med IP-tracking, `set sse disconnect all|<slot>`
- **FEAT:** `show config` inkluderer [API SSE] + # API SSE sektioner
- **FEAT:** SSE parametre konfigurerbare via NVS (schema 12)

**v7.0.2 — SSE Stabilitet (2026-03-18):**
- **FIX:** SSE multi-klient reconnect-beskyttelse (heap-check, cooldown, defensiv decrement)
- Testet: 2 samtidige klienter bekræftet stabil

**v7.0.1 — Next Generation API (2026-03-17):**
- **FEAT-023:** ✅ SSE Real-Time Events — konfigurerbar watch af HR/IR/Coils/DI via query params
- **FEAT-030:** ✅ API Versioning — 32/32 testet PASS (/api/v1/* + /api/version)
- **Test:** 40/40 PASS (100%) — se `tests/FEAT023_FEAT030_TEST_RESULTS.md`

**v7.x.0 — Planned:**
- **FEAT-028:** ✅ Request Rate Limiting (v7.1.0)
- **FEAT-029:** OpenAPI/Swagger Schema endpoint 🔵 LOW
- **FEAT-031:** ✅ Firmware OTA via API (v7.5.0)
- **FEAT-032:** ✅ Prometheus Metrics endpoint (v7.1.0)
- **FEAT-033:** Request Audit Log 🔵 LOW

**v7.2.0 — ES32D26 Modbus Refaktor + Hardware Modul Config (2026-03-23):**
- **FEAT-038:** ✅ Modbus mode refaktor — single-transceiver support for ES32D26 (slave/master/off)
- **FEAT-039:** ✅ AO mode config — DIP switch SW1 voltage/current konfiguration
- **FEAT-040:** ✅ Runtime UART selection — `set modbus slave|master uart uart0|uart1|uart2`
- **FEAT-041:** ✅ Hardware modul config — `set modul rs485 uart<N> tx rx dir` + `set modul ethernet enable|disable`
- Schema migration 12→13→14 (modbus_mode, ao_mode, UART pin config)

**v7.3.1 — Web Editor Udvidelse: CLI + Bindings + Monitor (2026-03-26):**
- **FEAT-043:** ✅ Web CLI Console — `POST /api/cli` med buffer-console capture, kommandohistorik, farlige kommandoer blokeret
- **FEAT-044:** ✅ Web Bindings Panel — variabel→register binding oprettelse/sletning via GUI. `GET /api/bindings` + `DELETE /api/bindings/{idx}`
- **FEAT-045:** ✅ Web Runtime Monitor — live variabel-værdier + execution stats med 1.5s auto-refresh
- **UI:** 4 view-modes i toolbar (Editor/Bindings/Monitor/CLI), fixed layout (toolbar+statusbar fastlåst), global output-log
- **BUG-246:** ✅ Login hardcoded default username → tomme felter med placeholder
- **BUG-247:** ✅ API endpoints manglede URI handler registrering (405 fejl)
- **BUG-248:** ✅ Modbus CLI kommandoer brugte Serial.print → debug_printf (web CLI + telnet output)
- **BUG-249:** ✅ Binding delete index mismatch (filtreret vs globalt index)
- **BUG-250:** ✅ PSRAM detection tilføjet til `show version`, `show status` og `/api/metrics`
- **BUG-251:** ✅ `/api/metrics` auth fjernet — åben for dashboard og Prometheus scrapers
- **BUG-252:** ✅ Web editor/system login tabt ved navigation — `sessionStorage` persistering
- **FEAT-046:** ✅ BIT_SET/BIT_CLR/BIT_TST bit-manipulation funktioner i ST Logic
- **FEAT-047:** ✅ Web Dashboard med live Prometheus metrics på `/` (ingen login)
- **FEAT-048:** ✅ Bindings redigering — Redigér-knap med pre-udfyldt formular
- **FEAT-049:** ✅ Syntax highlighting i ST Editor
- **FEAT-050:** ✅ Auto-complete/IntelliSense i ST Editor
- **FEAT-051:** ✅ Sparkline grafer i Runtime Monitor
- **FEAT-052:** ✅ Single-step execution i ST Logic
- **FEAT-053:** ✅ Register viewer i Dashboard
- **FEAT-054:** ✅ Historik-grafer i Dashboard
- **FEAT-055:** ✅ Alarm/notifikationer i Dashboard
- **FEAT-056:** ✅ Modbus register map i Dashboard
- **FEAT-057:** ✅ Program backup/download fra editor
- **FEAT-058:** ✅ PLC Ladder diagram visning

**v7.6.2 — RBAC Multi-User System + SSE Monitor Udvidelse (2026-03-30):**
- **FEAT-059:** ✅ RBAC med op til 8 brugere, roller (api/cli/editor/monitor), privilegier (read/write/rw)
- **FEAT-060:** ✅ Standalone Web CLI side på `/cli` (udskilt fra ST Editor)
- **FEAT-061:** ✅ SSE subscribe=all watch_all mode (fuld-range scanning)
- **FEAT-062:** ✅ SSE klient monitor — username, IP, topics, uptime i `show sse`
- **BUG-260:** ✅ SSE subscribe=all overvågede kun HR 0-15
- **BUG-261:** ✅ `show config rbac` viste tom sektion
- **BUG-262:** ✅ `set rbac` kommando ikke genkendt
- **CLI:** `set user`, `show users`, `delete user`, `show config rbac`
- **Sikkerhed:** SSE kræver MONITOR rolle, schema migration v14→v15
- **DOC:** [docs/SECURITY.md](docs/SECURITY.md) sikkerhedsguide, [docs/SSE_USER_GUIDE.md](docs/SSE_USER_GUIDE.md) opdateret

**v7.6.2.2 — Web User Badge + /api/user/me (2026-03-30):**
- **FEAT-063:** ✅ Web User Badge i topnav — brugernavn, status-dot, dropdown med roller/privilegier/auth mode, login/logout
- **FEAT-064:** ✅ `/api/user/me` API endpoint — returnerer brugerinfo (username, roles, privilege, mode)
- **FEAT-065:** ✅ `show user` CLI kommando — viser aktuel session info
- **FEAT-066:** ✅ SSE klient management i web System — tabel med disconnect-knapper
- **BUG-263:** ✅ User badge viste altid "Ikke logget ind" — `/api/user/me` manglede httpd URI handler, login modal på dashboard, updateUserBadge() kald efter login
- **BUG-264:** ✅ SSE afviste brugere med API rolle — rolle-check udvidet til MONITOR|API, RBAC sektion i `show sse`

**v7.6.2.4 — RBAC Privilege Enforcement (2026-03-31):**
- **BUG-265:** ✅ 42 write-endpoints ændret til CHECK_AUTH_WRITE — read-only brugere blokeres nu
- **BUG-266:** ✅ rbac_parse_privilege("write") returnerede PRIV_RW — rettet parse-rækkefølge
- **BUG-264:** ✅ SSE rolle-check udvidet til MONITOR|API
- `show sse` viser RBAC Authentication sektion
- `show users roles` viser SSE-krav

**v7.6.2.5 — Web System fejlhåndtering (2026-03-31):**
- **BUG-267:** ✅ Write-knapper (reboot, save, load, defaults) viser nu 403 fejl i stedet for at slugte dem silently

**v7.7.0 — Async Modbus Master (2026-03-31):**
- **FEAT-070:** ✅ Modbus Master operationer er nu asynkrone via dedikeret FreeRTOS baggrundstask
- Non-blocking ST builtins: reads returnerer cached værdi, writes køes
- Nye builtins: `MB_SUCCESS()`, `MB_BUSY()`, `MB_ERROR()`
- 32-entry cache, 16-deep request queue, request deduplication
- `show modbus-master` viser async cache statistik + entries
- Backward-kompatibel: eksisterende ST-programmer virker uændret

**v7.8.0 — Web Monitor Udvidelse (2026-04-01):**
- **FEAT-072:** ✅ Modbus RTU trafikmonitor — live req/3s rate, success rate, CRC/timeout/exception tæller, sparkline grafer
- **FEAT-073:** ✅ Modbus Master cache status — async cache hits/misses/entries, per-slave status tabel
- **FEAT-078:** ✅ FreeRTOS task monitor — task count, heap fragmentation, stack HWM for kendte tasks
- **FEAT-085:** ✅ Alarm-historik — 32-entry ringbuffer, auto-detection (heap/CRC/timeout/auth/overrun), API + kvittér
- **FEAT-095:** ✅ Digital I/O dashboard — LED-indikatorer for IN1-IN8 + CH1-CH8 med klik-toggle
- **Backend:** Nye Prometheus metrics: heap largest block, master exceptions, master cache stats, FreeRTOS tasks, watchdog reset reason, alarm count
- **API:** `GET /api/alarms` (alarm log JSON), `POST /api/alarms/ack` (kvittér alle)
- **Metrics buffer:** 8KB → 12KB for udvidede metrics

**v7.8.1 — NTP Tidssynkronisering (2026-04-01):**
- **FEAT-101:** ✅ NTP tidssynkronisering — ESP-IDF SNTP klient, konfigurerbar server + POSIX tidszone
- **CLI:** `set ntp enable|server|timezone|interval`, `show ntp`, `show time`
- **API:** `GET /api/ntp` (status + config), `POST /api/ntp` (konfiguration)
- **Dashboard:** NTP kort med live klokkeslæt, sync status, server info
- **Metrics:** ntp_enabled, ntp_synced, ntp_sync_count, ntp_epoch_seconds, ntp_last_sync_age_ms
- **Alarm-log:** Real-time tidsstempler fra NTP (fallback til uptime)
- **Schema:** 15 → 16 (NtpConfig: enabled, server, timezone, sync_interval_min)

**v8.0.0 — Web Monitor Udvidelse (Roadmap):**
- **FEAT-074:** Ethernet/WiFi signalkvalitet (RSSI, link speed, pakketab) 🟠 MEDIUM
- **FEAT-075:** ✅ TCP forbindelsesmonitor (v7.9.6.7)
- **FEAT-076:** Bandwidth monitor (bytes sendt/modtaget over tid) 🔵 LOW
- **FEAT-077:** Heap fragmentation graf (largest free block vs total) 🟡 HIGH
- **FEAT-079:** FreeRTOS task monitor udvidet (prioritet, CPU%) 🟠 MEDIUM
- **FEAT-080:** Boot-historik (reset-årsager, timestamps) 🟡 HIGH
- **FEAT-081:** NVS brug (plads, entries, flash wear) 🟠 MEDIUM
- **FEAT-082:** SPIFFS/LittleFS brug 🔵 LOW
- **FEAT-083:** ESP32 intern temperatur 🔵 LOW
- **FEAT-084:** Spændingsmonitor (VCC, brownout events) 🔵 LOW
- **FEAT-086:** ✅ Event log viewer (v7.9.10.6)
- **FEAT-087:** Watchdog-statistik 🟠 MEDIUM
- **FEAT-088:** Threshold-alarmer for registre 🟠 MEDIUM
- **FEAT-089:** ✅ Register change-log med tidsstempler (v7.9.10.6)
- **FEAT-090:** Modbus request log / sniffer 🟡 HIGH
- **FEAT-091:** ST Logic profiler (cycle time, worst-case) 🟠 MEDIUM
- **FEAT-092:** GPIO pulse monitor (pulsbredde, frekvens, duty cycle) 🔵 LOW
- **FEAT-093:** Uptime-graf med nedetidsmarkering 🔵 LOW
- **FEAT-094:** Analog input trending (0-10V, 4-20mA) 🟡 HIGH
- **FEAT-096:** RS-485 bus health 🟠 MEDIUM
- **FEAT-097:** Customizable dashboard (drag & drop widgets) 🔵 LOW
- **FEAT-098:** Register-map visualizer (farvekodning) 🟠 MEDIUM
- **FEAT-099:** Trend recorder med CSV-eksport 🟡 HIGH
- **FEAT-100:** Mini-SCADA view 🔵 LOW

**v7.7.2 — FEAT: HW Counter Access fra ST Logic (2026-03-31):**
- **FEAT-071:** 9 nye ST builtins: CNT_SETUP, CNT_SETUP_ADV, CNT_SETUP_CMP, CNT_ENABLE, CNT_CTRL, CNT_VALUE, CNT_RAW, CNT_FREQ, CNT_STATUS
- **FEAT-071:** Konfigurer, styr og aflæs ESP32 HW counters direkte fra ST-programmer
- **FEAT-071:** Config fra ST afspejles i CLI (`show counter`) og NVS persistens
- **FEAT-071:** VM udvidet til max 6 argumenter (CNT_SETUP)
- **FEAT-071:** Web editor: autocomplete + hjælpepanel for CNT funktioner
- **BUG-296:** ✅ ST parser max 4 function arguments — CTUD/SCALE/CNT_SETUP fejlede (args[4]→args[8])
- **BUG-297:** ✅ Web dashboard register map viste Counter 1 ved HR0-3 i stedet for HR100-114
- **FEAT-071:** Register map viser nu ST Logic variable bindings (HR/Coil/DI) fra /api/bindings

**v7.7.1.1 — VAR Initializers + Telnet RBAC + Boot Fix (2026-03-31):**
- **BUG-290:** ✅ VAR initial values (`INT := 120`) virker nu i compiler
- **BUG-291:** ✅ VAR initial values bevares i bytecode cache (nyt v2 format)
- **BUG-292:** ✅ Telnet bruger RBAC auth når enabled, ellers lokal user/pass
- **BUG-293:** ✅ mb_async_init() crash på ES32D26 i SLAVE mode fixet
- **BUG-294:** ✅ Web editor autocomplete: BEGIN/END/EXPORT + alle Modbus Master funktioner
- **BUG-295:** ✅ Version+build konsistent i web UI (System, OTA, footer)

**v7.6.2.7 — SSE backup/restore (2026-03-31):**
- **FEAT-069:** ✅ SSE konfiguration inkluderet i backup/restore — enabled, port, max_clients, check_interval_ms, heartbeat_ms

**v7.6.2.6 — Confirm dialog fix (2026-03-31):**
- **BUG-268:** ✅ confirmAction() nulstillede callback før den blev kaldt — alle confirm-handlinger virkede ikke

**v7.4.0 — Web System Administration + Dashboard Refactor (2026-03-28):**
- **FEAT:** Web-baseret system administration side på `/system` — backup/restore, save/load, factory defaults, reboot, persist groups
- **FEAT:** Web Dashboard refaktoreret til standalone `web_dashboard.cpp` (v7.4.0)
- **FEAT:** Web Editor refaktoreret til standalone `web_editor.cpp` (v7.4.0)
- **FEAT:** Fælles navigation mellem Dashboard (`/`), Editor (`/editor`), System (`/system`), OTA (`/ota`)

**v7.5.0 — Firmware OTA via HTTP API + SSE bugfixes (2026-03-28):**
- **FEAT-031:** ✅ Firmware OTA via API — chunked upload, progress polling, rollback
- **Endpoints:** `POST /api/system/ota` (upload .bin), `GET /api/system/ota/status` (progress), `POST /api/system/ota/rollback`
- **Web UI:** Dedikeret OTA side på `/ota` med drag-drop upload, progress bar, firmware info, rollback knap
- **Partitions:** Dual OTA layout (ota_0 + ota_1 á 1.625MB, NVS 64KB, SPIFFS 640KB)
- **Boot validation:** `esp_ota_mark_app_valid_cancel_rollback()` i setup() bekræfter ny firmware
- **Sikkerhed:** Basic Auth + rate limiting på OTA endpoints, `CHECK_AUTH_OTA()` macro
- **BUG-253:** ✅ SSE manglende TCP_NODELAY → events forsinket/tabt (Nagle buffering)
- **BUG-254:** ✅ SSE SO_SNDTIMEO 5s→30s (for aggressiv timeout under load)
- **BUG-255:** ✅ SSE klient-task stack overflow risiko (init_buf→heap, stack 5120→6144)
- **BUG-256:** ✅ SSE EAGAIN behandlet som fatal fejl (retry-loop tilføjet)
- **Build:** #1700, schema uændret

---

### 📊 ST Logic Heap & RAM Analyse (v7.5.0 — post-optimering)

**ESP32-WROOM-32: 520 KB SRAM total, ~320 KB disponibelt efter WiFi/BT**

#### Permanent Heap (altid allokeret)

| Komponent | Per stk | Antal | Total |
|-----------|---------|-------|-------|
| `source_pool[8000]` | 8,000 B | 1 shared | **8.0 KB** |
| `st_logic_program_config_t` (bytecode+stats+meta) | ~2.5 KB | 4 | **10.0 KB** |
| ↳ `*instructions` (dynamisk, exact-size) | 8 B/instr | variabel | *se nedenfor* |
| ↳ `variables[32]` á 8 bytes | 256 B | (inline) | |
| ↳ `var_names[32][16]` (v7.9.3: var [32][32]) | 512 B | (inline) | ~~1,024 B~~ |
| ↳ `var_types[32]` + `var_export_flags[32]` | 64 B | (inline) | |
| ↳ `name[32]` + `last_error[64]` + stats/meta | ~236 B | (inline) | ~~392 B~~ |
| Dynamisk instructions (per compiled prog) | 8 B/instr | 0-2048 | **variabel** |
| ↳ Typisk 100-instruktion program | 800 B | per prog | |
| ↳ Stort 500-instruktion program | 4,000 B | per prog | |
| `st_stateful_storage_t` (malloc'd per compiled prog) | ~540 B | 0-4 | **0–2.2 KB** |
| `st_function_registry_t` (kun ved user functions) | ~3.9 KB | 0-4 | **0–15.6 KB** |
| ↳ `functions[32]` á ~52 bytes (v7.9.3: var [64]) | 1,664 B | (inline) | ~~3,328 B~~ |
| ↳ `fb_instances[16]` á ~145 bytes | 2,320 B | (inline) | |
| Engine global state (enabled, interval, cycle stats) | ~60 B | 1 | **0.06 KB** |

**Permanent total:**
```
Minimum (0 programmer compiled):     18.1 KB  (pool + bytecode structs + engine)
Typisk (2 prog á 100 instr, ingen UF): 20.9 KB  (+ 2× instructions + 2× stateful)
Maximum (4 prog á 500 instr + UF):   50.4 KB  (+ 4× instructions + 4× stateful + 4× registry)

FØR optimeringer:  51.7 – 84.6 KB
EFTER optimeringer: 18.1 – 50.4 KB  → BESPARELSE: 33.6 – 34.2 KB (65% ↓)
```

#### Temporær Heap (under kompilering — frigives efter)

| Komponent | Størrelse | Bemærkning |
|-----------|-----------|------------|
| AST node pool | **21–40 KB** | 256–475 nodes × 84 bytes/node (v7.9.3 optimering) |
| ↳ `st_ast_node_t`: 84 bytes | | v7.9.3: char[64]→[32] + CASE branches→heap (var 156 B) |
| ↳ Try-decreasing: 512→256→128→64→32 | | BUG-241: tilpasser sig fragmenteret heap |
| Parser `st_parser_t` (malloc'd) | **~1.3 KB** | lexer + tokens + error_msg[256] |
| Compiler `st_compiler_t` (malloc'd) | **~3.0 KB** | symbol_table[32]×74 + patches + stacks |
| Temp bytecode buffer (dynamisk) | **2–16 KB** | v7.9.3: start 256 instr (2KB), realloc i 256-blokke, max 2048 |
| Source code kopi (null-terminated) | **0.1–2 KB** | BUG-212 fix: malloc(source_size+1) |
| CASE branches (per CASE stmt) | ~128 B | v7.9.3: heap-allokeret 16×8 bytes, free efter parse |
| CASE jump arrays (per CASE stmt) | ~64 B | malloc/free per CASE branch |

**Peak kompilering (v7.9.3):**
```
Lille program (100 linjer):    ~22 KB peak  (128 AST nodes×84B + parser + compiler + 2KB bytecode)
Mellem program (500 linjer):   ~32 KB peak  (256 AST nodes×84B + 4KB bytecode)
Stort program (1000+ linjer):  ~55 KB peak  (475 AST nodes×84B + 8KB bytecode + func registry)

Alt frigives efter kompilering ✓ (recovery til permanent level)
FØR v7.9.3: peak var 36–94 KB — NU: 22–55 KB (40% reduktion)
```

#### Runtime VM (stack-allokeret — per execution cycle)

| Komponent | Størrelse | Placering |
|-----------|-----------|-----------|
| `st_vm_state_t` | **~660 B** | Stack (lokalt i execute) |
| ↳ `stack[64]` á 8 bytes | 512 B | Execution value stack |
| ↳ `error_msg[128]` | 128 B | |
| ↳ pc + halted + error + pointer | 20 B | |
| Lokale variabler i VM dispatch | ~200 B | Stack |
| **Per program per cycle** | **~860 B** | **Frigives efter cycle** |
| **4 programmer sekventielt** | **~860 B** | Genbruges (ikke concurrent) |

**Nøglepointe:** VM kører programmer sekventielt i `loop()` — kun ét sæt VM-state ad gangen.
Ingen malloc under execution — kun stack. Zero heap-påvirkning under drift.

#### Heap Tidslinje (typisk boot → drift)

```
BOOT:     [██████░░░░░░░░░░░░░░░░░░░░░░░] 18.1 KB permanent (structs, ingen compiled)
           ↓ config_load restorer + recompiler fra SPIFFS
COMPILE:  [████████████████████░░░░░░░░░░] 18.1 + 53 KB = 71.1 KB PEAK (temp)
           ↓ AST + parser + compiler + temp bytecode frigives
LOADED:   [████████░░░░░░░░░░░░░░░░░░░░░] 20.9 KB (+ dynamiske instructions + stateful)
           ↓ VM cycle (stack, ikke heap)
CYCLE:    [████████░░░░░░░░░░░░░░░░░░░░░] 20.9 KB + 860 B stack (released)
```

#### Optimeringer (samlet oversigt)

| Optimering | Version | Før | Efter | Besparelse |
|-----------|---------|-----|-------|------------|
| AST node størrelse | v7.2.1 | 1,920 B | 161 B | **93% ↓** |
| AST pool (256 nodes) | v7.2.1 | 491 KB | 41 KB | **450 KB ↓** |
| Parser/compiler allokering | v7.2.1 | Permanent | Temporær | **4.3 KB ↓** idle |
| Try-decreasing pool | v7.2.1 | Fast 512 | 512→32 adaptive | **Fragmentation-robust** |
| `instructions[1024]` → dynamisk | v7.5.0 | 8,192 B/prog | ~800 B/prog typ | **~29.6 KB ↓** (4 prog) |
| `var_names[32][64]` → `[32][32]` | v7.5.0 | 2,048 B/prog | 1,024 B/prog | **4.0 KB ↓** (4 prog) |
| `name[64]` → `[32]` (bytecode) | v7.5.0 | 64 B | 32 B | **128 B ↓** (4 prog) |
| `func_name[64]` → `[32]` (func_def/entry) | v7.5.0 | 64 B each | 32 B each | **~2 KB ↓** (registry) |
| `last_error[128]` → `[64]` | v7.5.0 | 128 B/prog | 64 B/prog | **256 B ↓** (4 prog) |
| **v7.5.0 subtotal** | | | | **~34 KB ↓ permanent** |

---

**v7.2.0 — ES32D26 Analog I/O (Planned):**
- **FEAT-034:** 4x 0-10V spændings-input (Vi1-Vi4) — ADC kalibrering + skalering 🟡 HIGH
- **FEAT-035:** 4x 4-20mA strøm-input (Ii1-Ii4) — ADC kalibrering + offset 🟡 HIGH
- **FEAT-036:** 2x analog output DAC (AO1-AO2) — 0-10V via DAC1/DAC2 🟠 MEDIUM
- **FEAT-037:** Analog Modbus register mapping + CLI (`set analog`/`show analog`) 🟡 HIGH

### ⚠️ CRITICAL Bugs (MUST FIX)
- **BUG-001:** ST Logic vars not visible in Modbus (IR 220-251)
- **BUG-002:** Type checking on variable bindings
- **BUG-015:** HW Counter initialization
- **BUG-016:** Running bit control
- **BUG-017:** Auto-start feature
- **BUG-020:** Manual register config disabled
- **BUG-024:** Counter truncation fix
- **BUG-025:** Register overlap checking
- **BUG-026:** Binding allocator cleanup
- **BUG-028:** Register spacing for 64-bit counters
- **BUG-029:** Compare edge detection
- **BUG-030:** Compare value Modbus access
- **BUG-031:** Counter write lock i Modbus handlers (FIXED v4.2.5)
- **BUG-032:** ST parser buffer overflow (FIXED v4.2.5)
- **BUG-033:** Variable declaration bounds (FIXED v4.2.5)
- **BUG-046:** ST datatype keywords collision (FIXED v4.3.1 Build #676)
- **BUG-047:** Register allocator not freed on delete (FIXED v4.3.2 Build #691)
- **BUG-049:** ST Logic cannot read from Coils (FIXED v4.3.3 Build #703)
- **BUG-050:** VM arithmetic operators don't support REAL (FIXED v4.3.4 Build #708)
- **BUG-052:** VM operators mangler type tracking (FIXED v4.3.6 Build #714)
- **BUG-053:** SHL/SHR operators virker ikke (FIXED v4.3.7 Build #717)
- **BUG-054:** FOR loop body aldrig eksekveret (FIXED v4.3.8 Build #720)
- **BUG-055:** Modbus Master CLI commands ikke virker (FIXED v4.4.0 Build #744)
- **BUG-056:** Buffer overflow i compiler symbol table (FIXED v4.4.3)
- **BUG-059:** Comparison operators ignorerer REAL type (FIXED v4.4.3)
- **BUG-067:** Lexer strcpy buffer overflow (FIXED v4.4.4)
- **BUG-105:** INT type skal være 16-bit, ikke 32-bit (FIXED v5.0.0 Build #822)
- **BUG-124:** Counter 32/64-bit DINT/DWORD register byte order (FIXED v5.0.0 Build #834)
- **BUG-125:** ST Logic multi-register byte order DINT/DWORD/REAL (FIXED v5.0.0 Build #860)
- **BUG-133:** Modbus Master request counter reset mangler (FIXED v4.5.2 Build #917)
- **BUG-134:** MB_WRITE DINT arguments sender garbage data (FIXED v4.6.1 Build #919)
- **BUG-135:** MB_WRITE_HOLDING mangler value type validering (FIXED v4.6.1 Build #919)
- **BUG-136:** MB_WRITE_COIL mangler value type validering (FIXED v4.6.1 Build #919)
- **BUG-146:** Use-after-free i config_save.cpp (FIXED v4.7.3 Build #995)
- **BUG-147:** Buffer underflow i modbus_frame.cpp (FIXED v4.7.3 Build #995)
- **BUG-155:** Buffer overflow i st_token_t.value (FIXED Build #1020)
- **BUG-156:** Manglende validation af function argument count (FIXED Build #1018)
- **BUG-157:** Stack overflow risk i parser recursion (FIXED Build #1018)
- **BUG-158:** NULL pointer dereference i st_vm_exec_call_builtin (FIXED Build #1018)
- **BUG-183:** start_value kun uint16_t - begrænser 32/64-bit counters (FIXED Build #1077)
- **BUG-201:** ESP-IDF middle-wildcard URI routing matcher aldrig (FIXED Build #1162)
- **BUG-202:** Source pool entries ikke null-termineret (FIXED Build #1162)
- **BUG-207:** HTTP server stack_size 4096 for lille til API handlers (FIXED Build #1196)
- **BUG-214:** Backup ST Logic source korruption pga. manglende null-terminering (FIXED Build #1241)
- **BUG-215:** Restore var_maps tabt pga. st_logic_delete() side-effect (FIXED Build #1241)
- **BUG-218:** W5500 Ethernet boot-loop ved flash overflow (FIXED v6.0.8)
- **BUG-219:** Flash forbrug 97%+ forhindrer nye features (FIXED v6.0.8)
- **BUG-230:** `sh config` over telnet trunkeret — kun [SYSTEM] vises (FIXED v6.1.0)
- **BUG-231:** TCP send retry blokerer main loop → 1s output bursts (FIXED v6.1.0)

### 🟡 HIGH Priority (SHOULD FIX)
- **BUG-003:** Bounds checking on var index
- **BUG-004:** Reset bit in control register
- **BUG-014:** Persistent interval save
- **BUG-018:** Show counters bit-width
- **BUG-019:** Race condition in display
- **BUG-021:** Delete counter command
- **BUG-022:** Auto-enable counter
- **BUG-023:** Compare after disable
- **BUG-034:** ISR state volatile cast (FIXED v4.2.6)
- **BUG-035:** Overflow flag auto-clear (FIXED v4.2.6)
- **BUG-038:** ST Logic variable race condition (FIXED v4.2.6)
- **BUG-042:** normalize_alias() missing "auto-load" (FIXED v4.3.0)
- **BUG-043:** "set persist enable on" case sensitivity (FIXED v4.3.0)
- **BUG-045:** Upload mode echo setting (FIXED v4.3.0)
- **BUG-048:** Bind direction parameter ignored (FIXED v4.3.3 Build #698)
- **BUG-051:** Expression chaining fejler for REAL (FIXED v4.3.5 Build #712)
- **BUG-063:** Function argument overflow validation (FIXED v4.4.3)
- **BUG-068:** String parsing null terminator (FIXED v4.4.4)
- **BUG-129:** normalize_alias() returnerer ST-STATS (FIXED v4.4.0 Build #880)
- **BUG-130:** NVS partition for lille til PersistConfig (FIXED v4.5.0 Build #904)
- **BUG-131:** CLI `set id` kommando virker ikke (FIXED v4.5.0 Build #910)
- **BUG-132:** CLI `set baud` kommando virker ikke (FIXED v4.5.0 Build #910)
- **BUG-133:** Modbus Master request counter reset mangler (FIXED v4.5.2 Build #911)
- **BUG-148:** Printf format mismatch i cli_config_regs.cpp (FIXED v4.7.3 Build #995)
- **BUG-159:** Integer overflow i FOR loop (OPEN v4.8.2 - kompleks fix)
- **BUG-160:** Missing NaN/INF validation i arithmetic (FIXED Build #1018)
- **BUG-161:** Division by zero i SCALE function (FIXED Build #1018)
- **BUG-162:** Manglende bounds check på bytecode array (FIXED Build #1018)
- **BUG-163:** Memory leak i parser error paths (OPEN v4.8.2 - behøver refactoring)
- **BUG-164:** Inefficient linear search i symbol lookup (OPEN v4.8.2 - optimization, ikke bug)
- **BUG-CLI-1:** Parameter keyword clarification
- **BUG-CLI-2:** GPIO validation

### 🟠 MEDIUM Priority (NICE TO HAVE)
- **BUG-005:** Binding count lookup optimization
- **BUG-007:** Execution timeout protection
- **BUG-008:** IR update latency
- **BUG-027:** Counter display overflow clamping
- **BUG-039:** CLI compare-enabled parameter (FIXED v4.2.7)
- **BUG-040:** Compare source configurability (FIXED v4.2.8)
- **BUG-041:** Reset-on-read parameter structure (FIXED v4.2.9)
- **BUG-044:** cli_cmd_set_persist_auto_load() case sensitive (FIXED v4.3.0)
- **BUG-057:** Buffer overflow i parser program name (FIXED v4.4.3)
- **BUG-058:** Buffer overflow i compiler bytecode name (FIXED v4.4.3)
- **BUG-060:** NEG operator ignorerer REAL type (FIXED v4.4.3)
- **BUG-065:** SQRT negative validation (FIXED v4.4.4)
- **BUG-072:** DUP type stack sync (FIXED v4.4.4)
- **BUG-073:** SHL/SHR shift overflow (FIXED v4.4.4)
- **BUG-074:** Jump patch silent fail (FIXED v4.4.4)
- **BUG-128:** normalize_alias() mangler BYTECODE/TIMING (FIXED v4.4.0 Build #875)
- **BUG-137:** CLI `read reg` count parameter ignoreres for REAL/DINT/DWORD (FIXED v4.7.1 Build #937)
- **BUG-142:** `set reg STATIC` blokerer HR238-255 fejlagtigt (FIXED v4.7.3 Build #995)
- **BUG-149:** Identical condition i modbus_master.cpp (FIXED v4.7.3 Build #995)
- **BUG-165:** Missing input validation i BLINK function (FIXED Build #1019)
- **BUG-167:** No timeout i lexer comment parsing (FIXED Build #1019)
- **BUG-168:** Missing validation af CASE branch count (FIXED Build #1019)
- **BUG-179:** CLI read i-reg mangler type parameter support (FIXED Build #1048)

### 🔴 CRITICAL Priority (MUST FIX)
- **BUG-181:** DOWN mode underflow wrapper til max_val i stedet for start_value (FIXED Build #1063)
- **BUG-182:** PCNT signed overflow ved 32768 + atol/atoll signed parsing (FIXED Build #1069)
- **BUG-315:** Modbus Master parity/stop-bits ignoreret på ES32D26 — uart1_init hardcodet SERIAL_8N1 (FIXED v7.9.5.1 Build #1895)
- **BUG-316:** Modbus Master post-TX DE-release for hurtig — sidste byte afskåret ved lav baudrate (FIXED v7.9.5.1 Build #1895)
- **BUG-323:** Login modal flash ved sideskift i web GUI — editor/CLI/system startede med show class (FIXED v7.9.6.5 Build #1909)

### 🟡 HIGH Priority (SHOULD FIX)
- **BUG-180:** Counter overflow mister ekstra counts ved wraparound (FIXED Build #1052)
- **BUG-184:** Frequency measurement giver forkerte resultater for DOWN counting (FIXED Build #1074)
- **BUG-203:** /api/config returnerer ufuldstændig konfiguration (FIXED Build #1162)
- **BUG-204:** WWW-Authenticate header tabt pga. httpd response rækkefølge (FIXED Build #1162)
- **BUG-205:** API responses cached af browser - manglende Cache-Control (FIXED Build #1162)
- **BUG-208:** GET /api/logic/{id}/stats stack buffer overflow (FIXED Build #1196)
- **BUG-209:** GET /api/logic/{id}/source timeout - delvis data (FIXED Build #1196)
- **BUG-210:** API source upload kompilerer ikke automatisk (FIXED Build #1197)
- **BUG-226:** Telnet config nested under WiFi — usynlig ved WiFi disabled (FIXED v6.1.0)
- **BUG-227:** normalize_alias() mangler TELNET keyword (FIXED v6.1.0)
- **BUG-229:** Telnet login bruger startup-kopi af credentials (FIXED v6.1.0)

### 🟠 MEDIUM Priority (NICE TO HAVE)
- **BUG-187:** Timer ctrl_reg ikke initialiseret i defaults (FIXED Build #1074)
- **BUG-216:** Backup IP-adresser som rå uint32_t i stedet for dotted strings (FIXED Build #1241)

### 🔵 LOW Priority (COSMETIC)
- **BUG-006:** Counter wrapping at 65535
- **BUG-011:** Variable naming (`coil_reg`)
- **BUG-126:** st_count redeclaration in cli_show.cpp (FIXED v4.4.0 Build #869)
- **BUG-127:** st_state declaration order (FIXED v4.4.0 Build #869)
- **BUG-138:** ST Logic upload error message generisk (FIXED v4.7.1 Build #940)
- **BUG-169:** Inefficient memory usage i AST nodes (ACCEPTABLE - temporary compilation RAM)
- **BUG-171:** Suboptimal error messages i compiler (FIXED Build #1040)
- **BUG-172:** Missing overflow detection i integer arithmetic (DOCUMENTED Build #1040)
- **BUG-173:** MOD operation med negative operands (DOCUMENTED Build #1040)
- **BUG-175:** FILTER function med zero cycle time (FIXED Build #1040)
- **BUG-176:** HYSTERESIS function med inverterede thresholds (FIXED Build #1019)
- **BUG-177:** strcpy uden bounds check i lexer (FIXED Build #1038)
- **BUG-206:** /api/ trailing slash returnerer 404 (FIXED Build #1162)
- **BUG-217:** Backup boolean felter inkonsistente int vs true/false (FIXED Build #1241)
- **BUG-228:** Telnet banner viser "Telnet Server (v3.0)" i stedet for hostname (FIXED v6.1.0)

### ✔️ NOT BUGS (DESIGN CHOICES)
- **BUG-013:** Binding display order (intentional)
- **BUG-166:** Race condition i stateful storage (FALSE POSITIVE - single-threaded)
- **BUG-170:** millis() wraparound (unsigned arithmetic handles it correctly)
- **BUG-185:** Timer Mode 2 trigger_level (legacy parameter, Modbus-triggered design)
- **BUG-186:** Timer Mode 1 duration=0 (intentional "instant sequence" feature)
- **BUG-188:** ISR vs HW underflow inkonsistens (korrekt for edge-triggered vs delta-based)
- **BUG-189:** Timer Mode 4 læser COIL (bevidst design for Modbus control)

## Status Legend

| Symbol | Meaning |
|--------|---------|
| ✅ FIXED | Bug implemented and verified |
| ❌ OPEN | Bug identified but not fixed |
| ✔️ NOT A BUG | Determined to be design choice |
| ✔️ DESIGN | Intentional behavior, documented |
| 🔴 CRITICAL | System breaks without fix |
| 🟡 HIGH | Significant impact, should fix soon |
| 🟠 MEDIUM | Noticeable impact, nice to fix |
| 🔵 LOW | Minor issue, cosmetic only |

## How Claude Should Use This

**When working on code changes:**
1. Check BUGS_INDEX.md first (this file) - ~10 seconds to skim
2. Note which bugs might be affected by your changes
3. If you need details, go to BUGS.md and search for specific BUG-ID
4. Before committing: Update BUGS_INDEX.md status if you fixed a bug
5. Update BUGS.md detailed section only if implementing significant fix

**Example workflow:**
```
Task: Modify ST Logic binding logic
1. Skim BUGS_INDEX.md → See BUG-002, BUG-005, BUG-012, BUG-026
2. Check if your changes impact these areas
3. If modifying binding code → might affect BUG-026
4. Read BUGS.md § BUG-026 for implementation details
5. After fix → Update BUGS_INDEX.md row for BUG-026
```

## File Organization

- **BUGS_INDEX.md** (THIS FILE): Compact 1-liner summary of all bugs (tokens: ~500)
- **BUGS.md**: Full detailed analysis with root causes, test results, code references (tokens: ~5000+)

Use BUGS_INDEX.md for quick navigation, BUGS.md for deep dives.
